# GSettings schema path — required by GTK/libadwaita and xdg-desktop-portal
GSETTINGS_SCHEMA_DIR=/usr/share/glib-2.0/schemas
# Force dark theme as fallback when portal is unavailable
GTK_THEME=adw-gtk3-dark
