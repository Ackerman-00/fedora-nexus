# fedora-nexus autonomous-run relay state

status: complete
run_id: 32372487917

## Run 32372487917 (2026-08-20 — scheduled run, autonomous maintenance)

Auto-Updater commit a2174ec (13:02Z, bumped quickshell-git 532a060→9f80755).
Verified all 82 packages against upstream, tracked the quickshell-git build
(10884193) to success on all 4 chroots, install-tested 100% of the inventory,
and fixed one genuine dependency bug found by the install-test sweep:
hyprshot was missing `Requires: /usr/bin/getopt` (util-linux).

### 1. COPR build status — all 82 packages green

- quickshell-git 0.3.0^20260820110829git9f80755-1, build 10884193:
  **succeeded** fc43/44/45/rawhide. Head commit == upstream HEAD
  (`git ls-remote` 9f807554e106 == spec commit, gitdate 20260820110829 ==
  upstream committer date 2026-08-20T11:08:29Z).
- **hyprshot fix**: spec 1.3.0-1 -> 1.3.0-2 (added `Requires: /usr/bin/getopt`).
  Commit bf30d44 pushed 13:49Z; webhook fired immediately (build 10884468
  submitted 13:50:29Z) and **succeeded** on all 4 chroots. NVR 1.3.0-2 present
  in all 4 chroot repos and install-tested below.
- All other 80 packages have a latest build with state=succeeded (verified by
  querying each package's newest build via COPR API). The historical 108762xx
  failure wave (2026-08-18 SRPM-import outage) is fully superseded by newer
  succeeded builds of the same specs; the 108771xx canceled wave (global queue
  halt) likewise.

### 2. Version accuracy sweep — 82/82 verified, 0 stale

Four parallel research agents re-verified every package against upstream
(`gh api` releases/tags, `git ls-remote` HEAD for -git packages, `curl -sI`
Source0, artifact tear-down for prebuilt binaries). All 82 are up-to-date.
- -git packages confirmed at upstream HEAD: app2unit, appmenu-glib-translator,
  astal*, caelestia-*, hyprland-contrib, lazyvim-git, material-symbols-fonts,
  niri-git, noctalia-git, noctalia-greeter, quickshell-git,
  xwayland-satellite-git.
- Prebuilt artifacts torn open and version-verified from inside: fluxer
  (X-Fluxer-Version header 2026.819.105810), freebuff (0.0.66), ghostty (1.3.1,
  deb control + metainfo), helium-browser (0.15.6.1 in binary), heroic
  (2.22.1 + bundled legendary/gogdl/nile/comet), localsend (1.18.0; v1.18.1
  ships only Android APKs — correctly held), logseq (2.0.1 in app.asar),
  obsidian (1.13.7), opencode-desktop (1.18.19), protonplus (0.6.4),
  rootapp (0.9.127, sq.version in AppImage), starship (1.26.0),
  stoat-desktop (1.5.3), vesktop (1.6.7), zen-browser (1.21.15b in
  application.ini).
- Noteworthy: hyprland-plugins intentionally pinned to hyprpm commit 00862ca
  for hyprland 0.56.2 (verified via commit messages) — do not bump.

### 3. Install-verification sweep — 100% of inventory tested (82/82)

All tests from fresh `docker run` containers against the live ackerman/nexus
COPR repo (gpgcheck=0, `--disablerepo=fedora-cisco-openh264` to avoid the
known openh264 key/mirror flake). `rpm -V` clean unless noted. All on
fedora:44 unless noted.
- **quickshell-git** (changed binary): installs clean on fc43/44/45/rawhide.
- **hyprshot** (fixed this run): BEFORE fix, smoke test failed with
  `getopt: command not found`. AFTER fix (1.3.0-2), installs clean and
  `hyprshot --help` exits 0 on fc43/44/45/rawhide — verified from the COPR
  repo, fresh checker re-ran it independently.
- Full-coverage batch (all install clean, rpm -V clean, fc44): zen-browser,
  logseq, ghostty, aylurs-gtk-shell, mangowm, freebuff, gpu-screen-recorder,
  hyprland, hyprland-plugins, xdg-desktop-portal-hyprland, niri-git, vesktop,
  stoat-desktop, obsidian, heroic-games-launcher, localsend, protonplus,
  starship, waypaper, pyprland, matugen, nwg-look, hyprlock, cliphist,
  hyprdim, hypridle, hyprpaper, hyprsunset, hyprsysteminfo,
  xwayland-satellite-git, aquamarine, glaze, hyprcursor, hyprgraphics,
  hyprland-contrib, hyprland-guiutils, hyprland-protocols,
  hyprland-qt-support, hyprlang, hyprpicker, hyprpolkitagent, hyprqt6engine,
  hyprtoolkit, hyprutils, hyprwayland-scanner(-devel), hyprwire, libcava,
  mpvpaper, scenefx, wlroots, app2unit, appmenu-glib-translator, astal*,
  awww, bibata-cursor-theme, caelestia-*, cascadia-code-nerd-fonts,
  extension-manager, hellwal, lazyvim-git, ly, material-symbols-fonts,
  python3-materialyoucolor (import test: version 3.0.4), noctalia-greeter,
  opencode-desktop, rootapp, fluxer, helium-browser, hyprlauncher, hyprnome,
  hyprpwcenter, hyprshutdown, noctalia-git.
- Smoke tests (exit 0): starship 1.26.0, hyprlock v0.9.6, hyprshot --help,
  matugen 4.2.0, awww --help, quickshell-git. GUI-only apps (waypaper, zen,
  obsidian, vesktop, etc.) skip runtime smoke (need display) — recorded, not
  a failure.
- hyprwayland-scanner ships its binary in the -devel subpackage by design
  (spec layout) — installs and verifies clean.

### 4. Deep dependency audit — 82/82 covered

- hyprshot: **deps-fixed** — script invokes `getopt` at runtime (line 235,
  arg parsing) but spec lacked it; added `Requires: /usr/bin/getopt`
  (util-linux). Local rpmbuild + install + smoke verified. New built RPM's
  requires: bash, /usr/bin/getopt, /usr/bin/notify-send, grim, jq, slurp,
  wl-clipboard — all resolve in Fedora.
- Prebuilt RPMs (vesktop, stoat-desktop, freebuff, obsidian,
  heroic-games-launcher, localsend): built RPM auto-requires compared against
  spec's explicit Requires — exact match, no missing, no dead deps.
- quickshell-git: ldd 0 unresolved; requires qt6-qtbase/qtdeclarative/qtsvg/
  qtwayland, polkit, libwayland-client, libxcb — all resolve on all 4 chroots.
- All source-built packages: distro defaults intact (%{optflags} in CFLAGS/
  CXXFLAGS via %cmake/%meson/export; LTO enabled by default; no -march=native;
  no debuginfo disabled). Prebuilt repackages: prebuilt, nothing to optimize.

### 5. Docs / README health

- README accurate: 82-package table, "Supported releases: Fedora 43, 44, 45
  and Rawhide (x86_64)". COPR project has 4 chroots: fedora-rawhide-x86_64
  (= F46 post-branch), fedora-43-x86_64, fedora-44-x86_64, fedora-45-x86_64.
  Repo dirs == COPR packages 1:1 (82). No docs change needed.
- Fedora lifecycle (2026-08-20): F45 branched 2026-08-11 (GA ~Oct/Nov 2026),
  Rawhide = F46, F43 EOL ~1 month after F45 GA. COPR chroot list matches
  reality; no chroot to add or drop yet.

### 6. Open issues / PRs

- No open issues, no open PRs. Nothing to triage.

### 7. GitHub Actions health / webhook

- Fedora Nexus Auto-Updater (a2174ec, 13:02Z): success; its quickshell-git
  bump produced build 10884193 (succeeded).
- Webhook delivery history API still 403 (token lacks admin scope:
  `gh api hooks` -> 403). Verified empirically instead: the hyprshot fix push
  bf30d44 (13:49Z) was followed within seconds by build 10884468
  (submitted 13:50:29Z) for hyprshot 1.3.0-2 — webhook is alive and firing.
- opencode-schedule (this run): in progress; completing cleanly.
- No workflow faults; no changes to .github/workflows/ needed or made.

### 8. Handoff

- This run is complete: all 82 packages green; 1 real bug found and fixed
  (hyprshot getopt Requires, build 10884468 succeeded on all chroots);
  quickshell-git build verified; 100% install coverage. Repo in sync with
  origin (clean tree).
- No owner actions pending. (Cosmetic: COPR project description still omits
  Fedora 45 — owner may update via COPR web UI. Cosmetic changelog lags for
  vesktop 1.6.7 / stoat-desktop 1.5.3 / matugen 4.2.0 are artifacts of the
  workflow's version-only scanner; harmless, builds already shipped.)