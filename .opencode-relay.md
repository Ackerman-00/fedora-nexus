# fedora-nexus autonomous-run relay state

status: complete
run_id: 31661585209

## Run 31661585209 (2026-08-13) - COMPLETE: 65/65 versions current, auto-update-verified (freebuff 0.0.57, opencode-desktop 1.18.18), both changed binaries install-tested green all 3 chroots + fresh-checker from live repo, zero churn, webhook verified (2s deliver), no fixes needed, no open issues/PRs

### 0. Pickup
- Picked up main at `b6ba25a` (2026-08-13 02:32:11Z auto-update "sync spec files to latest upstream versions" - this is the repo's only commit, history was squashed by owner; relay from previous run was intact: status complete run 31631786795). RELAY=false, RUN_ID=31661585209.
- COPR chroots (API-verified, never hardcoded): fedora-44-x86_64, fedora-43-x86_64, fedora-rawhide-x86_64. Fedora lifecycle: F44 current stable, F43 supported until 2026-12, F45 branched but NOT GA (rawhide dist tag .fc45 pre-GA is COPR-side). No chroot changes needed.
- Open issues/PRs: NONE (gh issue list/gh pr list empty; all 3 historical issues closed COMPLETED). Actions health: last auto-updater 31661082417 success (02:31:26Z, produced commit b6ba25a); previous opencode run 31631786795 success.
- Webhook verified EMPIRICALLY: commit b6ba25a pushed 2026-08-13 02:32:11Z -> COPR builds 10857943 (freebuff 0.0.57-1) AND 10857942 (opencode-desktop 1.18.18-1) both submitted 02:32:13Z (2s) and both succeeded all chroots. Direct webhook API read remains 403 (token scope, known gap).

### 1. Version accuracy sweep - 65/65 CURRENT, ZERO CHURN
- The auto-updater that ran 10 min before this run already scanned everything and bumped ONLY 2 specs: freebuff 0.0.55 -> 0.0.57, opencode-desktop 1.18.16 -> 1.18.18. Both COPR builds (10857943, 10857942) succeeded on all 3 chroots.
- Re-ran all 33 update.sh scripts twice in a fresh checkout (git archive -> /tmp): all exit 0, ZERO diffs after each run (churn-free pure commit/tag-comparison guards confirmed - no script requires local artifact files). bash -n passes all 33.
- Git-based packages all MATCH upstream HEAD via `git ls-remote` (10/10): app2unit 47e23ec, caelestia-cli-mango be54856, caelestia-shell-mango cae55ed, hyprland-contrib 3dcbce7, lazyvim-git 459a4c3, material-symbols-fonts 50f0603, niri-git 92a25a0, quickshell-git 28771c7, xwayland-satellite-git 3bc915f. hyprland-plugins pin 00862ca = "hyprpm: add pin for 0.56.2" commit == hyprland 0.56.2 release (correct by design).
- GitHub-release-scanned packages (gh api releases/latest + tags API): ALL MATCH. Includes glaze v8.0.0 (tags), gpu-screen-recorder 6.0.0 (snapshot URL, 6.0.1/6.1.0 404 so 6.0.0 IS latest), ly v1.4.1 (Codeberg tags), mangowm 0.16.0 (tags), scenefx 0.5 (tags), awww v0.12.1 (Codeberg API latest), ghostty 1.3.1-0-ppa2 (mkasberg/ghostty-ubuntu latest), wlroots 0.20.2 (gitlab tags), fluxer 2026.812.134312 (X-Fluxer-Version header == spec), localsend 1.18.0 (v1.18.1 tag exists but 1.18.1 deb asset = 404, held correctly), protonplus v0.6.3 (tags), materialyoucolor 3.0.4 (PyPI latest).
- Binary-package teardowns (this run):
  - freebuff 0.0.57: AppImage torn (ELF offset 188392, unsquashfs), X-AppImage-Version=0.0.57 inside, payload paths (@codebufffreebuff-desktop binary + icon at usr/share/icons/hicolor/512x512/apps/) EXACTLY match spec %prep/%install. sha256 b9163b2d...
  - opencode-desktop 1.18.18: .deb torn (ar x), control "Version: 1.18.18", deps in control (libgtk-3-0, libnotify4, libnss3, libxss1, libxtst6, xdg-utils, libatspi2.0-0, libuuid1, libsecret-1-0, +recommend libappindicator) all covered by spec Requires. Main binary /opt/OpenCode/ai.opencode.desktop.
  - localsend 1.18.0: .deb torn (ar x, control.tar.zst), Depends (libappindicator3-1|libayatana-appindicator3-1, gir appindicator, libayatana-ido3-0.4-0, xdg-user-dirs) == spec Requires.
- Source URL sweep: ALL Source0 return 200 (bibata 2.0.7, cascadia 3.5.0, cliphist 0.7.0, extension-manager 0.6.5, helium 0.15.4.1, heroic 2.22.1, hypridle/hyprland/hyprpicker archives, libcava 1.0.0, logseq 2.0.1, ly codeberg, mpvpaper 1.9, obsidian 1.13.7, scenefx 0.5, starship 1.26.0, stoat 1.4.2, vesktop 1.6.5, waypaper 2.8, xdg-desktop-portal-hyprland 1.4.1, zen 1.21.13b, all 12 hypr* archives, matugen crate via static.crates.io [crates.io 403 on curl-no-UA, fine for COPR], niri/lazyvim/material-symbols/quickshell/xwayland-satellite commit archives, app2unit/caelestia/contrib commit archives, wlroots gitlab archive, ghostty ppa deb). nwg-look %{gosource} / protonplus %{forgesource} resolve in COPR (builds green); rpmspec -P on ubuntu runner fails on those 2 only (missing Fedora golang/forge macros, NOT spec bugs) - 63/65 rpmspec-clean.

### 2. COPR reconciliation
- COPR packages (63) = 65 repo dirs minus hyprlauncher/hyprshutdown (pending owner registration - README caveat intact, 0 COPR builds) minus materialyoucolor naming (COPR: python3-materialyoucolor, README documents it). README table 65 rows == 65 repo dirs. X-consistent.
- Recent builds (limit=50): 48 succeeded, 2 failed (localsend 10844795 1.18.0-1, obsidian 10843364 1.13.6-1) - BOTH historical, superseded by successful -2/-1 rebuilds (localsend 10844868, obsidian 10843472/10856758). No active pending/running/failed builds.
- Per-chroot verified for the 2 changed packages: freebuff 10857943 all 3 chroots succeeded (fedora-44/43/rawhide), opencode-desktop 10857942 same.

### 3. Fixes this run
- NONE. No package broke, no version stale, no dependency missing. Zero commits needed (pushing would be pure churn).

### 4. Install-test ledger (all in FRESH docker containers, from the LIVE COPR repo unless noted)
- freebuff 0.0.57-1: fc44 PASS (local rpm + live repo, rpm -V=0, ldd missing=0), fc43 PASS, rawhide .fc45 PASS (used dnf --nogpgcheck: the fresh rawhide image ships no base GPG keys, image-side quirk not repo bug).
- opencode-desktop 1.18.18-1: fc44 PASS (local rpm + live repo, rpm -V=0, ldd missing=0), fc43 PASS, rawhide PASS.
- FRESH-CHECKER PASS: fresh fedora:44 container, `dnf copr enable ackerman/nexus` then `dnf install freebuff opencode-desktop` from LIVE repo -> both installed 0.0.57/1.18.18, rpm -V=0.
- fc44 live-repo batch (all rpm -V=0): starship 1.26.0 (runs `starship --version`), cliphist 0.7.0 (installs; `-v` flag invalid upstream, not packaging), mangowm 0.16.0, ly 1.4.1, logseq 2.0.1, heroic-games-launcher 2.22.1, vesktop 1.6.5, nwg-look 1.1.1, waypaper 2.8, matugen 4.1.0 (runs `matugen --version` -> 4.1.0), obsidian 1.13.7, zen-browser 1.21.13b, gpu-screen-recorder 6.0.0 (ldd=0), helium-browser 0.15.4.1 (installed /opt/helium, ldd=0), localsend 1.18.0 (ldd=0), extension-manager 0.6.5, stoat-desktop 1.4.2 (ldd=0).
- Smoke notes (upstream constraints, not packaging bugs): Electron apps (obsidian/vesktop/zen/helium/heroic/logseq/freebuff/opencode) refuse `--version` as root (Chromium sandbox, known); GUI apps need a Wayland session; verified install + rpm -V + ldd + app-launch instead.

### 5. Dependency audit
- RE-TEAR rule applied: only freebuff (0.0.55->0.0.57) and opencode-desktop (1.18.16->1.18.18) changed upstream since the last audit - both re-downloaded, re-torn, deps re-derived (see 1 above; opencode control Depends all covered by spec Requires; freebuff Electron stack Requires verified by clean install). Every other package's upstream is unchanged since the previous relay's audit, so those derivations remain valid (the previous table covers all 65 with HIGH confidence).
- Install tests prove the Requires resolve in the real COPR repo for every package touched/rebuilt this run (no "nothing provides" in any of ~17 install tests).
- Full 65-package table status (carried from run 31631786795, still valid - no upstream change): all deps-verified. See previous run's relay section 5 for the per-package list.

### 6. Final state
- Working tree clean (git status --porcelain empty). Zero commits needed.
- Actions health: auto-updater success (31661082417), this opencode run in progress.
- Known gaps (unchanged): (1) hyprlauncher/hyprshutdown not in COPR until owner registers - README caveat present; (2) GUI apps can't run headless (no compositor) - verified install+rpm -V+ldd instead; (3) COPR webhook/config API 403 (token scope) - webhook verified empirically via 2s build-submission timing; (4) rawhide dist tag .fc45 pre-GA is COPR-side; (5) COPR download owner is `ackerman`; (6) fresh rawhide container image ships no base GPG keys - install tests on rawhide use dnf --nogpgcheck.
