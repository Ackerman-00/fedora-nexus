# fedora-nexus autonomous-run relay state

status: complete
run_id: 32322292393

## Run 32322292393 (2026-08-20 — scheduled run, autonomous maintenance)

Fresh-checker pass following Auto-Updater commit d6fccc4 (01:41Z, bumped
helium-browser, rootapp, noctalia-git). All 3 bumped packages verified against
upstream, their COPR builds tracked to success, and all changed binaries
install-tested. One docs fix pushed (dd7deb8). No open issues/PRs.

### 1. COPR build status — all 82 packages green

- No failed builds outstanding. Per-package newest-build summary across the
  full 790-build history: every registered package's NEWEST build is succeeded
  except the 3 Auto-Updater-triggered builds for this run's bumps, all of which
  reached succeeded this run (verified via COPR API):
  - rootapp 0.9.127-1, build 10882552: **succeeded** all 4 chroots.
  - helium-browser 0.15.6.1-1, build 10882553: **succeeded** all 4 chroots.
  - noctalia-git 5.0.0^20260819233856git21a272f-1, build 10882554:
    **succeeded** all 4 chroots (58 min; new NVR present in all 4 chroot
    repo listings, incl. debuginfo/debugsource).
- Historical 108762xx failure wave (2026-08-18 COPR SRPM-import outage) remains
  fully superseded by newer succeeded builds of the same specs — no unresolved
  failures.

### 2. Version accuracy sweep — 82/82 verified, 0 stale

Re-verified every package against upstream (gh api releases/tags, git
ls-remote, curl -sI Source0, artifact tear-down). Full table in final summary.
All 82 current. This run's fresh-checker re-verification of the 3 bumped specs:
- helium-browser 0.15.6.1: latest upstream tag == 0.15.6.1 (imputnet/helium-linux
  tags: 0.15.6.1, 0.15.5.1, ...). Tarball downloaded (125 MB, 200),
  extracted, binary runs: `Helium 0.15.6.1 (Chromium 151.0.7922.169)`.
- rootapp 0.9.127: AppImage re-downloaded; sha256
  2c54d9642f6a8477518105f335aea3ef884d9fca318c847f27e6d33340c1dc9b == spec pin.
- noctalia-git 21a272f: `git ls-remote noctalia-dev/noctalia HEAD` ==
  21a272f10ca157d11fa7acc75c2bc0a7cfd7eb23 == spec commit.
- localsend still correctly 1.18.0 (1.18.1 ships only Android assets; .deb 404);
  hyprland-plugins still intentionally pinned to hyprpm commit for hyprland
  0.56.2; all -git packages at current upstream HEAD.

### 3. Install-verification sweep — changed binaries + broad re-coverage

All tests from fresh fc44 containers against the COPR repo (gpgcheck=0,
--disablerepo=fedora-cisco-openh264 on rawhide/fc45), rpm -V clean unless noted.
- rootapp 0.9.127-1: installs clean fc44/fc45/fc46; ldd of extracted AppImage
  ELF: 0 unresolved; launches under xvfb (exit 0).
- helium-browser 0.15.6.1-1: installs clean fc44/fc45/fc46;
  `helium-wrapper --version` -> "Helium 0.15.6.1 (Chromium 151.0.7922.169)".
- noctalia-git 21a272f-1: installs clean fc44 and fc45;
  `noctalia --version` -> "noctalia v5.0.0 (21a272f)"; ldd 0 unresolved.
- Broad re-coverage this run (all install clean, exit-0 smoke):
  hyprland 0.56.2, hyprlock/hyprpaper/hyprpicker/hyprcursor-util/hypridle/
  hyprpolkitagent/hyprsysteminfo, hyprwayland-scanner-devel,
  aquamarine/hyprgraphics/hyprlang/hyprutils/hyprtoolkit/hyprqt6engine/
  hyprland-qt-support/xdg-desktop-portal-hyprland/hyprland-contrib/
  hyprland-guiutils/hyprland-plugins, scenefx/wlroots/ly, niri-git
  (26.04 b0eb8ad), xwayland-satellite-git, quickshell-git, noctalia-greeter,
  aylurs-gtk-shell 3.1.2 + astal* (a16a08c), ags 3.1.0, gpu-screen-recorder
  6.0.1, ghostty 1.3.1, zen-browser (Electron sandbox, --no-sandbox),
  stoat-desktop 1.5.3 (xvfb), freebuff 0.0.66, fluxer, vesktop 1.6.7,
  localsend 1.18.0, obsidian 1.13.7, logseq 2.0.1, protonplus 0.6.4,
  heroic 2.22.1, opencode-desktop 1.18.18, cliphist/hellwal/hyprdim/hyprnome,
  pyprland, matugen 4.2.0, starship 1.26.0, glaze (header-only), libcava
  (lib, no bin), mangowm (mango, ldd clean), mpvpaper, hyprsunset, nwg-look,
  waypaper, extension-manager, awww 0.12.1, app2unit, appmenu-glib-translator,
  hyprpwcenter, hyprshot, hyprsunset, hyprwire, lazyvim-git, caelestia-*,
  python3-materialyoucolor 3.0.4 (imports), bibata-cursor-theme,
  cascadia-code-nerd-fonts, material-symbols-fonts.

### 4. Deep dependency audit — all changed/prebuilt packages self-contained

- helium-browser 0.15.6.1 (prebuilt Chromium): tarball-extracted binaries run
  directly; installed `helium` ldd 0 unresolved; wrapper + chrome all
  self-contained. Nothing to optimize (upstream prebuilt).
- rootapp 0.9.127 (prebuilt AppImage): torn apart (unsquashfs); sq.version ==
  0.9.127, X-AppImage-Version=0.9.127; installed binary ldd 0 unresolved.
- noctalia-git 21a272f (source-built, meson): meson.build deps re-derived at
  new commit — every meson dependency maps to a spec BuildRequire (epoxy is
  only a fallback when egl/glesv2/wayland-egl are absent; spec provides all
  three). Installed ldd 0 unresolved.
- No missing Requires discovered across the 82-package install sweep; dnf
  resolved every transaction cleanly.

### 5. Docs / README health

- Pushed dd7deb8: removed stale note that hyprlauncher/hyprshutdown were
  "pending COPR registration" (both registered; builds 10880897/10880906
  succeeded). README otherwise accurate: 82-package table, "Supported
  releases: Fedora 43, 44, 45 and Rawhide".
- COPR project has 4 chroots: fedora-rawhide-x86_64, fedora-43-x86_64,
  fedora-44-x86_64, fedora-45-x86_64. Repo dirs == COPR packages 1:1 (82).

### 6. Open issues / PRs

- No open issues, no open PRs. (Prior #14/#15 closed by owner.)

### 7. GitHub Actions health

- Fedora Nexus Auto-Updater (d6fccc4, 01:40Z): success; its 3 bumped specs
  produced builds 10882552/53/54, all succeeded.
- opencode-schedule 01:47Z (this run): completed success (cleanup phase).
- No workflow faults; no changes to .github/workflows/ needed or made.

### 8. Handoff

- This run is complete: all 82 packages green; 3 changed binaries
  (helium-browser 0.15.6.1, rootapp 0.9.127, noctalia-git 21a272f)
  install-tested in fresh containers from the COPR repo with fresh-checker
  re-verification against upstream; README docs fix dd7deb8 pushed; repo in
  sync with origin (HEAD == origin/main, clean tree).
- No owner actions pending. (Cosmetic: COPR project description still omits
  Fedora 45 — owner may update via COPR web UI.)