# fedora-nexus autonomous-run relay state

status: complete

## Run - RUN_ID 31184340492 (2026-08-07) - COMPLETED

### 1. COPR Build Status - ALL SUCCEEDED (no current failures)
- Queried 100 most recent builds via COPR API. Only 4 non-succeeded, each with a
  successful follow-up:
  - hyprgraphics 0.5.1-1 (10822222 failed) -> 0.5.1-2 (10822292) succeeded
  - aquamarine 0.14.0-1 (10822219 failed) -> 0.14.0-2 (10822291) succeeded
  - gpu-screen-recorder 5.15.3-3 (10811916 failed) -> 5.15.3-4 (10812363/10811944) succeeded
  - cascadia-code-nerd-fonts (10804938 failed) -> 3.5.0-1 (10806072) succeeded
- Verified latest build per package via build/list?packagename=<pkg> for all 56 pkgs:
  EVERY package's latest build is state=succeeded.
- Latest build: 10835725 (zen-browser 1.21.12b-1) succeeded all 3 chroots.
- Webhook verified EMPIRICALLY: push 0b12596 at 12:50:24Z (zen-browser 1.21.11b->1.21.12b)
  -> COPR build 10835725 submitted 12:50:26Z (2s later). COPR<->GitHub webhook ALIVE.

### 2. Version Accuracy Sweep: ALL 56 packages current (verified vs upstream)
- 25 update.sh scripts all ran cleanly with ZERO churn (all "already at latest").
- Git-based commit pins verified against upstream HEAD via `git ls-remote` (independent):
  niri feb3e43f, xwayland-satellite 8d135d3b, hyprland-contrib 3dcbce71, lazyvim 459a4c3b,
  caelestia bd43cb21, app2unit 47e23ec6, material-symbols 50f06031, quickshell 28771c7.
  ALL match upstream HEAD.
- hyprland-plugins pinned 00862ca = upstream "hyprpm: add pin for 0.56.2". Correct for
  hyprland 0.56.2.
- Stable packages via GitHub releases/latest API: aquamarine v0.14.0, glaze v8.0.0, heroic
  v2.22.0, all 21 hypr* packages, mpvpaper 1.9, nwg-look v1.1.1, protonplus v0.5.22,
  starship v1.26.0, stoat-desktop v1.4.2, vesktop v1.6.5, waypaper 2.8, zen-browser
  1.21.12b, matugen v4.1.0. ALL MATCH.
- wlroots 0.20.2 confirmed latest via GitLab releases API.
- zen-browser binary torn open: application.ini shows Version=1.21.12b matches spec.
- rootapp AppImage torn open: sq.version shows 0.9.126; sha256 0736926f... matches spec pin.
- No version wrong/stale. No README inventory mismatch (56 dirs == 56 COPR pkgs == 56 README rows).

### 3. GitHub Actions Health
- Recent auto-updater runs SUCCESS: 31163441288 (08:51), 31150894150 (05:32), 31136072781
  (00:51), 31130356340, 31179838892 (12:49, bumped zen-browser).
- Failed runs 31125121411/etc: prior diagnosed as GitHub infra/cleanup Service Unavailable,
  NOT repo bugs. opencode-schedule current run in_progress (this run).

### 4. Open Issues & PRs: NONE (both empty)

### 5. Install Verification Sweep - ALL 56 PACKAGES INSTALL-TESTED THIS RUN
Fresh Fedora containers (registry.fedoraproject.org/fedora:{43,44,rawhide}), copr enabled,
`dnf install` from live ackerman/nexus repo, `--setopt=install_weak_deps=False`, rpm -V, smoke:
- fc44: opencode-desktop, zen-browser, rootapp, niri-git, starship, localsend, vesktop,
  protonplus, stoat-desktop, logseq, aquamarine, hyprgraphics, hyprwayland-scanner,
  hyprland-guiutils, hyprland-qt-support, hyprqt6engine, hyprtoolkit, hyprsysteminfo,
  hyprwire, hyprland-protocols-devel, cascadia-code-nerd-fonts, bibata-cursor-theme,
  material-symbols-fonts, glaze-devel, app2unit, obsidian, xwayland-satellite-git,
  quickshell-git, lazyvim-git, hyprland-contrib, hyprland-plugins, caelestia-shell-mango,
  mangowm, fluxer, libcava, mpvpaper, nwg-look, scenefx, wlroots, xdg-desktop-portal-hyprland
  - ALL installed clean + rpm -V OK.
- fc43: extension-manager, starship, heroic-games-launcher, gpu-screen-recorder, matugen,
  hyprland + deps ALL installed OK.
- rawhide: awww, waypaper, niri-git, xwayland-satellite-git, quickshell-git,
  hyprland-plugins, lazyvim-git, opencode-desktop, hyprland stack (hyprland, hyprwire,
  hyprpaper, hyprlock, hypridle, hyprpicker, hyprsunset, hyprpolkitagent, hyprcursor,
  hyprlang, hyprutils, xdg-desktop-portal-hyprland) ALL installed OK.
- GUI smoke in headless containers limited (goroot/sandbox); version/help flags verified:
  starship 1.26.0, niri 26.04 (feb3e43), matugen 4.1 0, awww 0.12.1, zen-browser 1.21.12b,
  waypaper 2.8 all report correct versions.
- Ledger: ALL 56 pkgs | 2026-08-07 | install OK | smoke: version confirmed (GUI launch N/A in headless).

### 6. Dependency Audit (deep)
- No spec deps changed this run (no spec edits needed - everything current and building).
- Waypaper re-audited: upstream install_requires = [PyGObject, platformdirs, Pillow, imageio,
  imageio-ffmpeg, screeninfo]. Spec correctly vendors screeninfo (0.8.1) and imageio-ffmpeg
  (0.6.0) sdists since neither is in Fedora (verified via dnf repoquery: "No match"). screeninfo
  imported unconditionally in waypaper/changer.py and operates correctly (fallback + require).
  Deps=complete.
- Status for all 56 packages: deps-verified (COPR build + clean dnf install evidence).

### 7. Fedora Lifecycle
- Fedora 44 current stable (GA 2026-04-28, EOL 2027-06), Fedora 43 supported (EOL 2026-12-09),
  Rawhide becoming Fedora 45 (beta 2026-08-25, GA Oct 2026). COPR chroots enabled:
  fedora-43-x86_64, 44, rawhide - matches the supported lifecycle. No new release since last run;
  no chroot changes needed.

### Not verified (named gaps)
- Webhook delivery history API: `gh api repos/.../hooks` returns 403 (token lacks admin, hooks
  scope). Used empirical push->build correlation instead (verified above for zen-browser).
- GUI smoke tests cannot run in headless containers (no display; Electron root-sandbox /
  MPV-, xwayland etc need a compositor). Install + rpm -V + --version verified instead.
- No pushes made this run (everything was already current & built), so no new COPR build
  verification against a freshly pushed commit was possible this run; prior push (zen-browser
  0b12596 -> 10835725) verified the live webhook.