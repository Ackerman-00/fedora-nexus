# opencode relay state

status: complete

## Run summary (2026-08-03 evening, scheduled run)
Full verification run. All work complete; no fixes required.

## Completed this run
- COPR health: last 100 builds checked. Only 3 failures, all superseded by
  succeeded builds: cascadia #10804938 (-> #10806072 3.5.0 OK), heroic #10788404
  (-> #10805731/#10805817 2.22.0-2/-3 OK), libcava #10755051 (-> #10788465
  1.0.0 OK). No unresolved failures.
- Full version sweep (26/26): all up-to-date.
  * GitHub releases (via gh api + websearch): mangowm 0.15.6, matugen v4.1.0,
    nwg-look v1.1.1, starship v1.26.0, heroic v2.22.0, localsend v1.17.0,
    obsidian v1.13.4, opencode v1.18.11, vesktop v1.6.5, scenefx 0.5,
    libcava 1.0.0, zen 1.21.10b, protonplus v0.5.22, nerd-fonts v3.5.0,
    extension-manager v0.6.5, bibata v2.0.7. All match specs.
  * Git snapshots match upstream HEAD (git ls-remote): niri feb3e43,
    xwayland-satellite 8d135d3, app2unit 47e23ec, caelestia bd43cb2,
    material-symbols 50f0603, quickshell 28771c7. All identical to spec pins.
  * Non-GitHub: fluxer X-Fluxer-Version header = 2026.731.153836 (matches
    spec); gpu-screen-recorder 5.15.3 confirmed inside source tarball
    (meson.build project version) and matches Arch latest; wlroots 0.20.2 is
    latest 0.20.x tag; rootapp 0.9.125 confirmed from sq.version INSIDE the
    AppImage, sha256 86ed0d76... matches spec pin.
  * All 26 Source0 URLs return 200 (curl -sIL). matugen crates.io download
    needs a browser UA (crates.io data-access policy blocks default curl UA on
    datacenter IPs); COPR downloads fine. Not a spec issue.
- Install-verification sweep (7 packages, fresh fedora:44 container, all
  INSTALL OK + rpm -V OK): libcava 1.0.0, xwayland-satellite-git, quickshell-git,
  mangowm (binary runs), obsidian 1.13.4-3 (ldd all resolved, mesa-libgbm
  pulled in), wlroots 0.20.2, caelestia-shell-mango (full dep chain installs).
  These were the packages NOT spot-tested last run; combined with last run's 22,
  whole repo now covered.
- Dependency deep audit (this run): mangowm meson.build deps (libdrm, xcb,
  xcb-icccm, wayland-server>=1.23.1, wlroots-0.20, xkbcommon, libinput>=1.27.1,
  wayland-client, libpcre2-8, scenefx-0.5>=0.5.0, pixman-1, libcjson,
  pangocairo) all present as BuildRequires. scenefx meson deps match spec.
  matugen crate is pure Rust (no system libs, no build.rs C); cargo/rust/
  gcc-c++ suffice. obsidian deb Depends (gtk3, nss, libxss1->libXScrnSaver,
  libxtst6->libXtst, xdg-utils, atspi, libuuid, libsecret, libnotify) all
  mapped in spec. localsend deb Depends (ayatana appindicator/ido, xdg-user-dirs)
  all mapped.
- GitHub Actions: last 12 runs all green (Auto-Updater + opencode-schedule).
- 0 open issues, 0 open PRs.
- All 20 update.sh pass bash -n; guards are pure version/commit comparison.
  README table matches repo dirs 26/26. Working tree clean.

## No issues/PRs to close
- 0 open issues, 0 open PRs.

## COPR builds verified
- No new builds triggered this run (nothing was outdated).
