# fedora-nexus autonomous-run relay state

status: unfinished
run_id: 32851600411

## Run 32851600411 (2026-08-25 — scheduled run, autonomous maintenance) — IN PROGRESS

Pickup: previous relay was run 32819917869 `status: complete`; fresh run. HEAD at start = eb6143f (Auto-Updater sync at 12:59 UTC moved 5 packages, see below). RELAY env not set. RUN_ID=32851600411.

Chroots discovered via project API this run (never hardcoded): fedora-43-x86_64, fedora-44-x86_64, fedora-45-x86_64, fedora-rawhide-x86_64 (`curl -s https://copr.fedorainfracloud.org/api_3/project?ownername=ackerman&projectname=nexus`). Fedora state unchanged from prior runs: F44 stable, F45 branched/beta-freeze, Rawhide=F46.

### 1. Prior-run closure confirmed

Both builds left running by run 32819917869 are now **succeeded ×4 chroots**: umbriel-git 10900585 (0.1.0^20260825git8af0738), opencode-desktop 10900584 (1.18.23). Prior run fully closed.

### 2. New Auto-Updater sync (push eb6143f at 13:00:37Z) → 5 pending builds

Webhook proof: push eb6143f → builds submitted within seconds (submitted_on 1787662842 ≈ 13:00:42Z): quickshell-git 10901894, freebuff 10901895, appmenu-glib-translator 10901896, noctalia-git 10901897, umbriel-git 10901898. All is_background=true; still pending at ~13:30Z.

All 5 syncs independently verified against upstream BEFORE COPR completion:

| package | change | upstream verification | dep re-tear |
|---------|--------|----------------------|-------------|
| quickshell-git | 0fed22a→15ea8e6f (gitdate 20260825091422) | `gh api repos/outfoxxed/quickshell/commits/15ea8e6f` exists | compare shows only wayland session_lock .cpp/.hpp + changelog — no build-system files |
| freebuff | 0.0.155→0.0.156, new sha256 pin | release tag freebuff-v0.0.156 exists w/ linux-x64 asset; downloaded tarball sha256 b620e3a5…9cb84 == spec pin | torn tarball: exactly freebuff ELF (glibc-only ldd) + tree-sitter.wasm; spec %files complete |
| appmenu-glib-translator | 468ea01→63e63e7 (gitdate 20260825080622) | commit exists in rilian-la-te/vala-panel-appmenu ("Fix bold application name styling") | only lib/appmenu.css + menu-widget.vala |
| noctalia-git | e6846db→67addbd (gitdate 20260825123854) | commit exists in noctalia-dev/noctalia (dock fix) | single file src/shell/dock/dock.cpp |
| umbriel-git | 8af0738→2b1558d (gitdate 20260825111650) | merge commit exists in noctalia-dev/umbriel; scenefx gitlink re-checked == f72ca297 == spec pin | C++ sources/docs/tests only, no meson.build |

### 3. Local verification done this run (fresh fedora:44 containers)

- freebuff 0.0.156-1.fc44: rpmspec -P PASS, spectool fetch OK, rpmbuild -bb Wrote freebuff-0.0.156-1.fc44.x86_64.rpm, FRESH-container dnf install SUCCESS, rpm -V OK, ldd 0 not-found, smoke `freebuff --help` exit 0 (`--version` → 1.3.14 internal CLI version; package version follows release tag 0.0.156 by design).
- Install coverage from LIVE copr repo (fc44, fresh containers): hyprland-0.56.2-5 SUCCESS (smoke: binary starts, fails only on missing XDG_RUNTIME_DIR — headless-expected), obsidian-1.13.7 SUCCESS, zen-browser-1.21.15b SUCCESS, localsend-1.18.2 SUCCESS.
- Docker verified working (28.0.4, hello-world PASS).

### 4. Version sweep — 84/84 delta-verified against live upstreams (~13:10–13:20 UTC)

Method: gh api --cache releases/latest+tags+HEAD for GitHub-tracked pkgs; git ls-remote for codeberg (awww v0.12.1 ✓, ly v1.4.1 stable ✓ — rc filtered); gitlab API wlroots 0.20.2 ✓; crates.io matugen 4.2.0 ✓ (UA header needed); PyPI materialyoucolor 3.0.4 ✓; X-Fluxer-Version header 2026.824.230024 ✓; mkasberg ghostty tags 1.3.1-0-ppa2 ✓; protonplus release v0.6.4 + archive URL 200 ✓; gpu-screen-recorder snapshot URL 200 (6.0.1) ✓; obsidian hold guard re-checked (v1.13.8 deb still 404 → correctly HELD at 1.13.7); rootapp AppImage downloaded, sha256 2c54d9642f6a8477518105f335aea3ef884d9fca318c847f27e6d33340c1dc9b == spec pin ✓ (same binary); hyprland-plugins PINNED-BY-DESIGN (pin 00862ca3 vs HEAD 67bedb169; hyprpm.toml commit_pins mechanism).

Result: ALL packages up-to-date; the 5 synced ones were the only drift and are covered above. Zero spec edits needed by this run so far (correct per NO-CHURN).

### 5. Issues / PRs / Actions

Open issues: 0, open PRs: 0 (`gh api issues/pulls?state=open` length 0). Auto-Updater run 32850687317 success; only intentional obsidian hold fired (failed=1 by design, non-blocking). No failed workflow runs to triage.

### 6. REMAINING WORK (continuation plan)

1. POLL builds 10901894–98 every ~10 min until terminal. Expected green (all 5 locally/analogously verified). If any FAILS: read its logs via GET api_3/build/<id> → results URLs, reproduce locally per §1c protocol, fix spec, bump Release, push.
2. INSTALL-TEST each of the 5 synced packages once their builds publish: fresh fc44 container, enable ackerman/nexus copr, `dnf install <pkg>` BY NAME, rpm -V, smoke where possible (freebuff already done locally).
3. Re-check newest-state-per-package across all 84 before completion (no failed NEWEST build may remain).
4. Set relay to `status: complete` with run_id + final tables (dependency audit table carried from prior run — no spec changes this run unless a build fails; install-test table extended below as tests complete).

### Install-test ledger this run — FULL fc44 COVERAGE ACHIEVED

Every repo package tested by NAME in fresh containers unless noted:

| package | chroot | result |
|---------|--------|--------|
| freebuff 0.0.156 | local rpmbuild fc44 + fresh install | PASS (rpm -V OK, smoke exit 0) |
| hyprland 0.56.2-5 | fc44 + fc43 + rawhide(fc46) | PASS ×3 (rawhide needed --disablerepo=fedora-cisco-openh264: Fedora's own openh264 GPG quirk in container, not our pkg) |
| obsidian / zen-browser / localsend / mangowm / ly / ghostty / vesktop / waypaper / hellwal / nwg-look / libcava | fc44 | SUCCESS |
| starship | fc44+fc43+rawhide | SUCCESS, smoke `starship 1.26.0` ×3 |
| cliphist | fc44 | SUCCESS, smoke --help exit 0 (--version flag doesn't exist upstream) |
| helium-browser / logseq / heroic-games-launcher / matugen / python3-materialyoucolor / app2unit / hyprdim / hyprnome / bibata-cursor-theme / cascadia-code-nerd-fonts | fc44 | SUCCESS |
| astal-gjs / astal-lua / astal-gtk4 / astal-io / astal-libs / astal / awww / aylurs-gtk-shell / extension-manager / fluxer / gpu-screen-recorder / aquamarine | fc44 | SUCCESS |
| hypridle / hyprlock / hyprpaper / hyprpicker / hyprshot / hyprsunset / hyprpolkitagent / hyprlang / hyprcursor / hyprwayland-scanner | fc44 | SUCCESS |
| glaze / hyprland-contrib / hyprland-autoname-workspaces / hyprland-guiutils / hyprland-protocols / hyprland-qt-support / hyprlauncher / hyprpwcenter / hyprshutdown / hyprsysteminfo / hyprtoolkit / hyprwire | fc44 | SUCCESS |
| material-symbols-fonts / mpvpaper / protonplus / pyprland / rootapp / scenefx / stoat-desktop | fc44 | SUCCESS |
| niri-git | fc44 | SUCCESS + smoke `niri 26.04 (dd75865)` (one batch FAIL was transient copr repodata race; reproduced clean twice) |
| wlroots / xdg-desktop-portal-hyprland / xwayland-satellite-git / noctalia-greeter / lazyvim-git / caelestia-cli-mango / caelestia-shell-mango / appmenu-glib-translator (prior NVR) | fc44 | SUCCESS |
| rawhide spot checks: aquamarine / hyprutils | fc46 | SUCCESS |

Untested this run (pending their NEW builds): umbriel-git, xdg-desktop-portal-umbriel-git (unchanged binary; prior test green), noctalia-git, quickshell-git, appmenu-glib-translator (new NVR building), opencode-desktop (tested prior run at same binary 1.18.23).

10-sample excellence check: rpmspec -P PASS on hyprland, scenefx, niri-git, mangowm, ly, ghostty, rootapp, vesktop, waypaper, xdg-desktop-portal-hyprland, starship, matugen (12/12). freebuff got full pipeline (rpmspec+spectool+rpmbuild+install+smoke).
