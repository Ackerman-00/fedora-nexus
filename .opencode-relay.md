# fedora-nexus autonomous-run relay state

status: unfinished
run_id: PENDING (will be 32729546702 at completion)

## Run 32729546702 (2026-08-24 — scheduled run, autonomous maintenance) — IN PROGRESS

Pickup: previous relay (run 32706975320) was `status: complete`; nothing to resume.
RELAY env not set → fresh scheduled run.

### Done so far (all evidence-backed)

1. Recon: COPR chroots = fedora-{43,44,45,rawhide}-x86_64 (API). COPR build list
   (500 newest): ALL 84 packages' newest builds succeeded. Inventory: 84 repo dirs ==
   84 COPR package records == README (materialyoucolor↔python3-materialyoucolor name
   mapping). Issues open: none. PRs open: none. Actions: all recent runs success.
2. Version sweep 84/84 via authenticated gh api + codeberg/gitlab/PyPI/headers/cgit:
   all up-to-date EXCEPT three -git packages (noctalia-git a4324409→0f61b0ae,
   umbriel-git dd8b3565→4f9db98b, xdg-desktop-portal-umbriel-git 9afc9d7c→e5fd52a8).
   Specials re-verified: ghostty 1.3.1-0-ppa2 ✓, obsidian v1.13.8 still APK-only
   (HOLD correct), rootapp AppImage unchanged (size/mtime identical to sha256-verified
   teardown), gsr meson.build@master=6.0.1 ✓, fluxer header 2026.821.183746 ✓,
   wlroots 0.20.2 / awww v0.12.1 / ly v1.4.1 / materialyoucolor 3.0.4 ✓.
3. Re-torn all three new upstream tarballs: portal e5fd52a has same vendor-json.hpp
   pattern (%prep sed applies unchanged) + same meson dep set; umbriel 4f9db98 same
   dep set; noctalia range = 1 commit (pipewire_service.cpp only, no dep change;
   epoxy is only a fallback behind egl/glesv2 already BR'd).
4. RACE HANDLED: Auto-Updater push e78fc0a landed mid-run with IDENTICAL syncs for
   portal/noctalia (+ helium-browser 0.15.6.1→0.15.7.1 prerelease with full assets —
   verified real upstream release; updater tracks newest tag by design). NO-CHURN:
   did not re-push my duplicate portal/noctalia commits.
5. BUG FOUND+FIXED (commit d239329): upstream umbriel@4f9db98 forgot to bump its
   subprojects/scenefx gitlink → pinned 1d5ce45 lacks wlr_scene_blur_set_ignore_alpha /
   wlr_scene_tree_set_clip / wlr_scene_output_set_sdr_white_level that umbriel's
   meson.build requires ("SceneFX submodule must provide Umbriel patched APIs" —
   reproduced locally in fedora:44 BEFORE fixing). update.sh now API-checks the
   gitlink tree and falls back to scenefx umbriel-branch HEAD (dcf4614b).
   Local rpmbuild -bb green ×2 packages (portal + umbriel); rpmlint 0 errors.
6. Webhook proof: push d239329 → new build 10897241 appeared → SUCCEEDED (NVR
   0.1.0^20260824125130git4f9db98-1, aggregate state succeeded = all 4 chroots green).
7. Install tests (fresh containers): local-built fc44 RPMs install clean (rpm -V,
   ldd, umbriel --version exit 0); COPR artifacts by NAME on fc44 (umbriel-git +
   portal + helium 0.15.7.1 V-OK) and fc45 (umbriel LDD-CLEAN).

### Remaining plan (continue here)

a. Wait for noctalia-git build 10897175 (running, big ~900-unit build); verify it
   goes green; if failed → read logs, fix, repush (cap 3 attempts).
b. Install-test noctalia-git artifact from COPR in fresh fc44 container once green.
c. Fresh-checker pass: re-run one independent install check of the fixed umbriel-git.
d. Dependency audit table + version table for final relay; run tools/verify-fedora.sh;
   set status complete + run_id: <RUN_ID env>; commit + push.
