# fedora-nexus autonomous-run relay state

status: complete
run_id: 31268406907

## Run - RUN_ID 31268406907 (2026-08-08) - COMPLETED

### 0. Pickup status
- Previous run (31259510810) declared `status: complete`. RELAY=false, so no
  relay continuation work. This run re-verified everything as a fresh checker.
  HEAD on main at pickup: edae9e9. Working tree was clean.

### 1. COPR Build Status
- Chroots discovered via COPR API (authoritative):
  **fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64**.
  fedora-45-x86_64 does NOT exist yet in COPR (F45 mass-branching 2026-08-11).
  Lifecycle: F44 = current stable (GA 2026-04-28, support to 2027-06-02),
  F43 supported until 2026-12-09, rawhide = F45 dev. No chroot enable/disable.
- All latest per-package builds succeeded for all 59 COPR package records
  (materialyoucolor = source-only base; python3-materialyoucolor succeeded).
  The 7 `failed` + 1 `canceled` builds in the recent window are all OLD and
  superseded by a later `succeeded` build for the same package (verified via
  per-package build/list).

### 2. NEW DEFECT FOUND & FIXED - caelestia cliphist hard-dependency
- Symptom: on fedora-43, `dnf install caelestia-cli-mango` failed with
  `nothing provides cliphist` (cli AND shell both hard-Require it).
- Root cause: cliphist is packaged in Fedora >= 44 only (koji: f44/f45,
  never f43). caelestia-cli-mango 2.0.0-2 and caelestia-shell-mango
  2.0.0-1 both had a bare `Requires: cliphist`, so both packages were
  uninstallable on F43. On F43, `dnf install caelestia-shell-mango`
  silently degraded to the OLD 1.0.0 NVR (masking the break).
- Fix (commit 965c9d1, pushed to main):
  - caelestia-cli-mango: `Requires: (cliphist if fedora-release >= 44)`,
    Release 2 -> 3, %changelog entry.
  - caelestia-shell-mango: same rich dep, Release 1 -> 2, %changelog entry.
- Verification:
  - Docker rebuild in clean fedora:43 (builddep + rpmbuild) RC=0 for both.
  - Clean fedora:43 install of both locally built RPMs: RC=0, rpm -V passes.
  - FRESH-CHECKER from live COPR (post-push builds): fc43, fc44, rawhide all
    `dnf install caelestia-cli-mango caelestia-shell-mango` -> Complete,
    RC=0, installed at the exact new NVRs, rpm -V clean. cliphist still
    pulled on fc44/rawhide (CLIPHIST-PRESENT), skipped on fc43.
- Webhook evidence (empirical): pre-push newest builds = 10838443
  (cli 2.0.0-2) / 10838456 (shell 2.0.0-1). After push NEW builds appeared:
  10838561 (cli 2.0.0-3, succeeded) / 10838560 (shell 2.0.0-2, succeeded).

### 3. Version Accuracy Sweep (100% of 56 packages re-verified)
- GitHub-release packages: `gh api releases/latest` tag == spec Version for
  all (extension-manager 0.6.5, glaze 8.0.0, mangowm 0.15.6, hyprland
  v0.56.2, all hypr-*, vesktop v1.6.5, localsend v1.17.0, logseq 2.0.1,
  bibata v2.0.7, obsidian v1.13.4, cascadia v3.5.0, heroic v2.22.0,
  starship v1.26.0, zen 1.21.12b, stoat v1.4.2, waypaper 2.8,
  opencode-desktop v1.18.15, scenefx 0.5, protonplus v0.5.22, aquamarine
  0.14.0, hyprutils 0.14.0, etc.).
- Git-pinned packages: `git ls-remote HEAD` == pinned spec commit for
  app2unit, caelestia-shell-mango, hyprland-contrib, lazyvim-git,
  material-symbols-fonts, niri-git, quickshell-git, xwayland-satellite-git.
  hyprland-plugins pinned 00862ca is a legitimate compatibility pin vs
  hyprland stable v0.56.2.
- caelestia-cli-mango: pinned commit 5c443b5 vs upstream HEAD 155b711
  (README-only 4-line change) - not a functional bump, skipped as no-churn.
  caelestia-shell-mango: upstream HEAD README-only movement left to the
  auto-updater (correct behavior, avoided churn rebuild).
- Non-GitHub upstreams: awww (codeberg) v0.12.1; wlroots (gitlab) 0.20.2;
  gpu-screen-recorder 6.0.0 (latest on cgit summary page); matugen
  (crates.io) 4.1.0; fluxer CDN `x-fluxer-version: 2026.731.153836` == spec;
  materialyoucolor pypi 3.0.4.
- Binary artifacts torn apart: rootapp AppImage sha256 == spec pin AND
  X-AppImage-Version=0.9.126 (via unsquashfs at ELF offset). All binary
  source URLs returned 200.
- Source-URL sweep: all `curl -sI` (macros expanded) return 200.

### 4. Spec / update.sh / Actions health
- `bash -n` on all 25 update.sh scripts: PASS.
- Dry-run of all update.sh in a throwaway clone: only
  caelestia-shell-mango.spec changed (upstream HEAD moved) - no other
  churn; no flawed up-to-date guards found.
- `rpmspec -P` over all specs: 0 parse failures.
- All recent Fedora Nexus Auto-Updater runs success; "Commit and Push"
  correctly skipped (no changes).

### 5. Install-Verification Sweep (fresh dnf install per name, all 3 chroots)
- 82 distinct rpm names derived via rpmspec -q over 56 specs.
- **fedora:44: 80/80 installable clean at the NEWEST NVR, 0 downgrades.**
- **fedora:43: 80/80 installable clean at the NEWEST NVR, 0 downgrades**
  (caelestia-cli/shell now at 2.0.0-3 / 2.0.0-2 - cliphist fix un-masked
  the silent downgrade).
- **fedora:rawhide: 80/80 installable clean at the NEWEST NVR.**
- The 2 "fails": glaze + hyprland-protocols are SOURCE-ONLY bases; dnf
  reports "Argument matches only source packages"; their real content is
  in -devel subpackages, both of which install cleanly on all 3 chroots.
- rpm -V passes on tested installs.

### 6. Dependency Audit
- Full-set fresh-install at newest NVRs on all 3 chroots proves every
  Requires resolves in the live COPR (no `nothing provides` after fix).
- cliphist: verified real Fedora package (F44+ only, koji f44/f45),
  rich dependency keeps F43 installable + F44/rawhide functional.
- Deep re-tear performed for caelestia-cli (cliphist usage in
  src/caelestia/subcommands/clipboard.py), rootapp (AppImage inner
  version), plus cliphist-in-Fedora verification.

### 7. Issues & PRs
- Open PRs: none. Open issues: none.

### 8. Gaps (stated)
- Webhook delivery-ids unreadable (token lacks admin:repo_hook). Webhook
  empirically alive (new COPR builds appeared after push).
- GUI apps not launched (headless runner). Install + rpm -V +
  version-from-artifact covered instead.
- caelestia-shell-mango README-only upstream commit not pushed (no-churn).
- Stale artifacts in rawhide metadata (noctalia-shell etc.): COPR-admin
  cleanup, out of repo scope, not user-facing.

### Closing note
Fixed 1 real user-facing bug (fedora-43 cliphist breakage), verified via
docker repro + live-COPR fresh-checker installs on all 3 chroots at exact
new NVRs, pushed as one atomic commit (965c9d1), webhook empirically
confirmed (new builds 10838561 / 10838560). All 82 rpm names installable
at newest NVR on every enabled chroot. No open issues/PRs. Repo clean.