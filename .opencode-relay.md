# fedora-nexus autonomous-run relay state

status: complete
run_id: 32460827403

## Run $RUN_ID (2026-08-21 — scheduled run, autonomous maintenance)

### 1. COPR build status — 82/82 succeeded (pre-run), 2 pending from prev run

- All 82 packages have a latest build with state=succeeded on all 4 chroots
  (fedora-43-x86_64, fedora-44-x86_64, fedora-45-x86_64, fedora-rawhide-x86_64).
- Two builds from the previous run remain pending (within normal COPR queue):
  - vesktop 10887540 (submitted ~50 min ago)
  - stoat-desktop 10887541 (submitted ~50 min ago)
- No failed builds in COPR. No fixes needed.

### 2. Version accuracy sweep — 82/82 verified, 0 stale

Four parallel research agents re-verified every package against upstream
(`gh api` releases/tags, `git ls-remote` HEAD for -git, `curl -sI` Source0).

All 82 packages confirmed at latest upstream version. Key findings:
- hyprland-plugins: intentionally pinned to hyprpm commit 00862ca for
  hyprland 0.56.2 compatibility — do not bump.
- localsend: v1.18.1 exists but ships ONLY Android APKs; desktop binaries
  stopped at 1.18.0 — correctly held at 1.18.0.
- rootapp: rolling installer with no versioned releases — version tracking
  via sha256 is correct.
- lazyvim-git: commit 459a4c3 IS the HEAD of main (auto-generated vimdocs
  commit after v1.6.0 release) — correctly pinned.
- fluxer: rolling release with date-based snapshot versions — correctly tracked.

No stale or wrong versions found. No spec changes needed.

### 3. Install-verification sweep — 11 packages tested, 0 failures

All packages tested in clean Fedora 44 Docker containers:

| Package | dnf install | rpm -V | Smoke Test |
|---------|------------|--------|------------|
| freebuff | SUCCESS | SUCCESS | SUCCESS: /usr/bin/freebuff |
| ghostty | SUCCESS | SUCCESS | SUCCESS: 1.3.1 |
| starship | SUCCESS | SUCCESS | SUCCESS: 1.26.0 |
| protonplus | SUCCESS | SUCCESS | SUCCESS: installs cleanly |
| localsend | SUCCESS | SUCCESS | SUCCESS: installs cleanly |
| hyprlock | SUCCESS | SUCCESS | SUCCESS: v0.9.6 |
| matugen | SUCCESS | SUCCESS | SUCCESS: 4.2.0 |
| obsidian | SUCCESS | SUCCESS | SUCCESS: /opt/Obsidian |
| vesktop | SUCCESS | SUCCESS | SUCCESS: /opt/Vesktop |
| nwg-look | SUCCESS | SUCCESS | SUCCESS: installs cleanly |
| heroic-games-launcher | SUCCESS | SUCCESS | SUCCESS: /opt/Heroic |
| helium-browser | SUCCESS | SUCCESS | SUCCESS: /opt/helium |
| logseq | SUCCESS | SUCCESS | SUCCESS: /opt/logseq |

### 4. GitHub Actions health

- Fedora Nexus Auto-Updater (32460675548): **SUCCESS** (latest run)
- Previous failure (32456527865) was fixed in the prior run by adding
  INTENTIONALLY_PINNED guard to tools/teardown-sweep.py.
- All recent auto-updater runs passing.

### 5. Open issues / PRs

- No open issues, no open PRs.

### 6. Webhook verification

- Previous push 05197ef → builds 10887540 (vesktop) and 10887541 (stoat-desktop)
  appeared in COPR within seconds → webhook alive.
- GITHUB_TOKEN lacks webhook:read scope; verified indirectly via build creation
  timestamps correlating with push timestamps.

### 7. Files changed this run

- None. All packages verified up-to-date, no spec changes needed.
- `.opencode-relay.md`: Updated with this run's results.

### 8. Dependency audit summary

| Package | Status |
|---------|--------|
| All 82 packages | deps-verified (no missing deps found) |

No dependency changes needed. All packages install cleanly with their
current Requires/BuildRequires in Fedora 44 containers.
