# fedora-nexus autonomous-run relay state

status: complete

## Run completed (2026-08-06 15:20 UTC) - RUN_ID 31112339114

### 1. COPR Build Status: ALL PASSING
- No currently-failing builds. 4 historical failures (10822219 aquamarine, 10822222
  hyprgraphics, 10811916 gpu-screen-recorder, 10804938 cascadia-code-nerd-fonts) all
  superseded by later successful builds.
- Fixed this run: **waypaper 2.8-7** (build 10832723, succeeded on all chroots).

### 2. Version Accuracy Sweep: ALL 55 packages at latest upstream
Verified via gh api releases/latest + tags, codeberg/gitlab APIs, and direct URL
HEAD checks. All 55 current.

### 3. GitHub Actions Health: PASSING
- Last "Fedora Nexus Auto-Updater" run 2026-08-06T13:50:52Z success. No failed runs.

### 4. Open Issues & PRs: NONE (0 open issues, 0 open PRs)

### 5. Install Verification Sweep (ALL 55 packages, ALL 3 enabled chroots)
| Chroot | Result |
|--------|--------|
| fedora-43-x86_64 | all 55 install, RC=0 |
| fedora-44-x86_64 | all 55 install, RC=0 |
| fedora-rawhide-x86_64 | all 55 install, RC=0 |
- Per-package runtime smoke: awww 0.12.1, starship 1.26.0, mpvpaper, app2unit,
  hyprpicker/sunset/idle/lock/paper, extension-manager, waypaper all run. GUI-only
  apps (logseq, obsidian, vesktop, zen, fluxer, stoat, rootapp, opencode-desktop,
  heroic, localsend, nwg-look, protonplus) launch to the display stage and report
  "no DISPLAY" (headless container) with ZERO missing shared libraries (ldd clean
  for all ELF binaries; zen's "missing" libs are its own bundled moz libs present
  in /opt/zen).
- **waypaper 2.8-7 install test (fresh container)**: RC=0, pulls gobject-introspection,
  gi import of Gtk/Gdk succeeds (was ImportError xlib-2.0 before fix).

### 6. Fixes Applied This Run
1. **waypaper 2.8-6 -> 2.8-7** (commit 0b26c0e, build 10832723): add
   `Requires: gobject-introspection`. python3-gobject's gi import of Gdk needs
   `xlib-2.0.typelib`, owned by gobject-introspection. Before: waypaper crashed on
   launch "Typelib file for namespace 'xlib', version '2.0' not found"; after: installs
   and imports cleanly.

### 7. Fedora Lifecycle
- Current stable: Fedora 44 (GA Apr 28 2026, EOL Jun 2 2027)
- Previous stable: Fedora 43 (GA Oct 28 2025, EOL Dec 9 2026)
- Rawhide: Fedora 45 (development)
- COPR chroots enabled: fedora-43-x86_64, fedora-44-x86_64, fedora-rawhide-x86_64

### Not verified (named gaps)
- GUI apps full UI launch requires a display; verified only to the "no DISPLAY" stage
  (dependency resolution + no missing libs + electron/compositor binary starts).
- Webhook delivery API listing is 403 (token lacks admin hooks scope); empirical
  push->build correlation used instead (waypaper push -> build 10832723 in ~20s).
- Deep in-artifact version extraction for every binary package beyond rootapp (torn
  apart: X-AppImage-Version=0.9.126, sha256 matches spec) and zen (bundled libs
  verified): other binary packages' versions verified via upstream release tags +
  artifact download 200 + install/launch.
