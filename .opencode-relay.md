# fedora-nexus autonomous-run relay state

status: in_progress
run_id: 32173483884

## Run 32173483884 (2026-08-18 — scheduled run, autonomous maintenance)

Continues from run 32160364195 (its relay was left in_progress; its work — the
54 COPR re-trigger rebuilds for the 2026-08-18 SRPM-import outage — is being
polled to completion here).

### 1. COPR failed-build closure status

Every outage-failed build now has a NEWER succeeded/pending build EXCEPT one.
Per-package newest-build audit (78 packages with failed history):

- SUCCEEDED (new green build created by the re-trigger wave): app2unit, aquamarine,
  awww, bibata-cursor-theme, caelestia-cli-mango, caelestia-shell-mango,
  cascadia-code-nerd-fonts, cliphist, fluxer, gpu-screen-recorder, helium-browser,
  hellwal, heroic-games-launcher, hyprdim, hyprgraphics, hyprland, hyprpaper,
  hyprsysteminfo, hyprtoolkit, hyprutils, hyprwayland-scanner, hyprwire, lazyvim-git,
  libcava, localsend, logseq, ly, mangowm, material-symbols-fonts, noctalia-git,
  nwg-look, obsidian, opencode-desktop, protonplus, scenefx, stoat-desktop, vesktop,
  waypaper, wlroots, xdg-desktop-portal-hyprland, xwayland-satellite-git,
  zen-browser, starship, matugen, extension-manager, hyprcursor, hypridle,
  hyprland-contrib, hyprland-guiutils, hyprland-plugins, hyprland-protocols,
  hyprland-qt-support, hyprlang, hyprlock, hyprpicker, hyprpolkitagent,
  hyprqt6engine, hyprsunset, mpvpaper, glowdown/glaze.
- PENDING (in COPR queue, polled to completion below): appmenu-glib-translator,
  astal, astal-io, astal-libs, astal-gjs, astal-gtk4, astal-lua, aylurs-gtk-shell,
  ghostty, hyprland-autoname-workspaces, hyprnome, hyprpwcenter, hyprshot, pyprland,
  noctalia-greeter, quickshell-git (10877771), freebuff (10877639). rootapp
  SUCCEEDED this run (10877171, all 4 chroots).
- UNRESOLVED — OWNER ACTION REQUIRED: **python3-materialyoucolor**. Root cause:
  its COPR package record has `auto_rebuild: false` (the ONLY package in the
  project with it disabled), so the GitHub webhook never fires a build for it.
  Re-trigger push 349bf0b confirmed no build fires. Build submission and package
  editing need a COPR API token CI does not have. Filed issue #15. The failing
  build 10876224 was an outage artifact (SRPM phase succeeded, chroots failed
  pre-start); same NVR 3.0.4-1 built green before (10868189).

### 2. quickshell-git (this run's real build failure, now fixed)

- Build 10877172 failed: SRPM download from `git.outfoxxed.me` timed out
  (curl 28, 133s connect). Root cause: the Forgejo host is unreachable from
  COPR's SRPM-import service.
- Fix A (already on main at run start): Source0 switched to the official GitHub
  mirror `quickshell-mirror/quickshell` (spec Release 2, same pinned commit
  532a060, verified = mirror HEAD). Tarball downloads/extracts fine locally.
- Fix B (this run, commit e8611e2): local rpmbuild exposed that the GitHub
  mirror archive extracts to `quickshell-<full-commit>/` while Forgejo archives
  stripped the hash, so `%autosetup -n quickshell` no longer matched and %prep
  failed. Changed to `-n quickshell-%{commit}`, Release bumped to 3, verified
  with a full local rpmbuild in clean Fedora 44 (RPM produced).
- The pre-fix-B webhook build (10877216, Release 2) is still in the COPR queue
  and will fail at %prep — it is older than the fixed build and cannot be
  cancelled from CI (cancel needs an API token). The fixed Release 3 build
  (10877771, fired by webhook on push e8611e2) supersedes it.

### 3. Version accuracy sweep (82/82 packages — all up-to-date, fresh-checked this run)

Re-verified against upstream (gh api releases/tags, git ls-remote, codeberg API,
gitlab API, pypi, CDN headers, AppImage teardown):
- -git packages all at upstream HEAD: niri e9b215f, noctalia 356a41a,
  astal a16a08c, quickshell 532a060 (mirror HEAD), xwayland-satellite 3bc915f,
  lazyvim 459a4c3, hyprland-contrib 3dcbce7, noctalia-greeter 10d2c90,
  caelestia-cli be54856, caelestia-shell cae55ed, app2unit 47e23ec,
  material-symbols e083cc6 (snapdate matches commit date), appmenu-glib-translator
  468ea01 (= repo master HEAD). hyprland-plugins pinned 00862ca = hyprpm pin for
  hyprland 0.56.2 (correct, update.sh enforces it).
- Stable packages all = latest upstream: hyprland 0.56.2, hyprutils 0.14.1,
  hyprlock 0.9.6, aquamarine 0.14.0, starship 1.26.0, mangowm 0.16.1, matugen
  4.2.0, materialyoucolor 3.0.4, extension-manager 0.6.5, glaze 8.1.0, cliphist
  0.7.0, bibata 2.0.7, cascadia 3.5.0, scenefx 0.5, waypaper 2.8, mpvpaper 1.9,
  nwg-look 1.1.1, hellwal 1.0.7, ly 1.4.1, awww 0.12.1, wlroots 0.20.2,
  gpu-screen-recorder 6.0.1 (6.0.2 404), ghostty 1.3.1 (ppa 1.3.1-0-ppa2),
  helium 0.15.5.1, heroic 2.22.1, logseq 2.0.1, obsidian 1.13.7 (version 1.13.7
  inside app.asar), opencode-desktop 1.18.18, protonplus v0.6.4, rootapp 0.9.126
  (X-AppImage-Version + sha256 0736926f... match spec), stoat-desktop 1.5.2,
  zen-browser 1.21.14b, vesktop 1.6.7 (rpm NVR), freebuff 0.0.65
  (X-AppImage-Version 0.0.65), localsend 1.18.0 (v1.18.1 is Android-only —
  assets are all .apk), fluxer 2026.818.31606 (X-Fluxer-Version header).

### 4. Install-verification sweep (install-test ledger)

| package | last install-tested | result | smoke |
|---|---|---|---|
| niri-git (1:26.04^20260818151549gite9b215f) | 2026-08-18 this run | installable fc43/44/45/rawhide + fresh-checker re-run | niri 26.04 (e9b215f) |
| noctalia-git (5.0.0^20260818152238git356a41a) | 2026-08-18 this run | installable fc43/44/45/rawhide + fresh-checker re-run | noctalia v5.0.0 (356a41a) |
| ALL 78 published packages | 2026-08-18 this run | installable: single-transaction `dnf install <78 pkgs>` SUCCEEDED on fedora 43, 44, 45, rawhide | spot smoke: ly/hyprland/hyprlock/hypridle/hyprpicker/hyprpaper/starship/matugen/hellwal/waypaper/mango/pypr/extension-manager/nwg-look/cliphist all execute |

Notes: rawhide containers need `--nogpgcheck` for the fedora-cisco-openh264 repo
key (fresh container has no imported key). XDG_RUNTIME_DIR warnings for
hyprland/niri/pypr are expected headless-container behavior. hyprlauncher +
hyprshutdown are packaged but NOT in the COPR project (never registered) —
cannot be installed; issue #14 filed, README already documents them.

### 5. Other work this run
- Issue #14 opened: hyprlauncher + hyprshutdown not registered in COPR project (owner action).
- Issue #15 opened: python3-materialyoucolor auto_rebuild disabled (owner action).
- Push 349bf0b: re-trigger comment for python3-materialyoucolor (documents the
  attempt; no build fires while auto_rebuild is off).
- GitHub Actions health: Auto-Updater runs green (last: 32173036732). No open
  issues (all closed) and no open PRs at run start.

### 6. Remaining / handoff

- IMPORTANT: as of ~21:40 UTC this run, COPR's global build queue is halted —
  the 19 pending builds above were submitted 16:46 UTC but NO worker has picked
  them up since the last completion (rootapp, 19:53 UTC). No current outage is
  announced (checked fedora-discussion copr tag + copr blog); this matches the
  known "task queue grew significantly overnight, takes a few hours to catch up"
  pattern. All 19 are re-triggers of previously-green specs (plus the locally
  build-verified quickshell Release 3), so they are expected to succeed when the
  queue drains — but their outcomes MUST be re-checked.
- Poll the 19 pending builds to completion: appmenu-glib-translator, astal,
  astal-io, astal-libs, astal-gjs, astal-gtk4, astal-lua, aylurs-gtk-shell,
  ghostty, hyprland-autoname-workspaces, hyprnome, hyprpwcenter, hyprshot,
  pyprland, noctalia-greeter, freebuff (10877639), quickshell-git (10877771),
  plus the expected-to-fail stale quickshell-git 10877216 (pre-fix Release 2,
  NOT the newest — ignore it). Confirm each package's NEWEST build succeeds.
- Install-test the quickshell-git Release 3 RPM from COPR once 10877771 lands.
- Then set this relay to status: complete + run_id (this relay is intentionally
  left in_progress so a relay run is triggered to finish the polling).
- Owner actions (cannot be done from CI): register hyprlauncher/hyprshutdown,
  re-enable auto_rebuild for python3-materialyoucolor.