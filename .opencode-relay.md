# opencode relay state

status: complete

## Run summary (2026-08-03, scheduled run)
Full verification run. All work complete.

## Completed this run
- COPR health: all builds green except 2 stale superseded failures (cascadia
  #10804938, heroic #10788404), both replaced by succeeded builds.
- Full version sweep (26/26): all up-to-date. Verified via GitHub releases/HEAD
  + artifact teardown. rootapp 0.9.125 from sq.version inside AppImage, sha256
  86ed0d76... matches spec pin. heroic bundled legendary 0.20.43/gogdl 1.2.1/nile
  1.1.2/comet 0.2.0 match spec pins. vesktop 1.6.5, obsidian 1.13.4, localsend
  1.17.0, opencode 1.18.11, zen 1.21.10b all verified from inside binaries.
  git snapshots match upstream HEAD. fluxer version confirmed via X-Fluxer-Version
  header. All Source URLs reachable (200).
- FIXED ONE BUG: obsidian missing `Requires: mesa-libgbm` (libgbm.so.1) - the
  bundled Electron binary dlopens it. Release 2->3. Commit d53a28d pushed to main.
  COPR build #10807760 SUCCEEDED in all 3 chroots. Re-tested in F44 container:
  installs clean, rpm -V OK, ldd ALL RESOLVED, mesa-libgbm pulled in. Web-search
  verified: Gentoo obsidian ebuild lists media-libs/mesa[gbm(+)] as runtime dep.
- Install-verification sweep (22 packages, F44 container, all INSTALL OK + rpm -V OK):
  mangowm, starship, matugen, app2unit, obsidian, opencode-desktop, scenefx,
  niri-git, rootapp, vesktop, fluxer, heroic-games-launcher, zen-browser, nwg-look,
  protonplus, bibata-cursor-theme, cascadia-code-nerd-fonts, extension-manager,
  gpu-screen-recorder, localsend, material-symbols-fonts. Smoke tests: mango
  0.15.6(release), starship 1.26.0, matugen 4.1.0, niri 26.04(feb3e43).
- Dependency deep audit: mangowm, scenefx, libcava, extension-manager upstream
  meson.build deps all present in specs. libappindicator-gtk3 + libayatana
  variants verified present in Fedora 44. rootapp/vesktop/heroic/obsidian/zen/
  localsend runtime deps verified via ldd (all resolve after install).
- GitHub Actions: last 10 Auto-Updater runs all green. No open issues, no open PRs.
- All update.sh scripts pass bash -n; up-to-date guards use pure version/commit
  comparison (no churn). Working tree clean.

## No issues/PRs to close
- 0 open issues, 0 open PRs.

## COPR builds verified
- #10807760 obsidian 1.13.4-3 succeeded (all 3 chroots).
