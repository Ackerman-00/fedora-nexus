# fedora-nexus autonomous-run relay state

status: complete
run_id: 31452181464

## Run 31452181464 (2026-08-11) - COMPLETE: fixed localsend 1.18.0-1 build failure (data.tar.zst + opt/ layout), verified 1.18.0-2 on all 3 chroots; 61/61 version sweep current; full install-test localsend PASS fc44/fc43/rawhide

### 0. Pickup
- Picked up main at `58576d2` (relay of run 31422800110, `status: complete`). Started as FRESH run (RELAY=false, RUN_ID=31452181464).
- COPR chroots (API): fedora-43-x86_64, fedora-44-x86_64, fedora-rawhide-x86_64.
- Fedora lifecycle: F44 stable, F43 supported, rawhide = F45 under development. Chroots match the supported set.

### 1. COPR build scan (priority 1) — localsend FIXED
- Latest 50 builds: 2 non-succeeded = **localsend 10844795 (1.18.0-1) failed** + **obsidian 10843364 (1.13.6-1) failed** (historical, already superseded by green 10843472).
- **Root cause (localsend)**: upstream v1.18.0 changed its DEB in two ways:
  1. data archive switched from `data.tar.xz` (<=1.17.0) to `data.tar.zst` (>=1.18.0) — spec hardcoded `tar xf data.tar.xz`.
  2. app tree moved from `usr/share/localsend_app` to `opt/localsend_app` — spec copied `usr/share/*` only.
- **Fix** (`localsend/localsend.spec`):
  - `%prep`: detect data archive format dynamically (`data.tar.zst data.tar.xz data.tar.gz` loop).
  - `%install`: copy `opt/localsend_app` -> `%{_libdir}/localsend_app` (keeps binary adjacent to `lib/` for `$ORIGIN/lib` RUNPATH); updated wrapper to `%{_libdir}/localsend_app/localsend_app`.
  - Updated `__requires_exclude_from`/`__provides_exclude_from` globals from `/usr/share/localsend_app` to `%{_libdir}/localsend_app`.
  - Release bumped 1 -> 2 (same version, spec-only fix per RELEASE REVISION DISCIPLINE).
- **Local verification** (clean containers, build+install): fc44 BUILD_OK INSTALL_OK, rawhide BUILD_OK, fc43 BUILD_OK. RPM requires clean (3 real deps, no bundled .so junk), provides clean (no leaked plugins).
- **Commit**: `f3b69ed` fix(autonomous): localsend: fix v1.18.0 build (data.tar.zst + opt/ layout). Pushed to main.
- **Webhook verification**: pre-push latest localsend build = 10844795 (failed). After push, new build **10844868** appeared and went `running` -> `succeeded` (confirmed webhook alive). NVR 1.18.0-2, all 3 chroots succeeded.
- **Fresh-checker install-test of COPR RPM** (downloaded from COPR, clean containers): fc44 INSTALL_EXIT=0 rpm -V clean ldd=0-not-found; rawhide INSTALL_EXIT=0 ldd=0; fc43 INSTALL_EXIT=0 ldd=0. PASSED all 3.
- Per-package latest build scan of ALL 61 COPR packages: **all succeeded**.

### 2. Version sweep (priority 2) — ALL 61 CURRENT, no-churn
- 31 update.sh scripts: all exit 0, all report up-to-date (exact same pins as main).
- 30 non-update.sh packages (GitHub API scan): all OK, 0 updates.
- materialyoucolor (PyPI): 3.04 == spec. opencode-desktop (anomalyco/opencode): v1.18.16 == spec.
- README (61 rows) == COPR packages (61) == repo spec dirs (61). No discrepancies.
- Nothing bumped.

### 3. Actions health (priority 3) — HEALTHY
- Auto-updater runs: 31451615303 (02:11) success, 31422370320 success, 31409642941 success. All green.
- 2 failed runs (31407635573, 31407634776) are workflow-file runs (.github/workflows/opencode-schedule.yml / opencode.yml) — known benign case (push modifying workflow file triggers run that GitHub rejects; token lacks workflows scope). NOT a real failure; auto-update job itself succeeds.

### 4. Issues / PRs
- Open issues: NONE. Open PRs: NONE. Nothing to reply/merge/close.

### 5. Install-test coverage ledger
- **localsend 1.18.0-2**: install-tested this run on fc44/fc43/rawhide from COPR RPM — PASS. (package touched/rebuilt this run)
- All other packages: latest build succeeded; no binary change since last run's full sweep (run 31422800110 did full 68/68 fc44 sweep + rawhide/fc43 spot checks).

### 6. Dependency audit table (61/61)

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---------|---------------------|---------|---------------|---------------|--------|
| localsend | libayatana-appindicator-gtk3, libayatana-ido-gtk3, xdg-user-dirs (+ bundled Flutter libs excluded) | all present | none | none | deps-fixed (deep-audited this run; ldd 0-not-found, requires/provides clean) |
| all other 60 packages | per-package (unchanged since prior audit) | COPR build succeeds | none identified | none | deps-verified |

Method: (a) all 61 COPR latest builds succeed -> BuildRequires correct; (b) full fc44 install sweep passed in prior run + localsend fresh-installed this run from COPR RPM on fc44/fc43/rawhide -> Requires resolve; (c) localsend ldd + rpm -qp --requires/--provides inspected this run (bundled plugins correctly excluded, no leaked sonames). Confidence: high for localsend (deep-audited); high-by-resolution for the other 60 (repeat of this reasoning locked in at run start).

### 7. Install-test table

| package | COPR build | dnf install test | smoke test | status |
|---------|-----------|-----------------|------------|--------|
| localsend | 10844868 (1.18.0-2, all 3 chroots succeeded) | PASS fc44/fc43/rawhide (exit 0) | binary runs (Gtk no-display warning only, expected in container) | installable |

### 8. Final state
- Working tree clean. Latest builds for all 61 packages green. localsend fix shipped (1.18.0-2). Run is COMPLETE.
