# opencode relay state

status: complete

## Run summary (2026-08-03, second run)
Follow-up verification run. All work complete. No pending items, no changes needed.

## Completed this run
- COPR health: only 1 failed build in last 50 (#10804938 cascadia-code-nerd-fonts,
  stale source-404) superseded by succeeded #10805052/#10805734/#10806072. Latest builds
  all green: mangowm 0.15.6-1 (#10806336) succeeded in all 3 chroots.
- No pending/running builds.
- Full version sweep re-verified (26/26): all still up-to-date. GitHub releases confirmed
  (heroic v2.22.0, opencode v1.18.11, matugen v4.1.0, nwg-look v1.1.1, bibata v2.0.7,
  cascadia v3.5.0, extension-manager v0.6.5, protonplus v0.5.22, scenefx 0.5, cava 1.0.0,
  zen, vesktop, localsend, obsidian, starship). git snapshots match upstream HEAD
  (app2unit 47e23ec, caelestia bd43cb2, material-symbols 50f0603, niri feb3e43,
  quickshell 28771c7, xwayland-satellite 8d135d3).
- Dependency audit completed for all 26 packages: every spec BuildRequires/Requires maps
  to a real upstream build dep (meson.pm/CMakeLists/Cargo.lock/Makefile/pkg-config files).
  No dead deps found (matugen openssl already dropped last run). Verified: mangowm, libcava,
  scenefx, gpu-screen-recorder, extension-manager, matugen, niri, quickshell,
  xwayland-satellite, app2unit, caelestia, protonplus. Runtime deps exist in Fedora
  (ripgrep, libappindicator-gtk3, gamemode, mangohud, libXScrnSaver, etc.).
- Install verification: all 26 packages install cleanly on F44 with `rpm -V OK`
  (gpu-screen-recorder only `P` cap flag = intentional setcap). F43/F44/rawhide spot checks
  pass. CLI smoke tests pass (starship 1.26.0, matugen 4.1.0); GUI apps verified by binary
  presence + ldd (all libs resolve).
- zen-browser: wrapper `zen-bin` matches tarball; aux sources (policies.json, desktop file,
  wrapper) in repo. opencode Requires ripgrep present in Fedora.
- GitHub Actions: last 10 Auto-Updater runs all green. No open issues, no open PRs.
- All 20 update.sh scripts pass `bash -n` and exit 0 (no churn). Working tree was clean.
- COPR project has 3 chroots (fedora-43/44/rawhide-x86_64), auto-rebuild confirmed working.

## No changes made
- No version bumps, no spec edits, no pushes of package files. Nothing needed updating.

## Completed this run
- COPR health: only failed build (cascadia-code-nerd-fonts #10804938, old 3.5.0 source-404)
  is superseded by succeeded builds #10805052/#10805734/#10806072. All other builds green.
- Full version sweep (26/26 packages): 25 up-to-date; ONE update found and applied —
  mangowm 0.15.5 -> 0.15.6 (released 2026-08-03T03:45Z, after last updater run). All git
  snapshots (app2unit, caelestia-shell-mango, material-symbols-fonts, niri-git,
  quickshell-git, xwayland-satellite-git) match upstream HEAD; gitdates verified against
  upstream commit dates. All stable versions verified against upstream tags/releases.
  Every Source URL verified reachable (200).
- mangowm 0.15.6: committed (41cf857), pushed to main; COPR build #10806336 SUCCEEDED in
  all 3 chroots (fedora-44/fedora-43/rawhide). Install test in F44 container: clean,
  `mango 0.15.6(release)` smoke test passed. Deps verified against upstream meson.build.
- Deep-audit spot checks: heroic bundled legendary 0.20.43 / gogdl 1.2.1 / nile 1.1.2 /
  comet 0.2.0 verified from extracted binaries (match spec pins). rootapp AppImage sha256
  86ed0d76... matches spec pin.
- Install-verification sweep: app2unit, extension-manager, starship, protonplus, matugen
  (F44); bibata-cursor-theme, cascadia-code-nerd-fonts, fluxer, heroic-games-launcher,
  localsend, nwg-look, obsidian, opencode-desktop (F43); vesktop, zen-browser, rootapp,
  gpu-screen-recorder, libcava, material-symbols-fonts, scenefx, wlroots, niri-git,
  xwayland-satellite-git, quickshell-git, mangowm (F44). All install cleanly, binaries
  present, smoke tests OK.
- GitHub Actions: last 6 Auto-Updater runs all succeeded. No open issues, no open PRs.
- All 20 update.sh scripts pass `bash -n`.

## COPR builds verified
- #10806336 mangowm 0.15.6-1 succeeded (all chroots).
