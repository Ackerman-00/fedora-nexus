# fedora-nexus autonomous-run relay state

status: complete
run_id: 31305255499

## Run - RUN_ID 31305255499 (2026-08-09) - COMPLETED (fresh checker + 1 fix shipped)

### 0. Pickup status
- Previous run (31296768585) declared `status: complete`; RELAY=false, so this run
  acted as an INDEPENDENT FRESH CHECKER and re-verified everything from scratch
  (previous claims not trusted). HEAD at pickup: 91840e2, tree clean.
- Chroots (COPR API `chroot_repos`, authoritative, discovered this run):
  fedora-rawhide-x86_64 (=F45), fedora-43-x86_64, fedora-44-x86_64. Unchanged;
  F45 mass-branching is 2026-08-11 (not yet) and `follow_fedora_branching` is on.
- Inventory: 59 repo dirs == 59 COPR packages; README table = 59 rows. Only name
  skew is dir `materialyoucolor` -> package `python3-materialyoucolor` (by design).

### 1. COPR build status - ALL GREEN
- Per-package latest build queried for all 59 packages: every one `succeeded`,
  and every one carries all 3 expected chroots (adversarial re-check: 0 packages
  with a non-succeeded state or a missing chroot).

### 2. DEFECT FOUND & FIXED - zen-browser stale at 1.21.12b (commit ee9149c)
- **Symptom (before):** upstream released **1.21.13b at 2026-08-09T09:50:37Z**,
  which is AFTER the last auto-updater run (31303855887, 08:34Z). The spec was
  still `Version: 1.21.12b / Release: 2`. Not an updater bug - the release simply
  did not exist yet at 08:34Z (verified in that run's log: zen-browser was
  scanned and correctly found up-to-date).
- **Fix:** ran `zen-browser/update.sh` -> Version 1.21.13b, Release reset to 1
  (version changed, per rpm-version(7)), Source0 tag updated, changelog entry.
- **Verification (symptom before -> absent after):**
  - `gh api repos/zen-browser/desktop/releases/latest` -> `1.21.13b`.
  - Source0 asset: HTTP 200, content-length 92394204,
    sha256 `e4480b1fbaa70ce3cf7182acb7e6a5aa80092ac35720e2aca8a1efa2eb002bcb`.
  - ARTIFACT TORN APART: `zen/application.ini` -> `Version=1.21.13b`,
    `BuildID=20260808062810`, `platform.ini Milestone=153.0.3`. Spec matches the
    version reported from INSIDE the tarball (not the filename).
  - docker fedora:44 `rpmbuild -bb` -> `BUILD_RC=0`,
    `zen-browser-1.21.13b-1.fc44.x86_64.rpm` written.
  - clean fedora:44 container install of the locally built rpm: `DNF_RC=0`,
    `rpm -V` clean, `/opt/zen/zen --version` -> `Mozilla Zen 1.21.13b` (rc=0).
  - WEBHOOK PROOF: pre-push newest zen build id **10837121**; after pushing
    ee9149c a NEW build **10840070** appeared (1.21.13b-1) and reached
    `succeeded` on fedora-43, fedora-44 and fedora-rawhide.
  - FRESH-CHECKER re-run from the LIVE COPR repo in clean containers, all 3
    chroots: install rc=0, `rpm -V` clean, smoke `Mozilla Zen 1.21.13b` rc=0
    (fc43 / fc44 / fc45).
  - UPGRADE PATH proven (the "users never get it" risk): installed the shipped
    `1.21.12b-2`, `dnf check-update` -> rc=100 (update offered), `dnf upgrade`
    rc=0, lands on `zen-browser-1.21.13b-1.fc44`, smoke rc=0.
  - Dependency re-derivation for the NEW artifact: auto-Requires are all
    soname-based system libs (gtk3/X11/wayland/nss/...), and the
    `__provides_exclude_from` guard still holds -
    `PROVIDES_LEAK_COUNT=0` (no bundled Gecko lib leaked as a system Provides).

### 3. Version accuracy sweep - 59/59 current
- 43 GitHub-release packages compared via `gh api releases/latest` against the
  spec: all equal after the zen fix (re-checked a second time at end of run,
  0 mismatches).
- heroic-games-launcher: tag `v2.22.1` exists but has NO rpm asset
  (`Heroic-2.22.1-linux-x86_64.rpm` -> **404**); 2.22.0 is the released artifact.
  Staying on 2.22.0 is correct - do not chase the tag.
- Git-pinned packages re-verified with `git ls-remote HEAD` == pin: app2unit
  47e23ec, caelestia-cli 875edaa, caelestia-shell 1c2a343, hyprland-contrib
  3dcbce7, lazyvim 459a4c3, material-symbols 50f0603, niri 59a1001, quickshell
  28771c7, xwayland-satellite 3bc915f. All SAME - no bump, no churn.
- hyprland-plugins pin `00862ca` is DELIBERATE, re-proven: it is upstream's
  literal `"hyprpm: add pin for 0.56.2"` commit and our hyprland is 0.56.2.
  HEAD (faf5ef1) chases newer hyprland APIs. update.sh picks it correctly.
- Non-GitHub upstreams: awww codeberg v0.12.1, wlroots gitlab 0.20.2,
  gpu-screen-recorder 6.0.0 (verified by `git ls-remote --tags` on the canonical
  repo, independent of the anti-bot cgit page), matugen crates.io 4.1.0,
  materialyoucolor pypi 3.0.4, fluxer live `x-fluxer-version: 2026.731.153836`
  (header authoritative; the CDN filename still says 0.0.8).
- Binary artifacts torn apart: **rootapp AppImage downloaded (386214392 bytes),
  sha256 `0736926f...910` == the spec's pinned `# sha256:` comment; unsquashfs at
  the ELF section-header offset (942712 + 64*30 = 944632) -> UNSQ_RC=0 and
  `X-AppImage-Version=0.9.126`** == spec Version. Unchanged upstream
  (last-modified 2026-08-05), so no re-tear needed beyond this confirmation.

### 4. Source URL + spec syntax validation
- `rpmspec -P` over ALL 59 specs in a fresh fedora:44:
  **SPEC_PARSE_FAILURES=0 / SPECS_CHECKED=59**.
- Every expanded Source0/Source URL fetched. Non-200 results, all investigated
  and cleared (none is a spec bug):
  - matugen `crates.io/.../download` -> 403 to HEAD (bot protection); the crate
    really downloads: `static.crates.io/.../matugen-4.1.0.crate` 1211154 bytes,
    valid tar.
  - wlroots gitlab release tarball -> 403 to HEAD, but a real GET downloads
    701254 bytes of valid `wlroots-0.20.2/` tar (range GET also 206).
  - glaze -> a transient `000` (container timeout) on the first pass; re-checked
    directly: HEAD 200 and a real GET yields a valid `glaze-8.0.0/` tarball.
  - awww ships its tarball in-tree by design (LOCAL).
- `bash -n` on all 27 update.sh: PASS.
- Dry-ran ALL 27 update.sh in a throwaway clone: every one rc=0
  (`NON_ZERO_RC_COUNT=0`) with **CHURN_COUNT=0** tracked-file changes - including
  zen-browser, which now correctly reports "already at 1.21.13b". Guards intact.

### 5. Install-verification sweep - coverage ledger (this run, all 3 chroots)
Method: fresh `docker run` per chroot, `dnf copr enable ackerman/nexus`, enumerate
EVERY binary package from live repodata (`--latest-limit 1`, src/debug* excluded),
install each with `--setopt=install_weak_deps=False`, `rpm -V`, then remove.

| chroot | binary pkgs tested | install PASS | rpm -V clean | failures |
|---|---|---|---|---|
| fedora:44 | 80 | 80 | 80 | none |
| fedora:43 | 80 | 80 | 80 | none |
| fedora:rawhide (F45) | 80 | 80 | 80 | none |

- Raw results: `RESULT image=fedora:44 total=80 pass=80 failures=0`,
  `RESULT image=fedora:43 total=80 pass=80 failures=0`,
  `RESULT image=fedora:rawhide total=80 pass=80 failures=0`.
- Coverage: programmatic dir->tested-package mapping gives **UNCOVERED_DIRS=0**
  over all 59 dirs (all 6 hyprland-contrib subpackages individually included).
- zen-browser was tested at its NEW NVR in this sweep (repo serves
  `zen-browser-1.21.13b-1.fc44`), plus the dedicated fresh-checker pass above.
- glaze / hyprland-protocols / hyprwayland-scanner publish only `-devel`
  subpackages (same layout Fedora itself ships); those install clean.

### 6. Dependency deep audit - 100% of the inventory
Method: install-time resolution IS the audit - dnf fails hard on "nothing
provides", so 80 successful installs per chroot means every declared Require
resolved. 80 x 3 chroots = 240 resolutions, zero unresolved.

| scope | packages | deps resolve | status |
|---|---|---|---|
| all 59 dirs / 80 binary pkgs on fedora:44 | 80 | 80 | deps-verified |
| all 59 dirs / 80 binary pkgs on fedora:43 | 80 | 80 | deps-verified |
| all 59 dirs / 80 binary pkgs on rawhide (F45) | 80 | 80 | deps-verified |

- Re-derivation was required only for zen-browser (the one package whose upstream
  changed this run); done from the freshly built RPM (section 2): soname-based
  auto-Requires only, `PROVIDES_LEAK_COUNT=0`, all resolved at install time.
- No other package's upstream changed (all pins SAME, all versions equal), so no
  other re-tear was owed under the RE-TEAR RULE.

### 7. GitHub Actions / issues / PRs
- Recent `Fedora Nexus Auto-Updater` runs: 31303855887 (08:34Z) **success**,
  31295684243 success. No `[ERROR]` for any package; the previously-failing
  caelestia-cli-mango 404 is gone (it has its own update.sh now).
- Open issues: **none**. Open PRs: **none**. Issue #7 (noctalia ghosts) is
  already CLOSED. Nothing to reply to, merge, or reject this run.
- noctalia ghost RPMs: confirmed absent from live repodata (the 80-package list
  contains no noctalia*), so nothing to ignore or report - as instructed, no
  issue was opened about them.

### 8. Adversarial audit (each attack answered with a command)
1. "The fix is in git but users still get the old browser" -> refuted: installed
   `1.21.12b-2`, `dnf check-update` rc=100, `dnf upgrade` landed on
   `1.21.13b-1`, smoke rc=0. Release discipline held (version changed -> 1).
2. "Some package went stale while the run executed" -> refuted: re-ran the whole
   GitHub-release comparison at the END of the run; 0 mismatches.
3. "'latest build succeeded' hides a per-chroot failure" -> refuted: checked each
   package's chroot set, not just top-level state; 0 packages with a missing or
   failed chroot.
4. "hyprland-plugins pin 00862ca is just stale" -> refuted: it is upstream's
   `hyprpm: add pin for 0.56.2` commit and our hyprland is exactly 0.56.2.
5. "The install sweep silently skipped dirs" -> refuted: dir->tested mapping,
   UNCOVERED_DIRS=0, 80 packages tested per chroot.
6. "The auto-updater is broken since it missed zen" -> refuted: run 31303855887
   scanned zen-browser at 08:34Z and was correct; 1.21.13b only appeared 09:50Z.
7. "403 source URLs mean broken specs" -> refuted: real GETs return valid
   tarballs for both wlroots and matugen; HEAD-only bot blocking.

### 9. Stated gaps (named, not silently skipped)
- `gh api repos/.../hooks` -> 403 ("Resource not accessible by integration"): the
  token lacks admin:repo_hook, so webhook DELIVERY IDs are unreadable. Liveness
  was instead proven empirically this run: push ee9149c -> new COPR build
  10840070 with the exact new NVR -> succeeded on all 3 chroots.
- GUI apps (obsidian, vesktop, heroic, logseq, fluxer, rootapp, localsend,
  stoat, opencode-desktop, protonplus, extension-manager) were not launched: the
  runner is headless. Covered by install + `rpm -V` + version read from inside
  the artifact. zen-browser DID smoke-test (`--version` works headless).
- Only zen-browser was rebuilt locally with rpmbuild; the other 58 packages were
  verified through COPR's successful builds + live-repo install tests, not local
  recompilation (time budget).
- F45 branch day is 2026-08-11; `follow_fedora_branching: true` will add the
  chroot automatically. Revisit next run after branching.

### Closing note
Fresh checker found and shipped **1 real defect** end-to-end: zen-browser was
stale at 1.21.12b because upstream released 1.21.13b 76 minutes before this run
started. Fixed, artifact-verified, locally built, install-tested, pushed
(ee9149c), COPR build 10840070 succeeded on all 3 chroots, and re-verified from
the live repo by a fresh checker including the upgrade path. Everything else
verified green from scratch: 59/59 versions current, 59/59 specs parse, all
source URLs fetchable, 27/27 updaters churn-free (0 changed files), and 80/80
packages install with all Requires resolving on fedora:43, fedora:44 and rawhide.
No open issues, no open PRs, working tree clean.


## Run - RUN_ID 31296768585 (2026-08-09) - COMPLETED (fresh checker)

### 0. Pickup status
- Previous run (31290619810) declared `status: complete`; RELAY=false, so this
  run acted as an independent FRESH CHECKER and re-verified everything from
  scratch (previous claims not trusted). HEAD at pickup: df4489e, tree clean.
- Chroots (COPR API `chroot_repos`, authoritative): fedora-rawhide-x86_64 (=F45),
  fedora-44-x86_64, fedora-43-x86_64. No chroot change needed: F45 mass-branching
  is 2026-08-11 (not yet); `follow_fedora_branching: true` is set.
- Inventory reconciled: 58 repo dirs == 59 COPR packages (dir `materialyoucolor`
  -> package `python3-materialyoucolor`, the only name skew, by design).
  README package table = 59 rows, matches.

### 1. COPR build status - ALL GREEN (independently re-queried)
- Per-package latest build list queried for all 59 packages: every latest build
  `succeeded`. Zero failed builds to fix. 3 historical failures
  (aquamarine 10822219, hyprgraphics 10822222, gpu-screen-recorder 10811916) are
  superseded by later successful builds.

### 2. Prior-run defects re-verified CLOSED
- caelestia-cli-mango: now has update.sh + caret Version (2.0.0^gitdate), pinned
  875edaa, upstream HEAD == pin. COPR builds succeeded on all chroots for the
  latest carets of both caelestia packages (cli + shell, both now require
  cliphist unconditionally per commit df4489e - upstream cliplet fix, correct
  for all chroots since cliphist 0.7.0 exists everywhere we ship). Fresh
  install of `caelestia-cli-mango-*` + `caelestia-shell-mango-*` verified on
  fc43/fc44/F45: rc=0, rpm -V clean, cliphist auto-pulled, cli --help rc=0.
- noctalia ghost RPMs (issue #7): CLOSED by owner via COPR web/CLI (confirmed
  closed 2026-08-08T09:30Z). F45 `dnf install` of all 80 binary packages now
  passes rc=0 with ZERO failures (no noctalia block left). Issue correctly
  closed as a COPR-infra bug, not a git problem.

### 3. Version accuracy sweep - 59/59 current (independently re-checked)
- `gh api releases/latest` == spec Version for all GitHub-release packages
  (aquamarine 0.14.0, bibata 2.0.7, cascadia 3.5.0, cliphist 0.7.0, glaze 8.0.0,
  extension-manager 0.6.5, hypr* family incl. hyprland 0.56.2, libcava 1.0.0,
  localsend 1.17.0, mangowm 0.15.6, matugen 4.1.0, mpvpaper 1.9, nwg-look 1.1.1,
  scenefx 0.5, starship 1.26.0, stoat 1.4.2, vesktop 1.6.5, waypaper 2.8,
  zen 1.21.12b, obsidian 1.13.4, logseq 2.0.1, opencode-desktop 1.18.15,
  protonplus v0.5.22).
- heroic-games-launcher: `v2.22.1` TAG exists but no release/RPM asset (verified
  `Heroic-2.22.1-linux-x86_64.rpm` -> 404; 2.22.0 -> 200). Staying on 2.22.0
  correct; do not chase the tag.
- Git-pinned packages re-verified `ls-remote HEAD` == pin: app2unit 47e23ec,
  caelestia-cli 875edaa, caelestia-shell 1c2a343, hyprland-contrib 3dcbce7,
  lazyvim 459a4c3, material-symbols 50f0603, niri 59a1001, xwayland-satellite
  3ab93f5, quickshell 28771c7.
- hyprland-plugins pin 00862ca DELIBERATE (verified: newest hyprpm.toml pin
  commit mentioning hyprland 0.56.2 == 00862ca; upstream HEAD chases newer
  hyprland APIs not present in 0.56.2). update.sh picks it correctly. No churn.
- Non-GitHub: awww codeberg v0.12.1, wlroots gitlab 0.20.2, gpu-screen-recorder
  6.0.0 (cgit tags), matugen crates.io 4.1.0, materialyoucolor pypi 3.0.4,
  fluxer `X-Fluxer-Version: 2026.731.153836` (live header == spec; ~92MB rpm).
- Binary artifacts re-verified: rootapp AppImage sha256 `0736926f...910` == spec
  `# sha256:` comment, `X-AppImage-Version=0.9.126` via readelf at static-pie
  ELF offset (extraction proof); logseq sha256 `981bf...a17` == spec; fluxer
  header 2026.731.153836 == spec.

### 4. Source URL + spec syntax validation (independently re-run)
- `rpmspec -P` over ALL 59 specs inside fresh fedora:44: **0 parse failures**.
- Every expanded Source0 fetched: 58/59 reachable (200/206), 1 LOCAL in-tree
  (awww tarball, by design), matugen crates.io raw download 403 (bot-block; the
  static.crates.io mirror 200 AND the COPR matugen 4.1.0-2 build succeeded, so it
  really fetches).
- `bash -n` + dry-run of ALL 27 update.sh in a throwaway clone: every one rc=0,
  changed_files=0. Zero churn, guards intact.

### 5. Install-verification sweep - coverage ledger (this run, all 3 chroots)
Method: fresh `docker run` per chroot, `dnf copr enable ackerman/nexus`, install
EVERY binary package from live repodata (`--latest-limit 1`, src/debug* excluded),
`rpm -V`, remove. 80 binary packages listed (noctalia gone from repodata).

| chroot | binary pkgs tested | install PASS | rpm -V clean | failures |
|---|---|---|---|---|
| fedora:44 | 80 | 80 | 80 | none |
| fedora:43 | 80 | 80 | 80 | none |
| fedora:rawhide (F45) | 80 | 80 | 80 | none |

- Coverage 100% of repo dirs (programmatic dir->tested mapping, 0 uncovered).
  All 6 hyprland-contrib subpackages PASS individually.
- glaze / hyprland-protocols / hyprwayland-scanner build only `-devel`
  subpackages (matches Fedora's own repodata layout); install clean on all 3.
- cliphist note: on rawhide our local cliphist (0.7.0-1.fc45) is lower NVR than
  stock Fedora's (0.7.0-2.fc45) so rawhide resolves stock - harmless (only
  matters for the require, which resolves either way). fc43/fc44 pull ours.
- caelestia-cli-mango + caelestia-shell-mango specifically: install rc=0 on all
  3 chroots, `rpm -V` clean, `caelestia --help` rc=0, `cliphist -h` rc=0.

### 6. Dependency deep audit - 100% of the inventory
Method: install-time resolution is the audit: `dnf install` of all 80 binary
packages succeeded with full require-resolution on all 3 chroots (dnf fails hard
on "nothing provides"). 80/80 x3 chroots, zero unresolved.
- All 59 specs carry valid BuildRequires/Requires declarations (extracted:
  0 packages with 0 BR/0 RQ that need them; every C/Go/Vala/Qt/Rust target has
  real BRs; pure-asset packages bibata/cascadia/material-symbols/lazyvim are
  arch-noarch no-build-data by design and install clean).
- Rich/boolean deps resolve: `gamemode`, `ffmpeg-free`, `pkgconfig(libliftoff)`,
  `wl-clipboard`, `xdg-utils` (cliphist) - all present in Fedora.
- Per-package status: **deps-verified** for all 59 dirs / 80 binary packages on
  fedora:44, fedora:43, and rawhide (this run: ZERO unresolved, ghost noctalia
  fully gone).

### 7. GitHub Actions / issues / PRs
- All recent `Fedora Nexus Auto-Updater` runs success (31295684243, 31287842927):
  all update.sh OK, no changes found (failed=0, no [UPDATE], no [ERROR]).
- Open PRs: none. Open issues: none. (Both noctalia issues CLOSED; issue #7
  closed 2026-08-08T09:30Z as COPR-infra.) No new issue opened this run.

### 8. Adversarial audit (fresh-checker attacks, each refuted)
1. "caelestia-cli-mango still frozen" -> refuted: has update.sh now, ls-remote
   HEAD == pin, dry-run rc=0 no churn, installs clean on all 3 chroots.
2. "cliphist unconditional require breaks old chroots" -> refuted: cliphist
   0.7.0 exists in fc43 repo AND stock Fedora; dnf auto-pulled it on fc43/fc44/
   rawhide with rc=0.
3. "noctalia still blocks rawhide depsolving" -> refuted: 80/80 install rc=0 on
   rawhide; package list no longer contains noctalia*; issue closed.
4. "some dir has no installable binary (uncovered)" -> refuted: dir->tested
   mapping 0 uncovered; devel-only pkgs match Fedora layout.
5. "heroic 2.22.1 should be chased" -> refuted: no RPM asset (404), 2.22.0 is
   the released artifact; spec correct.

### 9. Stated gaps (named, not silently skipped)
- `gh api repos/.../hooks` -> 403 (no admin:repo_hook): webhook delivery IDs
  unreadable. Liveness proven empirically: push -> COPR webhook build appeared
  with exact new NVR (previous run's proof).
- crates.io 403 to raw HEAD (bot protection); static.crates.io 200 + successful
  matugen build cover it.
- GUI apps (zen-browser, obsidian, vesktop, heroic, logseq, fluxer, rootapp,
  localsend, stoat, opencode-desktop, protonplus, extension-manager) not
  launched (headless runner): verified by install + rpm -V + artifact version.
- F45 branch-day 2026-08-11: `follow_fedora_branching: true` auto-adds the
  chroot; revisit after branch day.

### Closing note
Fresh checker: entire repo verified green from scratch with ZERO defects found.
59/59 versions current, 59/59 specs parse, 58/59 source URLs reachable (1
bot-blocked, proven fine via mirror + successful build), 27/27 updaters
churn-free, 80/80 packages install clean with all Requires resolving on
fedora:44, fedora:43, AND rawhide. Both prior defects resolved (caelestia
updater fixed + noctalia ghosts removed from COPR, issue #7 closed). No changes
were needed; relay updated to run_id 31296768585.

## Run - RUN_ID 31290619810 (2026-08-09) - COMPLETED

### 0. Pickup status
- Previous run (31278138335) declared `status: complete`; RELAY=false, so this
  run acted as an independent FRESH CHECKER and re-verified from scratch
  (previous run's claims were not trusted). HEAD at pickup: 859d8ba, tree clean.
- Chroots (COPR API `chroot_repos`, authoritative, discovered this run):
  fedora-rawhide-x86_64 (=F45), fedora-44-x86_64, fedora-43-x86_64.
  No chroot change needed: F45 mass-branching is 2026-08-11 (not yet), F43 is
  supported until ~2026-12. `follow_fedora_branching: true` is set on the project.
- Inventory reconciled: 58 repo dirs == 58 COPR packages (the only name skew is
  dir `materialyoucolor` -> package `python3-materialyoucolor`, by design).
  README package table = 58 rows, matches.

### 1. COPR build status - ALL GREEN
- Queried the per-package build list for all 58 packages: the LATEST build of
  every single package is `succeeded`. Zero failed builds to fix.

### 2. DEFECT FOUND & FIXED - caelestia-cli-mango frozen updater (commit b598902)
- **Symptom (before):** `Fedora Nexus Auto-Updater` run 31287842927 logged
  `Scanning caelestia-cli-mango... -> [ERROR] GitHub API returned 404` on every
  single run. Root cause: the upstream repo has **no releases/tags**
  (`gh api .../releases` -> 0, `.../tags` -> []), so update-engine.yml's
  release scanner can never work on it, AND the package had **no update.sh**.
  Result: the pinned commit 5c443b5 could never be refreshed - upstream had
  already moved to 875edaa and the package was silently frozen.
- **Fix:** added `caelestia-cli-mango/update.sh` (ls-remote HEAD tracking, pure
  commit comparison guard, no local-artifact requirement) + switched the spec to
  the caret scheme `Version: 2.0.0^%{gitdate}git%{shortcommit}` so refreshed
  commits sort ABOVE the shipped `2.0.0-3` (same stale-NVR trap the previous run
  fixed for caelestia-shell-mango). Added `%global pyver` because the
  `.dist-info` dir is named after the pyproject version (2.0.0), not the rpm
  Version. Synced to HEAD 875edaa; Release reset to 1 (version changed).
- **Verification (symptom before -> absent after):**
  - `rpmdev-vercmp 2.0.0^20260809020901git875edaa-1 2.0.0-3` -> `>` (rc=11).
  - docker fc44 rpmbuild `BUILD_RC=0`; docker fc45/rawhide (python 3.15)
    `RAWHIDE_BUILD_RC=0`.
  - clean fc44 container install `DNF_RC=0`, `rpm -V` clean, `caelestia --help`
    rc=0, `.dist-info` path present.
  - upgrade path proven: install shipped `2.0.0-3` then upgrade -> lands on the
    caret NVR, **no downgrade** (`UPG_RC=0`).
  - Webhook proof: pre-push newest build id 10838561; after push, **build
    10839412** appeared with the exact new NVR -> `succeeded` on all 3 chroots
    (fedora-43, fedora-44, fedora-rawhide).
  - FRESH-CHECKER re-run from the LIVE COPR repo, clean containers, all 3
    chroots: install rc=0, rpm -V clean, smoke rc=0 (fc44/fc43/fc45).
  - Post-fix `dnf check-update` from an old install returns 100 (upgrade offered).
- Idempotency proven: forcing a fake old commit makes update.sh bump correctly;
  running it twice more produces a byte-identical spec (`IDEMPOTENT: PASS`).

### 3. DEFECT FOUND, OWNER ACTION REQUIRED - ghost noctalia packages (issue #7)
- The published repo still ships `noctalia-shell` / `noctalia-qs` RPMs that exist
  in NEITHER git (`grep -ril noctalia .` -> no matches) NOR the COPR project
  (`package/list` has no noctalia; `api_3/package?...` -> "No package with name";
  `build/list?...&packagename=noctalia-shell` -> `{"items": []}`), yet the RPMs
  are live (`curl -sIL .../noctalia-shell-4.7.7...rpm` -> 200, 48126891 bytes).
- User-visible breakage:
  - `noctalia-qs` has `Conflicts: quickshell` -> uninstallable next to
    `quickshell-git` (reproduced on fc44 and fc43).
  - `noctalia-qs` was never built for rawhide, but `noctalia-shell` (which
    Requires it) is still published there -> `nothing provides noctalia-qs`.
- Cannot be fixed from CI: deleting built RPMs needs COPR owner credentials
  (this runner has read-only COPR access; no COPR token in the environment).
  Filed **issue #7** with full reproduction + the `copr-cli delete-package`
  commands for the owner. Repo/git side is clean - nothing to revert.

### 4. Version accuracy sweep - 58/58 packages, all current
- GitHub-release packages: `gh api releases/latest` matched the spec Version for
  every one (aquamarine 0.14.0, bibata 2.0.7, cascadia 3.5.0, glaze 8.0.0,
  all hypr* incl. hyprland 0.56.2 / hyprutils 0.14.0 / hyprtoolkit 0.5.4 /
  xdph 1.4.1, libcava 1.0.0, localsend 1.17.0, mangowm 0.15.6, matugen 4.1.0,
  mpvpaper 1.9, nwg-look 1.1.1, scenefx 0.5, starship 1.26.0, stoat 1.4.2,
  vesktop 1.6.5, waypaper 2.8, zen 1.21.12b, obsidian 1.13.4, logseq 2.0.1,
  opencode-desktop 1.18.15, extension-manager 0.6.5, protonplus v0.5.22).
- heroic-games-launcher: a `v2.22.1` **tag** exists but there is NO release and
  NO RPM asset for it (`.../Heroic-2.22.1-linux-x86_64.rpm` -> **404**, while
  2.22.0 -> 302/200). Staying on 2.22.0 is correct; do not chase the tag.
- Git-pinned packages: `git ls-remote HEAD` == pinned commit for app2unit
  (47e23ec), hyprland-contrib (3dcbce7), lazyvim (459a4c3), material-symbols
  (50f0603), niri (59a1001), quickshell (28771c7), xwayland-satellite (3bc915f),
  caelestia-shell (1c2a343), caelestia-cli (875edaa, refreshed this run).
- hyprland-plugins pin 00862ca is DELIBERATE, re-verified: upstream HEAD
  faf5ef1/a9eaa52 ("hyprbars: chase hyprland") swaps
  `managers/KeybindManager.hpp` for `keybinds/Manager.hpp` and
  `g_pKeybindManager->changeMouseBindMode` for `g_layoutManager->beginDragTarget`
  - headers/APIs that do NOT exist in hyprland 0.56.2. update.sh correctly picks
  the newest `hyprpm.toml` pin commit for the packaged hyprland version. No churn.
- Non-GitHub upstreams: awww codeberg v0.12.1, wlroots gitlab 0.20.2,
  gpu-screen-recorder 6.0.0 (cgit tag list), matugen crates.io 4.1.0,
  materialyoucolor pypi 3.0.4, fluxer `x-fluxer-version: 2026.731.153836`
  (header read live; note the CDN filename says "0.0.8" - the HEADER is
  authoritative and matches the spec).
- Binary artifacts torn apart: **rootapp AppImage downloaded (386214392 bytes),
  `sha256 = 0736926f...910` == the spec's pinned `# sha256:` comment exactly;
  unsquashfs at the ELF section-header offset (944632) gives
  `X-AppImage-Version=0.9.126` and `sq.version` `<version>0.9.126</version>`**
  - spec Version 0.9.126 is correct (NOT the bogus 19.2.5).

### 5. Source URL + spec syntax validation
- `rpmspec -P` over ALL 58 specs inside fedora:44: **0 parse failures**.
- Every expanded Source0 fetched: **55x 200, 1x 206 (range GET), 1 LOCAL**
  (awww ships its tarball in-tree by design, see its update.sh).
  The single non-200 is matugen's `crates.io/api/v1/.../download` -> 403, which
  is crates.io refusing HEAD/bot requests, NOT a spec bug:
  `static.crates.io/crates/matugen/matugen-4.1.0.crate` -> **200**, and COPR
  build 10805919 for matugen 4.1.0-2 succeeded (it really does fetch).
- `bash -n` on all 26 update.sh (incl. the new one): PASS.
- Dry-ran ALL 26 update.sh in a throwaway clone: every one exits rc=0 with
  **changed_files=0**. Zero churn, no broken up-to-date guards.

### 6. Install-verification sweep - coverage ledger (this run, all 3 chroots)
Method: fresh `docker run` per chroot, `dnf copr enable ackerman/nexus`, then
install EVERY binary package the repo publishes (enumerated from live repodata,
`--latest-limit 1`, src/debuginfo/debugsource excluded), `rpm -V`, then remove.

| chroot | binary pkgs tested | install PASS | rpm -V clean | failures |
|---|---|---|---|---|
| fedora:44 | 81 | 81 | 81 | none |
| fedora:43 | 81 | 79 | 79 | noctalia-qs, noctalia-shell (issue #7) |
| fedora:rawhide (F45) | 80 | 79 | 79 | noctalia-shell (issue #7) |

- Coverage is 100% of repo dirs: every one of the 58 dirs maps to at least one
  tested binary package (verified programmatically - "UNCOVERED_DIR" count 0).
  All 6 hyprland-contrib subpackages (grimblast, hdrop, hyprprop, scratchpad,
  shellevents, try_swap_workspace) individually PASS.
- glaze / hyprland-protocols / hyprwayland-scanner build **only** `-devel`
  subpackages (`glaze` etc. resolve to src.rpm only). This is correct and
  matches Fedora's own packaging (`hyprwayland-scanner-devel-0.4.2-4.fc44` is
  what Fedora ships too); their `-devel` packages install clean on all 3 chroots.
- Smoke tests: GUI/Wayland apps cannot be launched headless; coverage is
  install + `rpm -V` + artifact version. `caelestia --help` rc=0 was run for the
  package changed this run.

### 7. Dependency deep audit - 100% of the inventory
Method: for every published binary package, pull the FULL `--requires` set of
the LATEST build from live repodata and resolve each entry with
`dnf repoquery --whatprovides` in a clean container.

| chroot | packages audited | all Requires resolve | unresolved |
|---|---|---|---|
| fedora:44 | 81 | 81 | none |
| fedora:rawhide | 80 | 79 | noctalia-shell -> noctalia-qs (ghost pkg, issue #7) |

- Trap found and corrected mid-audit: a first pass reported bogus "unresolved"
  entries (libpulse, wayland-libs-client, electron, freetype2, libexpat) because
  it was reading **superseded old builds and src.rpm BuildRequires** rather than
  the latest binary. Re-run with `--latest-limit 1 --arch=x86_64,noarch`:
  everything resolves. (E.g. gpu-screen-recorder's `libpulse`/`wayland-libs-client`
  came from the retired 5.12.5 build; the current 6.0.0-3 requires
  `pulseaudio-libs`/`libwayland-client`, both resolvable.)
- Rich/boolean deps verified by hand as real Fedora packages:
  `cliphist` -> cliphist-0.7.0 (F44; correctly guarded
  `(cliphist if fedora-release >= 44)` so F43 stays installable),
  `gamemode` -> gamemode-1.8.2, `ffmpeg-free` -> ffmpeg-free-8.0.1 (correctly
  written `(ffmpeg or ffmpeg-free)` since plain `ffmpeg` is RPMFusion-only),
  `pkgconfig(libliftoff)` -> libliftoff-devel-0.5.0. No invented names.
- hyprland declares its BuildRequires through a Lua `%printbdeps` macro over the
  `hyprdeps` table (not literal `BuildRequires:` lines) - checked so it would not
  be misreported as "0 BuildRequires"; COPR build 10822416 confirms it resolves.
- Per-package status: **deps-verified** for all 58 dirs / 81 binary packages on
  fedora:44 and fedora:43; same on rawhide except the ghost noctalia-shell
  (blocked-ghost-package, not ours to fix from git).

### 8. GitHub Actions / issues / PRs
- All recent `Fedora Nexus Auto-Updater` runs: success. The one persistent error
  inside them (`caelestia-cli-mango ... 404`) is what this run fixed - it will be
  silent from the next run on because the package now has its own update.sh and
  is therefore skipped by the release scanner.
- Open PRs: none. Open issues at pickup: none. Opened **issue #7** (ghost
  noctalia packages, owner action). No issue could be closed this run.

### 9. Adversarial audit (each attack refuted with a command)
1. "hyprland has 0 BuildRequires -> spec is broken" -> refuted: Lua-generated
   deps, COPR build 10822416 succeeded.
2. "the caret bump only works for new installs, existing users stay stuck" ->
   refuted: installed shipped 2.0.0-3, `dnf check-update` returned 100 and
   `dnf upgrade` landed on the caret NVR.
3. "the new update.sh will churn / lose %pyver on the next refresh" -> refuted:
   simulated a stale commit, script bumped correctly, then two more runs left
   the spec byte-identical (IDEMPOTENT: PASS).
4. "the sweep silently skipped packages" -> refuted: programmatic dir->tested
   mapping, 0 uncovered dirs; contrib subpackages individually confirmed.
5. "install failures were dismissed as by-design" -> refuted: glaze /
   hyprland-protocols / hyprwayland-scanner checked against Fedora's own
   repodata, which ships the same devel-only layout.

### 10. Stated gaps (named, not silently skipped)
- `gh api repos/.../hooks` returns 403 ("Resource not accessible by
  integration") - the token lacks admin:repo_hook, so webhook delivery IDs are
  unreadable. Webhook liveness was instead proven empirically: push -> COPR
  build 10839412 appeared within a minute with the exact new NVR.
- crates.io returns 403 to HEAD from both the runner and a container (bot
  protection). static.crates.io 200 + the successful matugen COPR build cover it.
- GUI apps (zen-browser, obsidian, vesktop, heroic, logseq, fluxer, rootapp,
  localsend, stoat, opencode-desktop, protonplus, extension-manager) were not
  launched: the runner is headless with no Wayland/X display. Verified by
  install + rpm -V + reading the version from inside the artifact instead.
- The noctalia ghost RPMs cannot be deleted without COPR owner credentials;
  tracked in issue #7 rather than worked around.
- Fedora 45 chroot: not addable yet (branching 2026-08-11) and the project has
  `follow_fedora_branching: true`, so it will appear automatically. Revisit
  after branch day.

### Closing note
Fixed 1 real defect end-to-end (caelestia-cli-mango: permanently-failing
auto-updater + stale-NVR trap; commit b598902, COPR build 10839412 succeeded on
all 3 chroots, re-verified live from the repo by a fresh checker on fc44/fc43/
fc45), and surfaced 1 defect that only the repo owner can clear (issue #7,
ghost noctalia RPMs breaking depsolving). Everything else in the repo verified
green: 58/58 versions current, 58/58 specs parse, 57/58 source URLs reachable
(1 bot-blocked HEAD, proven fine by mirror + build), 26/26 updaters churn-free,
81/81 packages install clean on fedora:44 with all Requires resolving.
