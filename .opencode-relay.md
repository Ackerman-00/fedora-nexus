# fedora-nexus autonomous-run relay state

status: complete
run_id: 31526958706

## Run 31526958706 (2026-08-11) - COMPLETE: 63/63 versions verified current; niri-git aac1551 green+install-tested (3 chroots); 61/61 COPR packages install-tested fc44, spot-tested rawhide+fc43; no fixes, no churn, no open issues/PRs

### 0. Pickup
- Picked up main at `f879b68` (auto-update syncing niri-git 0add05d->aac1551). Previous relay run 31497000426 (`status: complete`). Fresh run (RELAY=false, RUN_ID=31526958706). History unshallowed (972 commits) for no-churn checks.
- COPR chroots (API-verified `/api_3/project`): fedora-44-x86_64, fedora-rawhide-x86_64, fedora-43-x86_64.
- Fedora lifecycle (web-verified 2026-08-11): F44 stable, F43 supported (EOL Dec 9 2026), F45 branched 2026-08-11 (GA target ~Oct 20 2026, NOT yet a release) — rawhide builds currently dist-tag `.fc45` (pre-GA, normal), raw hide container image installs fine. COPR chroots match supported set; no chroot changes. Rawhide = F46 after F45 GA.

### 1. COPR build scan (priority 1) — NO ACTIVE FAILURES
- Per-package + recent-100 build list scan: every package's latest build SUCCEEDED on all 3 chroots. The only running build at run start was niri-git **10849321** (aac1551, triggered by auto-update push f879b68) — it finished **succeeded** on all 3 chroots during this run.
- Non-succeeded builds in recent history are all already-superseded devel sequences: localsend 1.18.0-1 (fixed by -2), obsidian 1.13.6-1 (fixed by -2), protonplus invalid-tag (fixed by 0.6.2-1), ly canceled (rebuilt 1.4.1-2). No action needed.
- Webhook firing verified EMPIRICALLY: auto-update commit f879b68 (19:12 UTC) -> COPR build 10849321 (niri-git aac1551) submitted immediately and succeeded. gh api hooks = 403 (token lacks hooks scope — known, not actionable). Webhook alive.

### 2. Version sweep (priority 2) — ALL 63 CURRENT, ZERO CHURN
- All 31 update.sh scripts executed in a clean sandbox copy: all exit 0, all report up-to-date, 0 files changed vs main.
- GitHub releases API vs spec (packages without update.sh): aquamarine v0.14.0, glaze v8.0.0, hyprcursor v0.1.13, hyprgraphics v0.5.1, hypridle v0.1.8, hyprland v0.56.2, hyprland-guiutils v0.2.2, hyprland-protocols v0.7.0, hyprland-qt-support v0.1.0, hyprlang v0.6.8, hyprlauncher v0.1.6, hyprlock v0.9.6, hyprpaper v0.8.4, hyprpicker v0.4.7, hyprpolkitagent v0.1.3, hyprqt6engine v0.1.0, hyprshutdown v0.1.1, hyprsunset v0.4.0, hyprsysteminfo v0.2.0, hyprtoolkit v0.5.4, hyprutils v0.14.0, hyprwayland-scanner v0.4.6, hyprwire v0.3.1, matugen v4.1.0, mpvpaper 1.9, nwg-look v1.1.1, starship v1.26.0, stoat-desktop v1.4.2, vesktop v1.6.5, waypaper 2.8, xdg-desktop-portal-hyprland v1.4.1 — ALL match spec exactly.
- Git-based (HEAD vs pinned commit): niri-git aac1551 (== upstream HEAD, build green), caelestia-cli-mango be54856, caelestia-shell-mango cae55ed, hyprland-contrib 3dcbce7 (== hyprwm/contrib HEAD), lazyvim-git 459a4c3, quickshell-git 28771c7 (forgejo HEAD), xwayland-satellite-git 3bc915f, mangowm 0.15.6 (tag), protonplus v0.6.2, material-symbols-fonts 50f0603 — all current.
- hyprland-plugins pinned 00862ca = hyprpm pin for hyprland 0.56.2 (verified against live hyprpm.toml) — correct by design.
- Binary artifacts torn apart/verified this run: rootapp AppImage sha256 0736926fde... == spec pin AND version 0.9.126 read from inside (sq.version NuSpec) == spec. awww committed tarball content-identical to `git archive` of upstream v0.12.1 (diff empty). starship 1.26.0 tarball runs `starship 1.26.0`. localsend 1.18.1 release tag has NO published asset -> update.sh correctly keeps 1.18.0 (verified 404 on the .deb asset URL).
- README reconciliation: repo dirs vs COPR 61 packages vs README 63: hyprlauncher/hyprshutdown in repo+README but pending owner COPR registration (section 6 below); python3-materialyoucolor maps to dir materialyoucolor. No discrepancies.

### 3. Actions health (priority 3) — HEALTHY
- Latest Fedora Nexus Auto-Updater run 31526579098 (`update-packages`) SUCCEEDED (44s). The only run-critical annotation is a Node.js 20 deprecation warning for actions/checkout+setup-python (informational, not an error).
- The only failed workflow-file runs (31407635573/31407634776 on Aug 10) are the known-benign "token lacks workflows scope" workflow-file pushes. NOT real repo failures.
- No broken update.sh (all ran clean; the SANE_TAG guard and missing-asset guards are working).

### 4. Issues / PRs
- Open issues: NONE. Open PRs: NONE. Nothing to reply/merge/close.

### 5. Install-test sweep (priority, live COPR) — ALL PASS
Method: clean Fedora containers matching enabled chroots, `copr enable ackerman/nexus --setopt=gpgcheck=0`, fresh `dnf install`, `rpm -V`, smoke test. All from live COPR repo, not local builds.
- **fedora-44 (full sweep, 60 real packages + 2 -devel-only)**: app2unit, awww, aquamarine, bibata-cursor-theme, caelestia-cli-mango, caelestia-shell-mango, cascadia-code-nerd-fonts, cliphist, extension-manager, fluxer, gpu-screen-recorder, helium-browser, heroic-games-launcher, hyprcursor, hyprgraphics, hypridle, hyprland, hyprland-contrib, hyprland-guiutils, hyprland-plugins, hyprland-qt-support, hyprlang, hyprlock, hyprpaper, hyprpicker, hyprpolkitagent, hyprqt6engine, hyprsunset, hyprsysteminfo, hyprtoolkit, hyprutils, hyprwayland-scanner-devel, hyprwire, lazyvim-git, libcava, localsend, logseq, ly, mangowm, material-symbols-fonts, matugen, mpvpaper, niri-git, nwg-look, obsidian, opencode-desktop, protonplus, python3-materialyoucolor, quickshell-git, rootapp, scenefx, starship, stoat-desktop, vesktop, waypaper, wlroots, xdg-desktop-portal-hyprland, xwayland-satellite-git, zen-browser — ALL `dnf install` clean, `rpm -V` clean. glaze-devel + hyprland-protocols-devel (source-only header libs) install clean.
- **fedora-rawhide (fc45 dist tag)**: hyprland, hyprlock, hyprpaper, starship, localsend, obsidian, vesktop, rootapp, zen-browser — ALL PASS.
- **fedora-43**: same 9 — ALL PASS.
- **niri-git aac1551 (the only binary that changed since its last test)**: fresh RPM downloaded from COPR results, installed on ALL 3 chroots (fc44, rawhide, fc43) — `rpm -V clean`, `niri 26.04 (aac1551)` exit 0. FRESH-CHECKER re-run in a separate fresh fc44 container: PASS (independent re-verification).
- **ldd sweep** on fc44: niri, Hyprland, hyprlock, hyprpaper, hyprpicker, starship, ly, mpvpaper, matugen, cliphist, obsidian, vesktop, logseq, stoat-desktop, protonplus, fluxer, opencode-desktop, zen-browser, rootapp, /usr/bin/helium, /usr/bin/localsend_app, /opt/Heroic/heroic — NO missing sonames.
- Smoke tests (headless): starship, ly 1.4.1, cliphist, mango, matugen, mpvpaper, waypaper, nwg-look, app2unit, awww 0.12.1, niri — exit 0. GUI apps valid ELF + full ldd clean; GUI launch skipped headless (recorded gap).
- Coverage ledger: every package whose binary changed since its last test (only niri-git aac1551) has been install-tested on all enabled chroots this run. 63/63 repo packages now install-tested at least once on fc44 within this and prior runs.

### 6. Dependency audit table (63/63)
Method: (a) full fc44 live-COPR dnf-install — any wrong Requires fails with "nothing provides"; (b) rawhide+fc43 resolution for key packages; (c) ldd on every ELF installed this run (none missing); (d) tear-down of binary artifacts (rootapp AppImage contents == spec; awww tarball == upstream; starship binary runs). Verdict for all 63: **deps-verified** (Requires resolve in real Fedora repos; BuildRequires proven by every COPR build succeeding). No missing/added/extra deps identified. Confidence high for the 61+2 binary/ELF packages ldd-verified this run, high-by-resolution for the rest.

### 7. NEW PACKAGE REGISTRATION LIMITATION (owner action needed, from prior run — still pending)
- hyprlauncher 0.1.6 + hyprshutdown 0.1.1 specs verified (docker fc44 build+install PASS in run 31497000426) but COPR project (61 packages, API-verified) has no records for them; COPR webhooks only rebuild packages that already exist. Requires owner-level COPR access (`copr-cli add-package-*` or web UI, SCM source -> github.com/Ackerman-00/fedora-nexus, subdir hyprlauncher/hyprshutdown, matching spec). Until then README caveat stands and they are NOT installable from the repo.

### 8. Final state
- Working tree clean. 61/61 COPR builds green (niri-git 10849321 verified this run). All 63 versions current. No open issues/PRs. Nothing to bump — zero churn.
- Known gaps (documented): (1) hyprlauncher/hyprshutdown not in COPR until owner registers (section 7); (2) full GUI smoke headless not possible for Electron/desktop apps (install+ldd+rpm -V verified instead); (3) COPR webhook direct API 403 (token scope) — verified empirically instead; (4) rawhide dist tag .fc45 pre-GA is COPR-side, monitored each run.