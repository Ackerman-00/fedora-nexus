# fedora-nexus autonomous-run relay state

status: complete
run_id: 31986197407

## Run 31986197407 (2026-08-17T01:51Z — scheduled run, INDEPENDENT FRESH-CHECKER PASS, RELAY=false)

Full independent re-verification from scratch this session. Repo is HEALTHY. Nothing
is broken, stale, or outdated. **NO-CHURN: zero package spec commits** (the CORRECT
outcome — nothing to fix). The only commits this run: none to packages; the relay
record itself. Git identity verified: `opencode-agent[bot] <41898282+opencode-agent[bot]@users.noreply.github.com>`.

### 0. Pickup & chroots (DISCOVERED, not hardcoded)
- run_id = 31986197407 (this run). Previous relay (31965382968) was `complete`; no unfinished relay work.
- COPR chroots (API `chroot_repos`, authoritative): `fedora-rawhide-x86_64`, `fedora-44-x86_64`,
  `fedora-43-x86_64` ONLY — matches README and the supported set.
- Fedora lifecycle (websearch this session, endoflife.date / EOSL.date / isitpatched): F44 stable
  (GA 2026-04-28, EOL 2027-06-02), F43 supported (EOL 2026-12-09, one month after F45 GA), F45
  branched 2026-08-11 (GA target ~Oct/Nov 2026), Rawhide = 46. Enabled chroots == supported set;
  no README/chroot change needed.
- Open issues: 0. Open PRs: 0.

### 1. Failed COPR builds: NONE actionable
- Last 200 builds: 192 succeeded, 7 failed, 1 canceled; ZERO pending/starting/running.
- The 7 "failed" are ALL superseded or COPR background transients (verified per-package build lists):
  - wlroots #10869642: `is_background=true, submitter=null, version=null` (COPR background
    source-fetch transient, same as prior runs). Real published wlroots = #10868199 (0.20.2-3)
    succeeded on all 3 chroots.
  - hyprtoolkit #10868178 (0.5.4-2) superseded by #10868285 (0.5.4-3) SUCCEEDED.
  - hyprsysteminfo #10868177 (0.2.0-4) superseded by #10868289 (0.2.0-5) SUCCEEDED.
  - hyprpaper #10868172 (0.8.4-4) superseded by #10868291 (0.8.4-5) SUCCEEDED.
  - localsend #10844795, obsidian #10843364, protonplus #10841179, ly #10840572: background
    transients; each has a newer SUCCEEDED build (localsend 1.18.0-2, obsidian 1.13.7-1,
    protonplus 0.6.4-1, ly 1.4.1-2).
- Per-package LATEST build (authoritative): 62/63 = succeeded; the only non-succeeded latest is
  wlroots #10869642 (background transient, real published build succeeded).
- LATEST auto-updater bump verified: commit 7699278 (fluxer 2026.816.204155, stoat-desktop 1.5.1)
  pushed 2026-08-17T01:45:42Z → COPR builds #10870886 (fluxer) and #10870887 (stoat-desktop)
  submitted 2026-08-17T01:45:44Z, BOTH SUCCEEDED on all 3 chroots. Webhook path empirically
  ALIVE: build submission 2s after push commit.

### 2. Version accuracy sweep — 65/65 CURRENT (zero spec diffs)
- Fresh shallow clone `/tmp/fnx-sweep` @ HEAD 7699278 (then unshallowed in main repo for history).
- ALL 33 `update.sh` run in their dirs (as the workflow does): **33/33 exit 0**,
  `git status --porcelain` EMPTY after (zero spec diffs — no churn). No-churn guards confirmed:
  every update.sh compares a pure version/commit/sha string to the spec BEFORE touching files;
  ZERO scripts gate on local artifact existence (all `local_file_check=0`), so CI checkouts
  (gitignored tarballs absent) cannot fake "up to date".
- Replicated the workflow GH-release scanner (authenticated `gh api`/requests, 31 non-scripted GH specs):
  **31/31 MATCH** `releases/latest` (Hyprland 0.56.2 family, glaze 8.0.0 @ stephenberry/glaze,
  starship 1.26.0, stoat-desktop 1.5.1, vesktop 1.6.5, waypaper 2.8, mpvpaper 1.9,
  nwg-look 1.1.1, xdg-desktop-portal-hyprland 1.4.1, aquamarine 0.14.0, hyprlang 0.6.8,
  hyprland-qt-support 0.1.0, hyprqt6engine 0.1.0, etc.).
- `matugen`: 4.1.0 == InioX/matugen GH releases/latest (v4.1.0). MATCH. Source crate 200.
- `python3-materialyoucolor`: 3.0.4 == PyPI JSON API latest (3.0.4, uploaded 2026-08-05). MATCH.
- Codeberg: awww v0.12.1, ly v1.4.1 == latest release API tags. MATCH specs.
- Git/-snapshot pins vs live `git ls-remote HEAD`: niri 6062844==HEAD, xwayland-satellite
  3bc915f==HEAD, lazyvim 459a4c3==HEAD, hyprland-contrib 3dcbce7==HEAD, app2unit
  (Vladimir-csp/app2unit) 47e23ec==HEAD, caelestia-cli be54856==HEAD, caelestia-shell
  cae55ed==HEAD, material-symbols (google/material-design-icons) e083cc6==HEAD,
  quickshell 532a060==HEAD (git.outfoxxed.me), hyprland-plugins 00862ca = intentional 0.56.2
  hyprpm pin. All current.
- `localsend`: HELD at 1.18.0 CORRECT (v1.18.1 assets verified = NO linux-x86-64.deb; only
  Android/flatpak/macos/win zips; update.sh asset-guard skips tags without a linux .deb).
- `freebuff`: 0.0.63 == latest `freebuff-desktop-v*` tag (0.0.63; newer tags are the Codebuff
  CLI `v1.0.x` and `freebuff-v0.0.14x`, correctly filtered out by TAG_PREFIX="freebuff-desktop-v").
- `rootapp`: TEARED THE APPIMAGE this session (ELF section-header offset 944632, unsquashfs OK):
  `X-AppImage-Version=0.9.126` in Root.desktop == spec Version 0.9.126. Fresh-download sha256
  `0736926fde923819d276ac6d4a3afe65c6d87c734689af47276fc4ebf075d910` == spec `# sha256:` pin
  EXACTLY (worked-example protocol). Source0 200.
- `fluxer`: X-Fluxer-Version header = 2026.816.204155 == spec Version (update.sh guard). RPM
  internal Name fluxer_app 0.0.8 is the installer's own naming; X-Fluxer-Version is the repo's
  authoritative version and update.sh uses it. CURRENT.
- `ghostty`: PPA tag 1.3.1-0-ppa2 == spec ppatag/debver. CURRENT.
- `mangowm`: 0.16.1 == mangowm/mango releases/latest. CURRENT.
- Source0 reachability (curl -sI): ALL checked 200, including niri/xwayland-satellite/lazyvim/
  contrib/app2unit/caelestia-*/material-symbols/quickshell tarballs, matugen crate, materialyoucolor
  sdist, extension-manager, gpu-screen-recorder, rootapp, hyprland source, starship, vesktop,
  opencode-desktop, stoat-desktop zip, zen-browser, obsidian, helium, logseq, freebuff, bibata,
  cascadia, libcava, wlroots (gitlab.freedesktop.org), heroic (HeroicGamesLauncher v2.22.1 rpm).
- FULL TABLE (65/65): see §6.
- Cross-repo consistency: not authoritative; upstream was the arbiter throughout (STEP B/D).

### 3. Install-test sweep — FRESH containers, LIVE COPR repo (this session)
- **fc44 FULL 63/63 + hyprland-contrib subpackages** (`registry.fedoraproject.org/fedora:44`, fresh):
  `dnf copr enable ackerman/nexus` then one transaction installing all 63 COPR packages
  (glaze→`glaze-devel`, hyprland-protocols→`hyprland-protocols-devel`) →
  **INSTALL_EXIT=0** ("Complete!"), every requested package present. `rpm -Va` shows only
  container-environment artifacts (dnf.conf, machine-id, / perms, newuidmap/newgidmap caps) —
  NONE from nexus packages.
- **hyprland-contrib subpackages** (grimblast/hyprprop/scratchpad/shellevents/try_swap_workspace/
  hdrop): repoquery shows all 6 in the nexus repo; install EXIT=0; all 6 binaries present.
- **hyprwayland-scanner**: binary ships in `hyprwayland-scanner-devel` (spec has only
  `%files devel`); `dnf install hyprwayland-scanner` installs devel, binary present at
  `/usr/bin/hyprwayland-scanner`, `--version` prints 0.4.6. NOT a bug — spec design.
- **ldd sweep** (fresh fc44): starship/matugen/ghostty/niri/ly/helium/cliphist/hyprlock/
  hyprpaper/hyprpicker/hypridle/mpvpaper/nwg-look/waypaper/quickshell/gpu-screen-recorder/
  hyprland/xwayland-satellite → **TOTAL_MISSING=0** on every ELF binary.
- **Smoke (exit code / version printed)** — binaries execute:
  starship 1.26.0, matugen 4.1.0, niri 26.04 (6062844), Ly 1.4.1, Helium 0.15.5.1.
  Electron/GUI apps (fluxer, stoat-desktop, opencode-desktop, freebuff, vesktop, zen, rootapp,
  heroic, logseq) only fail on container-as-root `--no-sandbox` and no-display — NOT packaging bugs
  (fluxer verified: FATAL electron "Running as root without --no-sandbox" = expected in container).
- **fc43 FULL 63/63 + 6 subpackages**: INSTALL_EXIT=0, no dep errors.
- **rawhide(fc46) FULL 63/63 + 6 subpackages**: INSTALL_EXIT=0 after `--disablerepo=
  fedora-cisco-openh264` — the failed attempt was the Fedora repo's openh264 OpenPGP key
  failure (Fedora infra bug, unrelated to nexus), NOT a packaging/dep problem.
- **CHANGED BINARIES install-tested on ALL 3 chroots this run** (the two packages whose binary
  changed since the last run's install test): fluxer 2026.816.204155-1 and stoat-desktop 1.5.1-2
  → downloaded the actual built RPMs from COPR download server for fc44/fc43/rawhide, installed in
  fresh containers on each chroot, INSTALL_EXIT=0 everywhere, rpm -V clean, ldd 0 missing. Fresh-
  checker re-run (independent second container) re-installed both on fc44 green.
- hyprlauncher / hyprshutdown: directories exist + version-verified in repo, but NOT in COPR
  (pending owner registration — README caveat). Cannot install from repo; not actionable by CI.

### 4. Actions health & webhook
- Auto-Updater ("Fedora Nexus Auto-Updater"): runs 31985865470 (01:44Z, made the fluxer/stoat
  bump), 31965151562, 31948219700, 31932425443, 31920332365, 31901665013 all **success**.
- opencode-schedule: latest runs 31965382968 + 31961256492 success; historical failures
  31954211311 / 31948429184 (2026-08-16) were the gate-check quirk already fixed by commit c4ee3f8
  (relay fetched via GitHub raw API, state-free). Current run 31986197407 is the only in_progress.
- Webhook: `gh api repos/.../hooks` 403 (token lacks admin:repo_hooks — NAMED GAP, cannot read
  delivery IDs). Empirical proof instead: commit 7699278 pushed 01:45:42Z → fluxer build #10870886
  + stoat build #10870887 submitted 01:45:44Z (+2s) and SUCCEEDED on all 3 chroots. The push→build
  correlation is alive. No package push by THIS run (NO-CHURN), so no new push→build pair; the
  auto-updater pair above is the evidence.

### 5. Dependency audit (confidence: high)
- No Version/commit changed since prior deep audits — all 65 re-verified CURRENT this session (§2).
- Full install-resolve on all 3 chroots (fc44/fc43/rawhide) resolves EVERY Requires from the real
  Fedora+COPR repos (no "nothing provides"); `rpm -Va` clean (container artifacts only); `ldd`
  zero-missing on every ELF binary. Every BuildRequires satisfied by build (all source builds green).
- Two changed packages re-audited in depth:
  - fluxer (prebuilt Electron repackage, immutable ELFs): built RPM Requires = auto-generated
    sonames (libsmime3.so, libudev.so.1, libxkbcommon.so.0, libasound.so.2, libatk-1.0.so.0,
    libgtk-3.so.0, ...) + explicit (libXScrnSaver, libXtst, libuuid, nss, xdg-utils, at-spi2-core,
    gtk3, libnotify). All resolved at install on all 3 chroots. Provides application(app.fluxer.Fluxer).
    desktop + 8 icon sizes shipped. `%global __provides_exclude_from` prunes bundled Electron
    .so from being advertised system-wide (correct, prevents fluxer hijacking e.g. libvulkan).
  - stoat-desktop (prebuilt Electron zip repackage): Requires = gtk3/nss/at-spi2-core/cups-libs/
    dbus-libs/systemd-libs/libX*/libxcb/libxkbcommon/mesa-libgbm/alsa-lib/libsecret/
    libappindicator-gtk3/xdg-utils. All resolved on all 3 chroots. desktop + 256px icon +
    metainfo.xml shipped. Bundled Electron runtime under /opt/Stoat excluded from auto-provides.
- Prebuilt binaries (rootapp/obsidian/zen/vesktop/heroic/localsend/logseq/helium/fluxer/
  stoat-desktop/opencode-desktop/protonplus/gpu-screen-recorder/ghostty/waypaper/mpvpaper/
  starship/etc.): prebuilt, nothing to optimize — immutable ELFs, `%{optflags}` preserved via
  Fedora repackaging specs; `%global debug_package %{nil}` justified only here (prebuilt binaries).
  Source-built C/C++/Rust/Qt pkgs: built with distro %{optflags}+LTO (Fedora defaults), no
  -march=native/-O3 regressions.
- Status: deps-verified (63/63 COPR packages installable+install-tested on all 3 chroots this
  session; hyprlauncher/hyprshutdown not in COPR yet).
- AUDIT TABLE:
  | package | upstream deps found (this rebuild) | in spec | missing/added | extra/dropped | status |
  |---|---|---|---|---|---|
  | aquamarine | wayland, cairo, pango, libdrm, egl, glesv2 (meson) | == | none | none | deps-verified |
  | mangowm | libdrm,xcb-icccm,wayland-server>=1.23.1,wlroots-0.20>=0.20,xkbcommon,libinput>=1.27.1,scenefx,pixman,libcjson,pangocairo | == | none | none | deps-verified |
  | starship | self-contained static Rust | (none) | none | none | deps-verified (prebuilt static) |
  | wlroots | meson: wayland, xcb, libinput, libseat, cairo, pango, pixman, libdrm, ... (0.20.2) | == | none | none | deps-verified |
  | rootapp | .desktop X-AppImage-Version=0.9.126, bundled DotNetBrowser (repackaged AppImage) | == | none | none | deps-verified (prebuilt) |
  | vesktop | Electron (prebuilt AppImage->rpm) | == | none | none | deps-verified (prebuilt) |
  | fluxer | Electron repackage; auto sonames + explicit libXScrnSaver/libXtst/libuuid/nss/xdg-utils/at-spi2/gtk3/libnotify | == | none | none | deps-verified (prebuilt, RE-AUDITED this run) |
  | stoat-desktop | Electron repackage; gtk3/nss/at-spi2/cups/dbus/systemd/libX*/libxcb/libxkbcommon/gbm/alsa/libsecret/libappindicator/xdg-utils | == | none | none | deps-verified (prebuilt, RE-AUDITED this run) |
  | obsidian / zen / heroic / localsend / logseq / helium / freebuff / opencode-desktop / ghostty | prebuilt/Electron/repackaged | == | none | none | deps-verified (prebuilt) |
  | hyprland-contrib subpkgs (grimblast/hyprop/scratchpad/shellevents/try_swap_workspace/hdrop) | hyprctl, jq, slurp, grim, wl-clipboard, notify-send, procps-ng, socat, psmisc | == | none | none | deps-verified (all 6 installed + binaries present) |
  | niri-git / xwayland-satellite-git / quickshell-git / lazyvim-git / app2unit / caelestia-* / material-symbols / hyprland-plugins / hyprland-contrib | git-pinned; deps == upstream source tree | == | none | none | deps-verified |
  (remaining packages: deps-verified by full 3-chroot live-repo install RESOLVE — every Requires
  satisfied, rpm -Va clean, ldd 0 missing. No manual additions needed.)

### 6. Adversarial audit (2-3 concrete attacks, each checked with a command this session)
1. "The auto-updater bump (fluxer/stoat) introduced a wrong version or broken install" —
   verified both against upstream: stoat v1.5.1 IS stoatchat/for-desktop releases/latest with
   Stoat-linux-x64-1.5.1.zip present; fluxer X-Fluxer-Version header == 2026.816.204155. Both
   COPR builds succeeded on all 3 chroots, and BOTH built RPMs downloaded + installed green on
   every chroot in fresh containers + fresh-checker re-run. Repelled.
2. "Version drifted since the prior sweep" — re-queried ALL sources LIVE now: 33/33 update.sh
   exit 0 + zero diff; 31/31 GH releases/latest match; matugen 4.1.0; materialyoucolor 3.0.4
   (PyPI JSON API); freebuff 0.0.63 (desktop-tag filter); awww/ly (Codeberg API); ghostty PPA;
   all git pins == ls-remote HEAD. Repelled.
3. "The wlroots failed build = a real defect needing a fix" — #10868199 (0.20.2-3) SUCCEEDED on
   all 3 chroots and installed green + ldd-clean; #10869642 is `is_background=true, version=null,
   submitter=null` (COPR background source-fetch transient). Repelled.
4. "Rawhide broken = packages broken on rawhide" — initial EXIT=1 was the Fedora
   `fedora-cisco-openh264` repo OpenPGP key failure (infra bug, zero nexus involvement);
   with that repo disabled the FULL 63/63 + subpackages install EXIT=0 on rawhide. Repelled.
5. "hyprwayland-scanner not installed = broken package" — the binary is shipped in the
   `hyprwayland-scanner-devel` subpackage by spec design (`%files devel` is the only file list);
   `dnf install hyprwayland-scanner` installs it, `/usr/bin/hyprwayland-scanner --version` = 0.4.6.
   Repelled.
6. "Nothing done = run failed / skipped its job" — produced independent hard EVIDENCE this
   session: 33 update.sh exit codes + zero diffs; 31 GH-release matches; 65-row version table all
   `current`; 63/63 install green on fc44 + fc43 + rawhide; ldd 0 missing; 0 open issues/PRs;
   0 pending/starting/running builds; 0 actionable failures; fluxer+stoat built RPMs downloaded
   from COPR and install-tested on all 3 chroots.
   NO spec commits IS the correct NO-CHURN outcome, not a gap.

### 7. Final state & honest gaps
- This run's only commit: the relay record (status complete + run_id 31986197407). Working tree
  clean (`git status --porcelain` empty). NO package commits — nothing broken/stale/outdated (NO-CHURN).
- Honest gaps (named, not silent):
  (1) GUI apps / Wayland compositors verified via install + `rpm -Va` + `ldd` + `--version`/`--help`
      smoke only — cannot launch a graphical compositor/desktop client headless in CI (container has
      no Wayland socket / display server). Binaries confirmed present + execute; Electron apps only
      fail on container-as-root `--no-sandbox` and missing display (not packaging bugs).
  (2) Full 63/63 install-test ran on ALL 3 chroots (fc44/fc43/rawhide) this session.
      `rootapp` 386MB AppImage re-torn this session: X-AppImage-Version=0.9.126 + sha256
      `0736926f…d910` == spec pin, per the worked-example protocol.
  (3) COPR webhook/config API read 403 (GITHUB_TOKEN lacks admin:repo_hooks) — no package push this
      run so no new push→build pair from THIS run; webhook alive per §4 empirical correlation
      (auto-updater push 01:45:42Z → builds submitted 01:45:44Z, succeeded).
  (4) hyprlauncher / hyprshutdown: version-verified in repo but NOT in COPR (pending owner
      registration — README caveat); not installable from the repo. Not fixable from CI.
  (5) rawhide install required `--disablerepo=fedora-cisco-openh264` (Fedora infra OpenPGP bug
      on the openh264 repo); with it disabled everything installs cleanly.
  (6) Dependency audit table is deep-audited for the changed packages (fluxer/stoat) + sampled
      deep for key builds + full install-RESOLVE for every other package on all 3 chroots; a
      full manual file-by-file re-tear of all 65 packages was not repeated this session because
      NO package changed upstream since the prior deep audits (all versions/commits verified
      unchanged this session, §2) — RE-TEAR RULE satisfied (nothing upstream changed).

---

## Version-accuracy table (65/65) — all `current`
| package | spec Name | packaged | upstream latest | status |
|---|---|---|---|---|
| app2unit | app2unit | 1.4.4^git47e23ec | git 47e23ec6ab9 == HEAD | current |
| aquamarine | aquamarine | 0.14.0 | 0.14.0 | current |
| awww | aww | 0.12.1 | 0.12.1 (Codeberg) | current |
| bibata-cursor-theme | bibata-cursor-theme | 2.0.7 | 2.0.7 | current |
| caelestia-cli-mango | caelestia-cli-mango | ^gitbe54856 | git be5485624 == HEAD | current |
| caelestia-shell-mango | caelestia-shell-mango | 2.1.0^gitcae55ed | git cae55ed689 == HEAD | current |
| cascadia-code-nerd-fonts | cascadia-code-nerd-fonts | 3.5.0 | 3.5.0 | current |
| cliphist | cliphist | 0.7.0 | 0.7.0 | current |
| extension-manager | extension-manager | 0.6.5 | 0.6.5 | current |
| fluxer | fluxer | 2026.816.204155 | X-Fluxer-Version 2026.816.204155 | current |
| freebuff | freebuff | 0.0.63 | 0.0.63 (freebuff-desktop-v filter) | current |
| ghostty | ghostty | 1.3.1-0-ppa2 | 1.3.1-0-ppa2 (PPA) | current |
| glaze | glaze | 8.0.0 | 8.0.0 | current |
| gpu-screen-recorder | gpu-screen-recorder | 6.0.0 | 6.0.0 | current |
| helium-browser | helium-browser | 0.15.5.1 | 0.15.5.1 (GH releases) | current |
| heroic-games-launcher | heroic-games-launcher | 2.22.1 | 2.22.1 | current |
| hyprcursor | hyprcursor | 0.1.13 | 0.1.13 | current |
| hyprgraphics | hyprgraphics | 0.5.1 | 0.5.1 | current |
| hypridle | hypridle | 0.1.8 | 0.1.8 | current |
| hyprland | hyprland | 0.56.2 | 0.56.2 | current |
| hyprland-contrib | hyprland-contrib | ^git3dcbce7 | git 3dcbce715 == HEAD | current |
| hyprland-guiutils | hyprland-guiutils | 0.2.2 | 0.2.2 | current |
| hyprland-plugins | hyprland-plugins | 0.1^git00862ca | intl. 0.56.2 hyprpm pin (00862ca) | current |
| hyprland-protocols | hyprland-protocols | 0.7.0 | 0.7.0 | current |
| hyprland-qt-support | hyprland-qt-support | 0.1.0 | 0.1.0 | current |
| hyprlang | hyprlang | 0.6.8 | 0.6.8 | current |
| hyprlauncher | hyprlauncher | 0.1.6 | 0.1.6 | current (not yet in COPR) |
| hyprlock | hyprlock | 0.9.6 | 0.9.6 | current |
| hyprpaper | hyprpaper | 0.8.4 | 0.8.4 | current |
| hyprpicker | hyprpicker | 0.4.7 | 0.4.7 | current |
| hyprpolkitagent | hyprpolkitagent | 0.1.3 | 0.1.3 | current |
| hyprqt6engine | hyprqt6engine | 0.1.0 | 0.1.0 | current |
| hyprshutdown | hyprshutdown | 0.1.1 | 0.1.1 | current (not yet in COPR) |
| hyprsunset | hyprsunset | 0.4.0 | 0.4.0 | current |
| hyprsysteminfo | hyprsysteminfo | 0.2.0 | 0.2.0 | current |
| hyprtoolkit | hyprtoolkit | 0.5.4 | 0.5.4 | current |
| hyprutils | hyprutils | 0.14.1 | 0.14.1 | current |
| hyprwayland-scanner | hyprwayland-scanner | 0.4.6 | 0.4.6 | current |
| hyprwire | hyprwire | 0.3.1 | 0.3.1 | current |
| lazyvim-git | lazyvim-git | 0.1^git459a4c3 | git 459a4c3b10 == HEAD | current |
| libcava | libcava | 1.0.0 | 1.0.0 (GitHub) | current |
| localsend | localsend | 1.18.0 | 1.18.0 (v1.18.1=no linux .deb asset) | current |
| logseq | logseq | 2.0.1 | 2.0.1 | current |
| ly | ly | 1.4.1 | 1.4.1 (Codeberg) | current |
| mangowm | mangowm | 0.16.1 | 0.16.1 | current |
| material-symbols-fonts | material-symbols-fonts | 4.0.0^gite083cc6 | git e083cc60a == HEAD | current |
| materialyoucolor | python3-materialyoucolor | 3.0.4 | 3.0.4 (PyPI) | current |
| matugen | matugen | 4.1.0 | 4.1.0 (crates.io/GH) | current |
| mpvpaper | mpvpaper | 1.9 | 1.9 | current |
| niri-git | niri-git | 26.04^git6062844 | git 606284464 == HEAD | current |
| nwg-look | nwg-look | 1.1.1 | 1.1.1 | current |
| obsidian | obsidian | 1.13.7 | 1.13.7 | current |
| opencode-desktop | opencode-desktop | 1.18.18 | 1.18.18 | current |
| protonplus | protonplus | 0.6.4 | 0.6.4 (GH releases) | current |
| quickshell-git | quickshell-git | 0.3.0^git532a060 | git 532a0606c == HEAD | current |
| rootapp | rootapp | 0.9.126 | 0.9.126 (AppImage-internal) | current |
| scenefx | scenefx | 0.5 | 0.5 | current |
| starship | starship | 1.26.0 | 1.26.0 | current |
| stoat-desktop | stoat-desktop | 1.5.1 | 1.5.1 | current |
| vesktop | vesktop | 1.6.5 | 1.6.5 | current |
| waypaper | waypaper | 2.8 | 2.8 | current |
| wlroots | wlroots | 0.20.2 | 0.20.2 (GitLab tags) | current |
| xdg-desktop-portal-hyprland | xdg-desktop-portal-hyprland | 1.4.1 | 1.4.1 | current |
| xwayland-satellite-git | xwayland-satellite-git | 0.8.2^git3bc915f | git 3bc915f09 == HEAD | current |
| zen-browser | zen-browser | 1.21.14b | 1.21.14b | current |

## Install-test ledger (this session, fresh containers, LIVE COPR repo)
| package(s) | chroot | dnf install | smoke |
|---|---|---|---|
| all 63 COPR packages (glaze→devel, hyprland-protocols→devel) + hyprland-contrib subpkgs | fc44 | INSTALL_EXIT=0, every package present, rpm -Va clean (container artifacts only) | starship/matugen/niri/ly/helium green; electron(GUI) only container-as-root/no-display artifacts |
| fluxer 2026.816.204155-1 + stoat-desktop 1.5.1-2 (built RPMs from COPR) | fc44 | INSTALL_EXIT=0, rpm -V clean | fluxer: electron root-no-sandbox (expected); stoat: ldd 0 missing |
| all 63 COPR packages + 6 contrib subpkgs | fc43 | INSTALL_EXIT=0 | — |
| all 63 COPR packages + 6 contrib subpkgs (--disablerepo=fedora-cisco-openh264: Fedora openh264 OpenPGP infra bug, unrelated) | rawhide | INSTALL_EXIT=0 | — |
| fluxer + stoat-desktop (built RPMs) | fc43 | INSTALL_EXIT=0, rpm -V clean | — |
| fluxer + stoat-desktop (built RPMs) | rawhide | INSTALL_EXIT=0, rpm -V clean | — |
| FRESH-CHECKER re-run: fluxer + stoat-desktop built RPMs | fc44 | INSTALL_EXIT=0 | — |

## Pre-completion gate (PASS evidence)
1. `git status --porcelain` empty → PASS (verified clean, only relay modified this run).
2. Packages touched this run: NONE (NO-CHURN) → vacuously satisfied; the 2 packages whose binary
   changed since last test (fluxer, stoat-desktop) install-tested green on ALL 3 chroots +
   fresh-checker re-run with before/after (auto-updater bump vs live COPR RPM) evidence.
3. Coverage ledger updated above; every package whose binary changed since last test was tested.
4. Dependency audit covers 65/65 (deep re-audit of the 2 changed packages + full 3-chroot
   install-resolution).
5. Every claim backed by command output (COPR API, gh api, docker/dnf/rpm/ldd, curl -sI, sha256sum).
6. Named gaps listed in §7.
7. `.opencode-relay.md` on main: `status: complete` + `run_id: 31986197407` (this run's real
   GitHub run_id).