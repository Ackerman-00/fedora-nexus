# fedora-nexus autonomous-run relay state

status: complete
run_id: 31349886175

## Run 31349886175 (2026-08-10) - COMPLETED: repo-breaking spec fixed + zen "withdrawn build" trap fixed + 183 install tests

### 0. Pickup
- Picked up main at `3888fc4` ("auto-update: sync spec files to latest upstream versions",
  github-actions bot, 02:22:01Z). Previous relay (31330264510) was `status: complete`,
  so this run started as a FRESH CHECKER and did NOT trust its claims - everything below
  was re-derived from commands run this run.
- COPR chroots (API `chroot_repos`, authoritative): fedora-43-x86_64, fedora-44-x86_64,
  fedora-rawhide-x86_64. Rawhide currently builds as **fc45**.
- Fedora lifecycle (web-verified 2026-08-10): F44 stable (GA 2026-04-28, EOL 2027-06-02),
  F43 supported until 2026-12-09, F42 EOL, rawhide = F45. The 3 enabled chroots are
  correct; no chroot add/remove and no README release changes needed.
- Inventory reconciled programmatically: **61 repo dirs == 61 COPR packages == 61 README
  rows**. `materialyoucolor/` intentionally ships `python3-materialyoucolor.spec`.
- Open issues at pickup: none. Open PRs: none (both via `gh`).

### 1. PRIORITY 1 - main was BROKEN; fixed (protonplus)
- COPR build **10841179 failed** (the build triggered by pickup commit 3888fc4). Log
  (srpm-builds/10841179/builder-live.log.gz):
  `error: line 8: Illegal char '-' (0x2d) in: Version:        0.6.1-1`
  -> the spec could not even be PARSED, so no SRPM was produced.
- Root cause: the generic scanner in update-engine.yml copied upstream's re-roll tag
  `v0.6.1-1` into `%global tag`; protonplus derives Version from the tag
  (`%forgemeta` -> `%{fileref}`), and a dash is illegal in an RPM Version.
- Fix `a8fa132`: tag -> **v0.6.2** (real current upstream release, published
  2026-08-10T02:42:17Z), Release reset to 1 (Version changed, per rpm-version(7)), plus a
  new `protonplus/update.sh` that only accepts `^v[0-9]+(\.[0-9]+)*$`, verifies the archive
  is downloadable before bumping, and exits 0 when already current.
- Evidence: clean fedora:44 container - `rpmspec -P` parses; NVR `protonplus-0.6.2-1.fc44`;
  `rpmbuild -bb` OK; Source0 sha256
  `1adfc7892510da0d017d1186b43e41ea0138ca164cf6b1e0468d71b223181b48`; local rpm installs
  DNF_RC=0, rpm -V clean, ldd 0 missing.
- COPR after push: build **10841314 = succeeded on all 3 chroots** (43/44/rawhide).

### 2. PRIORITY 1b - zen-browser: users were still being served a WITHDRAWN build
- Symptom captured BEFORE the fix, clean fedora:44 container, only ackerman/nexus enabled:
  `dnf install zen-browser` -> **zen-browser-1.21.13b-1.fc44**, while
  `https://github.com/zen-browser/desktop/releases/download/1.21.13b/zen.linux-x86_64.tar.xz`
  -> **HTTP 404** (upstream deleted that release; `gh api` shows no 1.21.13b release/tag).
- A previous run had correctly reverted the spec to 1.21.12b-3, but that could never ship:
  `rpmdev-vercmp` -> `0:1.21.12b-3.fc44 < 0:1.21.13b-1.fc44`. Reverting the spec alone was
  not enough; only an Epoch can supersede a higher version.
- Fix `e6eb10f`: `Epoch: 1`, Release 3 -> 4, `Provides: zen-browser-avx2` qualified with
  `%{epoch}`, and update.sh now reads the Epoch from the spec and writes it into future
  changelog entries so a later bump cannot silently drop it.
  (`rpmdev-vercmp`: `1:1.21.12b-4.fc44 > 0:1.21.13b-1.fc44`.)
- Local proof: rpmbuild OK; Source0 sha256
  `47e1492bedca5ae4142f54a220559e0bef7a784fb530908f6059da766d16222f`; installing the built
  rpm then `dnf upgrade zen-browser` -> "Nothing to do" (NOT dragged back to 1.21.13b).
- COPR after push: build **10841458 = succeeded on all 3 chroots**.
- Symptom AFTER, fresh containers on every enabled chroot:
  f44 -> `zen-browser-1:1.21.12b-4.fc44`, f43 -> `...fc43`, rawhide -> `...fc45`;
  rpm -V clean; `zen-browser --version` -> `Mozilla Zen 1.21.12b` on all three.

### 3. Workflow hardening - PREPARED BUT BLOCKED (issue #8 filed)
- The real root cause of #1 is the generic scanner itself. Patch written and validated
  (yaml parses, embedded python `compile()`s, regex accepts v0.6.2/v2.0.7/v1.4.1 and
  rejects v0.6.1-1/v3.5.0-beta/v1.21.12b; checked all 30 scanner-managed dirs - no package
  would be frozen by it today).
- **Could not be pushed**: `! [remote rejected] main -> main (refusing to allow a GitHub
  App to create or update workflow .github/workflows/update-engine.yml without workflows
  permission)`. Contrary to the run brief, this token does NOT have workflows write.
- Action taken: commit reverted so main and the working tree stay clean, and the full
  patch + evidence filed as **issue #8** for the owner (or a token with `workflows`) to
  apply. Until then, protonplus is protected by its own update.sh.

### 4. INSTALL-VERIFICATION SWEEP - 61 packages x 3 chroots = 183 tests, ALL PASS
Method: fresh `registry.fedoraproject.org/fedora:{43,44,rawhide}` container per package,
`copr enable ackerman/nexus`, `dnf install <pkg>` from the repo (what a real user does),
then `rpm -V`.
- fedora-44: 61/61 install OK, rpm -V clean
- fedora-43: 61/61 install OK, rpm -V clean
- fedora-rawhide (fc45): 61/61 install OK, rpm -V clean  <- no rawhide ABI-wave breakage
- glaze / hyprland-protocols / hyprwayland-scanner ship only `-devel` subpackages (no base
  package of that name); tested as `glaze-devel`, `hyprland-protocols-devel`,
  `hyprwayland-scanner-devel` - all install clean.
- ldd sweep over every ELF in every installed package (F44): only 2 packages report
  `not found`, both benign and proven so:
  - zen-browser: internal Mozilla sonames under /opt/zen (resolved via the app's own
    RUNPATH) - normal Firefox-style packaging; the spec already excludes them from
    auto-Requires.
  - localsend: flutter plugin .so files reference `libflutter_linux_gtk.so`; that library
    IS shipped in the package, the main binary has `RUNPATH=$ORIGIN/lib` and resolves with
    0 missing, and the plugins are dlopen'd into a process that already has the engine
    loaded. Launch test reaches "cannot open display" (headless), i.e. it got past linking.
- Smoke tests run: `protonplus version` -> ProtonPlus 0.6.2 (exit 0);
  `zen-browser --version` -> Mozilla Zen 1.21.12b (exit 0). GUI apps not launched
  (headless runner) - stated gap.

### 5. VERSION-ACCURACY SWEEP - 100% of the 61-package inventory
- All 30 `update.sh` scripts were executed against a scratch copy of the repo (never the
  live tree): every one reported "no update needed" and produced **0 file changes**
  (`git status --porcelain` in the scratch copy = empty) -> no churn, and all pass `bash -n`.
- The guards are not vacuous (attack test): forcing cliphist to 0.6.0 and
  extension-manager to 0.6.0 in the scratch copy made both correctly detect and patch to
  0.7.0 / 0.6.5.
- All 31 scanner-managed specs compared against `gh api .../releases/latest`: every
  packaged version equals upstream latest.
- All 9 git-pinned packages compared against real `git ls-remote HEAD`: niri-git,
  xwayland-satellite-git, hyprland-contrib, lazyvim-git, material-symbols-fonts, app2unit,
  caelestia-cli-mango, caelestia-shell-mango are all **== HEAD**. hyprland-plugins is
  intentionally pinned to the hyprpm pin for hyprland 0.56.2 (00862ca), not main HEAD.
- Binary artifacts torn apart and read from INSIDE (not filenames):
  rootapp Root.AppImage sha256 `0736926f...5d910` == the spec's pinned comment, unsquashfs
  at ELF offset 944632 -> `X-AppImage-Version=0.9.126` == packaged version;
  obsidian .deb control -> 1.13.4; opencode-desktop .deb -> 1.18.15; localsend .deb ->
  1.17.0+58; vesktop .rpm -> vesktop-1.6.5-1; heroic .rpm -> heroic-2.22.1-1;
  starship binary `--version` -> starship 1.26.0; helium tarball -> 0.15.3.1.
  All 9 remaining prebuilt Source0 URLs return HTTP 200.
- Cross-check that nothing else is in zen's trap: every one of the 61 git spec NVRs
  (expanded with `rpmspec -q` inside a container, so macros are real) was compared against
  the latest SUCCEEDED COPR build and against `dnf repoquery` of the live repo:
  **drift count = 0** - the repo offers exactly what git says, for all 61.
- Also verified: all 61 specs parse under `rpmspec -q` (no other protonplus-style wedge).

| package | packaged version | upstream latest | status |
|---|---|---|---|
| app2unit | 1.4.4^20260801195144git47e23ec | HEAD 47e23ec | up-to-date |
| aquamarine | 0.14.0 | v0.14.0 | up-to-date |
| awww | 0.12.1 | 0.12.1 | up-to-date |
| bibata-cursor-theme | 2.0.7 | v2.0.7 | up-to-date |
| caelestia-cli-mango | 2.0.0^...git875edaa | HEAD 875edaa | up-to-date |
| caelestia-shell-mango | 2.0.0^...git1c2a343 | HEAD 1c2a343 | up-to-date |
| cascadia-code-nerd-fonts | 3.5.0 | v3.5.0 | up-to-date |
| cliphist | 0.7.0 | v0.7.0 | up-to-date |
| extension-manager | 0.6.5 | v0.6.5 | up-to-date |
| fluxer | 2026.731.153836 | same | up-to-date |
| glaze | 8.0.0 | v8.0.0 | up-to-date |
| gpu-screen-recorder | 6.0.0 | 6.0.0 | up-to-date |
| helium-browser | 0.15.3.1 | 0.15.3.1 (tarball opened) | up-to-date |
| heroic-games-launcher | 2.22.1 | v2.22.1 (rpm opened) | up-to-date |
| hyprcursor | 0.1.13 | v0.1.13 | up-to-date |
| hyprgraphics | 0.5.1 | v0.5.1 | up-to-date |
| hypridle | 0.1.8 | v0.1.8 | up-to-date |
| hyprland | 0.56.2 | v0.56.2 | up-to-date |
| hyprland-contrib | 0.1^...git3dcbce7 | HEAD 3dcbce7 | up-to-date |
| hyprland-guiutils | 0.2.2 | v0.2.2 | up-to-date |
| hyprland-plugins | 0.1^3.git00862ca | hyprpm pin for 0.56.2 | up-to-date |
| hyprland-protocols | 0.7.0 | v0.7.0 | up-to-date |
| hyprland-qt-support | 0.1.0 | v0.1.0 | up-to-date |
| hyprlang | 0.6.8 | v0.6.8 | up-to-date |
| hyprlock | 0.9.6 | v0.9.6 | up-to-date |
| hyprpaper | 0.8.4 | v0.8.4 | up-to-date |
| hyprpicker | 0.4.7 | v0.4.7 | up-to-date |
| hyprpolkitagent | 0.1.3 | v0.1.3 | up-to-date |
| hyprqt6engine | 0.1.0 | v0.1.0 | up-to-date |
| hyprsunset | 0.4.0 | v0.4.0 | up-to-date |
| hyprsysteminfo | 0.2.0 | v0.2.0 | up-to-date |
| hyprtoolkit | 0.5.4 | v0.5.4 | up-to-date |
| hyprutils | 0.14.0 | v0.14.0 | up-to-date |
| hyprwayland-scanner | 0.4.6 | v0.4.6 | up-to-date |
| hyprwire | 0.3.1 | v0.3.1 | up-to-date |
| lazyvim-git | 0.1^...git459a4c3 | HEAD 459a4c3 | up-to-date |
| libcava | 1.0.0 | 1.0.0 | up-to-date |
| localsend | 1.17.0 | v1.17.0 (deb opened: 1.17.0+58) | up-to-date |
| logseq | 2.0.1 | 2.0.1 | up-to-date |
| ly | 1.4.1 | codeberg v1.4.1 | up-to-date |
| mangowm | 0.15.6 | 0.15.6 | up-to-date |
| material-symbols-fonts | 4.0.0^...git50f0603 | HEAD 50f0603 | up-to-date |
| matugen | 4.1.0 | v4.1.0 | up-to-date |
| mpvpaper | 1.9 | 1.9 | up-to-date |
| niri-git | 26.04^...git59a1001 | HEAD 59a1001 | up-to-date |
| nwg-look | 1.1.1 | v1.1.1 | up-to-date |
| obsidian | 1.13.4 | v1.13.4 (deb opened) | up-to-date |
| opencode-desktop | 1.18.15 | v1.18.15 (deb opened) | up-to-date |
| protonplus | 0.6.2 | v0.6.2 | **FIXED-WRONG-VERSION** (was unparsable v0.6.1-1) |
| python3-materialyoucolor | 3.0.4 | 3.0.4 | up-to-date |
| quickshell-git | 0.3.0^...git28771c7 | pinned commit | up-to-date |
| rootapp | 0.9.126 | AppImage X-AppImage-Version=0.9.126 | up-to-date |
| scenefx | 0.5 | 0.5 | up-to-date |
| starship | 1.26.0 | v1.26.0 (binary opened) | up-to-date |
| stoat-desktop | 1.4.2 | v1.4.2 | up-to-date |
| vesktop | 1.6.5 | v1.6.5 (rpm opened) | up-to-date |
| waypaper | 2.8 | 2.8 | up-to-date |
| wlroots | 0.20.2 | 0.20.2 | up-to-date |
| xdg-desktop-portal-hyprland | 1.4.1 | v1.4.1 | up-to-date |
| xwayland-satellite-git | 0.8.2^...git3bc915f | HEAD 3bc915f | up-to-date |
| zen-browser | 1:1.21.12b | 1.21.12b (1.21.13b deleted upstream) | **UPDATED** (Epoch fix) |

### 6. DEPENDENCY AUDIT (100% coverage)
Evidence chain per package: (a) spec BR/Requires read; (b) COPR green on all 3 chroots
proves every BuildRequires resolves; (c) `dnf install` from the live repo in a clean
container on **all three** chroots proves every runtime Requires resolves (a wrong/renamed
name yields "nothing provides" there) - 183/183 passed; (d) ldd sweep over all installed
ELFs - no genuinely missing sonames; (e) upstream build files re-derived for the packages
touched this run.
- protonplus (re-derived from the 0.6.2 tarball): meson `dependency()` list = gtk4,
  glib-2.0, gio-unix-2.0, libadwaita-1>=1.6, json-glib-1.0, libsoup-3.0, gee-0.8,
  libarchive, libnotify, cairo, appstream, sdl3>=3.2.0 - **exactly** the spec's
  BuildRequires; `Requires: which` justified by src/utils/system.vala:462, hicolor and
  vulkan-loader are runtime/theme deps. Status: deps-verified.
- niri-git (re-derived from the pinned 59a1001 tarball): only pkg-config probe in build.rs
  is libinput (present); default features dbus/systemd/xdp-gnome-screencast are all covered
  (dbus-1, systemd, libpipewire-0.3). Status: deps-verified.
- zen-browser: prebuilt Gecko bundle; the spec's `__provides_exclude_from` /
  `__requires_exclude` are correct and were re-confirmed by the ldd analysis above.
- All other 58: deps-verified via (b)+(c)+(d) on three chroots. Confidence HIGH for the
  three re-derived above and for every package whose runtime libs were ldd-scanned;
  MEDIUM for source-built packages where no upstream re-derivation was needed this run
  because upstream did not change (RE-TEAR RULE: unchanged upstream -> prior derivation
  still valid; every changed package this run WAS re-derived).
- No package needed a dependency added or dropped this run.

### 7. Install-test coverage ledger (this run)
| chroot | packages tested | install OK | rpm -V clean |
|---|---|---|---|
| fedora-43-x86_64 | 61/61 | 61 | 61 |
| fedora-44-x86_64 | 61/61 | 61 | 61 |
| fedora-rawhide-x86_64 (fc45) | 61/61 | 61 | 61 |
Every package's binary state was re-tested this run, so no row is inherited from a
previous run. protonplus and zen-browser additionally got a fresh-checker re-run in
brand-new containers after their COPR builds finished.

### 8. GitHub Actions health
- No failed runs. "Fedora Nexus Auto-Updater" 31349563061 succeeded (it is what committed
  3888fc4 - it "succeeded" while producing an unbuildable spec, which is exactly what
  issue #8 addresses). opencode-schedule runs marked `cancelled` are
  concurrency-cancelled by design.

### 9. Webhook / COPR trigger verification
- `gh api repos/.../hooks` -> 403 "Resource not accessible by integration" (hook registry
  needs an admin scope this token lacks), so delivery IDs could not be read - stated gap.
- Empirical proof instead, per push:
  - push `a8fa132` (03:12Z) -> new build **10841314** appeared (submitted 03:12:29Z)
  - push `e6eb10f` (04:09Z) -> new build **10841458** appeared (submitted 04:09:20Z)
  Both went to `succeeded`, and neither was "skipped", confirming both NVRs were genuinely
  new (Release discipline held).

### 10. Issues / PRs
- Open issues at pickup: none. Open PRs: none.
- Opened **issue #8** (update-engine scanner can write RPM-illegal versions) with the
  validated patch, because the fix cannot be pushed with this token's permissions.
- No noctalia-* ghost RPMs were touched, reported or reopened (known COPR bug, per policy).

### 11. Named gaps (stated, not silently skipped)
- Workflow fix for update-engine.yml is NOT applied on main - blocked by missing
  `workflows` token permission; filed as issue #8 with the exact patch. This is the one
  piece of intended work this run could not land.
- GUI applications were installed and verified (deps resolve, rpm -V, ldd) but NOT
  launched: the runner is headless. Only CLI smoke tests (protonplus, zen --version) ran.
- GitHub webhook delivery IDs/statuses unavailable (403, admin scope); webhook liveness
  proven indirectly via push -> new-build correlation.
- Dependency re-derivation from upstream build files was done for the packages that
  changed (protonplus, niri-git sample, zen-browser); unchanged packages rely on
  build-green + clean-install-resolve + ldd rather than a fresh tarball re-derivation.
- `awww/awww-0.12.1.tar.gz` remains committed as Source0 (that package's design).

### 12. Final state
HEAD after this run: `e6eb10f` + this relay commit. Two atomic fixes pushed and each
verified by a green COPR build on all 3 chroots and by clean-container installs:
`a8fa132` protonplus 0.6.2-1 (build 10841314), `e6eb10f` zen-browser 1:1.21.12b-4
(build 10841458). Working tree clean, no failed COPR builds remaining, 61/61 packages
installable on all three enabled chroots.
