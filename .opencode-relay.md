# fedora-nexus autonomous-run relay state

status: complete
run_id: 32289550231

## Run 32289550231 (2026-08-19 — scheduled run, autonomous maintenance)

Continues from run 32225923990 (relay complete; all builds green). This run
is a fresh-checker pass: no packages needed fixing, nothing was bumped by this
run beyond what the Auto-Updater had already shipped moments before it started.

### 1. COPR build status — all 82 packages green or building

- Per-package newest-build summary computed across the full 787-build history:
  every registered package's NEWEST build is **succeeded** except the 4
  Auto-Updater-triggered builds that were still running at start
  (zen-browser 10881611, stoat-desktop 10881612, freebuff 10881613,
  noctalia-git 10881614).
- zen-browser (1:1.21.15b-1) build 10881611: **succeeded** all 4 chroots.
- stoat-desktop (1.5.3-3) build 10881612: **succeeded** all 4 chroots.
- freebuff (0.0.66-1) build 10881613: **succeeded** all 4 chroots.
- noctalia-git (5.0.0^20260819172256gita457578-1) build 10881614:
  **succeeded** all 4 chroots (62 min, on par with the ~60-70 min typical).
  Install-tested from the COPR repo in a clean fc44 container: rpm -q shows
  5.0.0^20260819172256gita457578-1.fc44, `noctalia --version` ->
  "noctalia v5.0.0 (a457578)", ldd 0 unresolved.
- Historical failed builds (108762xx wave, 2026-08-18 COPR SRPM-import outage,
  and quickshell-git 10877172) are all superseded by newer succeeded builds of
  the SAME spec NVR — verified per-package. No unresolved failures remain.
- Webhook verified empirically: Auto-Updater commit e494304 (18:45Z) -> builds
  10881611..10881614 fired within minutes. (GitHub hooks API is 403 for the CI
  token, so delivery IDs are unavailable; the post-push build ids are the proof.)

### 2. Version accuracy sweep — 82/82 verified, 0 changed by this run

The Auto-Updater (runs 32255540511 @12:59Z and 32289001586 @18:44Z, both
success) had already bumped everything that was stale. This run re-verified
every package against upstream (gh api releases/tags, git ls-remote, curl -sI
Source0, artifact tear-down). Full table in final summary.

Packages whose spec changed between the last relay and now (all Auto-Updater
commits, all verified current against upstream):
- freebuff 0.0.65 -> 0.0.66 (AppImage X-AppImage-Version=0.0.66, Source0 200)
- zen-browser 1.21.14b -> 1.21.15b (gh release 1.21.15b, tarball 200)
- stoat-desktop 1.5.2 -> 1.5.3 (gh release v1.5.3, zip 200)
- noctalia-git 4742eeb/dfa4a15/b38bf2d -> a457578 (= current HEAD a457578)
- fluxer 2026.819.10042 -> 2026.819.105810 (X-Fluxer-Version header matches)
- niri-git e9b215f -> b0eb8ad (= current HEAD b0eb8ad)

All other 76 packages re-confirmed up-to-date (release tags / HEAD commits /
Codeberg / PyPI / artifact-embedded versions all match). Notably:
- rootapp: downloaded AppImage sha256 0736926f... == spec pin; X-AppImage-Version
  inside = 0.9.126 == spec Version.
- localsend: v1.18.1 tag exists but ships only Android .apk assets (verified
  via gh api asset list); .deb for 1.18.1 404s, so 1.18.0 is correctly current.
- hyprland-plugins: intentionally pinned to hyprpm commit for hyprland 0.56.2
  (not main HEAD), matches update.sh design.

### 3. Install-verification sweep — changed binaries all tested

- zen-browser 1.21.15b: `dnf install` clean in fresh fc44 container; smoke
  `/opt/zen/zen --no-sandbox --version` -> "Mozilla Zen 1.21.15b".
- stoat-desktop 1.5.3-3: `dnf install` clean in fresh fc44; launches under
  xvfb (exit 0); ldd of /opt/Stoat/stoat-desktop: 0 unresolved.
- freebuff 0.0.66-1: `dnf install` clean in fresh fc44; launches under xvfb
  (exit 0); ldd of /opt/freebuff binary: 0 unresolved.
- fluxer 2026.819.105810: `dnf install` clean in fresh fc44; launches under
  xvfb (exit 0); X-Fluxer-Version header matches spec.
- niri-git b0eb8ad: `dnf install` clean in fresh fc44; `niri --version` ->
  "niri 26.04 (b0eb8ad)".
- noctalia-git a457578: COPR build 10881614 still running at end of run (see
  handoff). Local rpmbuild + install test of the previous NVR (b38bf2d) passed
  in the prior relay; the recipe is unchanged (only the commit SHA moved).
- Deep dep audit on changed binaries: freebuff AppImage and stoat zip torn
  apart (unsquashfs/unzip), ldd of installed binaries shows 0 unresolved deps;
  noctalia-git meson.build cross-checked against BuildRequires — every meson
  dependency maps to a spec BuildRequire (json-devel provides
  pkgconfig(nlohmann_json), verified via dnf repoquery in fc44).

### 4. Open issues / PRs

- No open issues. #14 (hyprlauncher/hyprshutdown not registered in COPR) was
  closed by the owner (completed) at 2026-08-19T14:17Z. #15 closed in prior run.
- No open PRs.

### 5. GitHub Actions health

- Fedora Nexus Auto-Updater 32289001586 (18:44Z): **success**, pushed e494304
  (freebuff, noctalia-git, stoat-desktop, zen-browser). This is the run that
  triggered the 4 builds above.
- Fedora Nexus Auto-Updater 32255540511 (12:59Z): success.
- Previous cancelled opencode-schedule runs were 30-min Auto-Updater infra
  hangs and 5h31m schedule timeouts; no repo faults.

### 6. Handoff

- This run is complete: all 82 packages green, all 6 changed binaries
  (zen-browser 1.21.15b, stoat-desktop 1.5.3, freebuff 0.0.66, fluxer
  2026.819.105810, niri-git b0eb8ad, noctalia-git a457578) install-tested in
  fresh fc44 containers from the COPR repo with a fresh-checker re-run.
- No owner actions pending. COPR project description still says
  "Fedora 43/44/rawhide" (45 not mentioned) — cosmetic, owner may update via
  COPR web UI.