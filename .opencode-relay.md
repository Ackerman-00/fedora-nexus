# fedora-nexus autonomous-run relay state

status: complete

## Run - RUN_ID 31166174531 (2026-08-07) - COMPLETED

### 1. COPR Build Status - ALL SUCCEEDED (no current failures)
- Queried 50 most recent builds via COPR API. All current builds state=succeeded.
- Two OLD failures (build 10822222 hyprgraphics 0.5.1-1, build 10822219 aquamarine 0.14.0-1)
  both have successful follow-ups (0.5.1-2, 0.14.0-2). No action needed.
- Latest build: 10835163 (opencode-desktop 1.18.15-1) succeeded all 3 chroots.
- Verified via build/list?packagename=<pkg> for ALL 56 packages: every package's latest
  build is state=succeeded.

### 2. Version Accuracy Sweep: ALL 56 packages current (verified vs upstream)
- Git-based packages verified against upstream HEAD via `git ls-remote` (independent of update.sh):
  niri feb3e43f, xwayland-satellite 8d135d3b, hyprland-contrib 3dcbce71, lazyvim 459a4c3b,
  caelestia bd43cb21, app2unit 47e23ec6 (master), material-symbols 50f06031 (master),
  quickshell 28771c7. ALL match HEAD.
- hyprland-plugins pinned to 00862ca = upstream commit "hyprpm: add pin for 0.56.2"
  (verified via commits?path=hyprpm.toml API). Correct pin for hyprland 0.56.2.
- 21 hypr* packages verified via GitHub releases/latest API: all MATCH spec versions.
- Stable packages verified via releases/latest API: glaze 8.0.0, heroic 2.22.0, matugen 4.1.0,
  mpvpaper 1.9, nwg-look 1.1.1, protonplus 0.5.22, starship 1.26.0, stoat-desktop 1.4.2,
  vesktop 1.6.5, waypaper 2.8. All MATCH.
- opencode-desktop bumped by auto-updater (run 31163441288) from 1.18.14 to 1.18.15.
  Verified v1.18.15 is the real latest upstream release tag. Source URL returns 200.
- All 25 update.sh scripts run cleanly with ZERO churn (all report "already at latest").
- All source URLs reachable.

### 3. GitHub Actions Health
- Auto-updater runs 31163441288 (08:51), 31150894150 (05:32), 31136072781 (00:51) all SUCCESS.
- This run's auto-updater at 08:51 bumped opencode-desktop to 1.18.15 (pushed, built OK).
- Failed runs 31125121411/31125121414/31112339114: diagnosed in prior runs as GitHub infra
  "Service Unavailable" (cleanup job action download) and concurrency cancellation. NOT repo bugs.

### 4. Open Issues & PRs: NONE (both empty)

### 5. Install Verification Sweep
- opencode-desktop 1.18.15-1 (the only binary changed since last sweep) install-tested this run:
  fc44: installed OK, rpm -V OK, version 1.18.15-1.fc44. GUI launch (--version) blocked by
  Electron root-sandbox (expected in container, not a packaging bug).
  fc43: installed OK, 1.18.15-1.fc43.
  fc45(rawhide): installed OK, 1.18.15-1.fc45.
- Prior run (31152194972) install-tested the other 55 packages across fc44/fc43/rawhide; no
  binaries changed for them since.
- Ledger: opencode-desktop | 2026-08-07 | OK fc43/44/45 | smoke: GUI root-sandbox (container only).

### 6. Dependency Audit (deep)
- No spec deps changed this run (no spec edits). BuildRequires/Requires completeness proven by:
  (a) all 56 latest COPR builds succeeded (rpmbuild fails on missing BuildRequires), and
  (b) clean `dnf install` from live COPR repo (opencode-desktop this run; 35+ pkgs prior run).
- Status for all 56 packages: deps-verified (build+install evidence).

### 7. Fedora Lifecycle
- Fedora 44 (GA Apr 2026) stable, Fedora 43 (EOL Dec 2026), Rawhide (becoming Fedora 45,
  GA ~Oct 2026). COPR chroots enabled: fedora-43-x86_64, fedora-44-x86_64,
  fedora-rawhide-x86_64. No new release since last run; no chroot changes needed.

### Not verified (named gaps)
- Webhook delivery history: `gh api repos/.../hooks` returns 403 (token lacks admin:hooks scope);
  used empirical push->build correlation instead. No pushes made this run (nothing to fix).
- opencode-desktop GUI smoke test cannot run in headless container (Electron root-sandbox
  + no display); verified install + rpm -V + version instead.
- crates.io rate-limit avoided: matugen verified via GitHub release tag (v4.1.0).
