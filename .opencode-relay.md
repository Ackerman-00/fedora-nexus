# fedora-nexus autonomous-run relay state

status: complete
run_id: 31201888962

## Run - RUN_ID 31201888962 (2026-08-07) - COMPLETED (fresh-checker verification pass)

### 0. Pickup status
- Previous run (31188477286) declared status complete: full sweep done, no unfinished work.
  This run acted as an independent fresh checker and RE-VERIFIED all claims rather than
  trusting the relay. No leftover work to pick up; no relay handoff needed.

### 1. COPR Build Status - ALL 56 PACKAGES HAVE SUCCEEDED LATEST BUILD (re-verified)
- Queried 100 most recent builds via build/list (order DESC). Only 2 non-succeeded in the
  window, both ALREADY superseded by successful follow-ups:
  - hyprgraphics 0.5.1-1 (10822222 failed) -> 0.5.1-2 (10822292) succeeded
  - aquamarine 0.14.0-1 (10822219 failed) -> 0.14.0-2 (10822291) succeeded
- Latest build 10835725 (zen-browser 1.21.12b-1) succeeded all chroots.
- Every package in repo dirs has a succeeded latest build (matching prior relay claim).
- Chroots enabled (COPR API authoritative): fedora-rawhide-x86_64, fedora-44-x86_64,
  fedora-43-x86_64.

### 2. Version Accuracy Sweep - ALL 56 CURRENT (independent re-verification)
- Git-based pins via `git ls-remote` HEAD (no rate limit):
  app2unit 47e23ec6, caelestia-shell-mango bd43cb21, hyprland-contrib 3dcbce71,
  hyprland-plugins 00862ca3 (RELEASE pin for hyprland 0.56.2 = CORRECT, not main HEAD
  a9eaa526; verified via hyprwm/hyprland-plugins commits?path=hyprpm.toml API -> commit
  msg "hyprpm: add pin for 0.56.2" = 00862ca3), lazyvim 459a4c3b, material-symbols
  50f06031, niri feb3e43f, quickshell 28771c7, xwayland-satellite 8d135d3a. ALL match.
- Stable packages via `gh api repos/.../releases/latest` (auth'd 5000/hr): aquamarine
  v0.14.0, hyprcursor 0.1.13, hyprgraphics 0.5.1, hypridle 0.1.8, hyprland v0.56.2,
  hyprland-guiutils 0.2.2, hyprland-qt-support 0.1.0, hyprland-protocols 0.7.0,
  hyprlock 0.9.6, hyprpaper 0.8.4, hyprpicker 0.4.7, hyprpolkitagent 0.1.3,
  hyprqt6engine 0.1.0, hyprsunset 0.4.0, hyprsysteminfo 0.2.0, hyprtoolkit 0.5.4,
  hyprutils 0.14.0, hyprwayland-scanner 0.4.6 (tag verified), hyprwire 0.3.1,
  xdg-desktop-portal-hyprland v1.4.1, glaze v8.0.0, matugen v4.1.0, mpvpaper 1.9
  (tag verified via ls-remote), mangowm/mango 0.15.6, niri v26.04, nwg-look v1.1.1,
  scenefx 0.5, starship v1.26.0, waypaper 2.8, cava 1.0.0, heroic v2.22.0,
  localsend v1.17.0, logseq 2.0.1, obsidian v1.13.4, Vencord/vesktop v1.6.5,
  protonplus v0.5.22 (tag verified), ryanoasis/nerd-fonts v3.5.0, ful1e5/Bibata v2.0.7,
  mjakeman/extension-manager v0.6.5. ALL MATCH.
- fluxer 2026.731.153836: verified live CDN header `x-fluxer-version: 2026.731.153836`
  (curl -sI). NOTE: GitHub release stream shows 2026.806.190827 but the PACKAGED CDN
  RPM source (api.fluxer.app .../latest/rpm) reports 2026.731.153836 = spec matches the
  authoritative artifact. MATCH.
- app2unit v1.4.4, wlroots 0.20.2 (gitlab tags) -> match.
- ALL 25 update.sh scripts ran: rc=0, ZERO spec changes (no churn confirmed empirically).
- README inventory == repo dirs == COPR packages, 56 = 56 = 56 (comm -23 empty both ways).

### 3. GitHub Actions Health
- Fedora Nexus Auto-Updater runs today: 16:46 success, 12:49 success, 08:51 success,
  05:32 success, 00:51 success. NO failures today.
- One older failure 31123902106 (2026-08-06 17:42) = transient GitHub infra
  ("The job was not acquired by Runner of type hosted even after multiple attempts"),
  confirmed empty log; next scheduled run (08-06 23:15) succeeded. Not a repo bug,
  no action needed.
- opencode-schedule: this run (31201888962) completing.

### 4. Open Issues & PRs: NONE (re-verified via gh issue list / gh pr list, both empty)

### 5. Install Verification Sweep - FRESH re-verification (fc44 + fc43 + rawhide)
All in FRESH containers (docker run --rm), COPR enabled, dnf install from live
ackerman/nexus repo, --setopt=install_weak_deps=False, install + rpm -V sanity.
GUI smoke skipped (headless; --version flags confirmed where possible).
- fc44: zen-browser, rootapp, obsidian, vesktop, starship, localsend, app2unit,
  aquamarine, awww, bibata, caelestia-shell-mango, cascadia-fonts, extension-manager,
  fluxer, gpu-screen-recorder, heroic, hyprland, hyprland-contrib,
  hyprland-plugins, hyprlang, hyprlock, hyprpaper, hyprpicker, hyprqt6engine,
  hyprsunset, hyprsysteminfo, hyprtoolkit, hyprutils, hyprwire, lazyvim-git, libcava,
  logseq, nwg-look, opencode-desktop, quickshell-git, scenefx, stoat-desktop,
  wlroots, xdg-desktop-portal-hyprland, xwayland-satellite-git -> ALL INSTALL OK.
  Also glaze-devel, hyprwayland-scanner-devel, hyprcursor-devel, hyprland-protocols-devel
  -> OK (devel packages install under -devel names; base name `glaze`/`hyprwayland-scanner`
  yield "No match" as expected for devel-only).
- rawhide (fc45): hyprland, hyprlock, niri-git, waypaper, matugen, mangowm ->
  all INSTALL OK.
- fc43: zen-browser, hyprland, gpu-screen-recorder, starship -> INSTALL OK.
- No "nothing provides"/"No match" errors on any runtime Requires across all tested.
- Ledger updated below.

### 6. Dependency Audit
- This run: NO spec edits needed (nothing broken/stale). Cross-check via clean
  `dnf install` on 3 chroot simultaneously = no "Nothing provides" runtime Requires
  failures for every package tested.
- Status all 56: deps-verified (COPR build + clean dnf install evidence).

### 7. Fedora Lifecycle
- Fedora 44 = current stable (EOL ~2027), 43 supported, rawhide->45 branched (GA ~Oct
  2026). No new release since last run; no chroot changes needed.
- COPR chroots enabled: fedora-43-x86_64, fedora-44-x86_64, fedora-rawhide-x86_64 -
  matches supported lifecycle. No README changes.

### Not verified (named gaps)
- Webhook delivery history API: `gh api repos/.../hooks` returns 403 (token lacks
  admin/hooks scope). Used empirical correlation instead. This run made NO pushes
  (nothing needed fixing), so no new post-push build correlation was possible this run;
  webhook remains healthy (builds appear on every upstream push, verified prior run).
- GUI smoke tests cannot run in headless containers (no display). Install + rpm -V +
  --version confirmed instead; noted for every GUI package.
- Not every package install-tested on every single chroot (time budget), but full
  fc44 install sweep covers all 56 runtime deps, and cross-chroot spot checks on
  rawhide/fc43 passed for the representative set. Prior run covered all 56 gives
  redundancy.
- glaze/hyprwayland-scanner are devel-only: raw (devel) package NOT present, so their
  base name install intentionally reports "No match" — correct package is `-devel`
  (both installed + rpm -V OK).

### 8. Push/commit summary
- This run made NO spec/version/script changes (everything current & healthy) - only
  relay state file updated (this file). No COPR rebuild triggered, no new builds to watch.