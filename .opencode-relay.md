# fedora-nexus autonomous-run relay state

status: complete

## Run - RUN_ID 31136353853 (2026-08-07) - COMPLETED

### 1. COPR Build Status - FIXED hyprland-plugins (the only failed build)
- FAILED build 10833802 (hyprland-plugins 0.1^2.gita9eaa52-1): auto-updater chased
  plugin main HEAD a9eaa52 ("hyprbars: chase hyprland"), requiring
  hyprland/src/keybinds/Manager.hpp, NOT in hyprland-devel 0.56.2 (which uses
  src/managers/KeybindManager.hpp).
- FIXED: pinned spec to hyprpm 0.56.2 compat commit 00862ca (bumpver 3 ->
  0.1^3.git00862ca-1), rewrote update.sh to select the hyprpm pin commit for the
  repo's hyprland version instead of blindly chasing main HEAD.
- Commit 4b09f77 -> build 10833931 SUCCEEDED all 3 chroots (fc43/fc44/rawhide).
- Local fc45 rpmbuild passed. Local dbuild verified, COPR authoritative.

### 2. Version Accuracy Sweep: ALL 56 packages current (verified vs upstream)
All current: every hypr* release, wlroots 0.20.2, scenefx 0.5, glaze 8.0.0,
matugen 4.1.0, niri/quickshell/app2unit/hyprland-contrib/material-symbols/
lazyvim/caelestia at HEAD (gitdate MARSH checks pass), heroic 2.22.0,
gpu-screen-recorder 5.15.3, localsend 1.17.0, obsidian 1.13.4, vesktop 1.6.5,
zen 1.21.11b, logseq 2.0.1, rootapp 0.9.126 (in-artifact X-AppImage-Version +
sha256 match), protonplus 0.5.22, fluxer X-Fluxer-Version 2026.731.153836,
waypaper 2.8, mpvpaper 1.9, bibata 2.0.7, cascadia nf 3.5.0. No stale versions.

### 3. GitHub Actions Health: PASSING (last auto-updater run success)
### 4. Open Issues & PRs: NONE
### 5. Install Verification - hyprland-plugins (changed pkg) on all 3 chroots
- fc43, fc44, fc45(rawhide): dnf install hyprland-plugin-hyprbars all succeed,
  flat missing libs, rpm -V clean, require hyprland=0.56.2 resolves.
- Fresh-checker re-run (fc44 fresh container): installs, ldd 0 missing.
- Note: plugin is a Hyprland plugin loaded into the Hyprland process; standalone
  dlopen shows undefined symbols (expected - runtime symbols from host).

### 6. README consistency FIXED (commits 6ae19aa, d22e625)
- README said 55, actual 56 dirs; added missing lazyvim-git row and corrected
  count; honored optional order.

### 7. Fedora Lifecycle
- Fedora 44 (GA Apr 2026) stable, Fedora 43 (EOL Dec 2026), Rawhide Fedora 45.
- COPR chroots enabled (project API): fedora-43-x86_64, fedora-44-x86_64,
  fedora-rawhide-x86_64. No new release since last run.

### Not verified (named gaps)
- hyprland plugins full runtime load requires a running Hyprland + display
  (headless container). Verified install + symbol presence in Hyprland binary.
- Line hooks API listing 403 (token lacks admin hooks scope); used empirical
  push->build correlation (pre-push build list vs post-push build 10833931).
- Other 55 packages not install-retested this run (no binary changed); covered
  by prior run's full sweep (all 55 install in all 3 chroots).