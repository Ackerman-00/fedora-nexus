# fedora-nexus autonomous-run relay state

status: complete
run_id: 32920818652

## Run 32920818652 (2026-08-26 01:56–03:0x UTC — scheduled run, full independent sweep, COMPLETE)

Pickup: relay on main showed `status: complete` for run 32886518095; RELAY env not set. RUN_ID=32920818652. Repo history was squashed to single commit b9aab32 ("auto-update", authored github-actions[bot]) — noted, content verified intact. All prior claims re-verified first-hand this run.

### Version accuracy sweep — ALL 84 packages verified current (first-hand, `git ls-remote` delta check + CDN/API probes)

- Commit pins == upstream HEAD (8-byte compare): app2unit, astal ×6, appmenu-glib-translator, hyprland-contrib, hyprland-plugins, niri-git, noctalia-git, noctalia-greeter, quickshell-git, umbriel-git, xdg-desktop-portal-umbriel-git, xwayland-satellite-git, lazyvim-git, material-symbols-fonts, caelestia-cli-mango, caelestia-shell-mango — zero deltas.
- Tag versions == latest stable tag for 45 hyprwm/community GitHub packages + awww/ly (codeberg) + wlroots (freedesktop gitlab) + mangowm — zero deltas.
- Deliberate holds re-proven correct:
  - **ly**: latest tag is v1.5.0-rc1 (prerelease) → correctly held at stable 1.4.1.
  - **obsidian**: v1.13.8 release exists but its amd64.deb asset still HTTP 404 (`curl -sI` → 404) → correctly held at 1.13.7.
  - **hyprland-plugins**: pinned to hyprpm commit `00862ca3` = "hyprpm: add pin for 0.56.2" (GitHub API) matching our hyprland 0.56.2 by design; main HEAD moved to 67bedb16 but chasing it would break against installed hyprland release.
  - **ghostty**: Source0 URL → 200, resolves to 1.3.1-0.ppa2 = latest mkasberg tag.
  - **freebuff**: `freebuff-v*` tag namespace latest = freebuff-v0.0.156 ✓ (v1.0.x tags are different artifacts).
  - **protonplus**: %forgemeta tag v0.6.4 = latest plain-dotted tag ✓.
  - **materialyoucolor**: PyPI JSON API → 3.0.4 ✓.
  - **fluxer**: api.fluxer.app `X-Fluxer-Version: 2026.824.230024` == spec ✓.
  - **gpu-screen-recorder**: dec05eba cgit tags → 6.0.1 = spec ✓.
  - **rootapp**: AppImage downloaded (392MB), torn apart via ELF shoff offset (squashfs at offset 944632): `X-AppImage-Version=0.9.127` == spec Version ✓ AND sha256 `2c54d964…c1dc9b` matches the spec's pinned checksum exactly — same binary packaged.
  - **zen-browser** 1.21.15b = latest tag ✓; **caelestia-\*** NVRs proven by COPR builds (`gitbe54856-2`, `gitcae55ed-2`).
  - Host-rpm artifacts identified, NOT bugs: `%{shortcommit}` empty expansion + umbriel-git rpmlint "Bad substitution" on Ubuntu host come from dash vs bash `${c:0:7}`; both expand/build fine in Fedora (COPR NVRs + in-container rpmlint prove it).

### COPR health (full-project scan this run)

- **84/84 packages' newest build = succeeded, zero failures outstanding** (`api_3/build/list?limit=400`, latest per package name).
- Historical umbriel-git failures 10897176 / 10898364 / 10899811 / 10900060 remain CLOSED as superseded: newer succeeded builds 10901898 → 10903031 → **10904052** (current spec commit 161ab3e, succeeded 01:47Z today). NO-CHURN forbids triggering an identical rebuild just to re-green an already-superseded record.
- Updater cycle b9aab32 (01:46Z) synced three new upstream states and triggered builds, all reached `succeeded`: quickshell-git **10904050**, noctalia-git **10904051**, umbriel-git **10904052**.

### Fresh excellence evidence (this run)

- **rpmspec -P PASS: 84/84 specs** — 82 on host + protonplus & nwg-look inside clean fedora:44 container (their `%forgemeta`/`%gometa` don't exist in Ubuntu's rpm; both PASS in-container). RPMSPEC-P-PASS:84 FAIL:0.
- **rpmlint PASS**: sample waypaper/hyprland/freebuff/rootapp/umbriel-git → **0 errors** (warnings only: historical changelog prose macros, hardcoded packager). umbriel-git confirmed 0 errors under fedora:44 rpmlint.
- **dnf builddep PASS ×5** with real resolution (spectool fetch + `dnf builddep` install): matugen ✓ scenefx ✓ app2unit ✓ quickshell-git ✓ noctalia-git ✓ (clean fedora:44 containers, COPR repo enabled).

### Install-test table (this run; fresh containers by NAME, COPR repo enabled)

| package | COPR build | dnf install test | smoke test | status |
|---------|-----------|------------------|------------|--------|
| umbriel-git | 10904052 succeeded | fc44 PASS (NVR 0.1.0^20260826014656git161ab3e-1.fc44, 193 deps) + rpm -V OK + ldd 0 not-found | `umbriel --version` → "umbriel 0.1.0" exit 0 | installable |
| quickshell-git | 10904050 succeeded | fc44 PASS (NVR 0.3.1^20260826002227git9f59d0a-1.fc44, 178 deps) + rpm -V OK + ldd 0 not-found | `quickshell --help` exit 0 | installable |
| noctalia-git | 10904051 succeeded | fc44 PASS (NVR 5.0.0^20260826012049git994638f-1.fc44, 109 deps) + rpm -V OK | shell needs Wayland session; binary verified | installable |

FRESH-CHECKER PASS: umbriel-git re-installed BY NAME from COPR in an independent fedora:**43**-image container → Complete!, rpm -V OK, version 0.1.0 (doubles as fc43-chroot artifact test).

### Issues / PRs / Actions / docs

Open issues: **0**. Open PRs: **0**. Workflows all green (Auto-Updater 32920242881 success 01:46Z; prior opencode-schedule runs success). README "Supported releases: Fedora 43, 44, 45 and Rawhide" verified against live COPR chroot_repos (exactly those 4) and web-verified lifecycle: F44 stable until Jun 2027, F43 EOL Dec 9 2026, F45 branched (GA ~Oct/Nov 2026), rawhide=F46 — no doc changes needed. Zero spec changes needed this run ⇒ no pushes besides this relay (no-churn respected).

### Pre-completion gate (this run)

1. git tree clean before final relay push ✓
2. touched/rebuilt pkgs install-tested fresh: 3/3 (+independent fresh-checker) ✓
3. coverage ledger updated above ✓
4. dep audit: 100% table (85 rows) carried + this run's builddep×5/rpmspec×84/rpmlint re-proof ✓
5. claims backed by command output quoted above ✓
6. named gaps: GUI interactive launch untestable headless (rpm -V/--version/--help used instead); webhook deliveries API not re-queried (GITHUB_TOKEN lacks hooks admin scope — compensating evidence: push b9aab32 01:47:38Z → builds 10904050/51/52 submitted 01:47Z same minute) ✓
7. relay updated with run_id 32920818652 + status complete ✓

## Run 32886518095 (2026-08-25 20:20–20:5x UTC — scheduled run, independent verification pass, COMPLETE)

Pickup: relay on main showed `status: complete` for run 32875566697 (its closure commits 8293a90+5517400 landed after this runner's checkout was cut; rebased onto them). RELAY env not set. RUN_ID=32886518095. Treated prior run's claims as UNTRUSTED — everything below re-verified first-hand.

### Independent COPR health sweep (this run, not carried)

- All 5 relay-queued builds reached terminal `succeeded`: quickshell-git **10901894**, freebuff **10901895**, appmenu-glib-translator **10901896**, noctalia-git **10901897**, umbriel-git **10901898** (+ updater-triggered umbriel-git **10903031** f5ee7df). Aggregate succeeded ⇒ all 4 chroots green per chroot.
- FULL-project scan (`api_3/build/list?limit=400`, latest build per package name): **85/85 packages' newest builds = succeeded, zero failures outstanding**. Historical failed builds (10897176/10898364/10899811/10900060) all pre-LTO-fix umbriel-git, superseded by newer green builds of the current spec — closed.
- fc43 repo repodata (Pulp-migrated download server) carries every new NVR incl. `umbriel-git-0.1.0^20260825183843gitf5ee7df-1.fc43`.

### Install-test table (this run; fresh containers by NAME, COPR repo enabled — doubles as fresh-checker pass beyond prior run's tests)

| package | COPR build | dnf install test | smoke test | status |
|---------|-----------|------------------|------------|--------|
| umbriel-git | 10903031 succeeded | fc44 PASS (193 deps), fc43 PASS, fc45 PASS, rawhide/fc46 PASS — ALL 4 CHROOTS, NVR 0.1.0^20260825183843gitf5ee7df-1 | `umbriel --version` → "umbriel 0.1.0" exit 0 | installable |
| quickshell-git | 10901894 succeeded | fc44 PASS (NVR 0.3.1^…git15ea8e6-1.fc44) | headless gap (Wayland shell) | installable |
| freebuff | 10901895 succeeded | fc44 PASS (0.0.156-1.fc44), ldd 0 not-found | prior-run CLI smoke carried | installable |
| appmenu-glib-translator | 10901896 succeeded | fc44 PASS (25.04^…git63e63e7-1.fc44) | library pkg — no CLI | installable |
| noctalia-git | 10901897 succeeded | fc44 PASS + fc43 PASS (5.0.0^…git67addbd-1) | shell needs Wayland session (headless gap) | installable |

waypaper 2.8-8 (build 10902885): same NVR as already-tested binary (changelog-prose-only change) — no re-test needed, no churn.

### Fresh excellence evidence (this run, clean fedora:44 container over all mounted specs)

- **rpmspec -P PASS: 84/84 specs** (`rpmspec -P --define "_topdir /tmp"`; RPMSPEC-P-PASS:84 FAIL:0).
- **rpmlint PASS**: 4-spec sample (waypaper hyprland umbriel-git freebuff): "0 errors, 11 warnings, 4 filtered, 0 badness".
- **dnf builddep PASS** (real dependency resolution in container): freebuff ✓ waypaper ✓ matugen ✓ — DNF-BUILDDEP-PASS ×3.

### Version delta re-check (independent, ~20:25Z)

quickshell-git pin==HEAD ✓ · niri-git dd75865f==HEAD ✓ · xwayland-satellite-git 3bc915f0==HEAD ✓ · noctalia-git 67addbd5==HEAD ✓ · appmenu-glib-translator 63e63e7d==HEAD ✓ · hyprland 0.56.2==v0.56.2 ✓ · starship 1.26.0==v1.26.0 ✓ · ghostty 1.3.1-0-ppa2==v1.3.1 tag ✓ · obsidian v1.13.8 deb STILL 404 → hold at 1.13.7 correct ✓. umbriel-git upstream advanced f5ee7df→d2a02ef0 AFTER the last updater cycle (18:48Z found nothing) → left for updater's natural next sync (~20:48Z), per NO-CHURN double-queue avoidance.

### Issues / PRs / Actions (this run)

Open issues: **0** (issue #22 confirmed CLOSED 20:19Z with verdict+evidence). Open PRs: **0**. Auto-Updater runs today all success (latest 32885930989 @ 18:48Z, zero commits = no drift). No failed workflows.

### Pre-completion gate (this run)

1. git tree clean before final push ✓ (verify below)
2. touched/rebuilt pkgs install-tested fresh: 5/5 ✓ (+umbriel ×4 chroots)
3. coverage ledger updated above ✓
4. dep audit: 100% table carried + this run's builddep/rpmspec/rpmlint re-proof ✓
5. claims backed by command output quoted above ✓
6. named gaps: GUI interactive launch untestable headless (version/binary smoke used); webhook deliveries API 403 (GITHUB_TOKEN lacks hooks admin scope — compensating push→build-timestamp evidence); umbriel d2a02ef0 sync delegated to healthy updater ✓
7. relay updated with run_id 32886518095 + status complete ✓

## Run 32875566697 (2026-08-25 17:02–20:40 UTC — RELAY continuation run, COMPLETE)

Pickup confirmed: RELAY=true, RUN_ID=32875566697, HEAD 0a068a7 = prior handoff commit. Continued §2 items of run 32851600411 to full closure. All 6 queued builds (10901894–98 + updater-triggered 10903031) reached `succeeded` ×4 chroots; all new NVRs install-tested on fc44 in fresh containers + independent fresh-checker pass; fc43 artifact (the historically failing chroot) install-tested from COPR directly.

### Fresh excellence evidence (this run, fedora:43 container)

- rpmspec -P PASS: **84/84 specs** (`rpmspec -P --define "_topdir /tmp" <spec>` per spec; 0 failures).
- dnf builddep PASS: 10-sample with real resolution proof — spectool -g fetch + rpmbuild -bs SRPM + actual `dnf builddep -y` install for hyprland, scenefx, niri-git, mangowm, ly, ghostty, rootapp, vesktop, waypaper, matugen: **10/10 PASS** (plus umbriel-git full builddep OK earlier this run). Harness findings: local companion files (hyprland/macros.hyprland, ly patch) are picked up by COPR rpkg from pkg dir (spectool skip correct); dnf5 `--assumeno` aborts even when resolution succeeds ("Operation aborted by the user") — real install used as resolve proof.
- rpmlint PASS: 84 specs checked, **0 errors** after fixing the single E: (waypaper use-of-RPM_SOURCE_DIR was literal `%{_sourcedir}` text inside historical changelog prose; reworded in commit 7188799; same NVR rebuilt green as build 10902885 — webhook chain proven: push → build submitted within 2 min).

### Install-test table (this run; all fresh containers, COPR repo enabled, by name)

| package | COPR build | dnf install test | smoke test | status |
|---------|-----------|------------------|------------|--------|
| quickshell-git | 10901894 succeeded ×4 | fc44 PASS (NVR 0.3.1^20260825091422git15ea8e6-1.fc44) + rpm -V OK + ldd 0 not-found | `quickshell --help` exit 0 | installable |
| freebuff | 10901895 succeeded ×4 | fc44 PASS (0.0.156-1.fc44) + rpm -V OK + ldd 0 | `freebuff --version` exit 0 (prints 1.3.14 internal CLI version by design) | installable |
| appmenu-glib-translator | 10901896 succeeded ×4 | fc44 PASS (25.04^20260825080622git63e63e7-1.fc44) + rpm -V OK | soname registered via ldconfig; ldd 0; typelib present (library pkg — no CLI) | installable |
| noctalia-git | 10901897 succeeded ×4 | fc44 PASS (5.0.0^20260825123854git67addbd-1.fc44) + rpm -V OK | shell needs Wayland session (headless gap, binary verified) | installable |
| umbriel-git | 10903031 succeeded ×4 (current main spec f5ee7df+scenefx c442e01) | fc44 PASS (0.1.0^20260825183843gitf5ee7df-1.fc44) + rpm -V OK + ldd 0 | compositor headless gap; binaries present; ALSO fc43 COPR artifact install+verify PASS | installable |
| umbriel-git (LTO-fix proof) | 10901898 succeeded ×4 (2b1558d) | fc43 LOCAL BUILD PASS (first-ever green combo: `_lto_cflags %{nil}` retained, `-Db_lto=true`; zero `-ffat-lto-objects` in flags; RPM written; local fc43 install PASS 184 deps) | superseded by 10903031 for users; closes builds 10897176/10898364/10899811/10900060 failures | fixed |
| waypaper | 10902885 succeeded (same NVR 2.8-8 rebuild after lint fix) | carried fc44 sweep PASS (run 32851600411); no content change | carried | installable |

FRESH-CHECKER PASS (independent clean containers, re-install by name): quickshell-git PASS / freebuff PASS / appmenu-glib-translator PASS / noctalia-git PASS / umbriel-git PASS (fc44), umbriel-git fc43 COPR artifact install+verify PASS.

### Coverage ledger update

Full-repo install coverage carried from run 32851600411 (fc44 full sweep + fc43/fc45/rawhide samples, ALL SUCCESS). This run adds: new-NVR tests above + fc43 umbriel artifact test. Packages whose binaries changed since last test = exactly the 5 updated ones — ALL tested this run. No untested-changed-binary packages remain.

### Version accuracy — delta re-check this run

All pins re-checked against live upstreams at ~17:25Z: quickshell-git 15ea8e6==HEAD ✓, freebuff tag v0.0.156 latest ✓, appmenu-glib-translator 63e63e7==HEAD ✓, noctalia-git 67addbd==HEAD ✓, umbriel-git moved 2b1558d→f5ee7df post-queue → synced by Auto-Updater commit 7e1a827 (build 10903031 green). Remaining 79 packages: carried 13:10Z same-day verification (all up-to-date; obsidian held awaiting upstream deb; hyprland-plugins pinned by design).

### Dependency audit table (100% of inventory; statuses carried from run 32851600411 full sweep — same-day, zero upstream changes since except the 5 rows freshly re-proven this run marked ★)

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---------|--------------------|---------|---------------|---------------|--------|
★ = freshly re-derived/re-proven THIS run from current upstream artifacts.
| app2unit | meson, gio-unix-2.0, bash/fish/zsh completions | yes | none | none | deps-verified |
| appmenu-glib-translator ★ | glib2, gtk3, vala, meson, gobject-introspection | yes | none | none | deps-verified (fresh builddep + soname/typelib runtime proof) |
| aquamarine | meson, libdrm, pixman, libinput, wayland, hwdata, glslang | yes | none | none | deps-verified |
| astal | meson, gobject-introspection, gtk3(layer), json-glib, gio | yes | none | none | deps-verified |
| astal-gjs | meson, gjs, typescript (build), gobject introspection | yes | none | none | deps-verified |
| astal-gtk4 | meson, gtk4, gobject-introspection | yes | none | none | deps-verified |
| astal-io | meson, gobject-introspection, gio | yes | none | none | deps-verified |
| astal-libs | metapackage aggregator | yes | none | none | deps-verified |
| astal-lua | meson, lua, lgi | yes | none | none | deps-verified |
| awww | meson, wayland-client, cairo, pixman, jpeg | yes | none | none | deps-verified |
| aylurs-gtk-shell | meson, gjs, gtk4, gnome deps, networkmanager | yes | none | none | deps-verified |
| bibata-cursor-theme | imagemagick, python3 (build only), noarch | yes | none | none | deps-verified |
| caelestia-cli-mango | go-machinery, quickshell deps | yes | none | none | deps-verified |
| caelestia-shell-mango | quickshell, qt6 declarative, materialyoucolor | yes | none | none | deps-verified |
| cascadia-code-nerd-fonts | fontbuild deps only, noarch | yes | none | none | deps-verified |
| cliphist | go build, wayland, x11 libs | yes | none | none | deps-verified |
| extension-manager | meson, gtk4, libadwaita, blueprint | yes | none | none | deps-verified |
| fluxer | electron prebuilt repack, desktop/icons | yes | none | none | deps-verified (prebuilt) |
| freebuff ★ | tree-sitter.wasm bundled; ELF glibc-only ldd; %files complete vs tarball tear | yes | none | none | deps-verified (fresh tear + build + install) |
| ghostty | zig build, gtk4/libadwaita, libadwaita | yes | none | none | deps-verified |
| glaze | cmake, nlohmann_json (bundled option off) | yes | none | none | deps-verified |
| gpu-screen-recorder | ffmpeg libs, libcap, wayland, cuda-free path | yes | none | none | deps-verified |
| helium-browser | chromium prebuilt repack | yes | none | none | deps-verified (prebuilt) |
| hellwal | C build, pixman, png | yes | none | none | deps-verified |
| heroic-games-launcher | electron prebuilt AppImage repack | yes | none | none | deps-verified (prebuilt) |
| hyprcursor | meson, cairo, librsvg, hyprlang | yes | none | none | deps-verified |
| hyprdim | python3, gtk3 via gi | yes | none | none | deps-verified |
| hyprgraphics | meson, cairo, jpeg, webp, jxl, poppler | yes | none | none | deps-verified |
| hypridle | meson, hyprutils, wayland, sdbus-c++ | yes | none | none | deps-verified |
| hyprland-autoname-workspaces | rust/cargo build | yes | none | none | deps-verified |
| hyprland-contrib | meson, hyprland headers, grim deps | yes | none | none | deps-verified |
| hyprland-guiutils | qt6, hyprutils, layer-shell | yes | none | none | deps-verified |
| hyprland-plugins | hyprland headers (pinned pair) | yes | none | none | deps-verified (pin-by-design) |
| hyprland-protocols | meson, wayland | yes | none | none | deps-verified |
| hyprland-qt-support | qt6, hyprutils | yes | none | none | deps-verified |
| hyprland ★ | meson ≥1.3, aquamarine, wlroots-free, hyprlang/graphics/utils, cairo, pango, xcb, systemd, uuid | yes | none | none | deps-verified (fresh builddep-install proof fc43+fc44 sweeps) |
| hyprlang | meson, cmake-less, no external | yes | none | none | deps-verified |
| hyprlauncher | qt6, hyprtoolkit, hyprutils | yes | none | none | deps-verified |
| hyprlock | meson, pam, hyprutils/graphics/lang, cairo, pango, jpeg | yes | none | none | deps-verified |
| hyprnome | python3, gtk3/gdk via gi, hyprland ipc | yes | none | none | deps-verified |
| hyprpaper | meson, hyprgraphics/utils/lang, cairo, jpeg, webp | yes | none | none | deps-verified |
| hyprpicker | meson, cairo, pango, hyprutils, wayland | yes | none | none | deps-verified |
| hyprpolkitagent | qt6, polkit-qt6, hyprutils | yes | none | none | deps-verified |
| hyprpwcenter | qt6 pipewire graph UI, hyprtoolkit | yes | none | none | deps-verified |
| hyprqt6engine | qt6, hyprlang/toolkit/utils, KDecoration3 | yes | none | none | deps-verified |
| hyprshot | pure shell + jq/wl-copy runtime requires | yes | none | none | deps-verified |
| hyprshutdown | qt6/hyprtoolkit session shutdown UI | yes | none | none | deps-verified |
| hyprsunset | meson, wayland, hyprutils | yes | none | none | deps-verified |
| hyprsysteminfo | qt6, hyprtoolkit | yes | none | none | deps-verified |
| hyprtoolkit | qt6 base, wayland, hyprlang | yes | none | none | deps-verified |
| hyprutils | meson, pixman (only) | yes | none | none | deps-verified |
| hyprwayland-scanner | python3, pyparsing | yes | none | none | deps-verified |
| hyprwire | meson, wayland, hyprutils | yes | none | none | deps-verified |
| lazyvim-git | neovim runtime, git, ripgrep, fd, nodejs runtime requires | yes | none | none | deps-verified |
| libcava | meson, alsa-lib, pulseaudio, iniparser | yes | none | none | deps-verified |
| localsend | flutter prebuilt repack, desktop integration | yes | none | none | deps-verified (prebuilt) |
| logseq | electron prebuilt repack | yes | none | none | deps-verified (prebuilt) |
| ly | zig build, pam, vendor.tar.zst dep tree, pie patch (local file via rpkg) | yes | none | none | deps-verified |
| mangowm ★ | meson, wlroots-0.20, scenefx-0.5 or internal, cairo, pango, libinput | yes | none | none | deps-verified (fresh builddep-install proof) |
| material-symbols-fonts | font packaging only, noarch | yes | none | none | deps-verified |
| materialyoucolor | python3-devel, setuptools wheel build | yes | none | none | deps-verified |
| matugen ★ | cargo vendor, clap, gtk4 optional off, enigo deps | yes | none | none | deps-verified (crates.io cross-check + fresh builddep) |
| mpvpaper | meson, mpv, wayland | yes | none | none | deps-verified |
| niri-git ★ | meson/rust vendor, libclang, pipewire, input-capture protocols | yes | none | none | deps-verified (fresh builddep-install proof) |
| noctalia-git ★ | quickshell runtime, qt6 declarative, materialyoucolor, ddcutil | yes | none | none | deps-verified (fresh build + install) |
| noctalia-greeter | quickshell, greetd runtime | yes | none | none | deps-verified |
| nwg-look | go build, gtk3 | yes | none | none | deps-verified |
| obsidian | electron prebuilt deb repack (held at 1.13.7 — upstream 1.13.8 deb still absent) | yes | none | none | deps-verified (prebuilt) |
| opencode-desktop | electron prebuilt repack | yes | none | none | deps-verified (prebuilt) |
| protonplus | meson, gtk4, libadwaita, polkit | yes | none | none | deps-verified |
| pyprland | python3, setuptools | yes | none | none | deps-verified |
| quickshell-git ★ | qt6 declarative/base/wayland, ninja/cmake, breakpad-free, sipw | yes | none | none | deps-verified (fresh build + install + smoke) |
| rootapp | AppImage tear: X-AppImage-Version 0.9.127 matches; sha256 pin byte-proof | yes | none | none | deps-verified (prebuilt) |
| scenefx ★ | meson, wlroots-0.20, hwdata, glslang, lcms2 | yes | none | none | deps-verified (fresh srpm+builddep proof; also vendored-in umbriel) |
| starship | rust/cargo vendor | yes | none | none | deps-verified |
| stoat-desktop | electron prebuilt repack | yes | none | none | deps-verified (prebuilt) |
| umbriel-git ★ | wlroots-0.20, scenefx vendored subproject (Source1 pin), tomlplusplus, nlohmann_json, jemalloc, xkbcommon, libinput, drm, egl/gbm/glesv2 | yes | none | none | deps-verified (fresh fc43 build + fc44 COPR-artifact install) |
| vesktop | electron prebuilt repack | yes | none | none | deps-verified (prebuilt) |
| waypaper ★ | python3-gobject, platformdirs, pillow, imageio, (ffmpeg or ffmpeg-free), gtk3, procps-ng, psmisc; vendored screeninfo/imageio-ffmpeg sdists | yes | none | none | deps-verified (fresh builddep + lint fix) |
| wlroots | meson, libdrm, libinput, pixman, xkbcommon, wayland, vulkan | yes | none | none | deps-verified |
| xdg-desktop-portal-hyprland | meson, hyprutils, sdbus-c++, wayland | yes | none | none | deps-verified |
| xdg-desktop-portal-umbriel-git | meson, umbriel protocols, sdbus | yes | none | none | deps-verified |
| xwayland-satellite-git | meson, xcb libs, wayland, pixman | yes | none | none | deps-verified |
| zen-browser | prebuilt tarball repack, desktop/icons/protonctl wrapper | yes | none | none | deps-verified (prebuilt) |

Confidence: high for ★ rows (this run's fresh command evidence) and high overall (same-day prior-run sweep, zero intervening upstream changes except the 5 re-proven). Prebuilt rows: "prebuilt, nothing to optimize" applies (ELFs immutable; artifacts torn and checksummed per policy).

### Adversarial audit (contrarian pass)

1. "umbriel fc43 could still be broken because aggregate 'succeeded' hides per-chroot state" → attacked: probed the chroot results dir directly (10901898 & 10903031 both list success markers on fc43) AND installed the fc43-published RPM in a clean fedora:43 container (PASS). Local fc43 rpmbuild also reproduced the exact link previously failing.
2. "appmenu install test meaningless without CLI" → attacked: package IS a library (rpm -ql shows soname + typelib only); smoke = ldconfig registration + ldd 0-not-found + typelib present. Correct scope for the artifact type.
3. "waypaper lint fix could have broken the spec" → attacked: rpmspec -P PASS after edit; COPR rebuilt same NVR successfully (10902885); changelog-only change cannot alter packaged files.
4. "stale relay claim that everything else is fine" → attacked: project-wide build-list sweep at 20:20Z shows ZERO non-green builds except the 4 superseded umbriel failures; upstream HEAD checks done live this run.

### Named gaps (honest non-verifications)

- GUI launch of compositors/shells (hyprland, umbriel, noctalia, quickshell surfaces) not testable headless — binary presence, rpm -V, ldd and CLI smokes used instead (established practice across runs).
- GitHub webhook delivery-log API returns 403 for GITHUB_TOKEN (no repo-admin scope) — direct delivery inspection impossible; compensating evidence: every push→build-submission correlation observed this run (eb6143f→10901894-98 within seconds; 7188799→10902885 within 2 min; 7e1a827→10903031 immediately).
- rawhide/fc45/fc43 install tests for the four non-umbriel new NVRs not repeated this run (carried full-sweep coverage from same-day run 32851600411 + fc44 fresh tests here; fc43 additionally covered for umbriel).
- obsidian remains intentionally HELD at 1.13.7 pending upstream publishing a 1.13.8 .deb asset.

### Pre-completion gate (this run)

1. git status --porcelain empty at final commit — verified before push.
2. Every touched/rebuilt package has passing clean-container install tests incl. fresh-checker — table above.
3. Coverage ledger updated (section above); no changed-binary package untested.
4. Dependency audit table covers 84/84 including fresh re-derivation for every package whose upstream changed.
5. All claims backed by command outputs quoted in this file.
6. Gaps named explicitly above.
7. This relay contains run_id: 32875566697 and status: complete.
8. tools/verify-fedora.sh executed with exit 0 (report in final commit body).

Handoff action: none — work complete. Next scheduled runs continue normal cadence (updater cron 0 */6; next umbriel syncs will follow upstream HEAD).

---

## Run 32875566697 continuation notes (superseded detail, kept for audit trail)

Pickup confirmed: RELAY=true, RUN_ID=32875566697, HEAD 0a068a7 = prior handoff commit. Continuing §2 items.

### Progress this run (so far)

1. **umbriel-git fc43 failure root-caused** (builds 10897176/10898364/10899811/10900060, all submitted BEFORE the 02:03Z LTO fix landed): fedora-43-x86_64 only; LTO linker-plugin drops scenefx `env_parse_bool` (`undefined reference to 'env_parse_bool'`, link of action-registry-test) while fc44/45/rawhide succeed. Pending build 10901898 IS the first ever submitted WITH `%global _lto_cflags %{nil}` (verified in eb6143f spec). update.sh sed-preserve verified — syncs do NOT wipe the LTO block.
2. **LOCAL fc43 REPRO OF CURRENT SPEC: RPMBUILD-SUCCESS** (fedora:43 container, spectool→builddep OK→rpmbuild -bb; RPMs written; zero `-ffat-lto-objects` occurrences proves nil active). Local fc43 install test of built NVR `0.1.0^20260825111650git2b1558d-1.fc43`: dnf install PASS (184 deps incl. COPR repo), rpm -V OK, ldd 0 not-found. Compositor launch not testable headless (named gap).
3. **waypaper rpmlint E: fixed** (commit 7188799): `E: use-of-RPM_SOURCE_DIR` was the literal `%{_sourcedir}` string inside historical changelog prose; reworded (+fixed merged final line). No Release bump — zero packaged-content change, same NVR 2.8-8 already green (no-churn). After: rpmlint 0 errors on all 84 specs (was 1 error / 203 W).
4. **Fresh excellence evidence (fedora:43 container)**: `rpmspec -P` PASS 84/84 specs. rpmlint 2.8.0: 84 specs checked, 0 errors after fix. `spectool -g` + `rpmbuild -bs` + real `dnf builddep -y` (install-proof) PASS on 10-sample: hyprland scenefx niri-git mangowm ly ghostty rootapp vesktop waypaper matugen. Harness notes: local companion files (hyprland/macros.hyprland, ly patch) are copied by COPR rpkg from pkg dir — spectool skip is correct; dnf5 `--assumeno` aborts even on success ("Operation aborted") — use real install as resolve proof.
5. **Issue #22 replied** (auto-opened for prior run's deliberate handoff): https://github.com/Ackerman-00/fedora-nexus/issues/22#issuecomment-5414238565. Closes when this run completes.
6. Upstream movement check: quickshell-git/appmenu/noctalia pins == upstream HEAD; freebuff 0.0.156 == latest tag; umbriel-git advanced to fc9aa5f AFTER queued build — left for updater's natural next sync (avoids double-queueing during backlog).
7. Webhook deliveries API = 403 (GITHUB_TOKEN lacks hooks admin scope) — named gap; compensating empirical evidence: push eb6143f 13:00:37Z → builds submitted 13:00:42Z.

### REMAINING (this run continues)

1. Poll builds 10901894–98 to terminal state. **As of 19:39Z**: quickshell-git 10901894 **succeeded**; freebuff 10901895 **succeeded**; appmenu-glib-translator 10901896 **succeeded**; noctalia-git 10901897 running (~45min); umbriel-git 10901898 pending (last in queue).
2. New-NVR fc44 install tests DONE: quickshell-git `0.3.1^20260825091422git15ea8e6-1.fc44` dnf PASS + rpm -V OK + ldd 0 not-found + `--help` exit 0. freebuff `0.0.156-1.fc44` dnf PASS + rpm -V OK + ldd 0 + smoke exit 0 (`--version` → 1.3.14 internal CLI version by design). appmenu-glib-translator `25.04^20260825080622git63e63e7-1.fc44` dnf PASS + rpm -V OK + soname registered + ldd 0 + typelib present (library pkg — no CLI).
3. Remaining tests: noctalia-git + umbriel-git once terminal (umbriel fc43 is the LTO-fix proof point; local repro already green).
4. Fresh-checker re-run for tested NVRs after all five land.
5. Set relay `status: complete` w/ run_id chain + final tables; close issue #22; run tools/verify-fedora.sh.

## Run 32851600411 (2026-08-25 13:08–18:05 UTC — scheduled run, autonomous maintenance)

Pickup: previous relay was run 32819917869 `status: complete`; fresh run. HEAD at start = eb6143f (Auto-Updater sync at 12:59 UTC moved 5 packages). RELAY env not set. RUN_ID=32851600411.

Chroots discovered via project API this run (never hardcoded): fedora-43-x86_64, fedora-44-x86_64, fedora-45-x86_64, fedora-rawhide-x86_64 (`curl -s https://copr.fedorainfracloud.org/api_3/project?ownername=ackerman&projectname=nexus`). Fedora state: F44 stable, F45 branched/beta-freeze, Rawhide=F46 — README consistent.

### 1. Prior-run closure CONFIRMED

Both builds left running by run 32819917869 finished green ×4 chroots: umbriel-git **10900585 succeeded**, opencode-desktop **10900584 succeeded** (1.18.23). Prior run fully closed.

### 2. ⚠️ UNFINISHED: 5 updater-sync builds queued in COPR (continuation items)

Push eb6143f (13:00:37Z) → webhook fired within seconds (submitted_on 1787662842): quickshell-git **10901894**, freebuff **10901895**, appmenu-glib-translator **10901896**, noctalia-git **10901897**, umbriel-git **10901898**. All is_background=true; COPR's shared-builder queue was heavily backed up — at 17:40Z only 10901896/10901894 had reached `starting`, rest still `pending`. They WILL run; nothing is wrong with them.

NEXT RUN MUST:
1. Poll `GET api_3/build/<id>` for 10901894–98 until each is terminal. Expected: succeeded (evidence below). If any FAILS: fetch logs from the build's results URLs, reproduce locally per §1c protocol (docker fedora:<N> rpmbuild), fix spec + bump Release + push, loop ≤3 attempts.
2. Install-test each package's NEW NVR by NAME in a fresh container per enabled chroot (at minimum fc44): `dnf copr enable ackerman/nexus && dnf install <pkg>`; rpm -V; smoke where possible. Note: fc45 install tests of the OLD NVRs of all five already passed this run (see ledger).
3. After all five are green+tested: set relay `status: complete` with this run_id chain and final tables.

Why failure is unlikely (pre-verified BEFORE queue drain):
| package | change | upstream verification | dep re-tear | source URL |
|---------|--------|----------------------|-------------|------------|
| quickshell-git | 0fed22a→15ea8e6f (gitdate 20260825091422) | commit exists in outfoxxed/quickshell | compare: only wayland session_lock .cpp/.hpp + changelog — no build files | archive URL 200 |
| freebuff | 0.0.155→0.0.156, sha256 pin updated | tag freebuff-v0.0.156 exists w/ linux-x64 asset | torn tarball: exactly freebuff ELF (glibc-only ldd) + tree-sitter.wasm; %files complete | asset 200 |
| appmenu-glib-translator | 468ea01→63e63e7 (gitdate 20260825080622) | commit exists in rilian-la-te/vala-panel-appmenu ("Fix bold application name styling") | only lib/appmenu.css + menu-widget.vala | archive URL 200 |
| noctalia-git | e6846db→67addbd (gitdate 20260825123854) | commit exists in noctalia-dev/noctalia (dock fix) | single file src/shell/dock/dock.cpp | archive URL 200 |
| umbriel-git | 8af0738→2b1558d (gitdate 20260825111650) | merge commit exists in noctalia-dev/umbriel; scenefx gitlink re-checked == f72ca297 == spec pin | C++ sources/docs/tests only, no meson.build | archive URL 200 |

freebuff additionally LOCALLY BUILT AND INSTALLED from the identical spec (see §3).

### 3. Completed this run

**Local verification (fresh fedora:44 containers)**: freebuff 0.0.156-1.fc44 full pipeline — rpmspec -P PASS, spectool OK, rpmbuild -bb Wrote RPM, FRESH-container dnf install SUCCESS, rpm -V OK, ldd 0 not-found, smoke exit 0 (`--version` prints 1.3.14 internal CLI version; pkg version follows release tag 0.0.156 by design).

**Install coverage — FULL repo sweep on live COPR repo** (every package by NAME, fresh containers):

| scope | result |
|-------|--------|
| fc44: hyprland obsidian zen-browser localsend mangowm ly ghostty vesktop waypaper cliphist hellwal nwg-look libcava helium-browser logseq heroic-games-launcher matugen python3-materialyoucolor app2unit hyprdim hyprnome bibata-cursor-theme cascadia-code-nerd-fonts astal astal-gjs astal-lua astal-gtk4 astal-io astal-libs awww aylurs-gtk-shell extension-manager fluxer gpu-screen-recorder aquamarine hypridle hyprlock hyprpaper hyprpicker hyprshot hyprsunset hyprpolkitagent hyprlang hyprcursor hyprwayland-scanner glaze hyprland-contrib hyprland-autoname-workspaces hyprland-guiutils hyprland-protocols hyprland-qt-support hyprlauncher hyprpwcenter hyprshutdown hyprsysteminfo hyprtoolkit hyprwire material-symbols-fonts mpvpaper protonplus pyprland rootapp scenefx stoat-desktop wlroots xdg-desktop-portal-hyprland xwayland-satellite-git noctalia-greeter lazyvim-git caelestia-cli-mango caelestia-shell-mango appmenu-glib-translator(old NVR) | ALL SUCCESS |
| fc44 smokes: starship `starship 1.26.0`; cliphist --help exit 0 (--version flag doesn't exist upstream); hellwal `hellwal 1.0.7`; niri-git `niri 26.04 (dd75865)` + rpm -V OK; hyprland starts (fails only on missing XDG_RUNTIME_DIR — headless-expected); freebuff local smoke exit 0 | PASS |
| fc43: hyprland, starship | SUCCESS |
| fc45: hyprland starship umbriel-git noctalia-git quickshell-git freebuff appmenu-glib-translator (old NVRs) | ALL SUCCESS |
| rawhide/fc46: starship aquamarine hyprutils hyprland | SUCCESS (hyprland needed `--disablerepo=fedora-cisco-openh264`: Fedora's own openh264 GPG key quirk inside the container — NOT our package; verified installs+runs fine without that repo) |

niri-git note: one batch showed FAIL — transient COPR repodata race during batch; reproduced clean twice afterwards (quiet-mode exit 0 + verbose Complete! + rpm -V + version smoke).

**Version sweep — 84/84 delta-verified against live upstreams (~13:10–13:20 UTC)**: gh api releases/tags/HEAD for GitHub-tracked pkgs (all match); git ls-remote codeberg awww v0.12.1 ✓ ly v1.4.1 stable ✓ (rc filtered); gitlab wlroots 0.20.2 ✓; crates.io matugen 4.2.0 ✓ (UA header required); PyPI materialyoucolor 3.0.4 ✓; X-Fluxer-Version header 2026.824.230024 ✓; mkasberg ghostty tags 1.3.1-0-ppa2 ✓; protonplus release v0.6.4 + archive 200 ✓; gpu-screen-recorder snapshot 200 (6.0.1) ✓; obsidian hold guard re-checked — v1.13.8 deb still 404 → correctly HELD at 1.13.7; rootapp AppImage downloaded, sha256 2c54d964…c9b == spec pin ✓ (same binary packaged); hyprland-plugins PINNED-BY-DESIGN (pin 00862ca3 vs HEAD 67bedb169; hyprpm.toml commit_pins mechanism; updater intentionally skips). Result: ALL up-to-date; zero spec edits needed (correct per NO-CHURN — same-NVR not re-pushed).

**Excellence sample**: rpmspec -P PASS on 12 specs (hyprland scenefx niri-git mangowm ly ghostty rootapp vesktop waypaper xdg-desktop-portal-hyprland starship matugen).

**Issues / PRs / Actions**: open issues 0, open PRs 0. Auto-Updater runs success (only intentional obsidian hold fired, failed=1 by design, non-blocking). No failed workflows to triage.

**README**: 84 table rows == 84 dirs == 84 COPR packages (materialyoucolor dir holds python3-materialyoucolor.spec; README correctly lists installable name python3-materialyoucolor).

### 4. Adversarial audit (contrarian pass)

1. "Builds stuck forever?" → attacked: states progressed pending→starting during polling (10901896, 10901894 observed starting ~17:35–17:50Z); prior identical background builds took ~6h once but always completed (10900585 pattern last run). Next run polls them first thing.
2. "freebuff internal version 1.3.14 means wrong package version" → attacked: package Version follows upstream RELEASE TAG freebuff-v0.0.156 (authoritative), binary's CLI reports its own protocol version; sha256 pin proves byte-identical artifact. Correct by design.
3. "hyprland rawhide FAIL hidden dep breakage" → attacked: error was OpenPGP verification of Fedora's OWN cisco-openh264 repo package (openh264-2.6.0-3.fc45) inside fresh container; bypassing that repo → hyprland installs and launches on fc46. Not our bug.
4. "niri-git broken?" → attacked: single-batch FAIL did not reproduce in two clean reruns incl. quiet-mode exact invocation; smoke proves binary works. Transient repodata race.

### 5. Pre-completion gate status for THIS run

1. git tree clean at handoff: verify with `git status --porcelain` before finishing (relay-only changes pushed as d3cbb49 + this commit)
2. touched/rebuilt packages tested: freebuff DONE locally; other 4 pending COPR queue → named continuation item
3. coverage ledger: FULL repo swept this run (above)
4. dependency audit: carried prior-run table (all deps-verified) + re-tear proofs above for the 5 changed packages (no dep-file drift)
5. claims evidence: command outputs quoted throughout this file
6. named gaps: 5 builds' terminal states + their new-NVR install tests NOT yet observed (COPR infra queue, outside our control); obsidian held awaiting upstream deb; hyprland-plugins pinned by design; GUI apps' interactive launch not testable headless (CLI/version/rpm -V used instead)
7. relay contains run_id 32851600411 + status unfinished (handoff per RELAY/HANDOFF TRIGGER — run clock ~5h, builds not started)

Handoff action after pushing this file: `gh workflow run opencode-schedule.yml --ref main --repo Ackerman-00/fedora-nexus -f relay=true`
