# fedora-nexus autonomous-run relay state

status: complete
run_id: 31679278418

## Run 31679278418 (2026-08-13) - COMPLETE: 65/65 versions current, auto-updater bumped ONLY niri-git (92a25a0 -> 11756db, build 10858577 green all 3 chroots), niri-git install-tested green fc44/fc43/rawhide + fresh-checker from live repo, full 65-package install-test coverage achieved (ledger now complete), zero churn, no fixes needed, no open issues/PRs

### 0. Pickup
- Picked up main at `a70a4d7` (2026-08-13 07:44Z auto-update "sync spec files to latest upstream versions" - repo's only commit, history squashed by owner; prior relay intact: status complete run 31661585209). RELAY=false, RUN_ID=31679278418.
- COPR chroots (API-verified, never hardcoded): fedora-44-x86_64, fedora-43-x86_64, fedora-rawhide-x86_64. Fedora lifecycle: F45 mass-branched 2026-08-11 (2 days ago), rawhide build tags .fc45 are COPR-side/pre-GA, F43 still supported. No chroot changes needed.
- Open issues/PRs: NONE (gh issue list / gh pr list empty). Actions health: last auto-updater 31678999323 (#823) success 07:44Z produced commit a70a4d7; not cascading to another run.
- Webhook verified EMPIRICALLY again: auto-updater pushed commit a70a4d7 ~07:45Z -> COPR build 10858577 (niri-git 26.04^20260813032646git11756db) submitted same minute (2s) and SUCCEEDED all 3 chroots. Direct webhook API read remains 403 (token scope, known gap).

### 1. Version accuracy sweep - 65/65 CURRENT, ZERO CHURN
- The auto-updater (run 31678999323/#823, 07:44Z) scanned everything and bumped ONLY niri-git: commit 92a25a0 -> 11756db (niri 26.04 20260813032646). That is the ONLY spec touched this run; build 10858577 succeeded fedora-44/43/rawhide.
- Re-ran all 33 update.sh scripts in a FRESH archived checkout (/tmp/freshrepo): all 33 exit 0, ZERO diffs vs HEAD (churn-free pure commit/tag guards). bash -n passes all 33.
- Git-pinned packages all MATCH upstream HEAD via git ls-remote (10/10): app2unit 47e23ec, caelestia-cli-mango be54856, caelestia-shell-mango cae55ed, material-symbols-fonts 50f0603 (repo google/material-design-icons), hyprland-contrib 3dcbce7, lazyvim-git 459a4c3, niri-git 11756db, quickshell-git 28771c7, xwayland-satellite-git 3bc915f. hyprland-plugins pin 00862ca = "hyprpm: add pin for 0.56.2" commit == hyprland 0.56.2 release (verified via gh api commit message; correct by design).
- GitHub-release packages ALL at latest via gh api releases/latest + tags: aquamarine v0.14.0, bibata 2.0.7, cascadia 3.5.0, cliphist 0.7.0, extension-manager 0.6.5, glaze v8.0.0, gpu-screen-recorder 6.0.0, helium 0.15.4.1, heroic 2.22.1, all hypr* (hyprland 0.56.2 = latest release; hyprcursor 0.1.13; hyprlang 0.6.8; hyprutils 0.14.0 etc.), libcava 1.0.0, logseq 2.0.1, mangowm 0.16.0, matugen v4.1.0, mpvpaper 1.9, nwg-look 1.1.1, obsidian 1.13.7, scenefx 0.5, starship v1.26.0, stoat 1.4.2, vesktop 1.6.5, waypaper 2.8, xdg-desktop-portal-hyprland v1.4.1, zen 1.21.13b. fluxer X-Fluxer-Version header 2026.812.134312 == spec (URL 200). ghostty: tag 1.3.1-0-ppa2 latest, asset ghostty_1.3.1-0.ppa2_amd64_24.04.deb returns 200. localsend held at 1.18.0-2 CORRECTLY (v1.18.1 tag exists but 1.18.1 deb asset = 404; update.sh guard worked). wlroots 0.20.2-2 (gitlab.freedesktop.org serves bot-check 403 to runner IP - COPR built it fine already; Arch/Nixpkgs confirm 0.20.2 latest). ly v1.4.1 (Codeberg), awww v0.12.1 (Codeberg), protonplus v0.6.3, materialyoucolor 3.0.4 (PyPI).
- Binary-package teardowns (this run):
  - rootapp 0.9.126 RE-TEAR: AppImage downloaded, sha256 = `0736926fde923819d276ac6d4a3afe65c6d87c734689af47276fc4ebf075d910` == spec comment; unsquashfs (offset 944632) extracted; X-AppImage-Version=0.9.126 in Root.desktop AND sq.version `<version>0.9.126</version>` in squashfs-root -> spec Version 0.9.126 CONFIRMED.
  - niri-git RE-TEAR (upstream source moved this run): extracted the 11756db tarball, Cargo.toml dep audit -> default features dbus/systemd/xdp-gnome-screencast; ALL native deps covered by existing spec BuildRequires (no new Requires needed).
- Source URL sweep (fixed the `${url##*:}` shell bug that was stripping `https://` and false-400ing): ALL Source0 return 200.
- Spec-vs-COPR NVR sync check: every current spec Version maps to a SUCCEEDED COPR build of the same version across all 65 (per-package verified via /tmp/urls.tsv + COPR build list) - no unsynced updates, nothing pending rebuild.

### 2. COPR reconciliation
- COPR packages (63) = 65 repo dirs minus hyprlauncher/hyprshutdown (pending owner registration - README caveat intact) minus materialyoucolor naming (COPR: python3-materialyoucolor). README 65 rows == 65 dirs. X-consistent.
- Recent builds (limit=100): 63 succeeded, 4 non-succeeded ALL historical & superseded - localsend 10844795 (1.18.0-1 fail, superseded 10844868 1.18.0-2 success), obsidian 10843364 (1.13.6-1 fail, superseded 10856758 1.13.7-1), protonplus 10841179 (fail, superseded 10850873 0.6.3-1), ly 10840572 (canceled, superseded 10842822). No active pending/running/failed builds.
- Only changed package this run: niri-git build 10858577 succeeded all 3 chroots, RPMs published (downloaded real RPMs from pulp content for all 3 chroots).

### 3. Fixes this run
- NONE. No package broke, no version stale, no dependency missing. Zero commits needed (pushing would be pure churn).

### 4. Install-test ledger (all FRESH docker containers, from LIVE COPR repo unless noted)
- niri-git (the ONLY binary changed this run) - full trilogy:
  - fc44 PASS: local rpm + dnf copr-enable (xwayland-satellite-git + Xwayland auto-resolved from COPR), rpm -V=0, ldd missing=0, `niri --version` = "niri 26.04 (11756db)".
  - fc43 PASS: same checks all green ("niri 26.04 (11756db)").
  - rawhide PASS: local .fc45 rpm + dnf --nogpgcheck (fresh rawhide image ships no base GPG keys - image-side quirk, not repo bug), rpm -V=0, ldd=0, version string correct.
- FRESH-CHECKER PASS: fresh fedora:44 container, `dnf copr enable ackerman/nexus` then `dnf install niri-git` from LIVE repo -> installed niri-git-26.04^20260813032646git11756db-1.fc44, rpm -V=0, `niri --version` = 26.04 (11756db).
- NEW fc44 live-repo coverage this run (all rpm -V PASS): app2unit, caelestia-cli-mango, caelestia-shell-mango, lazyvim-git, libcava, material-symbols-fonts, python3-materialyoucolor, mpvpaper, scenefx, wlroots, xdg-desktop-portal-hyprland (hyprland.portal file present), hyprland + hyprlock + hypridle + hyprpaper + hyprpicker + hyprsunset + hyprpolkitagent + hyprland-guiutils + hyprland-qt-support + hyprland-contrib + hyprland-plugins + hyprlang + hyprutils + hyprgraphics + hyprcursor + hyprtoolkit + hyprwire + hyprwayland-scanner + hyprqt6engine + hyprsysteminfo + aquamarine (23 hypr* apps/libs), glaze-devel + hyprland-protocols-devel (both are dev-only packages: main %package intentionally empty, -devel subpackage carries headers/pc/files - correct by design), rootapp 0.9.126 (AppImage-py runtime launches under xvfb), protonplus, awww (awww + awww-daemon), bibata-cursor-theme, cascadia-code-nerd-fonts (fonts installed), ghostty 1.3.1 (`ghostty --version` -> "Ghostty 1.3.1", ldd=0), fluxer (installs, rpm -V=0), xwayland-satellite-git + quickshell-git (ldd=0).
- COMBINED with prior relay ledger, ALL 65 packages now install-tested green from the live repo (complete coverage ledger established; the two dev-only packages tested as -devel).
- Smoke notes (upstream constraints, not packaging bugs): Electron apps refuse --version as root (Chromium sandbox); GUI apps need a Wayland session; verified install + rpm -V + ldd + version-string/launch instead.

### 5. Dependency audit
- RE-TEAR rule applied: only niri-git changed upstream since last audit (92a25a0 -> 11756db) - re-torn (see 1), deps re-derived: cargo default features dbus/systemd/xdp-gnome-screencast all covered by spec BuildRequires; Verified by clean install fc44/fc43/rawhide (no missing libs, no "nothing provides"). rootapp proactivity re-torn (0.9.126 minuscule diff, sha256 == spec). All other packages' upstream unchanged since previous relay audit (carried from run 31661585209/31631786795, still valid).
- Install tests prove Requires resolve in real COPR repo for every package covered this run (~40 new installs; no "nothing provides" anywhere).
- Full 65-package table status: all deps-verified. Spec-Vs-COPR NVR sync confirmed (section 1) - nothing waiting on rebuild.

### 6. Final state
- Working tree clean except this relay update. Only intended commit: relay with run_id 31679278418 + status complete.
- Actions health: auto-updater success (31678999323), this opencode run complete.
- Known gaps (unchanged): (1) hyprlauncher/hyprshutdown not in COPR until owner registers - README caveat present; (2) GUI apps can't run headless - verified install+rpm -V+ldd+version instead; (3) COPR webhook/config API 403 (token scope) - webhook verified empirically via 2s build-submission timing; (4) rawhide dist tag .fc45 pre-GA is COPR-side; (5) COPR download owner is `ackerman`; (6) fresh rawhide image ships no base GPG keys - rawhide tests use dnf --nogpgcheck; (7) gitlab.freedesktop.org (wlroots source) serves 403 bot-check to runner IP but COPR builds fine.
