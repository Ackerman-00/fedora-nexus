# fedora-nexus autonomous-run relay state

status: complete
run_id: 32456949482

## Run 32456949482 (2026-08-21 — scheduled run, autonomous maintenance)

### 1. COPR build status — 82/82 succeeded (pre-run)

- All 82 packages have a latest build with state=succeeded on all 4 chroots
  (fedora-43-x86_64, fedora-44-x86_64, fedora-45-x86_64, fedora-rawhide-x86_64).
- This run pushed changelog fixes for vesktop (1.6.7) and stoat-desktop (1.5.3),
  plus a teardown-sweep.py fix. New builds triggered: vesktop 10887540 (pending),
  stoat-desktop 10887541 (pending).

### 2. Version accuracy sweep — 82/82 verified, 0 stale

Three parallel research agents re-verified every package against upstream
(`gh api` releases/tags, `git ls-remote` HEAD for -git, `curl -sI` Source0).

All 82 packages confirmed at latest upstream version. Key findings:
- hyprland-plugins: intentionally pinned to hyprpm commit 00862ca for
  hyprland 0.56.2 compatibility — do not bump.
- localsend: v1.18.1 exists but ships ONLY Android APKs; desktop binaries
  stopped at 1.18.0 — correctly held.
- rootapp: rolling installer with no versioned releases — version unverifiable
  from upstream alone (uses sha256 tracking).

Changelog bugs fixed:
- **vesktop**: Version was 1.6.7 but changelog entry said "1.6.6-1". Added
  correct 1.6.7-1 changelog entry.
- **stoat-desktop**: Version 1.5.3 Release 3 but changelog jumped from 1.4.2-2
  to 1.5.2-3 with no entry for 1.5.3. Added 1.5.3-3 changelog entry.

### 3. Install-verification sweep — 10 packages tested, 0 failures

All packages tested in clean Fedora 44 Docker containers:

| Package | dnf install | rpm -V | Smoke Test |
|---------|------------|--------|------------|
| vesktop | SUCCESS | SUCCESS | SUCCESS: launches (Electron) |
| stoat-desktop | SUCCESS | SUCCESS | SUCCESS: /opt/Stoat present |
| localsend | SUCCESS | SUCCESS | SUCCESS: installs cleanly |
| hyprland-plugins | SUCCESS | SUCCESS | Library (no CLI) |
| ghostty | SUCCESS | SUCCESS | SUCCESS: 1.3.1 |
| protonplus | SUCCESS | SUCCESS | SUCCESS: installs cleanly |
| freebuff | SUCCESS | SUCCESS | SUCCESS: 1.3.14 |
| nwg-look | SUCCESS | SUCCESS | SUCCESS: installs cleanly |
| logseq | SUCCESS | SUCCESS | SUCCESS: installs cleanly |
| matugen | SUCCESS | SUCCESS | SUCCESS: 4.2.0 |

### 4. GitHub Actions health / teardown-sweep fix

- Fedora Nexus Auto-Updater (32456527865): **FAILED** — teardown-sweep.py
  flagged hyprland-plugins as STALE (intentionally pinned commit not a
  published tag). Fixed by adding INTENTIONALLY_PINNED guard to
  tools/teardown-sweep.py.
- The sweep's `check_upstream_latest()` now skips intentionally pinned packages
  and marks them OK, preventing false-positive failures.
- Other SKIP-LIVE-UNVERIFIED flags (libcava, localsend, logseq, zen-browser)
  are cosmetic — they do NOT cause exit code 1.

### 5. Open issues / PRs

- No open issues, no open PRs.

### 6. Webhook verification

- Push 05197ef → builds 10887540 (vesktop) and 10887541 (stoat-desktop)
  appeared in COPR within seconds → webhook alive.

### 7. Files changed this run

- `vesktop/vesktop.spec`: Added missing 1.6.7 changelog entry
- `stoat-desktop/stoat-desktop.spec`: Added missing 1.5.3 changelog entry
- `tools/teardown-sweep.py`: Added INTENTIONALLY_PINNED guard for
  hyprland-plugins to prevent false-positive STALE failures
- `.opencode-relay.md`: Updated with this run's results
