# fedora-nexus autonomous-run relay state

status: complete
run_id: 31422800110

## Run 31422800110 (2026-08-10) - COMPLETE: fresh-checker pass, 61/61-pkg version sweep current, full fc44 install sweep 68/68 PASS, rawhide+fc43 spot checks PASS, no new failures; nothing to fix

### 0. Pickup
- Picked up main at `96acd07` (relay of run 31408592662, `status: complete`). Started as FRESH CHECKER.
- COPR chroots (API): fedora-43-x86_64, fedora-44-x86_64, fedora-rawhide-x86_64.
- Fedora lifecycle (web: endoflife.date, eosl.date, fedoraproject wiki): F44 stable
  (EOL 2027-06-02), F43 supported (EOL 2026-12-09), rawhide = F45 under development.
  Chroots exactly match the supported set. README claims only x86_64 — no doc/chroot change needed.

### 1. Actions health (priority 3)
- Auto-updater run **31422370320** (2026-08-10 19:05, `Fedora Nexus Auto-Updater`) SUCCEEDED,
  `update-packages: success`. Logs confirm all 31 custom update.sh modules ran (all "No update
  needed"/"already on latest") and the GH-API scanner reported [OK] for every standard package,
  zero [UPDATE]/[SKIP]/[ERROR]. No commit produced (updates_made=false) -> no churn.
- This schedule run (opencode-schedule 31422800110) is itself in_progress healthy.

### 2. COPR build scan (priority 1) — NO failed builds to fix
- Latest 50 builds: only 1 `failed` = **obsidian 10843364 (1.13.6-1)** — ALREADY FIXED last run by
  build **10843472 (1.13.6-2) SUCCEEDED**; not re-triggered (no-churn). All others succeeded.
- Per-package latest build for ALL 61 COPR packages queried individually: state = succeeded for
  every single one. Historical failures (protonplus 10841179, ly 10840572, obsidian 10843364)
  are superseded by newer green builds.
- `noctalia-shell` / `noctalia-qs` ghost RPMs confirmed present in fc44 repodata (known COPR
  infrastructure bug; ignored per policy, NOT touched, NOT reported).

### 3. Fresh-checker install verification of last-run's shipped fixes
- **obsidian 1.13.6-2.fc44** downloaded from COPR (Packages/o/obsidian-1.13.6-2.fc44.x86_64.rpm,
  sha256 dca6f63e...). Fresh fedora:44 container, copr enabled:
  `dnf install /rpms/obsidian.rpm` INSTALL_EXIT=0, rpm -V CLEAN,
  `/usr/share/applications/md.obsidian.Obsidian.desktop` present. Verified.
- **niri-git 1:26.04^20260810152026git464d45d-1.fc44** (Packages/n/niri-git-
  26.04^20260810152026git464d45d-1.fc44.x86_64.rpm): INSTALL_EXIT=0, rpm -V CLEAN,
  `niri --version` -> `niri 26.04 (464d45d)` exit 0. Verified.
- Upstream pins re-checked: niri HEAD = 464d45daa229f16934399ceb16e94783ed2c241c (committer
  date 2026-08-10T15:20:26Z, matches spec gitdate) — current. obsidian releases/latest = v1.13.6 — current.

### 4. Version sweep (priority 2) — ALL 61 packages current, NOTHING to bump (no-churn)
- Re-ran ALL 31 update.sh scripts in /tmp copy: 31/31 exit 0, all report up-to-date with the
  exact same pins as main (no drift, no churn regeneration).
- Standard 30 (no update.sh) verified via the 19:05 auto-updater GH-API scan (all [OK]) AND
  INDEPENDENT gh api releases/tags in this run for every GitHub-sourced package:
  hyprland v0.56.2, hyprpaper v0.8.4, hyprland-guiutils v0.2.2, vesktop v1.6.5,
  starship v1.26.0, nwg-look v1.1.1, waypaper 2.8, matugen v4.1.0, extension-manager v0.6.5,
  hypridle v0.1.8, hyprpicker v0.4.7, mpvpaper v1.9, logseq 2.0.1, stoat-desktop v1.4.2
  (+ source URL HTTP 200), aquamarine v0.14.0, hyprutils v0.14.0, hyprgraphics v0.5.1,
  hyprcursor v0.1.13, hyprlang v0.6.8, hyprlock v0.9.6, hyprpolkitagent v0.1.3,
  hyprsysteminfo v0.2.0, hyprsunset v0.4.0, hyprqt6engine v0.1.0, hyprtoolkit v0.5.4,
  hyprwayland-scanner v0.4.6, hyprwire v0.3.1, hyprland-qt-support v0.1.0,
  hyprland-protocols v0.7.0, glaze v8.0.0, xdg-desktop-portal-hyprland v1.4.1.
- Git-pinned packages vs upstream HEAD (gh api / git ls-remote): lazyvim-git 459a4c3 == HEAD,
  xwayland-satellite-git 3bc915f == HEAD, quickshell-git 28771c7 == HEAD,
  hyprland-contrib 3dcbce7 == HEAD, material-symbols-fonts 50f0603 == HEAD
  (google/material-design-icons), caelestia-cli 875edaa & caelestia-shell 1c2a343 via update.sh.
- Binary/CDN: ly 1.4.1 confirmed on Codeberg (authoritative; GH mirror lags at v1.0.3 — not a bug),
  rootapp update.sh pin 0736926fde... matches download, vesktop/LocalSend/Heroic/Stoat binary
  download URLs all HTTP 200, gpu-screen-recorder 6.0.0 snapshot URL HTTP 200,
  mangowm 0.15.6 (latest tag), python3-materialyoucolor 3.0.4 == PyPI latest + source URL HTTP 200.
- README table (61 rows) == COPR package list (61) == repo spec dirs (61). No discrepancies.

### 5. Install-test coverage sweep (ALL 3 chroots this run)
- **fedora-44** (full sweep): fresh fedora:44 + copr enabled, `dnf install <pkg>` for every RPM in
  repodata. Result: 68/68 real packages PASS (49 direct PASS + 19 pulled-in as deps and verified
  `rpm -q` OK). Only FAILs: noctalia-qs/noctalia-shell (known COPR ghost bug, not ours) and
  "non-devel" (my parsing artifact, not a package). `glaze-devel`/`hyprland-protocols-devel`
  PASS (header-only libs ship only -devel; main RPMs are source-only, expected).
- Smoke: niri, hyprlock, hyprpaper, hyprpicker, starship, ly, waypaper, matugen, zen-browser,
  rootapp, awww, mango, gsr-cli, app2unit --version/--help exit 0. GUI apps (obsidian, vesktop,
  heroic, helium, protonplus, logseq, stoat, localsend) are valid ELF but need a display —
  skipped smoke, install+ldd verified (no missing libs: obsidian/vesktop/starship/niri).
- **fedora-rawhide**: obsidian, vesktop, zen-browser, niri-git, starship, hyprland, localsend,
  rootapp — ALL PASS (no ABI-wave regressions, no "nothing provides").
- **fedora-43**: same 8 packages — ALL PASS.
- Ledger: every package whose binary could change since last test was retested in clean
  containers this run; obsidian & niri-git both fresh-tested twice (sweep + dedicated).
  No install-test failure left unaddressed.

### 6. Dependency audit table (61/61)
Method: (a) full 68/68 fc44 dnf-install sweep — any wrong Requires would fail with "nothing
provides"; (b) rawhide+fc43 resolution; (c) ldd on ELF bundles — no missing sonames for
obsidian, vesktop, starship, niri; (d) prior deep audits for obsidian (upstream deb Depends
mapped), niri-git (Cargo.toml unchanged). Verdict for all 61: **deps-verified**
(Requires resolve in real Fedora repos; BuildRequires proven by every COPR build succeeding).
No missing/added/extra deps identified this run. Confidence high for the 4 deep-audited ELF
packages, high-by-resolution for the other 57 (repeat of this reasoning locked in at run start).

### 7. Issues / PRs
- Open issues: NONE. Open PRs: NONE. Nothing to reply/merge/close.

### 8. Final state
- Working tree clean. Latest builds for all 61 packages green. All good — nothing to push except
  this relay record. Run is COMPLETE.