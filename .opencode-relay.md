# fedora-nexus autonomous-run relay state

status: complete
run_id: 32405820903

## Run 32405820903 (2026-08-20 — scheduled run, autonomous maintenance)

Auto-Updater commit ed219a5 (18:51Z) bumped three packages: fluxer
2026.819.105810→2026.820.180738, freebuff 0.0.66→0.0.67, noctalia-git
02286bb→a064c06. This run tracked all three COPR builds to green, verified the
version-accuracy sweep on all 82 packages, install-tested the changed binaries
on all 4 chroots, and recorded the deep dependency audit. No fixes were
required this run — every package was already correct and current.

### 1. COPR build status — all 82 packages green

- fluxer 2026.820.180738-1, build **10885722**: **succeeded** on fc43/44/45/rawhide.
  Version from the authoritative CDN header `X-Fluxer-Version: 2026.820.180738`
  (curl -sI https://api.fluxer.app/dl/desktop/stable/linux/x64/latest/rpm).
- freebuff 0.0.67-1, build **10885723**: **succeeded** on fc43/44/45/rawhide.
  `X-AppImage-Version=0.0.67` inside the torn-open AppImage matches spec.
- noctalia-git 5.0.0^20260820140516gita064c06-1, build **10885724**:
  **succeeded** on fc43/44/45/rawhide (took ~40 min — large C++ project).
  Spec commit a064c06 == upstream HEAD (git ls-remote); gitdate 20260820140516
  == upstream committer date 2026-08-20T14:05:16Z.
- All other 79 packages have a latest build with state=succeeded (verified via
  COPR API per-package). The 108762xx (2026-08-18 SRPM-import outage) and
  108771xx (queue halt) waves are fully superseded by newer succeeded builds.

### 2. Version accuracy sweep — 82/82 verified, 0 stale

Four parallel research agents re-verified every package against upstream
(`gh api` releases/tags, `git ls-remote` HEAD for -git, `curl -sI` Source0,
artifact tear-downs). All 82 up-to-date. I independently re-verified every
-git commit against upstream HEAD (all match) and curl-checked Source0 URLs
(all 200, including ghostty's mkasberg ppa deb after macro substitution).

Three agent flags were investigated and dismissed as FALSE POSITIVES:
- fluxer "version should be 0.0.8": agent read the RPM's internal NVR metadata;
  the authoritative version for this CDN package IS the `X-Fluxer-Version`
  header (2026.820.180738), which matches the spec. Spec correct.
- freebuff "0.0.150 is latest": 0.0.150 is the *CLI* (freebuff-vX tags, npm
  install, tar.gz of a bare binary). The desktop app uses freebuff-desktop-vX
  tags; latest = 0.0.67 = spec. Asset URL for 0.0.67 returns 200.
- localsend "v1.18.1": v1.18.1 release ships ONLY Android APKs
  (gh api assets). Desktop binaries stopped at 1.18.0 — correctly held.

Noteworthy: hyprland-plugins intentionally pinned to hyprpm commit 00862ca for
hyprland 0.56.2 — do not bump (matches previous run's note).

### 3. Install-verification sweep — changed binaries tested on all chroots

All tests from fresh `docker run` containers against the live ackerman/nexus
COPR repo (gpgcheck=0, --disablerepo=fedora-cisco-openh264).
- fluxer: installs clean fc43/44/45/46(rawhide). rpm -V CLEAN. ldd 0
  unresolved. Smoke: Electron GUI, requires display/--no-sandbox — skipped
  (recorded, not a failure).
- freebuff: installs clean fc43/44/45/46. rpm -V CLEAN. ldd 0 unresolved.
- noctalia-git: installs clean fc43/44/45/46. rpm -V CLEAN. /usr/bin/noctalia
  present.
- FRESH-CHECKER pass: one extra clean-container run installing all three at
  once on fc44 — all install, rpm -V ALL CLEAN.
- Other 79 packages: unchanged binaries since their last install test (git
  specs == COPR built NVRs, verified by per-package comparison). Previous
  run's 100% coverage ledger still applies to them.

### 4. Deep dependency audit

| package | upstream deps found | in spec | status |
|---|---|---|---|
| fluxer (prebuilt) | RPM auto-requires: at-spi2-core, gtk3, libXScrnSaver, libnotify, libXtst, libuuid, nss, xdg-utils | identical list | deps-verified (high) |
| freebuff (prebuilt) | RPM auto-requires: zlib, nss, alsa-lib, gtk3, hicolor-icon-theme, at-spi2-core, libnotify, libdrm, mesa-libgbm, xdg-utils, libXScrnSaver, libXtst, libsecret, libappindicator-gtk3, /usr/bin/sh | identical | deps-verified (high) |
| noctalia-git (source) | ldd 0 unresolved; links system libs (pipewire, libjxl, libqalculate, libsecret, polkit, pam, libical, libsndfile, jemalloc, sdbus-c++, wireplumber, tomlplusplus, md4c, webp, xml2, cairo, pango, freetype, harfbuzz, librsvg, xkbcommon...) | BuildRequires list covers all linked sonames (verified by successful build + install on all 4 chroots) | deps-verified (high) |
| 79 remaining | unchanged since last audit; git spec == COPR built NVR (per-package comparison below) | no change | deps-verified (previous run's audit + this run's spec/build reconciliation) |

Spec↔COPR reconciliation: compared each package's git Version/commit against
its latest succeeded COPR build NVR — 78 exact matches, 4 macro/epoch
resolutions confirmed correct (ghostty %{ghostty_version}=1.3.1,
protonplus %{fileref}=0.6.4, xdg-desktop-portal-hyprland Epoch 1,
zen-browser Epoch 1). All 82 consistent.

Optimization discipline: no source-built package touched this run (no bumps,
no spec changes). Prebuilt repackages (fluxer, freebuff) are prebuilt —
nothing to optimize.

### 5. Docs / README health

- README accurate: 82-package table == 82 repo dirs == 82 COPR packages
  (verified counts). "Supported releases: Fedora 43, 44, 45 and Rawhide".
- COPR chroot list (fedora-rawhide-x86_64=F46, fedora-43/44/45-x86_64) matches
  reality. Fedora 45 branched 2026-08-11 (GA ~Oct 2026); F43 EOL ~1 month
  after F45 GA. No chroot to add or drop; no docs change needed.

### 6. Open issues / PRs

- No open issues, no open PRs. Nothing to triage.

### 7. GitHub Actions health / webhook

- Fedora Nexus Auto-Updater (32405346729, 18:50Z): success; its bumps produced
  builds 10885722/23/24 (all succeeded, tracked to green this run).
- Webhook API still 403 (token lacks admin scope). Verified empirically:
  auto-updater commit ed219a5 pushed 18:51:01Z → builds submitted 18:51:03Z
  → webhook alive and firing. All pushes this run followed the same path.
- opencode-schedule (this run): completing cleanly.

### 8. Handoff

- This run is complete: all 82 packages green; 3 auto-updated binaries
  (fluxer/freebuff/noctalia-git) tracked to succeeded builds and install-tested
  on all 4 chroots; no fixes needed; version sweep 82/82 up-to-date; repo in
  sync with origin (clean tree).
- No owner actions pending. (Cosmetic: COPR project description still omits
  Fedora 45 — owner may update via COPR web UI. Cosmetic changelog lags for
  vesktop 1.6.7 / stoat-desktop 1.5.3 / matugen 4.2.0 remain artifacts of the
  workflow's version-only scanner; harmless, builds already shipped.)