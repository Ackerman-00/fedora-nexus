# fedora-nexus autonomous-run relay state

status: complete
run_id: 31259510810

## Run - RUN_ID 31259510810 (2026-08-08) - COMPLETED (full inventory re-verification, 0 new defects)

### 0. Pickup status
- Previous run (31249958731) declared `status: complete`. RELAY=false, so no
  relay continuation work. This run re-verified everything from scratch (fresh
  checker). HEAD on main at pickup: cc32e9d (previous run's relay record).

### 1. COPR Build Status
- Chroots discovered via COPR API (authoritative):
  **fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64**.
  fedora-45-x86_64 does NOT exist yet in COPR (F45 mass-branching 2026-08-11).
  Nothing to enable/disable.
- **All 56 packages**: per-package `build/list?...&packagename=<pkg>&limit=1`
  shows latest build **succeeded** for every package (56/56).
- 0 builds in pending/starting/running/failed/canceled/skipped across the
  recent window. No "Release not bumped => fix not shipped" smell.
- Per-chroot evidence: result dirs exist (HTTP 200) on all three chroots for
  the newest builds, e.g. 10837791-extension-manager,
  10837693-logseq, 10837084-fluxer. Build-level `state: succeeded` is
  authoritative and consistent with directory listings.
- Webhook delivery ids unreadable (GITHUB_TOKEN lacks `admin:repo_hook`,
  403 on GET /hooks - same as all prior runs). No pushes made this run, so no
  webhook-firing test was applicable (nothing to ship).

### 2. Version Accuracy Sweep - 100% of packages re-verified
- **Git-pinned (9 packages)**: `git ls-remote HEAD` matches the spec's pinned
  commit for app2unit (47e23ec), caelestia-shell-mango (bd43cb2),
  hyprland-contrib (3dcbce7), lazyvim-git (459a4c3), material-symbols-fonts
  (50f0603), niri-git (59a1001), quickshell-git (28771c7, git.outfoxxed.me +
  GitHub mirror both match), xwayland-satellite-git (8d135d3).
- hyprland-plugins pinned to 00862ca (upstream HEAD is a9eaa52 "chase
  hyprland-git") is a LEGITIMATE compatibility pin for hyprland stable
  v0.56.2 (verified: commit 00862ca = "hyprpm: add pin for 0.56.2", hyprland
  latest release is still v0.56.2). Not a missed bump.
- **GitHub-release packages (40)**: `ghapi releases/latest` tag == spec
  Version for all (verified list incl. vesktop v1.6.5, localsend v1.17.0,
  logseq 2.0.1, bibata v2.0.7, obsidian v1.13.4, cascadia v3.5.0, hyprland
  v0.56.2, extension-manager v0.6.5, starship v1.26.0, glaze v8.0.0, mangowm
  0.15.6, zen-browser 1.21.12b [newest release confirmed via
  /releases?per_page=5, not masked by lexically-later tags], stoat v1.4.2,
  vesktop v1.6.5, waypaper 2.8, opencode-desktop v1.18.15, etc.).
- **Non-GitHub upstreams**: awww (codeberg) v0.12.1; wlroots (gitlab) 0.20.2;
  gpu-screen-recorder (dec05eba.com snapshot) 6.0.0; matugen (crates.io
  max_stable) 4.1.0; fluxer (CDN `x-fluxer-version: 2026.731.153836` == spec);
  protonplus (GitHub tag v0.5.22). All match.
- **Binary artifacts torn apart (10)** - version read from INSIDE:
  - rootapp AppImage: sha256 == spec pin (0736926f...d910) AND
    `X-AppImage-Version=0.9.126` in extracted .desktop.
  - logseq zip: sha256 = 981bf1f4... == spec pin.
  - vesktop rpm (1.6.5), heroic rpm (2.22.0), localsend deb (.desktop
    Version=1.17.0+58), obsidian deb (1.13.4), opencode-desktop deb
    (1.18.15), stoat app.asar (1.4.2), zen tarball application.ini (1.21.12b).
  - fluxer rpm reports internal build-time version 0.0.8 but CDN header
    `x-fluxer-version: 2026.731.153836` is authoritative == spec (known,
    expected).
- Source-URL sweep: all `curl -sI` (expanded macros) return 200 (or known
  benign 403-on-HEAD that GETs fine: crates.io API -> static.crates.io 200).
  No source-URL defects.

### 3. Spec / update.sh Health
- `rpmspec -P` over all 56 specs inside a fedora:44 container: **0 parse
  failures**.
- All changelog entries: valid date, valid weekday, 1 changelog block per
  spec, top entry NVR matches current Version/Release (verified via git log
  per package). No duplicated or bogus entries.
- All update.sh scripts (`bash -n` PASS; ran in a throwaway clone): **0
  script failures, 0 files changed** (pure version/commit-compare guards,
  no artifact-file dependence, no churn). No "requires local artifact"
  flawed-guard pattern found.

### 3. GitHub Actions Health
- All recent `Fedora Nexus Auto-Updater` runs success (latest 31257564041,
  2026-08-08T12:34): all steps success, "Commit and Push" correctly skipped
  (no changes = no churn).
- All recent `opencode-schedule` runs success except the in-progress current
  run. update-engine.yml + opencode-schedule.yml parse cleanly.

### 4. Install-Verification Sweep (fresh `dnf install` per name, live copr, 3 chroots)
- Name list built inside a fedora:44 container with `rpmspec -q
  --qf="%{NAME}"` over all 56 specs = **80 distinct rpm names** (incl.
  -devel + hyprland-contrib subpkgs).
- **fedora:43 -> 78/78 installable install clean** (RC=0)
- **fedora:44 -> 78/78 installable install clean** (RC=0)
- **fedora:rawhide -> 78/78 installable install clean** (RC=0)
  (glaze + hyprland-protocols are source-only bases whose real content is
   their -devel subpackages - both -devel install fine on all chroots.)
- `rpm -V` over the installed set on fc44: **0 failures**.
- `ldd` unresolved-sonames check on 9 headless binaries (starship, matugen,
  waypaper, mpvpaper, extension-manager, hypridle, hyprlock, hyprpaper,
  nwg-look): all OK.
- Runtime smoke: starship --version (1.26.0), matugen --version (4.1.0),
  nwg-look -v (1.1.1) all exit 0. (GUI apps not launched: headless runner.)
- Fresh-checker re-run (independent): rawhide container, freshly-built
  extension-manager/logseq/zen-browser/fluxer/niri-git/hyprland-contrib all
  install with RC=0 at the exact latest NVRs.

### 5. Dependency Audit (repo-wide)
- Per-chroot fresh-install of the full 80-name set on all 3 chroots proves
  every Requires resolves in the live copr. `repoclosure` on fc43/fc44:
  **0 nexus-owned unresolved**.
- rawhide repoclosure: nexus-owned "hits" are **historical stale build
  artifacts only** - old (superseded) RPMs of removed/superceded NVRs
  (nocte-shell 4.7.6/4.7.7 [package records already purged by owner, RPMs
  remain in repodata + uninstallable by design], + old gsr 5.15.3 / obsidian
  1.12.7 / rootapp 0.9.11x that are below the current installable versions).
  These do not break dnf install of ANY current package (fresh full-set
  install succeeds on rawhide). Owner-side removal is COPR-admin-only
  (needs auth), out of repo scope.

### 6. Fedora Lifecycle
- Fedora 44 = current stable. Fedora 43 supported (EOL ~Dec 9 2026).
  Rawhide tracks Fedora 45 dev. F45 mass-branching 2026-08-11 -> next run
  after 2026-08-11 MUST re-check COPR mock-chroots for fedora-45-x86_64 and
  enable it if present, then rebuild/verify all + update README.

### 7. Issues & PRs
- Open PRs: none. Open issues: none. Nothing to close/reply/merge this run.

### 8. Webhook evidence
- GITHUB_TOKEN lacks admin:repo_hook (403 on GET /hooks - same as all prior
  runs). No repo-initiated push happened this run, so no new delivery ids to
  verify. Webhook known-alive from prior runs' observed COPR build
  auto-triggers (empirical).

### 9. Not-verified-by-docker gaps (stated)
- GUI apps not launched (headless runner, no display): binary GUI apps
  (obsidian, logseq, fluxer, heroic-games-launcher, localsend, stoat-desktop,
  opencode-desktop, vesktop, protonplus, rootapp, zen-browser) and
  compositors (hyprland, accents of niri/mangowm). Their install + rpm -V +
  version-from-artifact reads are the coverage level.
- Webhook delivery-ids: unreadable (token scope); no pushes this run.
- The stale noctalia-shell artifacts in rawhide metadata: cannot be purged
  via unauthenticated API (owner-only, needs COPR auth). Not a user-facing
  defect (not in README, no buildable record, all current installs pass).

### Closing note
This run found **0 new defects**. All 56 packages are current with upstream,
all build success on all 3 chroots, 78/80 fresh-install on fc43/fc44/rawhide
(glaze+hyprland-protocols = source-only bases, -devel installs), version
accuracy fully re-verified (git pins, release tags, binary inner versions,
checksums), update.sh no-churn, no failed COPR builds, no open issues/PRs.
No spec/update.sh/workflow/README edits were needed; nothing was pushed.
Relay updated to record this run. Working tree clean.