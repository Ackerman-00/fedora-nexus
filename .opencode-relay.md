# fedora-nexus autonomous-run relay state

status: complete
run_id: 32001450088

## Run 32001450088 (2026-08-17T06:23Z — scheduled run, INDEPENDENT FRESH-CHECKER PASS, RELAY=false)

Full independent re-verification from scratch this session. Repo is HEALTHY. Nothing
is broken, stale, or outdated. **NO-CHURN: zero package spec commits** (the CORRECT
outcome — nothing to fix). The only commits this run: the relay record itself.
Git identity verified: `opencode-agent[bot] <41898282+opencode-agent[bot]@users.noreply.github.com>`.

### 0. Pickup & chroots (DISCOVERED, not hardcoded)
- run_id = 32001450088 (this run). Previous relay (31986197407) was `complete`; no unfinished relay work.
- COPR chroots (API `chroot_repos`, authoritative): `fedora-rawhide-x86_64`, `fedora-44-x86_64`,
  `fedora-43-x86_64` ONLY — matches README and the supported set.
- Fedora lifecycle (websearch this session, docs.fedoraproject.org / f-45 schedule): F44 stable
  (GA 2026-04-28), F43 supported (EOL ~1 month after F45 GA, i.e. ~Dec 2026), F45 BRANCHED
  2026-08-11 with GA target 2026-10-20 (NOT yet released), Rawhide = 46. Enabled chroots ==
  supported set; no README/chroot change needed.
- Open issues: 0. Open PRs: 0.

### 1. Failed COPR builds: NONE actionable
- Last 200 builds: 192 succeeded, 7 failed, 1 canceled; ZERO pending/starting/running.
- The 7 "failed" + 1 "canceled" are ALL superseded or COPR background transients (verified
  per-package build lists this session):
  - wlroots #10869642: `is_background=true, submitter=null, version=null` (COPR background
    source-fetch transient, same as prior runs). Real published wlroots = #10868199 (0.20.2-3,
    == current spec) succeeded on all 3 chroots.
  - hyprtoolkit #10868178 (0.5.4-2) superseded by #10868285 (0.5.4-3) SUCCEEDED.
  - hyprsysteminfo #10868177 (0.2.0-4) superseded by #10868289 (0.2.0-5) SUCCEEDED.
  - hyprpaper #10868172 (0.8.4-4) superseded by #10868291 (0.8.4-5) SUCCEEDED.
  - localsend #10844795, obsidian #10843364, protonplus #10841179, ly #10840572: background
    transients; each has a newer SUCCEEDED build (localsend 1.18.0-2, obsidian 1.13.7-1,
    protonplus 0.6.4-1, ly 1.4.1-2).
- Per-package LATEST build (authoritative): 62/63 = succeeded; the only non-succeeded latest is
  wlroots #10869642 (background transient, real published build 0.20.2-3 succeeded).
- **ADVERSARIAL RECONCILIATION (spec vs published)**: for every one of the 63 COPR packages,
  compared the latest git commit timestamp touching the package dir against the latest
  SUCCEEDED COPR build's submitted_on → **STALE = 0**. No spec is newer than its published
  build; every build was produced from the current spec.

### 2. Version accuracy sweep — 65/65 CURRENT (zero spec diffs)
- Fresh shallow clone `/tmp/fnx-run` @ HEAD 63663dd, then unshallowed the main repo for history.
- ALL 33 `update.sh` run in their dirs (as the workflow does): **33/33 exit 0**,
  `git status --porcelain` EMPTY after (zero spec diffs — no churn). No-churn guards audited
  (rootapp, obsidian, fluxer, freebuff, localsend): every guard compares a PURE version/commit/
  checksum string to the spec BEFORE touching files; ZERO scripts gate on local artifact
  existence. rootapp's guard = pure sha256 of the fresh-downloaded AppImage vs spec pin.
- Replicated the workflow GH-release scanner (authenticated `gh api`/requests, 32 non-scripted specs):
  **31/31 GitHub-sourced MATCH** `releases/latest` (Hyprland 0.56.2 family, glaze 8.0.0 @
  stephenberry/glaze, starship 1.26.0, stoat-desktop 1.5.1, vesktop 1.6.5, waypaper 2.8,
  mpvpaper 1.9, nwg-look 1.1.1, xdg-desktop-portal-hyprland 1.4.1, aquamarine 0.14.0,
  hyprlang 0.6.8, hyprland-qt-support 0.1.0, hyprqt6engine 0.1.0, matugen 4.1.0, etc.).
  The 32nd (python3-materialyoucolor) is PyPI-sourced and verified separately.
- `python3-materialyoucolor`: 3.0.4 == PyPI JSON API latest (3.0.4, uploaded 2026-08-05). MATCH.
- Codeberg: awww v0.12.1 (LGFae/awww), ly v1.4.1 == latest release API tags. MATCH specs.
- wlroots: `git ls-remote --tags` gitlab.freedesktop.org → newest tag 0.20.2 == spec 0.20.2. MATCH.
- mangowm: 0.16.1 == mangowm/mango releases/latest. MATCH.
- Git/-snapshot pins vs live `git ls-remote HEAD` (full-SHA compare): niri 6062844==HEAD,
  xwayland-satellite 3bc915f==HEAD, lazyvim 459a4c3==HEAD, hyprland-contrib 3dcbce7==HEAD,
  app2unit 47e23ec==HEAD, caelestia-cli-mango be54856==HEAD, caelestia-shell-mango
  cae55ed==HEAD, material-symbols e083cc6==HEAD, quickshell 532a060==HEAD (git.outfoxxed.me),
  hyprland-plugins 00862ca = intentional 0.56.2 hyprpm pin (changelog-documented). All current.
- `localsend`: HELD at 1.18.0 CORRECT — v1.18.1 release assets verified via GH API = NO
  linux-x86-64.deb (only Android/flatpak/macos/win zips); update.sh asset-guard skips tags
  without a linux .deb.
- `freebuff`: 0.0.63 == latest `freebuff-desktop-v*` tag (0.0.63; newer tags are the Codebuff
  CLI `v1.0.x` and `freebuff-v0.0.14x`, correctly filtered out by TAG_PREFIX="freebuff-desktop-v").
- `rootapp`: TEARED THE APPIMAGE this session (ELF section-header offset 944632, unsquashfs OK):
  `X-AppImage-Version=0.9.126` in Root.desktop == spec Version 0.9.126. Fresh-download sha256
  `0736926fde923819d276ac6d4a3afe65c6d87c734689af47276fc4ebf075d910` == spec `# sha256:` pin
  EXACTLY (worked-example protocol). Source0 200.
- `fluxer`: `curl -sI https://api.fluxer.app/dl/desktop/stable/linux/x64/latest/rpm` →
  `x-fluxer-version: 2026.816.204155` == spec Version. MATCH.
- `ghostty`: mkasberg/ghostty-ubuntu releases/latest = `1.3.1-0-ppa2` == spec ppatag/debver. MATCH.
- Spot-checked update.sh guard OUTPUT (not just exit code): obsidian 1.13.7, zen-browser
  1.21.14b, heroic 2.22.1, gpu-screen-recorder 6.0.0, logseq 2.0.1, helium 0.15.5.1,
  opencode-desktop 1.18.18, bibata 2.0.7, cascadia 3.5.0, extension-manager 0.6.5, libcava
  1.0.0, cliphist 0.7.0, scenefx 0.5, mangowm 0.16.1 — all report current.
- Source0 reachability (curl -sI): rootapp AppImage 200, ghostty .deb release 200,
  fluxer rpm endpoint 200, localsend/freebuff .deb/AppImage assets 200.
- FULL TABLE (65/65): see §6.
- Cross-repo consistency: not authoritative; upstream was the arbiter throughout (STEP B/D).

### 3. Install-test sweep — FRESH containers, LIVE COPR repo (this session)
- **fc44 FULL 69-package install** (`registry.fedoraproject.org/fedora:44`, fresh): the 63 COPR
  packages (glaze→`glaze-devel`, hyprland-protocols→`hyprland-protocols-devel`,
  hyprwayland-scanner→`hyprwayland-scanner-devel`; hyprlauncher/hyprshutdown excluded — NOT in
  COPR, owner registration pending) + all 6 hyprland-contrib subpackages (grimblast/hdrop/
  hyprprop/scratchpad/shellevents/try_swap_workspace) in ONE transaction →
  **INSTALL_EXIT=0** ("Complete!"), all 69 requested packages present in the log, no
  "nothing provides"/"No match" errors.
- **Smoke (binary executes + prints version)** — fresh fc44: starship 1.26.0, matugen 4.1.0,
  niri 26.04 (6062844), ly --help works, hyprwayland-scanner --version = 0.4.6. All green.
- **Changed binaries on ALL 3 chroots** (fluxer 2026.816.204155-1 + stoat-desktop 1.5.1-2 —
  the only builds newer than the prior relay's tests, both 2026-08-17T01:45Z): downloaded from
  the live COPR repo in fresh fc44/fc43/fc46-rawhide containers → install EXIT=0, `rpm -V`
  clean on all three, `ldd` 0 missing on both ELFs. FRESH-CHECKER re-run (independent second
  container) re-installed both on fc44 green.
- **No other binary changed** since the prior relay's 3-chroot tests (verified against COPR
  build list — newest builds are exactly fluxer/stoat from 01:45Z; nothing newer exists).
- hyprlauncher / hyprshutdown: directories exist + version-verified in repo (0.1.6 / 0.1.1,
  GH releases/latest MATCH) but NOT in COPR (pending owner registration — README caveat).
  Cannot install from repo; not actionable by CI.

### 4. Actions health & webhook
- Auto-Updater ("Fedora Nexus Auto-Updater"): runs 31985865470, 31965151562, 31948219700,
  31932425443, 31920332365, 31901665013 all **success** (6 most recent).
- opencode-schedule: prior relay runs 31986197407/31965382968/31961256492 success; historical
  2026-08-16 failures (31954211311 etc.) were the gate-check quirk already fixed by commit
  c4ee3f8. Current run 32001450088 is the only in_progress.
- Webhook: `gh api repos/.../hooks` 403 (token lacks admin:repo_hooks — NAMED GAP, cannot read
  delivery IDs). Empirical proof instead: commit 7699278 (auto-updater fluxer+stoat bump)
  pushed 2026-08-17T01:45:42Z → COPR builds #10870886 (fluxer) + #10870887 (stoat-desktop)
  submitted 01:45:44Z (+2s) and SUCCEEDED on all 3 chroots. The push→build path is alive.
  No package push by THIS run (NO-CHURN), so no new push→build pair from this run; the
  auto-updater pair above is the evidence.

### 5. Dependency audit (confidence: high)
- No Version/commit changed since prior deep audits — all 65 re-verified CURRENT this session (§2).
- Full install-resolve on the live repo (fc44, §3) resolves EVERY Requires from the real
  Fedora+COPR repos (no "nothing provides"); `rpm -V` clean; `ldd` zero-missing on the
  changed binaries; smoke binaries execute and print expected versions.
- Prebuilt binaries (rootapp/obsidian/zen/vesktop/heroic/localsend/logseq/helium/fluxer/
  stoat-desktop/opencode-desktop/protonplus/gpu-screen-recorder/ghostty/waypaper/mpvpaper/
  starship/etc.): prebuilt, nothing to optimize — immutable ELFs, `%{optflags}` preserved via
  Fedora repackaging specs; `%global debug_package %{nil}` justified only here (prebuilt binaries).
  Source-built C/C++/Rust/Qt pkgs: built with distro %{optflags}+LTO (Fedora defaults), no
  -march=native/-O3 regressions (unchanged specs, previously audited).
- Status: deps-verified (63/63 COPR packages installable+install-tested on fc44 this session
  in one fresh transaction; changed binaries on all 3 chroots; hyprlauncher/hyprshutdown not
  in COPR yet).
- AUDIT TABLE (this session's fresh-checker, no upstream changed → RE-TEAR RULE satisfied):
  | package | upstream deps found (this rebuild) | in spec | missing/added | extra/dropped | status |
  |---|---|---|---|---|---|
  | aquamarine | wayland, cairo, pango, libdrm, egl, glesv2 (meson) | == | none | none | deps-verified |
  | mangowm | libdrm,xcb-icccm,wayland-server,wlroots-0.20,xkbcommon,libinput,scenefx,pixman,libcjson,pangocairo | == | none | none | deps-verified |
  | starship | self-contained static Rust | (none) | none | none | deps-verified (prebuilt static) |
  | wlroots | meson: wayland, xcb, libinput, libseat, cairo, pango, pixman, libdrm, ... (0.20.2) | == | none | none | deps-verified |
  | rootapp | .desktop X-AppImage-Version=0.9.126, bundled DotNetBrowser (repackaged AppImage, RE-TORN this run) | == | none | none | deps-verified (prebuilt) |
  | vesktop / obsidian / zen / heroic / localsend / logseq / helium / freebuff / opencode-desktop / ghostty | prebuilt/Electron/repackaged | == | none | none | deps-verified (prebuilt) |
  | fluxer | Electron repackage; auto sonames + explicit libXScrnSaver/libXtst/libuuid/nss/xdg-utils/at-spi2/gtk3/libnotify | == | none | none | deps-verified (prebuilt, install-tested all 3 chroots this run) |
  | stoat-desktop | Electron repackage; gtk3/nss/at-spi2/cups/dbus/systemd/libX*/libxcb/libxkbcommon/gbm/alsa/libsecret/libappindicator/xdg-utils | == | none | none | deps-verified (prebuilt, install-tested all 3 chroots this run) |
  | hyprland-contrib subpkgs (grimblast/hyprop/scratchpad/shellevents/try_swap_workspace/hdrop) | hyprctl, jq, slurp, grim, wl-clipboard, notify-send, procps-ng, socat, psmisc | == | none | none | deps-verified (all 6 installed this run) |
  | niri-git / xwayland-satellite-git / quickshell-git / lazyvim-git / app2unit / caelestia-* / material-symbols / hyprland-plugins / hyprland-contrib | git-pinned; deps == upstream source tree | == | none | none | deps-verified |
  (remaining packages: deps-verified by full fc44 live-repo install RESOLVE — every Requires
  satisfied, rpm -V clean. No manual additions needed.)

### 6. Adversarial audit (2-3 concrete attacks, each checked with a command this session)
1. "The current spec is NEWER than what COPR published (stale published packages)" — for ALL
   63 COPR packages compared last spec-commit timestamp vs latest SUCCEEDED build submitted_on:
   STALE = 0. Every build was produced from the current spec. Repelled.
2. "A flawed update.sh guard returns 0 while upstream has a newer version" — audited the guard
   logic directly (rootapp=sha256-compare, obsidian=ls-remote tag vs spec + .deb-existence
   check, fluxer=X-Fluxer-Version header, freebuff=tag-prefix filter, localsend=asset guard),
   ran all 33 with zero diffs, AND independently confirmed the versions via GH API / PyPI /
   Codeberg / GitLab / PPA / AppImage-tear / header. Repelled.
3. "The wlroots failed build = a real defect needing a fix" — #10868199 (0.20.2-3) SUCCEEDED
   on all 3 chroots and matches the current spec (Release 3); #10869642 is `is_background=true,
   version=null, submitter=null` (COPR background source-fetch transient). Repelled.
4. "Re-repelling 'the latest versions were wrong/stale'" — re-queried ALL sources LIVE now:
   33/33 update.sh exit 0 + zero diff; 31/31 GH releases/latest match; materialyoucolor 3.0.4
   (PyPI JSON); awww 0.12.1 + ly 1.4.1 (Codeberg API); wlroots 0.20.2 (GitLab ls-remote);
   rootapp AppImage torn → 0.9.126 + sha256 == pin; fluxer header 2026.816.204155;
   localsend held at 1.18.0 (v1.18.1 no linux .deb); ghostty PPA 1.3.1-0-ppa2. Repelled.
5. "Nothing done = run failed / skipped its job" — produced independent hard EVIDENCE this
   session: 33 update.sh exit codes + zero diffs; 31 GH-release matches; 65-row version table
   all `current`; STALE=0 reconciliation over all 63 COPR packages; full 69-package fresh
   fc44 install EXIT=0 ("Complete!"); changed binaries install green on fc44+fc43+rawhide with
   rpm -V clean + ldd 0 missing; smoke tests pass; 0 open issues/PRs; 0 pending/starting/
   running builds; 0 actionable failures.
   NO spec commits IS the correct NO-CHURN outcome, not a gap.

### 7. Final state & honest gaps
- This run's only commit: the relay record (status complete + run_id 32001450088). Working tree
  clean (`git status --porcelain` empty). NO package commits — nothing broken/stale/outdated (NO-CHURN).
- Honest gaps (named, not silent):
  (1) GUI apps / Wayland compositors verified via install + `rpm -V` + `ldd` + `--version`/
      `--help` smoke only — cannot launch a graphical compositor/desktop client headless in CI
      (no Wayland socket / display server). Electron apps only fail on container-as-root
      `--no-sandbox` and missing display (not packaging bugs).
  (2) Full 69-package install-test ran on fc44 this session; changed binaries (fluxer/stoat)
      on ALL 3 chroots. fc43 + rawhide full-set installs were done by the prior relay
      (31986197407) and nothing changed since (STALE=0, no newer builds) — not re-run in full
      this session to avoid re-downloading several GB for zero binary delta.
  (3) COPR webhook/config API read 403 (GITHUB_TOKEN lacks admin:repo_hooks) — no package push
      this run so no new push→build pair from THIS run; webhook alive per §4 empirical
      correlation (auto-updater push 01:45:42Z → builds submitted 01:45:44Z, succeeded).
  (4) hyprlauncher / hyprshutdown: version-verified in repo but NOT in COPR (pending owner
      registration — README caveat); not installable from the repo. Not fixable from CI.
  (5) rawhide install required `--disablerepo=fedora-cisco-openh264` in the prior relay; this
      session's rawhide spot-checks used the same disablerepo (Fedora openh264 OpenPGP infra
      bug, unrelated to nexus).
  (6) Dependency audit: deep-re-torn rootapp this session; fluxer/stoat install-tested with
      rpm -V + ldd on all 3 chroots; full install-RESOLVE for every package on fc44. A full
      manual file-by-file re-tear of all 65 packages was not repeated because NO package
      changed upstream since the prior deep audits (all versions/commits verified unchanged
      this session, §2) — RE-TEAR RULE satisfied.

---

## Version-accuracy table (65/65) — all `current`
| package | spec Name | packaged | upstream latest | status |
|---|---|---|---|---|
| app2unit | app2unit | 1.4.4^git47e23ec | git 47e23ec6ab == HEAD | current |
| aquamarine | aquamarine | 0.14.0 | 0.14.0 | current |
| awww | aww | 0.12.1 | 0.12.1 (Codeberg) | current |
| bibata-cursor-theme | bibata-cursor-theme | 2.0.7 | 2.0.7 | current |
| caelestia-cli-mango | caelestia-cli-mango | ^gitbe54856 | git be548562 == HEAD | current |
| caelestia-shell-mango | caelestia-shell-mango | 2.1.0^gitcae55ed | git cae55ed689 == HEAD | current |
| cascadia-code-nerd-fonts | cascadia-code-nerd-fonts | 3.5.0 | 3.5.0 | current |
| cliphist | cliphist | 0.7.0 | 0.7.0 | current |
| extension-manager | extension-manager | 0.6.5 | 0.6.5 | current |
| fluxer | fluxer | 2026.816.204155 | X-Fluxer-Version 2026.816.204155 | current |
| freebuff | freebuff | 0.0.63 | 0.0.63 (freebuff-desktop-v filter) | current |
| ghostty | ghostty | 1.3.1-0-ppa2 | 1.3.1-0-ppa2 (PPA) | current |
| glaze | glaze | 8.0.0 | 8.0.0 | current |
| gpu-screen-recorder | gpu-screen-recorder | 6.0.0 | 6.0.0 | current |
| helium-browser | helium-browser | 0.15.5.1 | 0.15.5.1 (GH releases) | current |
| heroic-games-launcher | heroic-games-launcher | 2.22.1 | 2.22.1 | current |
| hyprcursor | hyprcursor | 0.1.13 | 0.1.13 | current |
| hyprgraphics | hyprgraphics | 0.5.1 | 0.5.1 | current |
| hypridle | hypridle | 0.1.8 | 0.1.8 | current |
| hyprland | hyprland | 0.56.2 | 0.56.2 | current |
| hyprland-contrib | hyprland-contrib | ^git3dcbce7 | git 3dcbce715 == HEAD | current |
| hyprland-guiutils | hyprland-guiutils | 0.2.2 | 0.2.2 | current |
| hyprland-plugins | hyprland-plugins | 0.1^git00862ca | intl. 0.56.2 hyprpm pin (00862ca) | current |
| hyprland-protocols | hyprland-protocols | 0.7.0 | 0.7.0 | current |
| hyprland-qt-support | hyprland-qt-support | 0.1.0 | 0.1.0 | current |
| hyprlang | hyprlang | 0.6.8 | 0.6.8 | current |
| hyprlauncher | hyprlauncher | 0.1.6 | 0.1.6 | current (not yet in COPR) |
| hyprlock | hyprlock | 0.9.6 | 0.9.6 | current |
| hyprpaper | hyprpaper | 0.8.4 | 0.8.4 | current |
| hyprpicker | hyprpicker | 0.4.7 | 0.4.7 | current |
| hyprpolkitagent | hyprpolkitagent | 0.1.3 | 0.1.3 | current |
| hyprqt6engine | hyprqt6engine | 0.1.0 | 0.1.0 | current |
| hyprshutdown | hyprshutdown | 0.1.1 | 0.1.1 | current (not yet in COPR) |
| hyprsunset | hyprsunset | 0.4.0 | 0.4.0 | current |
| hyprsysteminfo | hyprsysteminfo | 0.2.0 | 0.2.0 | current |
| hyprtoolkit | hyprtoolkit | 0.5.4 | 0.5.4 | current |
| hyprutils | hyprutils | 0.14.1 | 0.14.1 | current |
| hyprwayland-scanner | hyprwayland-scanner | 0.4.6 | 0.4.6 | current |
| hyprwire | hyprwire | 0.3.1 | 0.3.1 | current |
| lazyvim-git | lazyvim-git | 0.1^git459a4c3 | git 459a4c3b10 == HEAD | current |
| libcava | libcava | 1.0.0 | 1.0.0 (GitHub) | current |
| localsend | localsend | 1.18.0 | 1.18.0 (v1.18.1=no linux .deb asset) | current |
| logseq | logseq | 2.0.1 | 2.0.1 | current |
| ly | ly | 1.4.1 | 1.4.1 (Codeberg) | current |
| mangowm | mangowm | 0.16.1 | 0.16.1 | current |
| material-symbols-fonts | material-symbols-fonts | 4.0.0^gite083cc6 | git e083cc60 == HEAD | current |
| materialyoucolor | python3-materialyoucolor | 3.0.4 | 3.0.4 (PyPI) | current |
| matugen | matugen | 4.1.0 | 4.1.0 (crates.io/GH) | current |
| mpvpaper | mpvpaper | 1.9 | 1.9 | current |
| niri-git | niri-git | 26.04^git6062844 | git 606284464 == HEAD | current |
| nwg-look | nwg-look | 1.1.1 | 1.1.1 | current |
| obsidian | obsidian | 1.13.7 | 1.13.7 | current |
| opencode-desktop | opencode-desktop | 1.18.18 | 1.18.18 | current |
| protonplus | protonplus | 0.6.4 | 0.6.4 (GH releases) | current |
| quickshell-git | quickshell-git | 0.3.0^git532a060 | git 532a0606c == HEAD | current |
| rootapp | rootapp | 0.9.126 | 0.9.126 (AppImage-internal) | current |
| scenefx | scenefx | 0.5 | 0.5 | current |
| starship | starship | 1.26.0 | 1.26.0 | current |
| stoat-desktop | stoat-desktop | 1.5.1 | 1.5.1 | current |
| vesktop | vesktop | 1.6.5 | 1.6.5 | current |
| waypaper | waypaper | 2.8 | 2.8 | current |
| wlroots | wlroots | 0.20.2 | 0.20.2 (GitLab tags) | current |
| xdg-desktop-portal-hyprland | xdg-desktop-portal-hyprland | 1.4.1 | 1.4.1 | current |
| xwayland-satellite-git | xwayland-satellite-git | 0.8.2^git3bc915f | git 3bc915f09 == HEAD | current |
| zen-browser | zen-browser | 1.21.14b | 1.21.14b | current |

## Install-test ledger (this session, fresh containers, LIVE COPR repo)
| package(s) | chroot | dnf install | smoke |
|---|---|---|---|
| all 63 COPR packages (glaze→devel, hyprland-protocols→devel, hyprwayland-scanner→devel) + all 6 hyprland-contrib subpkgs | fc44 | INSTALL_EXIT=0 ("Complete!"), all 69 present | starship/matugen/niri/ly/hyprwayland-scanner green |
| fluxer 2026.816.204155-1 + stoat-desktop 1.5.1-2 (live COPR RPMs) | fc44 | INSTALL_EXIT=0, rpm -V clean | ldd 0 missing |
| fluxer + stoat-desktop (live COPR RPMs) | fc43 | INSTALL_EXIT=0, rpm -V clean | — |
| fluxer + stoat-desktop (live COPR RPMs, --disablerepo=fedora-cisco-openh264) | rawhide (fc46) | INSTALL_EXIT=0, rpm -V clean | — |
| FRESH-CHECKER re-run: fluxer + stoat-desktop | fc44 | INSTALL_EXIT=0, rpm -V clean, ldd 0 missing | — |

## Pre-completion gate (PASS evidence)
1. `git status --porcelain` empty → PASS (verified clean after relay commit).
2. Packages touched this run: NONE (NO-CHURN) → vacuously satisfied; the 2 packages whose
   binary changed since the prior relay's test (fluxer, stoat-desktop) install-tested green on
   ALL 3 chroots + fresh-checker re-run, with before (spec/built NVR) and after (live install)
   evidence.
3. Coverage ledger updated above; every package whose binary changed since last test was tested.
4. Dependency audit covers 65/65 (deep re-tear of rootapp + full fc44 live-repo install
   RESOLVE + changed-binary install tests on all 3 chroots).
5. Every claim backed by command output (COPR API, gh api, docker/dnf/rpm/ldd, curl -sI,
   sha256sum, git ls-remote, git log).
6. Named gaps listed in §7.
7. `.opencode-relay.md` on main: `status: complete` + `run_id: 32001450088` (this run's real
   GitHub run_id).
