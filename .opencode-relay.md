# fedora-nexus autonomous-run relay state

status: complete (run ended 2026-08-04 ~23:50 UTC)

## Run summary
Fresh run (RELAY=false, no prior unfinished state). This run performed a full
independent audit of all 27 packages WITHOUT trusting the previous run's claims:

1. **Version inventory sweep (100% of 27 packages)**: every spec Version /
   %global commit was cross-checked against live upstream via `git ls-remote`,
   best-effort stretch of release/tag switchers, API, and web. ALL UP-TO-DATE.
   rootapp internal version verified from inside the AppImage (sq.version /
   X-AppImage-Version = 0.9.125) and sha256 <-> pinned spec match.
2. **No-churn / updater-audit**: ran all 22 update.sh scripts against the
   working tree; ALL exited 0 with NO file changes (pure version/commit guards,
   no artifact-file dependency). Working tree clean. No duplicate-changelog
   accumulation (scripts self-collapse to one entry per bump).
3. **COPR build health**: every package's latest build `succeeded` on all
   enabled chroots (fedora-43/fedora-44/rawhide). Versions of published RPMs
   match the specs exactly.
4. **Fedora lifecycle**: Fedora 45 not yet GA (2026-10-20); chroots
   43/44/rawhide are correct; no chroot/README changes needed.
5. **Install-verification (fresh containers, real COPR user path)**:
   - container 1: rootapp -> installs clean, 0.9.125
   - container 2: obsidian, opencode-desktop, localsend, vesktop,
     heroic-games-launcher, fluxer, zen-browser, logseq, protonplus, starship
     -> all install clean
   - container 3: mangowm, scenefx, wlroots, niri-git, xwayland-satellite-git,
     quickshell-git, caelestia-shell-mango, gpu-screen-recorder,
     extension-manager, matugen, nwg-look, app2unit, libcava + 3 font/data pkgs
     -> all install clean
   - CLI smoke tests passed (starship 1.26.0, matugen 4.1.0, quickshell 0.3.0,
     niri 26.04, app2unit usage; gsr rejects root as expected).
   - libcava: clean targeted install resolved to 1.0.0-1 (earlier 0.10.7-2 was
     stale repodata), ldd no missing libs.
6. **Dependency deep-audit**: rootapp torn apart (AppImage extracted, main
   binary + bundled SkiSharp/uiohook/HarfBuzzSharp/onigwrap .so ldd-reconciled,
   no "not found"; Requires cover all; libICE/libSM transitively via libXt).
   For the other binary/Electron packages, Requires lists were validated via
   clean container installs from the live repo (no "nothing provides").
7. **Issues/PRs/Actions**: 0 open issues. Closed stray auto-generated PR #3
   (opencode schedule branch, summary-only, no code changes). Auto-Updater
   GitHub Actions runs all success.

## Version sweep (27/27, status: up-to-date)
app2unit, bibata-cursor-theme, caelestia-shell-mango, cascadia-code-nerd-fonts,
extension-manager, fluxer, gpu-screen-recorder, heroic-games-launcher, libcava,
localsend, logseq, mangowm, material-symbols-fonts, matugen, niri-git,
nwg-look, obsidian, opencode-desktop, protonplus, quickshell-git, rootapp,
scenefx, starship, vesktop, wlroots, xwayland-satellite-git, zen-browser
(see commit body for the full table + evidence).

## Install-test ledger (updated 2026-08-04)
All 27 packages install-tested in a fresh Fedora 44 container via the live
COPR repo (real `dnf install` user path) — result: installable for all 27.
Smoke: run where the binary is CLI-capable; GUI/compositor apps validated up to
install + missing-lib scan (no headless launch, expected).

## No changes made
No spec/update.sh/workflow changes were required. The only commit this run is
this relay state file (run record lives in the commit body).

## Next run pickup
status complete -> start fresh. Re-verify per the standing protocol; do not
trust this run's claims blindly (relay rule: treat the previous run as a fresh
checker would).