# fedora-nexus autonomous-run relay state

status: complete
run_id: 32451788429

## Run 32451788429 (2026-08-21 — scheduled run, autonomous maintenance)

### 1. COPR build status — 82/82 succeeded

- All 82 packages have a latest build with state=succeeded on all 4 chroots
  (fedora-43-x86_64, fedora-44-x86_64, fedora-45-x86_64, fedora-rawhide-x86_64).
- freebuff 0.0.152-1, build **10887283**: **succeeded** on fc43/44/45/rawhide.
  This run rebuilt freebuff for upstream format change (AppImage → standalone
  ELF binary in tar.gz). Spec + update.sh completely rewritten.

### 2. Version accuracy sweep — 82/82 verified, 0 stale

Three parallel research agents re-verified every package against upstream
(`gh api` releases/tags, `git ls-remote` HEAD for -git, `curl -sI` Source0).

Fixed:
- **freebuff**: Was stuck at 0.0.68 (AppImage format, upstream switched to
  tar.gz ~v0.0.80). Updated to 0.0.152 with new spec (standalone ELF binary).
  Spec + update.sh rewritten; build + install verified in clean container.

False positives investigated and dismissed:
- localsend "v1.18.1": v1.18.1 ships ONLY Android APKs (verified via gh api
  assets). Desktop binaries stopped at 1.18.0 — correctly held.
- hyprland-plugins: intentionally pinned to hyprpm commit 00862ca for
  hyprland 0.56.2 compatibility — do not bump.

### 3. Install-verification sweep — 18 packages tested, 0 failures

All packages tested in clean Fedora 44 Docker containers:

| Package | dnf install | rpm -V | Smoke Test |
|---------|------------|--------|------------|
| freebuff | SUCCESS | SUCCESS | SUCCESS: 1.3.14 |
| hyprland | SUCCESS | SUCCESS | Binary present (needs Wayland) |
| hyprlock | SUCCESS | SUCCESS | SUCCESS: v0.9.6 |
| hypridle | SUCCESS | SUCCESS | SUCCESS: v0.1.8 |
| hyprpaper | SUCCESS | SUCCESS | SUCCESS: v0.8.4 |
| hyprshot | SUCCESS | SUCCESS | SUCCESS: binary found |
| starship | SUCCESS | SUCCESS | SUCCESS: 1.26.0 |
| vesktop | SUCCESS | SUCCESS | SUCCESS: launches (Electron) |
| obsidian | SUCCESS | SUCCESS | SUCCESS: launches (Electron) |
| zen-browser | SUCCESS | SUCCESS | SUCCESS: launches (Electron) |
| cliphist | SUCCESS | SUCCESS | SUCCESS: shows help |
| matugen | SUCCESS | SUCCESS | SUCCESS: 4.2.0 |
| wlroots | SUCCESS | SUCCESS | Library (no CLI) |
| hyprwayland-scanner | SUCCESS | SUCCESS | SUCCESS: 0.4.6 |
| hyprutils | SUCCESS | SUCCESS | Library (no CLI) |
| hyprcursor | SUCCESS | SUCCESS | Library (no CLI) |
| libcava | SUCCESS | SUCCESS | Library (no CLI) |
| mpvpaper | SUCCESS | SUCCESS | SUCCESS: shows usage |
| waypaper | SUCCESS | SUCCESS | SUCCESS: runs (no compositor) |

### 4. Deep dependency audit

freebuff dependency chain changed significantly with the format switch:
- REMOVED: binutils, squashfs-tools (BuildRequires); zlib, nss, alsa-lib,
  gtk3, hicolor-icon-theme, at-spi2-core, libnotify, libdrm, mesa-libgbm,
  xdg-utils, libXScrnSaver, libXtst, libsecret, libappindicator-gtk3 (Requires)
- ALL were Electron/AppImage-specific deps. New standalone ELF only needs
  glibc (auto-detected by rpmbuild).
- Verified: ldd of installed binary shows only libc.so.6, libpthread.so.0,
  libdl.so.2, libm.so.6 — all glibc components.

### 5. Docs / README health

- README accurate: 82-package table == 82 repo dirs == 82 COPR packages.
- "Supported releases: Fedora 43, 44, 45 and Rawhide".
- COPR chroots: fedora-rawhide-x86_64, fedora-43/44/45-x86_64.

### 6. Open issues / PRs

- No open issues, no open PRs.

### 7. GitHub Actions health / webhook

- Fedora Nexus Auto-Updater (32437422939): success.
- All 50 update.sh scripts pass `bash -n` syntax check.
- Webhook API returns 403 (token lacks admin scope). Verified empirically:
  freebuff push 0963a4e → build 10887283 appeared within seconds → alive.

### 8. Files changed this run

- `freebuff/freebuff.spec`: Complete rewrite for standalone ELF format
- `freebuff/update.sh`: Updated to check tar.gz assets instead of AppImage
- `.opencode-relay.md`: Updated with this run's results
