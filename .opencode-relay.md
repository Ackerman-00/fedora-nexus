# opencode relay state

status: in_progress

## Run summary (2026-08-04 scheduled run, started 03:45)
Autonomous maintainer run.

## Completed so far
- COPR health: recent 100 builds -> 3 old failures (cascadia 10804938,
  heroic 10788404, libcava 10755051) ALL verified transient & superseded by
  later successful builds. No unresolved build failures.
- Package inventory: 26 repo dirs == 26 COPR packages == 26 README rows.
  No discrepancies.
- Version sweep (100%, 26/26): all up-to-date. Verified via gh api releases,
  git ls-remote HEAD for -git pkgs, X-Fluxer-Version header, GSR cgit page,
  wlroots gitlab API, and unsquashfs teardown of rootapp AppImage (sha
  86ed0d76... matches spec pin; sq.version + X-AppImage-Version = 0.9.125).
  All Source0 URLs return HTTP 200 (verified via curl -sI).
- Actions health: Auto-Updater + opencode-schedule all green. All 20
  update.sh pass `bash -n` and report up-to-date (no churn) when run.
- Issues: 0 open. PRs: 0 open.
- Dependency deep audit: reviewed all specs. Found 1 real fix:
  opencode-desktop had DUPLICATE `Requires: mesa-libgbm` lines -> removed
  one. (Previous run's fluxer/opencode/scenefx fixes re-verified correct.)

## FIX PUSHED (pending COPR build verify)
- opencode-desktop: removed duplicate mesa-libgbm Requires.

## REMAINING
- Verify COPR build for opencode-desktop after push (state succeeded).
- Install-verification sweep (>=5 spot checks + opencode). Docker verified
  working.
- README still matches (no version changes made this run).
- Final summary + verification tables.

## Traps
- packages.fedoraproject.org returns 404 for some subpackages; verify
  through parent page or web search.