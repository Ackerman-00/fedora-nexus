# opencode relay state

status: unfinished

## Completed this run
- Inventory: 26 package dirs, 26 COPR packages, README matches. No open issues/PRs.
- Full version sweep: all 26 packages verified up-to-date vs upstream EXCEPT quickshell-git, now refreshed.
- Fixed dependency audit issues in 4 specs and pushed commit `817dc4e` to main:
  - heroic-games-launcher.spec: +at-spi2-core, libnotify, libuuid, libXtst, xdg-utils (matches upstream RPM requires)
  - obsidian.spec: +at-spi2-core, libnotify, libsecret, libuuid, libXScrnSaver, libXtst, xdg-utils (matches upstream deb Depends; AutoReqProv: no)
  - vesktop.spec: replaced util-linux-core with libuuid (upstream declares libuuid/libuuid1)
  - fluxer.spec: +libXScrnSaver, libnotify, libuuid, xdg-utils (dlopen'd runtime deps missing from auto-gen)
  - quickshell-git.spec: bumped to upstream HEAD 28771c7 (gitdate 20260802135341)
- GitHub Actions health: update-engine.yml all green (last 10 runs success). No open issues/PRs.
- COPR build verification (push 817dc4e):
  - 10805816 obsidian 1.13.4-2: SUCCEEDED (all chroots). Install-tested F44: OK, all 7 new Requires resolved.
  - 10805817 heroic-games-launcher 2.22.0-3: SUCCEEDED. Install-tested F44: OK, all 5 new Requires resolved.
  - 10805818 vesktop 1.6.5-2: SUCCEEDED. Install-tested F44: OK, libuuid resolved (util-linux-core gone).
  - 10805820 fluxer 2026.731.153836-3: SUCCEEDED. Install-tested F44: OK, all 4 new Requires resolved.
  - 10805819 quickshell-git: still RUNNING (source build) - must verify before finishing.
- Rotating spot-check install tests (F44): localsend OK, rootapp OK, opencode-desktop OK,
  niri-git OK (needs xwayland-satellite-git co-install, then `niri --version` = 26.04 feb3e43),
  zen-browser OK, starship OK (smoke: starship 1.26.0).

## Unfinished / Next steps
1. Poll quickshell-git build 10805819 until it finishes. If succeeded, download + install-test in F44.
   If failed, diagnose, fix, re-push.
2. Final summary: version verification table (26 rows), dependency audit table (26 rows), install-test table.
3. Set this file status to "complete" (or delete it) when done.

## Builds to poll
- COPR API: https://copr.fedorainfracloud.org/api_3/build/10805819
- Results base: https://download.copr.fedorainfracloud.org/results/ackerman/nexus/fedora-44-x86_64/10805819-quickshell-git/

## Traps
- util-linux-core does NOT provide libuuid on Fedora chroots (vesktop used it wrongly). libuuid is the real provider.
- Electron apps dlopen libnotify/libsecret/libXtst/libXScrnSaver/libuuid — auto-generated soname Requires miss them, so specs must declare them (fluxer case).
- COPR result dirs now list via Pulp/content server; direct RPM URL pattern still works.
- niri-git Requires xwayland-satellite-git (same repo) - install both together in tests.
