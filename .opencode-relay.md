# fedora-nexus autonomous-run relay state

status: complete
run_id: 31706023338

## Run 31706023338 (2026-08-13) - COMPLETE: auto-updater bumped niri-git (720c388) + freebuff (0.0.59); I fixed fluxer (2026.812.134312 -> 2026.813.125222, local build + install test + COPR build 10862427 green); all 3 changed packages install-tested green fc44/fc43/rawhide + fresh-checker from live repo; 65/65 versions verified current after my fix; no open issues/PRs; no churn

### 0. Pickup
- Picked up main at `ab2f243` (2026-08-13 13:27Z auto-update "sync spec files to latest upstream versions"). RELAY=false, RUN_ID=31706023338.
- COPR chroots (API-verified, never hardcoded): fedora-44-x86_64, fedora-43-x86_64, fedora-rawhide-x86_64. Fedora lifecycle: F45 mass-branched 2026-08-11, Rawhide is now 46 devel (rawhide build release tag observed .fc46), F43/F44 stable still supported. No chroot changes needed; README already correct.
- Open issues/PRs: NONE (gh issue list / gh pr list empty). Actions health: auto-updater run 31704959511 completed SUCCESS 13:26Z (bumped niri-git 11756db->720c388 and freebuff 0.0.57->0.0.59, commit ab2f243); no failed runs.

### 1. What the auto-updater did this run (verified)
- niri-git: commit 11756db -> 720c388 (gitdate 20260813130615, version 26.04^20260813130615git720c388, Release 1%{?dist}). `git ls-remote` confirms 720c388 = upstream HEAD. COPR build 10862301 SUBMITTED 2s after commit (webhook), SUCCEEDED all 3 chroots.
- freebuff: 0.0.57 -> 0.0.59 (build 10862302 succeeded). `git ls-remote --tags` confirms freebuff-desktop-v0.0.59 is the newest desktop tag (the repo's releases/latest = freebuff-v0.0.149 is the CLI, correctly NOT used; update.sh prefix filter handles this).
- No other package changed.

### 2. Fixes this run
- **fluxer** (my fix): upstream CDN X-Fluxer-Version header now consistently reports 2026.813.125222 (15/15 sampled requests after rollout completed; early requests during rollout alternated 2026.812.134312/2026.813.125222 between CDN edges). Spec was stale at 2026.812.134312. Ran update.sh -> Version 2026.813.125222, Release reset 1%{?dist}, changelog replaced. Verified:
  - Update.sh guard is pure version comparison (no churn): re-run after bump exits 0 with "no update needed".
  - Local rpmbuild in clean fc44 container = SUCCESS, RPM fluxer-2026.813.125222-1.fc44.x86_64.rpm.
  - Install test of locally-built RPM in fresh fc44 = PASS (dnf+dep resolve, rpm -V OK).
  - Teardown of the source RPM: `rpm2cpio | cpio -idmv` extracted /opt/Fluxer; app.asar package.json = fluxer_app 0.0.8 (no embedded version string; version lives in the X-Fluxer-Version header, consistent with how update.sh derives it); no missing deps vs spec Requires.
  - COPR build 10862427 (triggered by push) SUCCEEDED all 3 chroots.
  - Fresh-checker from live repo fc44 installs fluxer-2026.813.125222-1.fc44 cleanly, rpm -V OK.
  - Commit: 28744e1 "fix(autonomous): fluxer: bump 2026.812.134312 -> 2026.813.125222".
  - Webhook: build 10862427 appeared ~30s after push 28744e1; direct webhook API read is 403 (token scope, known gap) - webhook verified empirically.

### 3. Version accuracy sweep - 65/65 CURRENT (after my fluxer fix)
- Re-ran all 33 update.sh scripts in a FRESH archived checkout (/tmp/freshrepo): all 33 exit 0, ZERO diffs vs main except fluxer (which I already fixed upstream). bash -n: all pass.
- Git-pinned packages all MATCH upstream HEAD via git ls-remote (10/10): app2unit 47e23ec, caelestia-cli-mango be54856, caelestia-shell-mango cae55ed, hyprland-contrib 3dcbce7, lazyvim-git 459a4c3, material-symbols-fonts 50f0603, niri-git 720c388, quickshell-git 28771c7, xwayland-satellite-git 3bc915f. hyprland-plugins pin 00862ca verified = commit "hyprpm: add pin for 0.56.2" (matches hyprland 0.56.2 release; intentional by design).
- GitHub-release packages all at latest via gh api releases/latest: aquamarine v0.14.0, bibata 2.0.7, cascadia 3.5.0, cliphist 0.7.0, extension-manager 0.6.5, glaze v8.0.0, helium 0.15.4.1, heroic 2.22.1, all hypr* (hyprland 0.56.2, hyprcursor 0.1.13, hyprlang 0.6.8, hyprutils 0.14.0, etc), libcava 1.0.0, logseq 2.0.1, mangowm 0.16.0, matugen v4.1.0, mpvpaper 1.9, nwg-look 1.1.1, obsidian 1.13.7, opencode-desktop v1.18.18, scenefx 0.5, starship v1.26.0, stoat 1.4.2, vesktop 1.6.5, waypaper 2.8, xdg-desktop-portal-hyprland v1.4.1, zen 1.21.13b, protonplus v0.6.3, ghostty 1.3.1 (asset 200).
- Non-GitHub: awww v0.12.1 (Codeberg API), ly v1.4.1 (Codeberg API), gpu-screen-recorder 6.0.0 (dec05eba.com snapshot tar 200), fluxer 2026.813.125222 (header, fixed), wlroots 0.20.2 (GitLab API releases 200, source tar 200), materialyoucolor 3.0.4 (PyPI, sdist 200).
- localsend held at 1.18.0 CORRECTLY (v1.18.1 tag exists but 1.18.1 deb asset = 404; spec built fine). rootapp 0.9.126 RE-TORN (see 5).
- hyprlauncher/hyprshutdown: not in COPR (owner-registration caveat in README intact); versions 0.1.6 / 0.1.1 match latest GitHub releases.

### 4. Install-test ledger (all FRESH docker containers, from LIVE COPR repo unless noted)
- Changed binaries this run: niri-git 720c388, freebuff 0.0.59, fluxer 2026.813.125222.
- niri-git: local rpms from build 10862301 installed in fc44/fc43/rawhide (rpm -V=0, `niri --version` = "niri 26.04 (720c388)", ldd missing=0).
- freebuff: fc44 + fc43 + rawhide from live repo (install + rpm -V=0; AppImage payload /opt/freebuff/@codebufffreebuff-desktop + chrome-sandbox setuid present). Electron smoke: `xvfb-run freebuff --no-sandbox` starts app init under xvfb (container user-ns restriction blocks sandboxed launch - environment, not packaging).
- fluxer: fc44 local RPM install PASS + fc44/fc43/rawhide live-repo install + rpm -V=0; smoke `xvfb-run fluxer --no-sandbox --version` starts app under xvfb.
- FRESH-CHECKER PASS (independent): ONE fresh fc44 container, `dnf copr enable ackerman/nexus` + `dnf install niri-git freebuff fluxer` from LIVE repo -> all 3 install, rpm -V=0, `niri --version` = 26.04 (720c388).
- Prior ledger (run 31679278418): ALL 65 packages already install-tested green; nothing else's binary changed this run, so coverage remains complete.

### 5. Dependency audit (prebuilt-binary re-tears this run)
- RE-TEAR rule applied to packages whose upstream changed: niri-git (new tarball 720c388 extracted; Cargo.toml default features dbus/systemd/xdp-gnome-screencast - all covered by existing spec BuildRequires, no new deps; Cargo.lock unchanged feature set), freebuff 0.0.59 (AppImage torn: offset 188392, X-AppImage-Version=0.0.59, bin @codebufffreebuff-desktop, tray libs in usr/lib, icon path matches spec %install exactly; installed green so Requires resolve), fluxer (RPM torn, /opt/Fluxer + wrapper deps all resolve in install tests).
- rootapp 0.9.126 RE-TORN: AppImage downloaded, sha256 = `0736926fde923819d276ac6d4a3afe65c6d87c734689af47276fc4ebf075d910` == spec pinned comment; unsquashfs offset 944632; X-AppImage-Version=0.9.126 confirmed. Spec current.
- All other packages' upstream unchanged since previous relay audits (carried from run 31679278418) - deps remain valid. Install tests across ~40 packages this and prior runs show no "nothing provides".

### 6. Final state
- Working tree clean. Two commits on main since pickup: auto-update ab2f243 (niri/freebuff) + my fluxer fix 28744e1.
- Actions health: all recent runs success (auto-updater 31704959511, prior opencode runs). No failed COPR builds active (only historical superseded: localsend 10844795 1.18.0-1, obsidian 10843364 1.13.6-1 - both superseded by successful newer builds).
- Webhook verified empirically for my push: fluxer build 10862427 submitted ~30s after commit 28744e1, SUCCEEDED.
- Known gaps (unchanged): (1) hyprlauncher/hyprshutdown not in COPR until owner registers - README caveat present; (2) GUI apps can't run headless - verified install+rpm -V+ldd+version/xvfb-start instead; (3) COPR webhook/config API 403 (token scope) - webhook verified empirically via 30s build-submission timing; (4) rawhide dist tag .fc46 pre-GA is COPR-side; (5) COPR download owner is `ackerman`; (6) fresh rawhide image ships no base GPG keys - rawhide tests use dnf --nogpgcheck.