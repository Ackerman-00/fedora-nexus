# opencode relay state

status: complete

## Run summary (2026-08-02)
All work complete. No pending items.

## Completed this run
- COPR health: all recent builds succeeded; the only failed build (cascadia-code-nerd-fonts
  #10804938, old 3.5.0 source-404) was already fixed by the previous run (3.4.0 + /releases/latest
  updater) and is superseded by succeeded builds #10805052/#10805734. GitHub Actions: all green.
- Full version sweep: all 26 packages verified against upstream (git HEADs for app2unit,
  caelestia-shell-mango, material-symbols-fonts, niri-git, quickshell-git, xwayland-satellite-git
  all match pinned commits; stable versions all match upstream releases/tags; gitdates match
  committer dates). Every Source URL verified reachable (200/206); only rootapp pins a checksum
  (sha256 86ed0d76... confirmed by tearing the AppImage apart).
- Tear-apart verification of every binary package (version read from INSIDE the artifact):
  rootapp 0.9.125 (X-AppImage-Version + sq.version), obsidian 1.13.4, opencode-desktop 1.18.11,
  localsend 1.17.0, heroic 2.22.0 (bundled legendary 0.20.43 / gogdl 1.2.1 / nile 1.1.2 / comet
  0.2.0 all verified from the bundled binaries), vesktop 1.6.5, zen-browser 1.21.10b
  (application.ini), starship 1.26.0, fluxer (API header 2026.731.153836, rpm payload verified).
  All match the specs.
- Dependency deep audit (all 26 packages): specs complete vs upstream build systems
  (meson.build/CMakeLists/Cargo.toml/go.mod checked). One fix: matugen had dead BuildRequires
  pkgconfig(openssl) (no openssl crate anywhere in Cargo.lock; TLS via rustls) - removed, bump to
  Release 2. Commit 1d21d5c.
- matugen 4.1.0-2 verified: COPR build 10805919 succeeded in all 3 chroots; install + `matugen
  --version` OK in F44 container.
- Install-verification sweep (F44 containers): 20+ packages installed cleanly from COPR and smoke
  tested - app2unit, extension-manager, matugen, nwg-look, bibata-cursor-theme, mangowm,
  protonplus, gpu-screen-recorder, libcava(+devel), caelestia-shell-mango (pulls quickshell-git),
  cascadia-code-nerd-fonts, niri-git, xwayland-satellite-git, scenefx, wlroots, starship, obsidian,
  localsend, zen-browser, opencode-desktop, vesktop, heroic-games-launcher, fluxer, rootapp.
  Electron apps refuse root/sandbox in containers (expected) but binaries present and launch.
- Open issues/PRs: none.
- README: 26 packages, matches inventory and COPR package list.
