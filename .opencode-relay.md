# fedora-nexus autonomous-run relay state

status: complete
run_id: 31278138335

## Run - RUN_ID 31278138335 (2026-08-08) - COMPLETED

### 0. Pickup status
- Previous run (31268406907) declared `status: complete`. RELAY=false, so this
  run acted as a fresh checker. HEAD at pickup: 88367a0 (single squashed
  commit). Working tree was clean.
- Chroots (COPR API, authoritative): fedora-rawhide-x86_64 (F45 dev),
  fedora-44-x86_64 (stable), fedora-43-x86_64 (supported to 2026-12-09).
  F45 mass-branching 2026-08-11; no COPR f45 chroot yet. No chroot changes.

### 1. COPR Build Status
- All 58 COPR packages have their LATEST build `succeeded`. The 3 failures
  in the recent window (hyprgraphics 0.5.1-1, aquamarine 0.14.0-1,
  gpu-screen-recorder 5.15.3-3) are OLD and superseded by later succeeds.
- Webhook empirically confirmed: each push immediately produced new COPR
  builds (10838841 xwayland-satellite, 10838947 caelestia-shell-mango).
- `gh api .../hooks` reads still 403 (no admin:repo_hook scope); gap noted.

### 2. xwayland-satellite-git refresh (commit 58d2eff, pushed)
- Auto-updater had not yet picked up new upstream HEAD `3bc915f` (2026-08-08)
  vs pinned `8d135d3`. Committed the refresh: spec Version
  `0.8.2^20260808203058git3bc915f`, Release 1.
- Verified: local fc44 docker build (BUILD_RC=0), install (DNF_RC=0,
  rpm -V PASS, ldd resolved). COPR build 10838841 succeeded on all chroots;
  fresh live-COPR install verified the caret NVR.

### 3. NEW DEFECT FOUND & FIXED - caelestia-shell-mango stale-upgrade trap
- Root cause: Version was static `2.0.0` while update.sh reset Release to 1
  on every commit refresh. So each new-commit build rolled back to `2.0.0-1`,
  which sorts BELOW the shipped cliphist-fix `2.0.0-2`. dnf could never
  upgrade to newer upstream commits; users stayed stuck on 50fec11 even after
  upstream moved to 1c34243.
- Fix (commit da9fa37, pushed to main): switched spec to the caret scheme
  `Version: 2.0.0^%{gitdate}git%{shortcommit}` (like quickshell-git /
  niri-git) and taught caelestia-shell-mango/update.sh to rewrite the Version
  line on refresh (BASE_VER strips `^...` so future refreshes stay caret-consistent).
- rpm.labelCompare verified: `2.0.0^20260808172149git1c2a3-1` > `2.0.0-2`.
- Verification: docker fc44 build/install/rpm -V PASS; COPR build 10838947
  succeeded; fresh live-COPR install pulls the caret NVR; `dnf check-update`
  reports "already at newest". Future upstream commits will now upgrade.
- caelestia-cli-mango checked for the same trap: its Release is monotonic
  (2.0.0-3 current, next refresh bumps Release, no caret needed) -> no change.

### 4. Version / Checksum / Binary Audit (58 packages)
- All GitHub-release packages match spec Version (extension-manager 0.6.5,
  glaze 8.0.0, mangowm 0.15.6, hyprland v0.56.2, hypr-*, vesktop v1.6.5,
  localsend v1.17.0, logseq 2.0.1, bibata v2.0.7, obsidian v1.13.4,
  cascadia v3.5.0, heroic v2.22.0, starship v1.26.0, zen 1.21.12b,
  stoat v1.4.2, waypaper 2.8, opencode-desktop v1.18.15, scenefx 0.5,
  protonplus v0.5.22, aquamarine 0.14.0, hyprutils 0.14.0, etc.).
- Git-pinned git pkgs: ls-remote HEAD == pinned commit (no churn) for
  app2unit, hyprland-contrib, lazyvim, material-symbols, niri-git,
  quickshell-git, xwayland (now 3bc915f). hyprland-plugins 00262ca is a
  legit compatibility pin. caelestia-cli HEAD-only change is README, no-churn.
- Non-GitHub: awww (codeberg v0.12.1), wlroots (gitlab 0.20.2),
  gpu-screen-recorder 6.0.0, matugen crates.io 4.1.0 (static.crates.io 200;
  api 403 is a runner IP rate-limit, not a spec issue), fluxer header matches,
  materialyoucolor pypi 3.0.4.
- Artifacts torn apart: rootapp AppImage sha256==spec AND inner
  X-AppImage-Version=0.9.126; zen inner Version=1.21.12b; obsidian deb 1.13.4;
  starship --version 1.26.0; localsend/vesktop/heroic/stoa/opencode-desktop
  all as spec. All source URLs 200.

### 5. Install Sweep (fresh dnf install, all 3 chroots)
- **fedora:44: 55/56 installable clean at NEWEST NVR (all contents; the 1
  skip = glaze source-only base, its -devel is what installs and does PASS).**
- **fedora:43 and fedora:rawhide: same 55/56 result, all clean.**
- devel subpackages (glaze-devel, hyprland-protocols-devel,
  python3-materialyoucolor) RC=0 on 44/43/rawhide. rpm -V clean on tested.

### 6. Spec / update.sh / Actions health
- bash -n all update.sh (25): PASS. rpmspec -P over all specs: 0 parse fails.
- Dry-run all update.sh in throwaway clone: every `update.sh` exits
  `no-change rc=0` (needs working GITHUB_TOKEN; dummy token 401s cascadia).
  No churn. No up-to-date-guard flaws besides the shell caret issue (fixed).
- All Auto-Updater GitHub-Action runs success.

### 7. Issues & PRs
- Open PRs: none. Open issues: none.

### 8. Gaps (stated)
- Webhook delivery-ids unreadable via API (repo_hook scope). Webhook proven
  alive via `push -> copr build appears`.
- crates.io API 403 from this runner (IP rate-limit); static.crates.io 200;
  COPR buildproves it pulls crates fine.
- GUI apps not launched (headless). Install+rpm -V+artifact-version covered.
- caelestia-cli HEAD is README-only upstream movement: no-churn, skipped.
- Fedora-45 chroot appears after mass-branching 2026-08-11; revisit then.

### Closing note
Fixed 2 real defects this run: (1) xwayland-satellite-git pinned stale
upstream (refreshed 3bc915f, built+installed clean), (2) caelestia-shell-mango
stale-NVR trap (downgrade-to-agent beta caret scheme; dnf now upgrades past
2.0.0-2). Both pushed atomically (58d2eff, da9fa37), webhook-confirmed, both
verified end-to-end in clean docker (build + install + rpm -V) and via live
COPR installs. All 58 packages newest-built; install sweep green on 44/43/
rawhide; no open issues/PRs. Repo clean.