# opencode relay state

status: complete

## Run summary (2026-08-03 scheduled run, started 23:33)
Autonomous maintainer run. Full verification performed; 3 dependency fixes
made and pushed; awaiting COPR builds.

## Completed this run
- COPR health: no unresolved failures (cascadia 10804938 & heroic 10788404
  were transient, superseded by successful rebuilds).
- Package inventory 26/26 matches README + COPR package list (26 exactly).
- Version sweep (100%, 26/26): all up-to-date. Verified via gh api releases,
  git ls-remote HEAD for -git packages, X-Fluxer-Version header, gpu-screen-
  recorder project page, wlroots gitlab, and unsquashfs teardown of rootapp
  AppImage (sha 86ed0d76... = spec pin, version 0.9.125).
- GitHub Actions: Auto-Updater + opencode-schedule all green; update.sh all
  20 pass bash -n and report up-to-date (no churn). Working tree clean.
- 0 open issues, 0 open PRs.
- Dependency deep audit: all 26 packages audited. Most deps-verified
  (localsend, obsidian, vesktop, heroic, niri-git, xwayland-satellite-git,
  mangowm, wlroots, gpu-screen-recorder, libcava, extension-manager,
  matugen, nwg-look ok). Identified 3 real gaps -> FIXED.

## Fixes pushed (commit ca3f816 -> triggers COPR rebuild)
1. fluxer: match upstream rpm Requires (rpm -qpR): added at-spi2-core, gtk3,
   libXtst, nss; dropped dead gamemode/mangohud. Release 3->4.
2. opencode-desktop: add Require mesa-libgbm (libgbm.so.1 NEEDED by bundled
   Electron, same gap obsidian had). Release 1->2.
3. scenefx: add BuildRequires pkgconfig(xkbcommon) >= 1.8.0 (hard dep in
   meson.build, was only resolved via meson fallback). Release 1->2.

## In-flight COPR builds (from commit ca3f816, must verify succeed)
- 10808825 opencode-desktop-1.18.11-2 (was running)
- 10808824 fluxer-2026.731.153836-4 (was importing)
- 10808823 scenefx-0.5-2 (was importing)
NEXT: verify all 3 reach succeeded on all 3 chroots; if any fails, diagnose
logs and fix. If all succeed, done (mark status complete).

## RESULT (all 3 SUCCEEDED on all chroots, verified - RUN COMPLETE)
- 10808825 opencode-desktop-1.18.11-2: succeeded
- 10808824 fluxer-2026.731.153836-4: succeeded
- 10808823 scenefx-0.5-2: succeeded
- Install-verified all 3 in fresh fedora:44: fluxer, opencode-desktop,
  scenefx all dnf install + rpm -V OK. opencode-desktop correctly links
  libgbm.so.1 -> mesa-libgbm-26.1.5-1.fc44.

## Install-verification sweep (this run, fresh fedora:44, all dnf install)
- starship 1.26.0: install OK, --version smoke exit 0.
- matugen 4.1.0, nwg-look 1.1.1, extension-manager 0.6.5: install + verify OK.
- rootapp 0.9.125: 140 pkgs deps resolve, install + rpm -V OK.
- cascadia-code-nerd-fonts 3.5.0: install + rpm -V OK.

## Traps found
- crates.io /api/v1/crates download 403s from this runner (IP throttle) but
  the URL works in COPR (matugen build succeeded); static.crates.io direct
  returns 200. Not a spec bug.
- packages.fedoraproject.org returns 404 for subpackages (mesa-libgbm) -
  verify via the parent "mesa" page or web search.