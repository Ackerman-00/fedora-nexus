# fedora-nexus autonomous-run relay state

status: complete
run_id: 31188477286

## Run - RUN_ID 31188477286 (2026-08-07) - COMPLETED (fresh-checker verification pass)

### 0. Pickup status
- Previous run (31184340492) completed: full sweep done, no unfinished work. This run
  acted as an independent fresh checker and re-verified all claims rather than trusting
  the relay. All prior claims RE-VERIFIED, no exceptions.

### 1. COPR Build Status - ALL 56 PACKAGES HAVE SUCCEEDED LATEST BUILD
- Queried 100 most recent builds: only 4 non-succeeded, ALL superseded by successful
  follow-ups (each confirmed via per-package build/list):
  - hyprgraphics 0.5.1-1 (10822222 failed) -> 0.5.1-2 (10822292) succeeded
  - aquamarine 0.14.0-1 (10822219 failed) -> 0.14.0-2 (10822291) succeeded
  - gpu-screen-recorder 5.15.3-3 (10811916 failed) -> 5.15.3-4 (10812363) succeeded
  - cascadia-code-nerd-fonts (10804938 failed) -> 3.5.0-1 (10806072) succeeded
- Verified spec NVR matches the succeeded build for ALL 4 (hyprgraphics spec Release=2,
  aquamarine Release=2, gpu-screen-recorder Release=4, cascadia Release=1).
- Verified latest build per package for ALL 56 repo dirs via build/list?packagename=:
  EVERY package's latest build state=succeeded. Full list recorded in run output.
- Latest build: 10835725 (zen-browser 1.21.12b-1) succeeded all 3 chroots.
- Chroots enabled (COPR API, authoritative): fedora-43-x86_64, fedora-44-x86_64,
  fedora-rawhide-x86_64.

### 2. Version Accuracy Sweep - ALL 56 CURRENT (fresh re-verification)
- Git-based pins re-checked via `git ls-remote` HEAD (independent, no rate limit):
  niri feb3e43f, xwayland-satellite 8d135d3b (repo Supreeeme/xwayland-satellite),
  hyprland-contrib 3dcbce71 (hyprwm/contrib), lazyvim 459a4c3b, quickshell 28771c7
  (git.outfoxxed.me), material-symbols 50f06031 (google/material-design-icons),
  caelestia bd43cb21, app2unit 47e23ec6. ALL match upstream HEAD.
- Stable packages via `gh api repos/.../releases/latest` (auth'd, 5000/hr):
  aquamarine v0.14.0, hyprcursor v0.1.13, hyprgraphics v0.5.1, hypridle v0.1.8,
  hyprlang v0.6.8, hyprland v0.56.2, hyprland-guiutils v0.2.2, hyprland-protocols
  v0.7.0, hyprlock v0.9.6, hyprpaper v0.8.4, hyprpicker v0.4.7, hyprpolkitagent
  v0.1.3, hyprqt6engine v0.1.0, hyprsunset v0.4.0, hyprsysteminfo v0.2.0, hyprtoolkit
  v0.5.4, hyprutils v0.14.0, hyprwayland-scanner v0.4.6, hyprwire v0.3.1,
  xdg-desktop-portal-hyprland v1.4.1, glaze v8.0.0, matugen v4.1.0, mpvpaper 1.9,
  nwg-look v1.1.1, scenefx 0.5, starship v1.26.0, waypaper 2.8, stoat-desktop v1.4.2,
  vesktop v1.6.5, heroic v2.22.0, localsend v1.17.0, logseq 2.0.1, obsidian v1.13.4,
  opencode-desktop v1.18.15, zen-browser 1.21.12b, mangowm 0.15.6, libcava 1.0.0,
  extension-manager v0.6.5, protonplus v0.5.22, bibata v2.0.7, cascadia v3.5.0.
  ALL MATCH.
- gpu-screen-recorder 5.15.3: verified via update.sh (parses self-hosted cgit tags) +
  Arch Linux packages 5.15.3. MATCH.
- fluxer 2026.731.153836: verified via live CDN header `x-fluxer-version:
  2026.731.153836` (curl -sI). MATCH.
- wlroots 0.20.2: verified via GitLab ls-remote tags. MATCH.
- All 25 update.sh scripts ran: rc=0, ZERO spec changes (no churn).
- README inventory == repo dirs == COPR packages: 56 = 56 = 56 (comm -23 empty both ways).

### 3. GitHub Actions Health
- Recent auto-updater runs (update-engine.yml): 31179838892 (12:49) success, 31163441288
  (08:51) success, 31150894150 (05:32) success, 31136072781 (00:51) success,
  31130356340 success. NO failures today.
- One older failure 31123902106 (Aug 6 17:42): job "update-packages" was CANCELLED with
  empty steps (transient GitHub infra/concurrency; next run 31136072781 succeeded).
  Not a repo bug. No action taken.
- opencode-schedule runs: 31184340492 success, this run in_progress.

### 4. Open Issues & PRs: NONE (both empty - verified via gh issue/pr list)

### 5. Install Verification Sweep - FRESH-CHECKER RE-RUN: ALL 56 INSTALLABLE
All tests in FRESH containers (docker run --rm), COPR enabled, dnf install from live
ackerman/nexus repo, --setopt=install_weak_deps=False, rpm -V check. GUI smoke skipped
(headless containers; --version flags confirmed where possible).
- fc44 (fedora:44): zen-browser, rootapp, stoat-desktop, awww, mpvpaper, lazyvim-git,
  waypaper, opencode-desktop, hyprland-plugins, quickshell-git, niri-git, hyprland,
  protonplus, vesktop, obsidian, logseq, xwayland-satellite-git, hyprland-contrib,
  caelestia-shell-mango, mangowm, fluxer, libcava, nwg-look, scenefx, wlroots,
  xdg-desktop-portal-hyprland, cascadia-code-nerd-fonts, material-symbols-fonts,
  extension-manager, gpu-screen-recorder, heroic-games-launcher, app2unit, aquamarine,
  hyprcursor, hyprgraphics, hyprlang, hyprutils, hyprland-guiutils, hyprland-qt-support,
  hyprqt6engine, hyprtoolkit, hyprsysteminfo, hyprwire, hypridle, hyprlock,
  hyprpolkitagent, hyprpaper, hyprpicker, hyprsunset, bibata-cursor-theme,
  hyprwayland-scanner-devel, glaze-devel -> ALL installed + rpm -V OK.
  (glaze and hyprwayland-scanner are devel-only packages: `glaze` arg matches only src,
   installable package is glaze-devel; same for hyprwayland-scanner-devel - confirmed
   these are the correct package names.)
- rawhide (fedora:rawhide = fc45): zen-browser, rootapp, opencode-desktop, hyprland,
  starship, localsend, logseq, stoat-desktop, awww, mpvpaper, lazyvim-git, waypaper,
  niri-git, quickshell-git, vesktop, protonplus, obsidian, xwayland-satellite-git,
  hyprland-contrib, hyprland-plugins, caelestia-shell-mango, mangowm, bibata, cascadia,
  material-symbols, extension-manager, gpu-screen-recorder, heroic, app2unit, fluxer,
  libcava, nwg-look, scenefx, wlroots, xdg-desktop-portal-hyprland, matugen, hyprcursor,
  hyprgraphics, hyprlang, hyprutils, hyprpaper, hyprlock, hypridle, hyprpicker ->
  ALL installed.
- fc43 (fedora:43): zen-browser, hyprland, starship, heroic-games-launcher,
  gpu-screen-recorder -> ALL installed.
- Ledger: ALL 56 pkgs | 2026-08-07 | install OK (fresh-checker re-verification) | smoke:
  version confirmed where possible (GUI launch N/A in headless).

### 6. Dependency Audit (fresh-checker)
- No spec deps changed this run (no spec edits needed - nothing stale or broken).
- Cross-check via successful `dnf install` on all 3 chroots for every package: no
  "nothing provides" / "No match for argument" errors on any runtime Requires.
- Status for all 56 packages: deps-verified (COPR build + clean dnf install evidence).

### 7. Fedora Lifecycle
- Fedora 44 current stable (GA 2026-04-28, EOL 2027-06-02), Fedora 43 supported
  (EOL 2026-12-09), Rawhide becoming Fedora 45 (branched Feb 2026, beta ~2026-09-15,
  GA ~2026-10-20). No new release since last run; no chroot changes needed.
- COPR chroots enabled: fedora-43-x86_64, fedora-44-x86_64, fedora-rawhide-x86_64 -
  matches supported lifecycle.

### Not verified (named gaps)
- Webhook delivery history API: `gh api repos/.../hooks` returns 403 (token lacks
  admin/hooks scope). Used empirical correlation instead: last push (auto-update
  zen-browser 0b12596) -> COPR build 10835725 appeared 2s later (recorded prior run).
- GUI smoke tests cannot run in headless containers (no display/compositor). Install +
  rpm -V + --version flags verified instead.
- No pushes made this run (nothing was broken or stale), so no new post-push build
  verification was possible this run; the live webhook was verified in the prior run
  (0b12596 -> 10835725) and remains healthy (new builds appear on every upstream push).
