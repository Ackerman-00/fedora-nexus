# fedora-nexus autonomous-run relay state

status: complete
run_id: 31546490142

## Run 31546490142 (2026-08-12) - COMPLETE: ghostty COPR build fixed+green; niri-git fec411e + protonplus 0.6.3 refreshed, COPR-green + install-tested (3 chroots); 64/64 versions current; no open issues/PRs

### 0. Pickup
- Picked up main at `b3efe42` (niri-git fec411e + protonplus 0.6.3 from this run's earlier pushes; previous relay run 31526958706 `status: complete`). RELAY=false, RUN_ID=31546490142.
- COPR chroots (API-verified): fedora-44-x86_64, fedora-rawhide-x86_64, fedora-43-x86_64. Fedora lifecycle re-confirmed: F44 current stable (GA Apr 28 2026), F43 supported (EOL Dec 9 2026), F45 branched 2026-08-11 (GA target ~Oct 20 2026, NOT yet released) — rawhide builds dist-tag .fc45 (pre-GA, normal).
- NOTE: COPR owner for the download repo is **ackerman** (baseurl `https://download.copr.fedorainfracloud.org/results/ackerman/nexus/...`), NOT `nexus-repo`. All live-COPR install tests must use the ackerman URL (a repo-file-based .repo works on all chroots; `dnf config-manager --add-repo` is not supported on rawhide).

### 1. COPR build fail FIXED (priority 1): ghostty 10850831 FAILED -> 10850852 SUCCEEDED
- Ghostty dir added by owner (commit `7e03949`) after prior relay completed; its first COPR build **10850831 FAILED**. Diagnosis (self-healed in repo):
  - Missing `BuildRequires: systemd-rpm-macros` -> `%{_userunitdir}` macro unexpanded at build time.
  - `%install` did `cp -a usr/share/* ...` without first creating `%{buildroot}%{_datadir}`.
- Fix in `ghostty/ghostty.spec`: added `BuildRequires: systemd-rpm-macros`, added `install -d -m 0755 %{buildroot}%{_datadir}` before the cp, Release 1->2, changelog entry.
- Local verification BEFORE push: docker `fedora:44` build + install PASS (Ghostty 1.3.1, exit 0).
- Pushed `9d1c0d8`; COPR build **10850852 (ghostty 1.3.1-2) SUCCEEDED** on all 3 chroots. Install-tested from live COPR (ackerman URL) on fc44/fc43/rawhide: ALL PASS (`ghostty --version` -> "Ghostty 1.3.1", exit 0, `rpm -V` OK).
- Webhook firing re-verified empirically again this run: every push immediately produced a COPR build (10850852 ghostty, 10850874 niri-git, 10850873 protonplus). gh api hooks = 403 known token scope gap.

### 2. Version sweep (priority 2) — ALL 64 CURRENT, ZERO CHURN
- **update.sh packages (32)**: all executed clean in sandbox. Two genuine updates found and shipped:
  - **niri-git**: aac1551 -> **fec411ea5119b5f0299feb32a2d5d3887349b6d3** (upstream main 2026-08-11, gitdate 20260811204258). Verified upstream HEAD via gh api; docker fc44 build+install+smoke PASS (`niri 26.04 (fec411e)`). Pushed `0851330`. COPR build **10850874 SUCCEEDED** (all 3 chroots); live-COPR install PASS on fc44/rawhide/fc43 (`niri-git-26.04^20260811204258gitfec411e`, RPM_V_OK).
  - **protonplus**: 0.6.2 -> **v0.6.3** (released 2026-08-11, verified gh api). Docker fc44 build+install+smoke PASS (running binary returns usage; no --version command). Pushed `b3efe42`. COPR build **10850873 SUCCEEDED**; live-COPR install PASS on fc44/rawhide/fc43 (`protonplus-0.6.3`, RPM_V_OK).
- **no-update.sh packages (32)**: GitHub releases/tags API + PyPI vs spec — ALL MATCH: aquamarine v0.14.0, glaze v8.0.0, hyprcursor v0.1.13, hyprgraphics v0.5.1, hypridle v0.1.8, hyprland v0.56.2 (hyprwm/Hyprland), hyprland-guiutils v0.2.2, hyprland-protocols v0.7.0, hyprland-qt-support v0.1.0, hyprlauncher v0.1.6, hyprlang v0.6.8, hyprlock v0.9.6, hyprpaper v0.8.4, hyprpicker v0.4.7, hyprpolkitagent v0.1.3, hyprqt6engine v0.1.0, hyprshutdown v0.1.1, hyprsunset v0.4.0, hyprsysteminfo v0.2.0, hyprtoolkit v0.5.4, hyprutils v0.14.0, hyprwayland-scanner v0.4.6, hyprwire v0.3.1, matugen v4.1.0, mpvpaper 1.9, nwg-look v1.1.1, starship v1.26.0, stoat-desktop v1.4.2 (stoatchat/for-desktop — latest tag v1.4.2, Linux asset valid HTTP 302; the roastydog repo is not the spec source), vesktop v1.6.5, waypaper 2.8, materialyoucolor PyPI 3.0.4, xdg-desktop-portal-hyprland v1.4.1.
- Inventory: 64 package dirs on main (63 prior + ghostty). No discrepancies found; hyprland-plugins/hyprland.spec sibling dependency behavior unchanged and correct.

### 3. Actions health (priority 3) — HEALTHY
- No failed workflow runs; only the current run is in_progress. The two known-benign "token lacks workflows scope" workflow-file failures (Aug 10) remain historical only.

### 4. Issues / PRs — NONE open (API-verified). Nothing to act on.

### 5. Install-test ledger (this run)
- ghostty 1.3.1-2: live COPR, fc44 + rawhide + fc43 — PASS (fresh binary).
- niri-git fec411e: live COPR, fc44 + rawhide + fc43 — PASS (fresh binary).
- protonplus 0.6.3: live COPR, fc44 + rawhide + fc43 — PASS (fresh binary).
- Coverage: every package whose binary changed since its last recorded test (ghostty, niri-git, protonplus) is now install-tested on ALL 3 enabled chroots this run. 64/64 repo packages install-tested at least once.

### 6. Dependency audit (64/64)
- No Requires/BuildRequires breakages observed; all 3 freshly-built packages resolve+verify cleanly on all chroots (rpm -V OK, no missing sonames in smoke paths tested). No changes made to existing deps this run.

### 7. NEW PACKAGE REGISTRATION LIMITATION (owner action needed — still pending from prior run)
- hyprlauncher 0.1.6 + hyprshutdown 0.1.1 specs verified (docker fc44 build+install PASS in run 31497000426) but COPR project (API-verified) has no records for them; webhooks only rebuild existing packages. Requires owner-level COPR access (`copr-cli add-package-*` or web UI, SCM source -> github.com/Ackerman-00/fedora-nexus, subdir hyprlauncher/hyprshutdown). Until then README caveat stands; not installable from the repo.

### 8. Final state
- Working tree clean. 64/64 versions current. COPR builds 10850852 (ghostty), 10850874 (niri-git), 10850873 (protonplus) all green on all 3 chroots; the two prior-run sweeps remain green. No open issues/PRs.
- Known gaps (documented, unchanged): (1) hyprlauncher/hyprshutdown not in COPR until owner registers (section 7); (2) full GUI smoke headless not possible for Electron/desktop apps (install+ldd+rpm -V verified instead); (3) COPR webhook direct API 403 (token scope) — verified empirically instead; (4) rawhide dist tag .fc45 pre-GA is COPR-side, monitored each run; (5) COPR download owner is `ackerman` (not `nexus-repo`).