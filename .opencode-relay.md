# fedora-nexus autonomous-run relay state

status: complete
run_id: 31324002858

## Run - RUN_ID 31324002858 (2026-08-09) - COMPLETED (2 fixes shipped + 1 new package shipped)

### 0. Pickup status
- Previous run (31305255499) declared `status: complete`; this run acted as an
  INDEPENDENT FRESH CHECKER and re-verified everything from scratch. HEAD at
  pickup: 56eb621, tree clean (later owner push 4619b1d "added: ly-greeter"
  arrived during the run and was folded in).
- Chroots (COPR API `chroot_repos`, authoritative): fedora-rawhide-x86_64
  (=F45), fedora-43-x86_64, fedora-44-x86_64. Unchanged; F45 mass-branching is
  2026-08-11 (not yet) and `follow_fedora_branching` is on.
- Inventory corrected this run: after the owner's push the repo has 61 dirs ==
  61 COPR packages (new `ly`). README table was 59 rows and was MISSING both
  `ly` AND `helium-browser`; fixed to 61 rows this run.
- No open issues, no open PRs (both verified empty via gh api at end of run).

### 1. COPR build status at end of run - ALL GREEN
- protonplus 10840548 (0.6.0-2) SUCCEEDED on all 3 chroots.
- ly 10840565 (1.4.1-1, its FIRST build-ever) SUCCEEDED on all 3 chroots
  (fedora-43-x86_64, fedora-44-x86_64, fedora-rawhide-x86_64).
- helium-browser 10840434 (0.15.3.1-1), heroic-games-launcher 10840416
  (2.22.1-4), localsend 10840377, opencode-desktop 10840376, bibata 10840375,
  cascadia 10840374 all succeeded.
- zen-browser: latest SUCCEEDED build is 10840070 (1.21.13b-1). The 404-SRPM
  failure (10840371, ~4.2s) was a transient re-trigger hitting deleted upstream
  1.21.13b assets (upstream re-ran its release workflow; current upstream
  latest = 1.21.12b). The shipped NVR 1.21.13b-1 is fine and strictly newer
  than upstream's current latest; the update.sh asset-existence guard (commit
  9a6e2bd) prevents any churn. NO ACTION TAKEN - verified, correct as-is.

### 2. DEFECT FOUND & FIXED #1 - protonplus 0.6.0 missing new zig deps (commit 346ce99)
- **Symptom (before):** auto-update 56eb621 bumped only `%global tag` to v0.6.0;
  COPR build 10840472 failed on all 3 chroots at meson configure:
  `ERROR: Dependency "sdl3" not found (tried pkg-config)`.
- **Fix:** added `BuildRequires: pkgconfig(sdl3) >= 3.2.0`, plus the other new
  upstream requires missed at the tag-only bump: `pkgconfig(appstream)`,
  `pkgconfig(cairo)`, `pkgconfig(gio-unix-2.0)`, `pkgconfig(libnotify)`.
  Bumped Release 1 -> 2 (`Release: 2%{?dist}`), changelog entry added.
- **Verification (symptom before -> absent after):**
  - Fedora 44 repodata cross-check: pkgconfig(sdl3)=SDL3-devel,
    pkgconfig(appstream)=appstream-devel, pkgconfig(cairo)=cairo-devel,
    pkgconfig(gio-unix-2.0)=glib2-devel, pkgconfig(libnotify)=libnotify-devel.
  - clean fedora:44 container reprobuild: `rpmspec -P` OK, `builddep` OK,
    `rpmbuild -bb` `BUILD_RC=0`, produced protonplus-0.6.0-2.fc44.x86_64.rpm.
  - clean fedora:44 container install from COPR: `DNF_RC=0`, `rpm -V` clean,
    ldd resolves libSDL3.so.0 and all new deps.
  - COPR build 10840548 triggered by the push; ended `succeeded` on all 3
    chroots (verified live from the API).

### 3. DEFECT FOUND & FIXED #2 - ly PIE patch would fail every COPR build (commit d64d711)
- **Symptom (before):** owner's new `ly/` (commit 4619b1d) shipped
  `0001-build-enable-pie.patch` with WRONG hunk line counts
  (`@@ -63,13 +63,15 @@` while the hunk actually spans `-63,14`), so GNU patch
  would fail at %prep with `patch unexpectedly ends in middle of line` - meaning
  the very FIRST COPR build of ly would fail.
- **Fix:** regenerated an equivalent patch (adds `exe.pie = true;` + blank line
  right after the addExecutable block, anchored to build.zig lines 72-79) that
  applies cleanly. No functional change, correct hunk offsets.
- **Verification (symptom before -> absent after):**
  - `patch -p1 --dry-run` on pristine build.zig from ly-1.4.1.tar.gz: OK.
  - clean fedora:44 container reprobuild: `rpmspec -P` OK, builddep OK,
    `spectool -g` OK (both Source0 codeberg archive and Source1 vendor tarball),
    `rpmbuild -bb` `BUILD_RC=0`.
  - local built rpm install in clean fedora:44 (COPR enabled): `INSTALL_RC=0`,
    `rpm -V ly` CLEAN, ldd resolves all libs, `/usr/bin/ly --help` prints usage.
  - Pushed -> COPR build 10840565 (FIRST build ever for ly) triggered,
    `succeeded` on all 3 chroots; confirmed the ly COPR package record exists
    (id 3194517, source_type scm/rpkg, auto_rebuild true) and produces
    builds on push.
  - Downloaded the COPR-SHIPPED rpm (200 from the `ly/` subdir path), installed
    in a clean fedora:44 container with ackerman/nexus enabled:
    `INSTALL_RC=0`, `rpm -V` CLEAN, `ly --version` -> `Ly version 1.4.1`.
- **Dependency availability verified for all 3 chroots:** zig 0.16.0 exists on
  F43 (zig-0.16.0-1.fc43) and F44 (0.16.0-1.fc44), rawhide (0.16.0-2.fc45);
  zig-rpm-macros ships on all three; pam-devel/xcb/help2man/systemd macros OK.
  Git-tag craft: codeberg ls-remote is flaky (empty twice) but the archive
  URL HEAD is 200; spec Version 1.4.1 matches the v1.4.1 release. Note: the
  GitHub mirror's `releases/latest` is stale (v1.0.3) - do NOT use it for ly.

### 4. README corrected (commit d64d711)
- Table was 59 rows and missing both `ly` and `helium-browser`. Added both
  (helium-browser summary taken from its spec Summary; ly = "Lightweight TUI
  display manager"), bumped header count to 61. 61 README rows == 61 repo dirs
  == 61 COPR packages.

### 5. Gaps (named, not silently skipped)
- GUI desktop apps (zen-browser, obsidian, vesktop, heroic, logseq, fluxer,
  rootapp, localsend, stoat, opencode-desktop, protonplus, extension-manager,
  ly) were not launched: runner is headless (no Wayland/X). Verified by
  install + `rpm -V` + reading version from the artifact (ly: `ly --version`).
- codeberg `git ls-remote` flaky (empty twice) - mitigated by archive HEAD 200
  + GitHub-mirror tag fallback in update.sh; noted, not a repo defect.
- COPR download server returned 500 for the flat results path; the real RPM
  lives under the `ly/` package subdir (200). Repo-agnostic infra quirk.
- The known COPR ghost-RPM bug (pre-existing, previously tracked) remains; not
  re-opened (rule: it is COPR infra, owns fixed in the prior run record).

### Closing note
Two real defects fixed end-to-end this run and BOTH verified by passing COPR
builds on all 3 chroots: protonplus 0.6.0-2 (build 10840548 - new zig-era deps
missing from the tag-only auto-update) and ly 1.4.1-1 (build 10840565, its very
first build - broken PIE patch hunk offsets). Plus one README correction
(59 -> 61 rows, adding the owner's new ly and the previously-missing
helium-browser). No open issues, no open PRs, working tree clean, HEAD d64d711.