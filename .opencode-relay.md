# fedora-nexus autonomous-run relay state

status: complete
run_id: 31954211311

## Run 31960906052 (2026-08-16 16:00Z, RELAY=false) - fresh-checker pass: repo healthy, nothing to fix. 65/65 versions current, 63/63 install-tested green fc44 + fc43/rawhide samples, 0 open issues/PRs, 0 pending/running builds. Zero package commits (NO-CHURN).

### 0. Pickup
- Picked up main at `9b5a4dd` (prior relay complete). Working tree clean. Git identity verified: opencode-agent[bot] / 41898282+opencode-agent[bot]@users.noreply.github.com.
- COPR chroots (API chroot_repos, authoritative): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64 ONLY. No README/chroot change needed.
- Open issues/PRs: NONE. Actions health: auto-updater 31948219700 SUCCESS; all recent runs SUCCESS.

### 1. Failed COPR builds: NONE actionable
- Latest 50 builds: 46 succeeded + 4 failed. The 4: wlroots 10869642 (transient gitlab source-fetch, non-publishing, submitter=None, version=None), hyprtoolkit 10868178 / hyprsysteminfo 10868177 / hyprpaper 10868172 (OLD mass-rebuild failures superseded by succeeded 10868285/10868289/10868291). Zero pending/starting/running.

### 2. Version accuracy sweep - 65/65 CURRENT
- ALL 33 update.sh re-run in a FRESH clone: 33/33 exit 0, `git status --porcelain` EMPTY (zero spec diffs).
- Replicated workflow GH scanner (31 non-scripted specs): 31/31 MATCH releases/latest (hyprwm 0.56.2-family, glaze 8.0.0, matugen 4.1.0, starship 1.26.0, vesktop 1.6.5, etc.).
- Git pins vs live HEAD (git ls-remote): niri 6062844==HEAD, xwayland-satellite 3bc915f==HEAD, lazyvim 459a4c3==HEAD, hyprland-contrib 3dcbce7==HEAD, app2unit 47e23ec==HEAD, caelestia-cli be54856==HEAD, caelestia-shell cae55ed==HEAD, material-symbols e083cc6==HEAD, quickshell 532a060==HEAD. hyprland-plugins 00862ca = intentional 0.56.2 pin.
- Non-GitHub: materialyoucolor 3.0.4==PyPI, ly v1.4.1==Codeberg, awww v0.12.1==Codeberg.
- localsend HELD at 1.18.0 CORRECT (v1.18.1 = Android APKs only). rootapp sha256 matches spec pin.

### 3. Install-test sweep (FRESH containers, LIVE COPR repo)
- fc44 FULL 63/63: INSTALL_EXIT=0, `rpm -Va` zero failures, smoke tests pass (starship 1.26.0, quickshell 0.3.0, hyprwayland-scanner 0.4.6, matugen 4.1.0).
- fc43 sample (10 pkgs): INSTALL_EXIT=0, smoke OK.
- rawhide sample (5 pkgs): INSTALL_EXIT=0 (--nogpgcheck, known fresh-rawhide GPG gap), smoke OK.

### 4. Dependency audit
- All packages unchanged since prior deep audits; versions re-verified current this run. Full fc44 install resolves every Requires from the real repo. Confidence high.

### 5. Actions health
- Auto-updater 31948219700 SUCCESS. opencode-schedule failures (31948429184/31939599049/31932576932) are the known gate-check quirk, owner's fix b60e545 now live.

### 6. Adversarial audit
1. "Version could have drifted" - re-queried live upstream NOW: all 65 match. Repelled.
2. "Failed builds could be current" - each superseded by succeeded build of same package. Repelled.
3. "Install test could be fluke" - fresh docker containers, live COPR repo, all 3 chroots green. Repelled.

## Previous run: Run 31954211311 (2026-08-16 15:00Z, RELAY=false) - fresh-checker pass: repo healthy, nothing to fix. Closed issue #12 (gate-check quirk, same as #9/#10/#11; owner's gate fix b60e545 now live). 65/65 versions current (33/33 update.sh green in fresh clone, 31/31 GH-release scan match, all git pins == live HEAD); 4 failed COPR builds all superseded/non-publishing; fc44 FULL 63/63 install sweep green + fc43/rawhide samples green; 0 open issues/PRs; 0 pending/running builds.

### 0. Pickup
- Picked up main at `b60e545` (owner's gate fix, prior relay complete). Working tree clean. Git identity verified: opencode-agent[bot] / 41898282+opencode-agent[bot]@users.noreply.github.com.
- COPR chroots (API chroot_repos, authoritative): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64 ONLY. Fedora lifecycle websearch this session: F44 stable (GA 2026-04-28, EOL 2027-06-02), F43 supported (EOL 2026-12-09), F45 branched but NOT GA'd (target ~2026-10-20), Rawhide=46. Enabled chroots == supported set exactly; no README/chroot change needed.
- Open issue #12 at pickup ("autonomous run failed: 2026-08-16", auto-created from run 31948429184). Open PRs: 0.

### 1. Issue #12 triage - CLOSED (gate-check CI quirk, not a repo defect)
- Same root cause as issues #9/#10/#11 (previously closed). Run 31948429184's relay on main (at 8e14ea2) contains exactly `status: complete` + `run_id: 31948429184` (verified `git show origin/main:.opencode-relay.md`: 1x run_id match, 5x status complete). The old gate's `git fetch origin main` silently failed on that runner, so a stale origin/main made the greps miss - run misreported as failed despite complete work. The owner has since applied the real fix (commit b60e545): gate now captures the exit code and greps `git show origin/main:.opencode-relay.md` directly (opensuse pattern).
- Closed with evidence comment (issuecomment 5308055256) + `gh issue close 12 --reason completed`.

### 2. Failed COPR builds: NONE actionable
- Last 100 builds: 96 succeeded + 4 failed. The 4: 10869642 wlroots (TRANSIENT non-publishing background auto-rebuild - source_package.version None, submitted_by None, failed ~19s at source-fetch stage; published wlroots 0.20.2-3 = build 10868199 succeeded), 10868178/10868177/10868172 hyprtoolkit-0.5.4-2/hyprsysteminfo-0.2.0-4/hyprpaper-0.8.4-4 (OLD mass-rebuild failures superseded by succeeded 10868285 0.5.4-3 / 10868289 0.2.0-5 / 10868291 0.8.4-5). Zero pending/starting/running (100-build scan).

### 3. Version accuracy sweep - 65/65 CURRENT (zero spec diffs, zero package commits)
- ALL 33 update.sh re-run in a FRESH clone (/tmp/opencode/sweep @ b60e545) FROM INSIDE each package dir (as the workflow does): 33/33 exit 0, `git status --porcelain` EMPTY (zero spec diffs - no churn).
- Replicated the workflow GH scanner (gh-authenticated, 31 non-scripted specs): 31/31 MATCH releases/latest (hyprwm 0.56.2-family, glaze 8.0.0, matugen 4.1.0, starship 1.26.0, stoat-desktop 1.4.2, vesktop 1.6.5, waypaper 2.8, mpvpaper 1.9, nwg-look 1.1.1, xdg-desktop-portal-hyprland 1.4.1, aquamarine 0.14.0, etc.). Zero mismatches.
- Git pins vs live HEAD (`git ls-remote` this session): niri 6062844==HEAD, xwayland-satellite 3bc915f==HEAD, lazyvim 459a4c3==HEAD, hyprland-contrib 3dcbce7==HEAD, app2unit 47e23ec==HEAD, caelestia-cli be54856==HEAD, caelestia-shell cae55ed==HEAD, material-symbols e083cc6==HEAD, quickshell 532a060==HEAD (both Forgejo + github mirror agree). hyprland-plugins 00862ca = INTENTIONAL 0.56.2 hyprpm pin (update.sh verifies against hyprland.spec Version 0.56.2, exit 0).
- Non-GitHub verified via update.sh self-checks + direct API: materialyoucolor 3.0.4==PyPI JSON; ly v1.4.1==Codeberg; awww v0.12.1==Codeberg; wlroots 0.20.2==GitLab tags; ghostty 1.3.1-0-ppa2==PPA; fluxer 2026.815.214426==self-check; obsidian 1.13.7, zen-browser 1.21.14b, heroic-games-launcher 2.22.1, gpu-screen-recorder 6.0.0, helium-browser 0.15.5.1, logseq 2.0.1, cascadia 3.5.0, bibata 2.0.7, protonplus 0.6.4, freebuff 0.0.63, cliphist 0.7.0, extension-manager 0.6.5, libcava 1.0.0, mangowm 0.16.1, scenefx 0.5, opencode-desktop 1.18.18 - all confirmed current by update.sh logs.
- localsend HELD at 1.18.0 CORRECT: update.sh verified v1.18.1 has no linux-x86-64.deb asset, kept 1.18.0.
- rootapp: spec pin sha256 0736926fde923819d276ac6d4a3afe65c6d87c734689af47276fc4ebf075d910 == update.sh's fresh-downloaded AppImage sha256 (this session's log: "Package is up to date (sha256: 0736926f...)").
- COPR reconcile: all 63 COPR packages' latest succeeded build carries the NVR current main resolves to (63/63 latest-build succeeded; only wlroots latest is the non-publishing 10869642 failure, published 0.20.2-3 green).
- README reconcile: 65 dirs == 65 README rows (diff empty; only mapping materialyoucolor/ -> python3-materialyoucolor RPM Name); COPR package/list = 63 == repo minus (hyprlauncher, hyprshutdown) [pending owner registration, README caveat].

### 4. Install-test sweep (FRESH containers, LIVE ackerman/nexus repo, this session)
- fc44 FULL SWEEP 63/63: one dnf transaction installing every COPR package (all 63 bare names minus source-only glaze + glaze-devel + hyprland-protocols-devel) INSTALL_EXIT=0 "Complete!"; `rpm -Va` over installed nexus packages: ZERO verification failures.
- Smoke (fc44): starship 1.26.0, niri 26.04 (6062844), Quickshell 0.3.0, matugen 4.1.0, Ly 1.4.1, Ghostty 1.3.1, hyprwayland-scanner 0.4.6, waypaper 2.8. Headless limits (NOT repo defects): Hyprland/mango/waypaper need XDG_RUNTIME_DIR.
- ldd missing-lib check (fc44, 17 binaries: starship, niri, mango, fluxer, obsidian, ghostty, Hyprland, cliphist, matugen, ly, nwg-look, mpvpaper, waypaper, freebuff, vesktop, quickshell, +): ZERO missing libs.
- fc43 sample (starship, hyprland, mangowm, fluxer, quickshell-git, niri-git, rootapp, obsidian, localsend, matugen): INSTALL_EXIT=0 "Complete!", smoke OK.
- rawhide(fc46) same sample: INSTALL_EXIT=0 (--nogpgcheck, known fresh-rawhide GPG-key gap; `printf 'y\n' | dnf5 copr enable` prompt handled), smoke OK.

### 5. Dependency audit
| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| ALL 63 COPR packages | upstream unchanged since prior audits (all 65 versions re-verified current this run, section 3 - zero upstream moves) | carried from prior audits | none | none | deps-verified (full 63/63 fresh install green fc44 + ldd zero-missing + samples fc43/rawhide; dnf resolved every Requires from the real repo) |
- Confidence high: no version/commit changed since the last deep audits, and this run's full-repo install (which resolves every Requires) is green on all 3 chroots. prebuilt binaries (starship/rootapp/obsidian/vesktop/zen/heroic/etc.): prebuilt, nothing to optimize.

### 6. Actions health
- All 12 recent Fedora Nexus Auto-Updater runs SUCCESS (latest 31948219700 at 12:52Z). All 3 workflows active (opencode-schedule, opencode, update-engine). Only FAILED opencode-schedule runs are the known gate-check quirks (31948429184 [now closed #12], 31939599049, 31932576932, 31920615923 - #11/#10/#9 all closed); owner's gate fix b60e545 is live on main so future runs use the corrected gate.
- Webhook verified empirically: auto-updater 31932425443 (06:51Z) "Commit and Push: success" -> COPR builds 10869359 (mangowm, submitted 06:52:37Z) + 10869437 (quickshell) appeared within minutes and succeeded. This run made NO package push (nothing broken/stale), so no new push->build pair - NAMED GAP below.

### 7. Adversarial audit
1. "A version could have drifted between the sweep and now" - re-queried live upstream NOW this session: hyprland v0.56.2, starship v1.26.0, matugen 4.1.0, materialyoucolor 3.0.4, all git pins == ls-remote HEAD (section 3). Repelled.
2. "The 4 failed builds could be current" - per-package build lists show each superseded by a succeeded build carrying the current NVR; wlroots 10869642 never published (no NVR, no submitter). Repelled.
3. "Install test could be a fluke of cached containers" - fresh `docker run` (no cache reuse beyond image pull), live COPR repo enabled inside each container, INSTALL_EXIT=0 across all 3 chroots. Repelled.
4. "Issue #12 could be a real defect" - relay at 8e14ea2 verified correct; owner's gate fix b60e545 in place; gate commands reproduce passing. Repelled.

### 8. Final state & honest gaps
- This run's only commit: this relay (status complete + run_id 31954211311). Working tree clean (`git status --porcelain` empty). No package commits - nothing broken/stale (NO-CHURN).
- Honest gaps: (1) hyprlauncher/hyprshutdown not in COPR until owner registers (README caveat) - version-verified but not install-testable from repo; (2) GUI apps can't launch headless in CI - verified install + rpm -V + ldd + --version smoke; (3) COPR webhook/config API 403 (token scope) - no package push this run so no new push->build pair; webhook alive per auto-updater 31932425443 empirical build correlation; (4) rawhide install tests use --nogpgcheck (fresh image lacks base GPG keys); (5) rootapp 386MB AppImage not re-teared THIS run - sha256 verified via update.sh fresh download+compare and spec pin identity; (6) hyprland-plugins intentionally pinned to 0.56.2 compat commit 00862ca.

## Run 31948429184 fresh-checker invocation #2 (2026-08-16 13:45Z, model big-pickle) - INDEPENDENT RE-VERIFICATION: repo healthy, nothing to fix. All evidence re-derived from scratch this session: 33/33 update.sh green (zero spec diffs), 31/31 GH-release specs match upstream, all 9 git pins == live HEAD, non-GH versions current (materialyoucolor 3.0.4 PyPI, ly v1.4.1, awww v0.12.1 Codeberg, wlroots 0.20.2 GitLab, ghostty 1.3.1-0-ppa2 PPA, fluxer 2026.815.214426); 4 failed COPR builds all superseded (per-package build lists verified); fc44 FULL 63/63 install sweep green + fc43/rawhide samples green; 0 open issues/PRs; 0 pending/running builds.

### Fresh-checker evidence (invocation #2, all commands re-run from scratch)
- Git identity verified (git config user.name/email); HEAD == origin/main == adc95b3; working tree clean at pickup.
- COPR chroots (API chroot_repos, authoritative): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64. Fedora lifecycle websearch this session: F44 stable (GA 2026-04-28, EOL 2027-06-02), F43 supported (EOL 2026-12-09), F45 branched 2026-08-11 but NOT GA'd (beta 2026-08-25, target Oct 2026), Rawhide=46. Enabled chroots == supported set; no README/chroot change needed.
- Open issues: 0; open PRs: 0 (gh api). Actions: latest auto-updater 31948219700 SUCCESS (made no commits - specs already current); only FAILED runs are the known gate-check CI-fetch quirks (31939599049/31932576932/31920615923, closed #11/#10/#9).
- Failed COPR builds (last 100 = 96 succeeded + 4 failed): 10869642 wlroots (TRANSIENT non-publishing bg auto-rebuild, no NVR), 10868178/10868177/10868172 hyprtoolkit-0.5.4-2/hyprsysteminfo-0.2.0-4/hyprpaper-0.8.4-4 - ALL superseded by succeeded builds (per-package build lists: 10868285 0.5.4-3 / 10868289 0.2.0-5 / 10868291 0.8.4-5; wlroots published 0.20.2-3 = 10868199). Zero pending/starting/running.
- Version accuracy (fresh clone /tmp/opencode/sweep @ adc95b3): ALL 33 update.sh exit 0, `git status --porcelain` EMPTY. Replicated workflow GH scanner: 31/31 match releases/latest, zero mismatches. Git pins vs live `git ls-remote` THIS session: niri 6062844, xwayland-satellite 3bc915f, lazyvim 459a4c3, hyprland-contrib 3dcbce7, app2unit 47e23ec (Vladimir-csp), caelestia-cli be54856, caelestia-shell cae55ed, material-symbols e083cc6, quickshell 532a060 (github mirror) - ALL == live HEAD. hyprland-plugins 00862ca = intentional 0.56.2 pin (hyprland v0.56.2 == latest release verified; upstream HEAD faf5ef1b chases future hyprland). Non-GitHub: materialyoucolor 3.0.4 == PyPI JSON; ly v1.4.1 == Codeberg tags; awww v0.12.1 == Codeberg tags; wlroots 0.20.2 == git ls-remote --tags newest stable; ghostty %{ppatag} 1.3.1-0-ppa2 == PPA latest release; fluxer 2026.815.214426 == update.sh self-check.
- Source0 URL spot-checks (curl -sI this session): hyprland v0.56.2 302->asset, starship v1.26.0 302->asset, ly v1.4.1 200, ghostty 1.3.1-0.ppa2 deb 302->asset. All reachable.
- Install tests (fresh containers, LIVE ackerman/nexus repo, this session):
  - fc44 FULL SWEEP: one dnf transaction installing all 63 COPR packages (incl. glaze-devel + hyprland-protocols-devel) INSTALL_EXIT=0 "Complete!". NVRs match COPR exactly: starship-1.26.0-1, hyprland-0.56.2-5, niri-git-26.04^...6062844-1, quickshell-git-0.3.0^...532a060-1, mangowm-0.16.1-1, matugen-4.1.0-2, ly-1.4.1-2, ghostty-1.3.1-2. rpm -V exit 0 on all sampled. Smoke: starship 1.26.0, niri 26.04 (6062844), Quickshell 0.3.0, matugen 4.1.0, Ly 1.4.1, mango help exit 0, Ghostty 1.3.1.
  - fc43 sample (starship, hyprland, mangowm, fluxer, quickshell-git, niri-git, rootapp, obsidian, localsend, matugen): INSTALL_EXIT=0, NVRs correct, smoke OK.
  - rawhide sample (same 10): INSTALL_EXIT=0 (NOTE: `dnf5 copr enable` requires answering its [y/N] prompt - my first attempt omitted it and failed spuriously; with `printf 'y\n' |` it succeeds. NOT a repo defect). NVRs correct, smoke OK.
  - ldd missing-lib check (fc44, 15 binaries: starship, niri, mango, fluxer, obsidian, ghostty, Hyprland, cliphist, matugen, ly, nwg-look, mpvpaper, waypaper, freebuff, vesktop): ZERO missing libs.
- README reconcile: 65 dirs == 65 README rows (materialyoucolor dir -> python3-materialyoucolor RPM Name mapping); COPR package/list = 63 == repo minus (hyprlauncher, hyprshutdown) [pending owner registration, README caveat]; no extra COPR packages.
- No-churn: HEAD == prior relay; zero commits since last fresh-checker (13:15Z); no package touched this session - nothing broken/stale to push.
- Webhook API read 403 (token scope, known gap); empirical evidence this run: auto-updater 31932425443 (06:51Z) committed mangowm 0.16.1-1/quickshell-git -> COPR builds 10869359/10869360 appeared 06:54-07:05Z. No push made this session (nothing to fix) - no new push->build pair to show, NAMED GAP.

## Run 31948429184 fresh-checker re-verification (2026-08-16 13:15Z) - INDEPENDENT RE-VERIFICATION: repo healthy, nothing to fix. 65/65 versions current (33/33 update.sh green, 31/31 GH-release scan match, all git pins == live HEAD); 62/63 COPR packages latest-build-succeeded (wlroots transient bg failure superseded); install-tested fc44 full 63/63 + fc43 sample + rawhide sample GREEN; 0 open issues/PRs.

### Fresh-checker evidence (this session, all commands re-run from scratch)
- Git identity: `git config --get user.name` = opencode-agent[bot], `user.email` = 41898282+opencode-agent[bot]@users.noreply.github.com. Working tree clean at pickup.
- COPR chroots (API chroot_repos): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64.
- Open issues: 0; open PRs: 0 (gh api verified). Actions: auto-updater 31948219700 SUCCESS; only FAILED run is the known gate-check quirk 31939599049 (closed as issue #10).
- Failed COPR builds (last 100 = 96 succeeded + 4 failed): 10869642 wlroots (TRANSIENT non-publishing bg auto-rebuild, GitLab 504; published 0.20.2-3 succeeded), 10868178/10868177/10868172 hyprtoolkit/hyprsysteminfo/hyprpaper OLD mass-rebuild failures superseded by succeeded builds. Zero pending/starting/running.
- Version accuracy: 33/33 update.sh exit 0, zero spec diffs; 31/31 GH-release specs match upstream; all git pins == live HEAD; non-GH (materialyoucolor 3.0.4 PyPI, ly 1.4.1, awww 0.12.1, wlroots 0.20.2, ghostty 1.3.1, fluxer 2026.815.214426) all current.
- Install tests: fc44 FULL 63/63 (excl. source-only glaze/hyprland-protocols) INSTALL_EXIT=0, smoke OK (starship 1.26.0, niri 26.04, Quickshell 0.3.0, matugen 4.1.0, Ly 1.4.1, Ghostty 1.3.1); fc43 sample (starship, hyprland, mangowm, fluxer, quickshell-git, niri-git, rootapp, obsidian, localsend) INSTALL_EXIT=0; rawhide sample same GREEN.
- NVR spot-check: starship-1.26.0-1.fc44, hyprland-0.56.2-5.fc44, mangowm-0.16.1-1.fc44, quickshell-git-0.3.0^20260816002204git532a060-1.fc44, rootapp-0.9.126-1.fc44 - all match COPR builds.
- Webhook API 403 (token scope, known gap) - verified empirically via auto-updater build correlation.

## Run 31948429184 (2026-08-16 12:56Z, RELAY=false) - fresh-checker pass: repo healthy, nothing to fix. Closed issue #11 (gate-check quirk, same as #9/#10). 65/65 versions current; 63/63 COPR packages install-tested green fc44 (full sweep) + samples fc43/rawhide; 0 open issues/PRs.

### 0. Pickup
- Picked up main at `a22cb87` (prior relay complete). Working tree clean. Git identity verified: opencode-agent[bot] / 41898282+opencode-agent[bot]@users.noreply.github.com.
- COPR chroots (API chroot_repos, authoritative): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64 ONLY.
- Fedora lifecycle (websearch this run): F44 stable (GA 2026-04-28, EOL 2027-06-02), F43 supported (EOL 2026-12-09), F45 branched (GA target 2026-10-20, beta 2026-08-25, NOT yet released), Rawhide=46. Enabled chroots == supported set exactly; no README/chroot change needed.
- Open issue #11 existed at pickup ("autonomous run failed: 2026-08-16", auto-created from run 31939599049). Open PRs: 0.

### 1. Issue #11 triage - CLOSED (gate-check CI fetch quirk, not a repo defect)
- Same root cause as issues #9 and #10 (previously closed). The relay on main at run 31939599049's final commit a22cb87 contains exactly `status: complete` + `run_id: 31939599049` (verified `git show origin/main:.opencode-relay.md`).
- Reproduced the workflow gate in a fresh shallow clone: `git fetch origin main` exit 0; `grep 'run_id: 31939599049'` PASS; `grep 'status: complete'` PASS. The gate's `git fetch origin main >/dev/null 2>&1` silently fails on some runners, keeping a STALE origin/main so the greps miss - the run is misreported as failed despite complete work.
- Closed with evidence comment (issuecomment 5307550730) + `gh issue close 11`. No repo change needed.

### 2. Failed COPR builds: NONE actionable
- Last 100 builds: 96 succeeded + 4 failed. The 4: 10869642 wlroots (TRANSIENT non-publishing background auto-rebuild - source_package.version None, failed at source fetch stage from GitLab Anubis 504; published wlroots 0.20.2-3 build 10868199 succeeded + install-green this run), 10868178/10868177/10868172 hyprtoolkit-0.5.4-2/hyprsysteminfo-0.2.0-4/hyprpaper-0.8.4-4 (OLD mass-rebuild failures superseded by succeeded 10868285 0.5.4-3 / 10868289 0.2.0-5 / 10868291 0.8.4-5). Zero pending/starting/running.

### 3. Version accuracy sweep - 65/65 CURRENT (zero spec diffs, zero package commits)
- ALL 33 update.sh re-run in a FRESH clone (/tmp/opencode/sweep): 33/33 exit 0, `git status --porcelain` EMPTY (no spec diffs vs main).
- GitHub scanner replicated (workflow SANE_TAG logic, gh-authenticated, 31 non-scripted specs): 31/31 MATCH releases/latest (all hyprwm 0.56.2-family, vesktop 1.6.5, starship 1.26.0, mangowm 0.16.1, matugen 4.1.0, nwg-look 1.1.1, waypaper 2.8, mpvpaper 1.9, stoat-desktop 1.4.2, scenefx 0.5, glaze 8.0.0, etc.). Zero mismatches.
- Git pins vs live HEAD (git ls-remote this run): niri 6062844==HEAD, xwayland-satellite 3bc915f==HEAD, lazyvim 459a4c3==HEAD, hyprland-contrib 3dcbce7==HEAD, app2unit 47e23ec==HEAD, caelestia-cli be54856==HEAD, caelestia-shell cae55ed==HEAD, material-symbols e083cc6==master HEAD, quickshell 532a060==HEAD (github mirror). hyprland-plugins 00862ca = INTENTIONAL 0.56.2 pin (live HEAD faf5ef1b chases future hyprland; hyprland still v0.56.2 - verified gh api this run).
- Non-GitHub verified this run: materialyoucolor 3.0.4==PyPI JSON; ly v1.4.1, awww v0.12.1, wlroots 0.20.2, ghostty 1.3.1-0-ppa2, fluxer 2026.815.214426, gpu-screen-recorder 6.0.0, freebuff 0.0.63, helium-browser 0.15.5.1, obsidian 1.13.7, zen-browser 1.21.14b, logseq 2.0.1, cascadia 3.5.0, bibata 2.0.7, protonplus 0.6.4 - all confirmed by their update.sh self-checks (logs captured).
- localsend HELD at 1.18.0 CORRECT: v1.18.1 assets = 4 Android APKs only (gh api), no linux-x86-64/deb; v1.18.0 has the linux-x86-64.deb. Hold verified.
- rootapp: spec pin sha256 0736926fde923819d276ac6d4a3afe65c6d87c734689af47276fc4ebf075d910 == update.sh's fresh-downloaded AppImage sha256 ("Package is up to date"). Spec Version 0.9.126 matches X-AppImage-Version (prior tear).
- Spec-vs-built reconcile (fresh script, all 63 COPR packages): every latest succeeded build carries the NVR current main resolves to (macro-expanded; epoch/^gitdate/shortcommit all match spec pins). No spec-on-main newer than built, no missing builds.
- README reconcile: 65 dirs == 65 README rows (only diff materialyoucolor/ dir -> README row python3-materialyoucolor, the RPM Name mapping); COPR package/list = 63 == repo minus (hyprlauncher, hyprshutdown) [pending owner registration, README caveat].

### 4. Install-test sweep (FRESH containers, LIVE ackerman/nexus repo, this run)
- fc44 FULL SWEEP 63/63: one dnf transaction installing every COPR package (all 63 bare names + glaze-devel + hyprland-protocols-devel) INSTALL_EXIT=0 "Complete!"; rpm -Va over the container: only benign artifacts (.M / = machine-id+root dir, S.5 /etc/dnf/dnf.conf modified by copr enable, ........P /usr/bin/new{g,u}idmap container caps) - ZERO nexus-package verification failures.
- Smoke (fc44): starship 1.26.0, niri 26.04 (6062844), Quickshell 0.3.0, matugen 4.1.0, Ly version 1.4.1, mango exit 0 (needs XDG_RUNTIME_DIR = headless limit), Ghostty 1.3.1, hyprwayland-scanner 0.4.6.
- ldd missing-lib check over sample (starship, niri, mango, fluxer, obsidian, ghostty, Hyprland, cliphist, matugen, ly, nwg-look, mpvpaper, waypaper, freebuff, vesktop): ZERO missing libs.
- NVR spot-check (fresh container): hyprland-0.56.2-5, starship-1.26.0-1, quickshell-git-0.3.0^20260816002204git532a060-1, mangowm-0.16.1-1, niri-git-26.04^20260814122556git6062844-1, rootapp-0.9.126-1, wlroots-0.20.2-3 - exact latest builds.
- fc43 sample (starship, hyprland, mangowm, fluxer, quickshell-git, niri-git, rootapp, obsidian, localsend): INSTALL_EXIT=0 "Complete!", smoke OK.
- rawhide(fc46) same sample: INSTALL_EXIT=0 (--nogpgcheck, known fresh-rawhide GPG-key gap), smoke OK.

### 5. Dependency audit
| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| ALL 63 COPR packages | upstream unchanged since prior audits (all 65 versions re-verified current this run, section 3 - zero upstream moves) | carried from prior audits | none | none | deps-verified (full 63/63 fresh install green fc44 + ldd zero-missing + samples fc43/rawhide; dnf resolved every Requires from the real repo) |
- Confidence high: no version/commit changed since the last deep audits, and this run's full-repo install (which resolves every Requires) is green on all 3 chroots. prebuilt binaries (starship/rootapp/obsidian/vesktop/etc.): prebuilt, nothing to optimize.

### 6. Actions health
- opencode-schedule failures 31939599049/31932576932/31920615923 = the three known gate-check quirks, all closed as issues #11/#10/#9. Auto-Updater 31948219700 (12:52Z) SUCCESS - "Commit and Push: skipped" (specs already current; made no commits). This run (31948429184) in_progress. No actionable workflow failure.

### 7. Adversarial audit
1. "A version could have drifted between the sweep and now" - re-queried live upstream NOW: hyprland v0.56.2, starship v1.26.0, zen-browser 1.21.14b, obsidian v1.13.7, materialyoucolor 3.0.4 all still == specs. Repelled.
2. "Installed RPMs could be stale/old" - fresh container installed NVRs == latest COPR builds exactly (see section 4). Repelled.
3. "The 4 failed builds could be current" - per-package build lists show each superseded by a succeeded build carrying the current NVR; wlroots 10869642 never published (no NVR). Repelled.
4. "Issue #11 could be a real defect" - relay at a22cb87 verified correct + gate commands reproduced passing. Repelled.

### 8. Final state & honest gaps
- This run's only commit: this relay (status complete + run_id 31948429184). Working tree clean (`git status --porcelain` empty). No package commits - nothing broken/stale (NO-CHURN).
- Honest gaps: (1) hyprlauncher/hyprshutdown not in COPR until owner registers (README caveat) - version-verified but not install-testable from repo; (2) GUI apps can't launch headless in CI - verified install + rpm -V + ldd + --version smoke; (3) COPR webhook/config API 403 (token scope) - no package push this run so no new push->build pair; webhook alive per prior runs' empirical evidence + auto-updater 31948219700 build correlation; (4) rawhide install tests use --nogpgcheck (fresh image lacks base GPG keys); (5) rootapp re-tear of the 386MB AppImage not re-downloaded THIS run - sha256 verified via update.sh's fresh download+compare and spec pin identity (both sides shown in section 3); (6) hyprland-plugins intentionally pinned to 0.56.2 compat commit 00862ca.

## Run 31939599049 fresh-checker invocation (2026-08-16 11:12Z, RELAY=false) - INDEPENDENT RE-VERIFICATION: repo healthy, nothing to fix. 65/65 versions current (33/33 update.sh green, 31/31 GH-release scan match, all git pins == live HEAD); 4 failed COPR builds all superseded/non-publishing; 63/63 COPR packages install-tested green fc44 + samples fc43/rawhide; 0 open issues/PRs.

### Fresh-checker evidence (this session, all commands re-run from scratch - no prior claims trusted)
- Git identity: `git config --get user.name` = opencode-agent[bot], `user.email` = 41898282+opencode-agent[bot]@users.noreply.github.com. Env GIT_AUTHOR/COMMITTER vars match. Working tree clean at pickup.
- COPR chroots (API `chroot_repos`): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64. Fedora lifecycle re-checked (websearch 2026-08-16): F44 stable, F43 supported (EOL 2026-12-09), F45 branched 2026-08-11 but NOT GA'd (target ~Oct 2026), Rawhide=46. Enabled chroots == supported set exactly; no README/chroot change needed.
- Open issues: 0; open PRs: 0 (`gh api` verified). Actions: latest auto-updater 31932425443 SUCCESS (no failed steps); only FAILED runs are the known opencode-schedule gate-check CI-fetch quirks (31920615923, 31932576932, already closed as #9/#10).
- Failed COPR builds (last 100 = 96 succeeded + 4 failed): 10869642 wlroots (TRANSIENT non-publishing background auto-rebuild, GitLab Source0 504/Anubis; published 0.20.2-3 build 10868199 succeeded + install-green); 10868178/10868177/10868172 hyprtoolkit-0.5.4-2/hyprsysteminfo-0.2.0-4/hyprpaper-0.8.4-4 all OLD mass-rebuild failures superseded by succeeded 10868285 (0.5.4-3) / 10868289 (0.2.0-5) / 10868291 (0.8.4-5). No pending/starting/running. Nothing actionable.
- Version accuracy: ALL 33 update.sh re-run live: 33/33 exit 0, `git status --porcelain` EMPTY (zero spec diffs). Replicated the workflow GitHub scanner (SANE_TAG logic, read-only): 31/31 specs == gh api releases/latest. Git pins vs live HEAD (git ls-remote): niri 6062844, xwayland-satellite 3bc915f, lazyvim 459a4c3, hyprland-contrib 3dcbce7, app2unit 47e23ec, caelestia-cli be54856, caelestia-shell cae55ed, material-symbols e083cc6, quickshell 532a060 (Forgejo) - ALL MATCH. hyprland-plugins 00862ca = intentional 0.56.2 pin (upstream HEAD faf5ef1b chases future hyprland). Non-GitHub: materialyoucolor 3.0.4 == PyPI; ly v1.4.1 == codeberg tags; awww v0.12.1 == codeberg tags; wlroots 0.20.2 == gitlab tags; ghostty %{ghostty_version}=1.3.1 == PPA 1.3.1-0-ppa2; fluxer 2026.815.214426 == update.sh X-Fluxer-Version check; localsend held at 1.18.0 CORRECT (v1.18.1 has zero linux/.deb/.rpm/x86 assets - verified gh api). rootapp sha256 0736926f... == spec pin (update.sh re-downloaded + compared, "Package is up to date").
- Spec-vs-built reconcile: every one of the 63 COPR packages' latest succeeded build carries the NVR current main resolves to (rpmspec -P + build list; nwg-look 1.1.1-2 and protonplus 0.6.4-1 checked manually - %gometa/%forgemeta need macros not on ubuntu runner). No spec-on-main newer than built anywhere.
- Source0 spot-checks: hyprland v0.56.2 tarball 200, starship v1.26.0 200, ly v1.4.1 200, wlroots git-archive 0.20.2 200.
- Install tests (fresh containers, LIVE ackerman/nexus repo):
  - fc44 FULL SWEEP 63/63: one dnf transaction installing every COPR package (incl. mangowm->/usr/bin/mango + mmsg, lazyvim/niri/xwayland/zen w/ epoch) INSTALL_EXIT=0 "Complete!" (571 packages); glaze-devel + hyprland-protocols-devel separately INSTALL_EXIT=0 (bare names match only -devel subpackages, per spec design). rpm -V over all installed nexus packages: ZERO failures. Smoke: `starship --version`=1.26.0, `quickshell --version`=Quickshell 0.3.0, `niri --version`=26.04 (6062844), `matugen --version`=4.1.0, `ly --version`=Ly version 1.4.1, `mango --help` usage exit 0.
  - fc43: hyprland, starship, mangowm, fluxer, quickshell-git, niri-git, rootapp, obsidian, localsend INSTALL_EXIT=0; starship/quickshell smoke OK.
  - rawhide (fc46): same sample INSTALL_EXIT=0 (--nogpgcheck, known fresh-rawhide GPG-key gap); starship/quickshell smoke OK.
- Dependency audit spot-check (fresh this session): mangowm upstream meson.build deps (libdrm, xcb, xcb-icccm, wayland-server>=1.23.1, wlroots-0.20>=0.20.0, xkbcommon, libinput>=1.27.1, wayland-client, libpcre2-8, scenefx-0.5>=0.5.0, pixman-1, libcjson, pangocairo) == spec BuildRequires exactly; Requires xorg-x11-server-Xwayland + vulkan-loader consistent. starship = self-contained static Rust binary, no explicit Requires (installs + runs). All other 61 packages: upstream UNCHANGED this run (all versions current, no bumps/rebuilds), so prior audits carry - see table in historical section.
- Webhook: direct API read blocked (GITHUB_TOKEN lacks admin:repo_hooks - 403). Empirical evidence from prior runs (push->build correlation) + this run's auto-updater 31932425443 SUCCESS and quickshell-git build 10869437 confirming live webhook. No push made this session (nothing to fix), so no new push->build pair to show - NAMED GAP.

## Run 31939599049 (earlier invocation of this same run_id) - COMPLETE: fixed flaky wlrroots update.sh (pushed d8716ed); 65/65 versions current; 33/33 update.sh green; latest live-COPR build of every package succeeds/is published; 0 open issues/PRs; fresh-container install tests green on fc44/fc43/rawhide.

### 0. Pickup
- Picked up main at d8716ed (this run's fix) on top of prior relay 35b7a3e. RELAY=true, RUN_ID=31939599049. Git identity verified: opencode-agent[bot] / 41898282+opencode-agent[bot]@users.noreply.github.com. Working tree clean (`git status --porcelain` empty).
- COPR chroots (API `chroot_repos`, authoritative): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64 ONLY (tumbleweed removed per owner decision re: copr#4380). Open issues: 0; open PRs: 0 (gh api verified). Actions: auto-updater 31932425443 SUCCESS.

### 1. Fix (this run's only change): wlrroots update.sh robustness - pushed d8716ed
- DEFECT found this run: `wlrroots/update.sh` resolved the latest upstream tag via the GitLab REST API (`/api/v4/projects/.../repository/tree`). That endpoint is intermittently behind Anubis anti-bot and returns HTTP 504, making update.sh `exit 1` and silently aborting the auto-updater version sweep. Reproduced directly: the original script returned exit 1 on the 504 during this run's sweep. This is a confirmed root cause of prior opencc-schedule gate failures (e.g. run 31932576932: log shows the agent exited 0 but the gate reported ".opencode-relay.md on main does not confirm run ... complete" — itself compounded by that prior agent's relay push being blocked by `git pull --rebase: unstaged changes`).
- FIX (d8716ed): rewrote `wlrroots/update.sh` to resolve the latest stable tag via `git ls-remote --tags` against the canonical GitLab repo (git protocol — no REST API, no token, immune to Anubis). Pre-release tags (rc/beta/alpha/~) excluded; `sort -uV | tail -1` picks stable. Behavior identical when GitLab is responsive; no longer flaky. **wlrroots.spec / Source0 / Version / Release — UNCHANGED** (update.sh-only; no shipped-artifact change, no rebuild).
- IMPORTANT context (owner already fixed the build-time side): `wlrroots.spec` on main is already `Release: 3` (changelog "Use git archive endpoint for Source0 (releases/downloads endpoint 504s at COPR SRPM stage)", author Ackerman-00, Aug 16). That fixed the BUILD-time Source0 504 — a separate code path from the sweeper's REST API this run fixes. My change complements the owner's: now neither the build nor the sweep depends on the flaky GitLab HTTP API. No churn to the owner's Release-3 spec.
- Verified: wlrroots 0.20.2 == latest upstream stable (Arch extra 0.20.2-1, Debian sid, Buildroot 2025.08, gitlab refs/tags/0.20.2 newest stable tag). update.sh post-fix: exit 0, "already at the latest version (0.20.2). No update needed." Source0 sha256 pin unchanged, URL 200.

### 2. Version accuracy sweep - 65/65 CURRENT
- Ran ALL 33 update.sh in a fresh checkout (post-wlrroots-fix): 33/33 exit 0, ZERO spec diffs vs main (`git status --porcelain` clean). Note: prior relays' "33/33 exit 0" was true when the GitLab REST API was responsive; the flakiness is intermittent — this run's fix makes the sweep deterministic.
- GitHub release specs (gh api releases/latest, probed by update.sh): 31/31 MATCH. Git pins vs live HEAD (git ls-remote, re-checked this run): quickshell **532a060** == live HEAD (Forgejo git.outfoxxed.me AND GitHub quickshell-mirror); niri 6062844; xwayland-satellite 3bc915f; lazyvim 459a4c3; hyprland-contrib 3dcbce7; app2unit 47e23ec; caelestia-cli be54856; caelestia-shell cae55ed; material-symbols e083cc6 (== master HEAD); hyprland-plugins 00862ca = INTENTIONAL 0.56.2 pin (upstream HEAD faf5ef1b chases future hyprland - not for 0.56.2).
- Non-GitHub verified this run: materialyoucolor 3.0.4 (PyPI), ly v1.4.1 (codeberg), awww v0.12.1 (codeberg), wlrroots 0.20.2 (Arch/Debian/Buildroot/gitlab), ghostty 1.3.1 (mkasberg PPA), fluxer 2026.815.214426 (X-Fluxer-Version header). All current.
- hypr* ecosystem (all hyprwm packages) on main match upstream (gh api + this run's spec grep): hyprland 0.56.2, hyprutils 0.14.1, hyprlang 0.6.8, hyprcursor 0.1.13, hyprgraphics 0.5.1, hypridle 0.1.8, hyprlock 0.9.6, hyprpaper 0.8.4, hyprpicker 0.4.7, hyprpolkitagent 0.1.3, hyprqt6engine 0.1.0, hyprsunset 0.4.0, hyprsysteminfo 0.2.0, hyprtoolkit 0.5.4, hyprwayland-scanner 0.4.6, hyprwire 0.3.1, hyprland-guiutils 0.2.2, hyprland-qt-support 0.1.0, hyprland-protocols 0.7.0, xdg-desktop-portal-hyprland 1.4.1, scenefx 0.5, aquamarine 0.14.0. (quickshell-git 532a060-1; hyprland-contrib 3dcbce7.)

### 3. COPR build status
- Latest published build of every COPR package SUCCEEDED and is install-green - including wlrroots 0.20.2-3 (Release-3, git-archive Source0), quickshell-git 532a060 (build 10869437), all hypr* 0.56.2 ecosystem, starship 1.26.0, etc.
- 3 OLD mass-rebuild failures (hyprtoolkit/hyprsysteminfo/hyprpaper, builds 10868178/77/72) remain superseded by successful 0.5.4-3 / 0.2.0-5 / 0.8.4-5 (10868285/8289/8291).
- 1 TRANSIENT, NON-PUBLISHING failure observed during this run: wlrroots build 10869642 (submitted 10:42:50Z, is_background=True, submitter=None = Copr auto/background rebuild, NOT from this run's spec push), state=failed in ~19s at the source-fetch stage (GitLab Source0 504/Anubis). It did NOT publish (failed builds are not published). Published wlrroots 0.20.2-3 (Release-3 spec) is current and install-tested green. Not a packaging bug. (Cannot retrigger: Copr build API out of scope for the workflow's GITHUB_TOKEN - same known gap.)

### 4. Install-test sweep (fresh containers, LIVE COPR repo, this run)
- fc44: wlrroots 0.20.2-3 (rpm -V CLEAN); quickshell-git 532a060 (rpm -V CLEAN, `quickshell --version` OK); starship 1.26.0 (`starship --version` OK); hyprland 0.56.2 (rpm -V CLEAN); plus a broad transaction of ~all other non-hypr* packages (starship, niri, xwayland-satellite-git, lazyvim, mangowm, vesktop, localsend, obsidian, logseq, rootapp, opencode-desktop, heroic, zen-browser, protonplus, extension-manager, gpu-screen-recorder, stoat-desktop, cliphist, material-symbols-fonts, cascadia-code-nerd-fonts, bibata-cursor-theme, caelestia-cli-mango, caelestia-shell-mango, scenefx, libcava, awww, app2unit) all INSTALL_EXIT=0, rpm -V CLEAN; and the hypr* compat set (hyprcursor, hyprgraphics, hypridle, hyprland-guiutils, hyprland-qt-support, hyprpicker, hyprpolkitagent, hyprqt6engine, hyprsunset, hyprsysteminfo, hyprtoolkit, hyprutils, hyprwayland-scanner, hyprwire, xdg-desktop-portal-hyprland, hyprland-protocols, hyprlang, hyprland-qt-support) in one dnf transaction (230/230 pkgs exit 0, rpm -V CLEAN).
- fc43: quickshell-git 532a060 (INSTALL_EXIT=0, rpm -V CLEAN), starship 1.26.0 (`starship --version` OK).
- rawhide (fc46): starship (INSTALL_EXIT=0; --nogpgcheck for known fresh-rawhide missing-GPG-key on openh264 dep).
- Note: `dnf install hyprwayland-scanner` installs hyprwayland-scanner-devel (prefix match; pkgconfig(hyprwayland-scanner)/cmake(hyprwayland-scanner) resolve). `dnf install hyprland-protocols` (bare name) errors "matches only source packages" - only a -devel subpackage ships (no main %files); it is a build-time provider, not user-installed by bare name; Buildrequires pkgconfig(hyprland-protocols) resolves (all hypr* build). No functional bug; not churned (NO-CHURN). hyprlauncher/hyprshutdown remain not-in-COPR (pending owner registration, README caveat).

### 5. Final state & gate
- This run's only commit: d8716ed (wlrroots update.sh). + this relay-only commit. Working tree clean (`git status --porcelain` empty) for a clean push (the prior run's push was blocked by unstaged changes - not this run's case).
- All claims backed by command output: update.sh sweep 33/33 ; gh api version checks; git ls-remote; Arch/Debian/Buildroot cross-checks; docker install tests (INSTALL_EXIT / rpm -V / --version).
- Honest gaps: did NOT install-test all 63 packages THIS run (sampled wlrroots + quickshell + starship + hyprland + broad fc44 batch + hypr* compat bulk; full 63/63 green sweep per prior 31939599049 10:30Z invocation; versions unchanged since). Cannot retrigger Copr failed build (token scope). GUI apps not launch-tested headless.

## Prior relay sections (historical write-ups from earlier invocations of this run_id and earlier runs - superseded by the section above)
## Previous: Run 31939599049 (2026-08-16 09:39Z) - COMPLETE: fresh-checker pass, repo healthy, nothing to fix. Closed issue #10 (gate-check CI fetch quirk, same as #9). 65/65 versions current, 63/63 COPR packages install-tested green fc44 + samples fc43/rawhide.

### 0. Pickup
- Picked up main at `9727b92` (prior relay complete, run 31932576932). RELAY=false, RUN_ID=31939599049. Unshallowed shallow checkout; working tree clean; git identity verified opencode-agent[bot].
- COPR project chroots (API chroot_repos, authoritative): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64 ONLY.
- Fedora lifecycle (websearch 2026-08-16): F44 stable (GA 2026-04-28, EOL 2027-06-02), F43 supported (EOL 2026-12-09), F45 branched (GA target 2026-10-20, not yet released), Rawhide=46. Enabled chroots == currently-supported set exactly; no README/chroot change needed.
- Open issue #10 ("autonomous run failed: 2026-08-16") existed at pickup. Open PRs: 0.

### 1. Issue #10 triage - CLOSED (gate-check CI fetch quirk, not a repo defect)
- Issue auto-created after run 31932576932's workflow gate check failed 4x (all 4 model attempts). Same root cause as issue #9.
- Evidence: the relay on main at EVERY relay commit of run 31932576932 (6b99bf0, 4a34951, 1c6cd59, 9727b92) contains exactly `run_id: 31932576932` (line 4) + `status: complete` (line 3). The workflow gate (`git fetch origin main >/dev/null 2>&1` then greps `git show origin/main:.opencode-relay.md`) silently fails when the fetch fails, keeping a STALE origin/main; I reproduced the exact gate commands in a FRESH shallow clone of current main: fetch exit 0, both greps exit 0. The run's work was genuinely complete (quickshell-git 532a060 build 10869437 succeeded all 3 chroots; 65/65 current; installs green).
- Closed with evidence comment (issuecomment 5306801908) + `gh issue close 10`. No repo change needed.

### 2. Failed COPR builds: NONE actionable
- Latest 100 builds: 97 succeeded + 3 failed. The 3 (10868178 hyprtoolkit-0.5.4-2, 10868177 hyprsysteminfo-0.2.0-4, 10868172 hyprpaper-0.8.4-4) are OLD mass-rebuild failures, superseded by succeeded builds: hyprtoolkit-0.5.4-3 (10868285), hyprsysteminfo-0.2.0-5 (10868289), hyprpaper-0.8.4-5 (10868291). Zero pending/starting/running (verified state counts over last 100).

### 3. Version accuracy sweep - 65/65 CURRENT (zero spec diffs, zero changes needed)
- ALL 33 update.sh re-run in a FRESH clone (in each package dir, exactly like update-engine.yml): 33/33 exit 0, `git status --porcelain` = 0 diffs. hyprland-plugins correctly exits 0 via its pin guard ("Plugins at hyprland 0.56.2 pin (00862ca). No update needed.").
- GitHub scanner (replicated workflow SANE_TAG logic via gh api, 31 non-scripted GitHub specs): 31/31 MATCH releases/latest (vesktop 1.6.5, starship 1.26.0, all hyprwm 0.56.2-family, mangowm 0.16.1, stoat-desktop 1.4.2, waypaper 2.8, matugen 4.1.0, etc.). Zero mismatches.
- materialyoucolor: 3.0.4 == PyPI latest (json API).
- Git pins vs live HEAD (gh api commits/HEAD + git ls-remote): niri 6062844==HEAD, xwayland-satellite 3bc915f==HEAD, lazyvim 459a4c3==HEAD, hyprland-contrib 3dcbce7==HEAD, app2unit 47e23ec==HEAD, caelestia-cli be54856==HEAD, caelestia-shell cae55ed==HEAD, material-symbols e083cc6==master HEAD, quickshell 532a060==HEAD (Forgejo ls-remote AND GitHub mirror). hyprland-plugins 00862ca = INTENTIONAL 0.56.2 pin.
- Non-GitHub: ly v1.4.1 (codeberg tags), awww v0.12.1 (codeberg LGFae/awww, update.sh self-verifies), wlroots 0.20.2 (gitlab tags API, newest 0.20.2), fluxer 2026.815.214426 (X-Fluxer-Version header), ghostty 1.3.1 (PPA latest tag 1.3.1-0-ppa2).
- Binary packages: localsend HELD at 1.18.0 CORRECT (v1.18.1 has ONLY android APKs, no linux-x86-64/deb assets; v1.18.0 has the linux-x86-64.deb - verified via gh api asset lists). zen-browser 1.21.14b == latest tag. helium-browser 0.15.5.1, freebuff 0.0.63, obsidian 1.13.7, logseq 2.0.1, opencode-desktop 1.18.18, protonplus 0.6.4 all verified by their update.sh logs.
- **rootapp RE-TEARED**: downloaded AppImage fresh (386MB), sha256 = 0736926fde923819d276ac6d4a3afe65c6d87c734689af47276fc4ebf075d910 == spec pin EXACTLY. Extracted via ELF section-header offset (944632) with unsquashfs; Root.desktop X-AppImage-Version=0.9.126 AND Comment "Root 0.9.126" == spec Version 0.9.126. COPR build 10868147 = 0.9.126-1 succeeded.
- README reconcile: 65 dirs == 65 README rows (materialyoucolor dir -> python3-materialyoucolor RPM Name); COPR package/list = 63 == repo minus (hyprlauncher, hyprshutdown) [pending owner registration, README caveat]; no extra COPR packages, no ghost-package dirs.

### 4. Actions health
- Only FAILED runs are opencode-schedule 31920615923 + 31932576932 (both the gate-check CI fetch quirk, closed #9/#10). This run in_progress. Auto-Updater 31932425443 + 31920332365 both SUCCESS. No actionable workflow failure.

### 5. Install-test sweep (FRESH containers, LIVE COPR repo, dnf copr enable ackerman/nexus)
Fresh-checker pass, ALL 63 COPR packages install-tested clean on fedora:44 + quickshell-git (only binary changed since prior sweep) on ALL 3 chroots + samples on fc43/rawhide:
- quickshell-git 0.3.0^20260816002204git532a060-1: fc44 INSTALL_EXIT=0 rpm -V CLEAN `quickshell --version`="Quickshell 0.3.0"; fc43 same NVR fc43 clean; rawhide(fc46) clean (--nogpgcheck, known fresh-rawhide GPG-key gap). Build 10869437 succeeded all 3.
- fc44 FULL 63/63: mangowm 0.16.1-1 (mango --help exit 0), hyprland 0.56.2-5, obsidian 1.13.7-1, vesktop 1.6.5-2, zen-browser 1.21.14b-1, rootapp 0.9.126-1, localsend 1.18.0-2, logseq 2.0.1-3, heroic-games-launcher 2.22.1-4, fluxer 2026.815.214426-1, freebuff 0.0.63-1, helium-browser 0.15.5.1-1, starship 1.26.0-1, ly 1.4.1-2, hyprlock/hypridle/hyprpaper/hyprpicker/hyprsunset/hyprpolkitagent, cliphist 0.7.0-1, niri-git 6062844-1, xwayland-satellite-git 3bc915f-1, lazyvim-git 459a4c3-1, hyprland-contrib 3dcbce7-3, hyprland-plugins 00862ca-1, app2unit 47e23ec-1, caelestia-cli-mango be54856-2, caelestia-shell-mango cae55ed-2, extension-manager 0.6.5-2, nwg-look 1.1.1-2, gpu-screen-recorder 6.0.0-4, libcava 1.0.0-1, waypaper 2.8-8, mpvpaper 1.9-2, matugen 4.1.0-2, awww 0.12.1-3, wlroots 0.20.2-3, scenefx 0.5-2, ghostty 1.3.1-2, protonplus 0.6.4-1, opencode-desktop 1.18.18-1, stoat-desktop 1.4.2-2, bibata-cursor-theme 2.0.7-1, cascadia-code-nerd-fonts 3.5.0-1, material-symbols-fonts 4.0.0^...e083cc6-1, python3-materialyoucolor 3.0.4-1, aquamarine 0.14.0-2, glaze-devel 8.0.0-1, hyprcursor/hyprgraphics/hyprlang/hyprland-guiutils/hyprland-qt-support/hyprqt6engine/hyprsysteminfo/hyprtoolkit/hyprutils/hyprwire, hyprland-protocols-devel 0.7.0-1, hyprwayland-scanner-devel 0.4.6-1, xdg-desktop-portal-hyprland 1.4.1-2. ALL INSTALL_EXIT=0, rpm -V CLEAN. (glaze/hyprland-protocols/hyprwayland-scanner install only as -devel - base names match only source packages, per spec design.)
- fc43 samples: hyprland, fluxer, starship, mangowm all clean.
- rawhide(fc46) samples: hyprland, fluxer, starship, mangowm all clean (--nogpgcheck).
No "nothing provides" errors anywhere.

### 6. Dependency audit
| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| quickshell-git 532a060 | CMake find_package Qt6 (Gui/Qml/Quick/QuickControls2/Widgets/ShaderTools/DBus/Network/WaylandClient + private), CLI11, WaylandScanner, XCB; pkg_check jemalloc/libdrm/gbm/egl/pipewire/polkit/wayland; same set as audited commits (532a060 = core/scriptmodel reentrancy code fix, not a build-system change) | 26 BuildRequires (clii11, cmake, qt6-*, jemalloc-devel, pipewire-devel, polkit-devel, wayland-devel, etc.) + 7 Requires | none | none | deps-verified (build 10869437 + fresh live-repo install green all 3 chroots, rpm -V CLEAN) |
| mangowm 0.16.1 | meson: libdrm, xcb, wayland-server>=1.23.1, wlroots-0.20>=0.20.0, xkbcommon, libinput>=1.27.1, libpcre2-8, scenefx-0.5, pixman, cjson, pangocairo (prior audit) | unchanged | none | none | deps-verified (build 10869359 + install green fc44/fc43/rawhide this run) |
| ALL OTHER 61 | upstream unchanged since prior audits, versions re-verified current this run (section 3) | carried | - | - | deps-verified (carried; this run install-tested all 63 COPR packages green fc44, samples fc43/rawhide) |

### 7. Adversarial audit
1. "quickshell-git could have drifted" - re-ran ls-remote Forgejo + GitHub mirror NOW: HEAD=532a060 == spec pin. Repelled.
2. "the 3 failed COPR builds could be current" - API build list shows per-package latest SUCCEEDED builds (10868285/10868289/10868291) carry the current NVRs; the 3 failed are older superseded -4/-2 releases. Repelled.
3. "version sweep could miss a package" - 33 update.sh + 31 GH-scan + materialyoucolor + 9 git-pins + non-GitHub individually verified = 65/65, zero diffs. Repelled.
4. "rootapp could be stale/wrong version" - fresh AppImage sha256 == spec pin, X-AppImage-Version 0.9.126 == spec Version. Repelled.
5. "localsend hold could be wrong" - v1.18.1 asset list (gh api) = android-only; v1.18.0 has linux-x86-64.deb. Hold correct. Repelled.
6. "gate-check quirk diagnosis for #10 could be wrong" - relay content at all 4 relay commits verified via git show; gate commands reproduced passing in a fresh shallow clone. Repelled.

### 8. Known gaps (unchanged)
1. hyprlauncher/hyprshutdown pending COPR owner registration (README caveat) - not installable from repo.
2. GUI apps can't launch headless in CI - verified via install + rpm -V + ldd + --version smoke (headless launch blocked by missing display; known).
3. COPR webhook/config API 403 (token scope) - no package push this run so N/A; verified empirically via push->build timing in prior runs.
4. Rawhide install tests use --nogpgcheck (fresh image lacks base GPG keys).
5. gitlab.freedesktop.org intermittently behind Anubis (wlroots update.sh worked this run; version verified via gitlab tags API).
6. hyprland-plugins intentionally pinned to 0.56.2 compat commit 00862ca.

## Fresh-checker re-verification #2 (independent re-invocation of run 31932576932)
Picked up main at `1c6cd59` (working tree clean, git identity verified opencode-agent[bot]). Re-ran EVERY check independently in a fresh /tmp/opencode/sweep clone + fresh Fedora containers. Result: repo healthy and complete; nothing to fix, no package commits this invocation.

### 0. Pickup
- HEAD == origin/main == 1c6cd59; `git status --porcelain` clean; full history present (1048 commits).
- COPR project chroots (API chroot_repos, authoritative): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64 ONLY.
- Fedora lifecycle (websearch 2026-08-16): F44 latest stable (EOL 2027-06-02), F43 supported (EOL 2026-12-09), F45 preview (branched 2026-08-11, GA 2026-10-20), Rawhide=46. Enabled chroots == supported set exactly; no README/chroot change needed.
- Open issues: 0. Open PRs: 0 (gh api).

### 1. Failed COPR builds: NONE actionable
- Latest 100 builds: 97 succeeded + 3 failed. The 3 (10868178 hyprtoolkit-0.5.4-2, 10868177 hyprsysteminfo-0.2.0-4, 10868172 hyprpaper-0.8.4-4) are OLD, superseded by succeeded builds: hyprtoolkit-0.5.4-3 (10868285), hyprsysteminfo-0.2.0-5 (10868289), hyprpaper-0.8.4-5 (10868291). Zero pending/starting/running/importing.

### 2. Version accuracy sweep - 65/65 CURRENT
- ALL 33 update.sh in fresh clone: 33/33 exit 0, ZERO spec diffs (incl. wlroots - gitlab.freedesktop.org API reachable this invocation, reports 0.20.2 up to date; releases API confirms tags 0.20.2 newest stable).
- GitHub scanner (replicated workflow SANE_TAG logic, gh-authenticated, 31 repos): 31/31 MATCH releases/latest. Zero mismatches.
- Git pins vs live HEAD (git ls-remote this invocation): niri 606284464==HEAD; xwayland-satellite 3bc915f==HEAD; lazyvim 459a4c3==HEAD; hyprland-contrib 3dcbce7==HEAD; app2unit 47e23ec==HEAD (Vladimir-csp/app2unit); caelestia-cli be54856==HEAD; caelestia-shell cae55ed==HEAD; material-symbols e083cc6==main HEAD (google/material-design-icons; master branch stale); quickshell 532a060==HEAD on BOTH Forgejo and GitHub mirror; hyprland-plugins 00862ca = INTENTIONAL 0.56.2 pin (live HEAD faf5ef1b chases future hyprland).
- Non-GitHub verified: materialyoucolor 3.0.4 (PyPI JSON); ly v1.4.1 (codeberg tags, tarball 200); awww v0.12.1 (codeberg tags v0.12.1, tarball 200); fluxer 2026.815.214426 (X-Fluxer-Version header, Source0 200); ghostty 1.3.1 / ppatag 1.3.1-0-ppa2 (mkasberg/ghostty-ubuntu latest tag); gpu-screen-recorder 6.0.0 (snapshot tarball 200); wlroots 0.20.2 (gitlab releases API).
- Binary packages: localsend 1.18.0 hold CORRECT (v1.18.1 latest tag but NO linux x86-64/deb assets); zen-browser 1.21.14b == latest tag; helium-browser 0.15.5.1 == latest tag.
- **rootapp RE-TEARED**: downloaded AppImage fresh, sha256 = 0736926fde923819d276ac6d4a3afe65c6d87c734689af47276fc4ebf075d910 == spec pin EXACTLY; extracted via ELF section-header offset (944632) with unsquashfs; X-AppImage-Version=0.9.126 and Comment "Root 0.9.126" in Root.desktop == spec Version 0.9.126.
- README reconcile: 65 dirs == 65 README rows (materialyoucolor dir -> python3-materialyoucolor RPM Name mapping); COPR package/list = 63 == repo minus (hyprlauncher, hyprshutdown) [pending owner registration, README caveat]; no extra COPR packages.

### 3. Fresh-checker install test: quickshell-git 532a060 (build 10869437, ONLY binary changed since prior full sweep)
Live COPR repo install in CLEAN containers, exact NVR verified per chroot:
- fedora:44: quickshell-git-0.3.0^20260816002204git532a060-1.fc44.x86_64, INSTALL_EXIT=0, rpm -V CLEAN, ldd clean, `quickshell --version` = "Quickshell 0.3.0 (revision , distributed by Fedora)".
- fedora:43: same NVR fc43, INSTALL_EXIT=0, rpm -V CLEAN, ldd clean, --version OK.
- fedora:rawhide(fc46): same NVR fc46, INSTALL_EXIT=0 (--nogpgcheck for known fresh-rawhide missing-GPG-key on openh264), rpm -V CLEAN, ldd clean, --version OK.
No "nothing provides" errors anywhere.

### 4. Dependency audit
| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| quickshell-git 532a060 | identical to prior audit (532a060 = core/scriptmodel reentrancy code fix, not a build-system change) | unchanged | none | none | deps-verified (build 10869437 + fresh live-repo install green all 3 chroots, ldd clean) |
| ALL OTHER 64 | upstream unchanged since prior audits, versions re-verified current this invocation | carried | - | - | deps-verified (carried; prior ledger green, no binaries changed since) |

### 5. Actions health
- This run (opencode-schedule 31932576932, re-invocation) in_progress. Auto-Updater 31932425443 SUCCESS. Only FAILED opencode-schedule 31920615923 = resolved gate-check CI fetch quirk (closed as issue #9). No actionable failure.

### 6. Adversarial audit
1. "quickshell-git could have drifted" - re-ran ls-remote NOW on Forgejo AND GH mirror: both == 532a060 == spec pin. Repelled.
2. "build 10869437 could have failed a chroot" - API state=succeeded; fresh live-repo installs from all 3 chroots returned the exact NVR with per-chroot disttag. Repelled.
3. "version sweep could miss a package" - 33 update.sh + 31 GH-scan + materialyoucolor + git pins + non-GH individually verified = 65/65. Repelled.
4. "rootapp could be stale" - AppImage sha256 == pinned checksum + X-AppImage-Version 0.9.126 == spec Version. Repelled.

### 7. Known gaps (stated, unchanged)
1. hyprlauncher/hyprshutdown pending COPR owner registration (README caveat).
2. GUI apps can't launch headless in CI - verified via install + rpm -V + ldd + --version smoke.
3. COPR webhook/config API 403 (token scope) - verified empirically via push->build timing in prior invocations; no package push this invocation so N/A here.
4. Rawhide install tests use --nogpgcheck (fresh image lacks base GPG keys).
5. gitlab.freedesktop.org intermittently behind Anubis (wlroots update.sh worked this invocation; version verified via releases API).
6. hyprland-plugins intentionally pinned to 0.56.2 compat commit 00862ca.

## Fresh-checker re-verification (independent, run 31932576932, current invocation)
Picked up main at `4a34951` (working tree clean, git identity verified opencode-agent[bot]). Independently re-ran EVERY check in a fresh /tmp clone + fresh Fedora containers (did NOT trust the prior relay text). Result: repo healthy and complete; nothing to fix.

### 0. Pickup
- main == origin/main == 4a34951; `git status --porcelain` clean.
- COPR chroots (API chroot_repos): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64 ONLY.
- Open issues: 0. Open PRs: 0.

### 1. Failed COPR builds: NONE actionable
- Latest 100 builds: 97 succeeded + 3 failed. The 3 (10868178 hyprtoolkit-0.5.4-2, 10868177 hyprsysteminfo-0.2.0-4, 10868172 hyprpaper-0.8.4-4) are OLD, ALL superseded by succeeded builds matching repo specs: hyprtoolkit-0.5.4-3 (10868285), hyprsysteminfo-0.2.0-5 (10868289), hyprpaper-0.8.4-5 (10868291). Zero pending/starting/running.

### 2. Version accuracy sweep - 65/65 CURRENT
- ALL 33 update.sh in fresh clone: 32 exit 0 NO-DIFF; wlroots exit 1 ONLY (gitlab.freedesktop.org API+git blocked by Anubis - git ls-remote timed out). wlroots 0.20.2 confirmed latest stable via Debian tracker (0.20.2-1 testing/unstable since 2026-08-09), Arch wlroots0.20 (0.20.2-1), Phoronix (0.20 released 2026-03-26); Source0 HTTP 200; spec Version 0.20.2/Release 3 matches COPR 10868199. No spec change (documented infra gap).
- GitHub scanner (gh api releases/latest, authenticated, 31 repos): 31/31 MATCH. Zero mismatches.
- materialyoucolor: 3.0.4 == PyPI latest 3.0.4; COPR 10868189 succeeded.
- Git pins vs live HEAD (this invocation): niri 6062844==HEAD; lazyvim 459a4c3==HEAD; material-symbols e083cc6==master HEAD; hyprland-contrib 3dcbce7==HEAD; app2unit 47e23ec==master HEAD; caelestia-cli be54856==HEAD; caelestia-shell cae55ed==HEAD; quickshell 532a060==HEAD (Forgejo + GitHub mirror); xwayland-satellite 3bc915f==HEAD (5x ls-remote + gh api consistent; first ls-remote returned anomalous 3ba30b1 = transient hiccup). hyprland-plugins 00862ca = INTENTIONAL 0.56.2 pin.
- README: 65 dirs == 65 rows; COPR list 63 (2 pending + materialyoucolor->python3-materialyoucolor). Consistent.

### 3. Fresh-checker install test: quickshell-git 532a060 (only binary changed since prior sweep)
COPR build 10869437 (succeeded, all 3 chroots), NVR 0.3.0^20260816002204git532a060-1. Live COPR repo install in clean containers:
- fedora:44: clean, rpm -q ...fc44, rpm -V CLEAN, ldd no missing libs, `quickshell --version`="Quickshell 0.3.0"
- fedora:43: clean, NVR fc43, rpm -V CLEAN, ldd clean, --version OK
- fedora:rawhide(fc46): clean (--nogpgcheck for known fresh-rawhide missing-GPG-key), NVR fc46, rpm -V CLEAN, ldd clean, --version OK
All green. No "nothing provides" errors.

### 4. Dependency audit
- No upstream version changes since prior audits (65/65 current) -> no dep deltas.
- quickshell-git 532a060: prior audit deps Qt6/CLI11/WaylandScanner/XCB/jemalloc; 532a060 is a core/scriptmodel code fix (ScriptModel::setValues reentrancy), NOT a build-system/deps change. Fresh install green all 3 chroots (ldd clean) confirms dep set resolves from real repo. deps-verified.
- Others: carried (re-verified current, installs green). Confidence high.

### 5. Actions health
- opencod-schedule 31932576932 = THIS run (in_progress). Auto-Updater 31932425443 (06:51Z) = SUCCESS (commit 16f0aec).
- No current actionable failure. Most recent FAILED opencod-schedule (31920615923, 01:54Z) = resolved gate-check CI-fetch quirk, closed issue #9.

### 6. Adversarial audit
1. "quickshell-git could have drifted since 532a060" - re-ran ls-remote on Forgejo+GH mirror NOW: HEAD=532a060. Still current.
2. "build 10869437 could have failed a chroot" - api_3/build/10869437 state=succeeded; live-repo install from all 3 chroots returned exact NVR. Not failed.
3. "version sweep could miss a new tag" - GH scanner hit releases/latest 31 repos THIS invocation (all matched); update.sh re-ran vs live upstream (32 NO-DIFF). Verified, not assumed.
4. "wlroots update.sh failure could mean missed version" - ruled by Debian/Arch/Phoronix (0.20.2 newest); Source0 200; COPR 10868199 succeeded.

### 7. Known gaps (stated)
1. hyprlauncher/hyprshutdown pending COPR owner registration (README caveat).
2. GUI apps can't launch headless in CI - verified via install + rpm -V + ldd + --version smoke.
3. COPR webhook/config API 403 (token scope) - verified empirically via push->build timing.
4. Rawhide install tests use --nogpgcheck (fresh image lacks base GPG keys).
5. gitlab.freedesktop.org behind Anubis (wlroots update.sh unreachable; version verified via Debian/Arch/web).
6. hyprland-plugins intentionally pinned to 0.56.2 compat commit 00862ca.

## Run 31932576932 continuation (2026-08-16 07:25Z) - COMPLETE: bumped quickshell-git 78672a8 -> 532a060 (core/scriptmodel: handle reentrancy in ScriptModel::setValues), all 65 versions current, install-tested green on all 3 chroots.

### 0. Pickup continuation
- Picked up main at `6b99bf0` (prior relay complete). RUN_ID=31932576932 (same run, continuation).
- COPR project chroots (API-verified): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64 ONLY.
- Open issues/PRs: NONE (gh api verified).

### 1. Failed COPR builds: NONE actionable
- All recent builds succeeded. No pending/starting/running.

### 2. Version accuracy sweep - 65/65 CURRENT (1 genuine upstream move detected)
- ALL 33 update.sh in a fresh /tmp/opencode/sweep clone: 32 exit 0 no-diff; 1 detected genuine upstream move:
  - **quickshell-git 78672a8 -> 532a060**: upstream quickshell master advanced to "core/scriptmodel: handle reentrancy in ScriptModel::setValues" (Aug 16, 2026). Commit verified via GitHub mirror quickshell-mirror/quickshell master HEAD = 532a0606cafb05851a203e65be22c9a057447ee2. Source tarball URL returns 302 (GitHub redirect). update.sh idempotent after push ("already at latest commit").
- All 32 non-scripted GitHub specs re-scanned via gh api releases/latest: ALL MATCH.
- Git pins vs live HEAD ALL MATCH: niri 6062844, xwayland-satellite 3bc915f, quickshell 532a060 (just updated).
- Non-GitHub verified: materialyoucolor 3.0.4 (PyPI), ly v1.4.1 (Codeberg), awww v0.12.1 (Codeberg), ghostty 1.3.1 (PPA), fluxer 2026.815.214426 (X-Fluxer-Version header), wlroots 0.20.2 (gitlab tags).
- README reconcile: 65 repo dirs == 65 README rows; COPR package/list = 63 (hyprlauncher + hyprshutdown pending). Consistent.

### 3. quickshell-git fix (this run's only change) - pushed and verified
- Commit abfeee1: 78672a8 -> 532a060 (Release: 1%{?dist} - new upstream commit = fresh version).
- Local docker rpmbuild on fc44: BUILD_EXIT=0, RPM produced (0.3.0^20260816002204git532a060-1.fc44.x86_64.rpm).
- Local install test (fc44): INSTALL_EXIT=0, rpm -V CLEAN, ldd OK, `quickshell --version` = "Quickshell 0.3.0".
- COPR build 10869437: state=succeeded, all 3 chroots (fedora-43/fedora-44/rawhide).
- Live-repo install tests (fresh containers, LIVE COPR repo):
  - fc44: INSTALL_EXIT=0, rpm -V CLEAN, ldd OK, `quickshell --version` = "Quickshell 0.3.0".
  - fc43: INSTALL_EXIT=0, rpm -V CLEAN, ldd OK, `quickshell --version` = "Quickshell 0.3.0".
  - rawhide (fc46): INSTALL_EXIT=0, rpm -V CLEAN, ldd OK, `quickshell --version` = "Quickshell 0.3.0" (--nogpgcheck for fresh rawhide GPG keys).

### 4. Dependency audit
| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| quickshell-git 532a060 | identical to prior audit (532a060 is a code fix in core/scriptmodel, not a build system change) | unchanged from prior audit | none | none | deps-verified (build 10869437 + install green all 3 chroots) |
| ALL OTHER 64 packages | upstream unchanged since prior audits, versions re-verified current this run | carried | - | - | deps-verified (carried; prior install-test ledger green) |

### 5. Final state
- 1 commit this run: abfeee1 (quickshell-git 532a060) + relay update. Working tree clean.
- Actions health: all recent runs SUCCESS. No open issues, no open PRs.
- Adversarial audit: (1) "commit could be stale" - GitHub mirror confirms 532a060 is master HEAD; (2) "build could fail" - COPR build 10869437 succeeded all 3 chroots + live-repo installs green; (3) "version sweep could be wrong" - 65/65 cross-checked via gh api / codeberg API / PyPI / gitlab tags / X-Fluxer-Version; (4) "quickshell-git could be churn" - prior build was 78672a8 (10869360), new NVR is 532a060 (10869437) = genuine upstream move, not a re-sync.
- Known gaps (unchanged): (1) hyprlauncher/hyprshutdown not in COPR until owner registers - README caveat; (2) GUI apps can't run headless - verified install+rpm-V+ldd+smoke instead; (3) COPR webhook/config API 403 (token scope) - verified empirically via push->build timing; (4) rawhide install tests use --nogpgcheck (fresh image lacks base GPG keys); (5) hyprland-plugins intentionally pinned to 0.56.2 compat commit 00862ca.

### 6. Webhook verification
- Webhook/config API 403 (token scope, known gap). Verified EMPIRICALLY: before push latest quickshell-git build was 10869360 (78672a8); after abfeee1 push a NEW build 10869437 appeared (532a060, submitted_at=1786867800). Build succeeded. Webhook alive.

---

## Previous run record (Run 31932576932 initial, 2026-08-16 06:55Z) - COMPLETE

### 0. Pickup
- Picked up main at `16f0aec` (auto-updater 31932425443, 06:51:25Z). RELAY=false, RUN_ID=31932576932. Unshallowed the shallow checkout (git fetch --unshallow) to run NO-CHURN checks.
- COPR project chroots (API-verified via chroot_repos): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64 ONLY.
- Fedora lifecycle (prior runs): F44 stable (EOL 2027-06-02), F43 supported (EOL 2026-12-09), Rawhide=46. No README/chroot change needed.
- Open issues/PRs at pickup: issue #9 OPEN ("autonomous run failed: 2026-08-16"), 0 comments; no open PRs.
- Git identity verified: opencode-agent[bot] / 41898282+opencode-agent[bot]@users.noreply.github.com.

### 1. Failed COPR builds: NONE actionable
- Latest 100 builds scanned: 97 succeeded; the only 3 non-succeeded are OLD superseded mass-rebuild failures (10868178 hyprtoolkit-0.5.4-2, 10868177 hyprsysteminfo-0.2.0-4, 10868172 hyprpaper-0.8.4-4) — all superseded by succeeded rebuilds (10868285/10868289/10868291, current NVRs). Zero pending/starting/running.

### 2. Auto-updater bump verification - mangowm 0.16.1 + quickshell-git 78672a8 CORRECT
- Commit 16f0aec bumped mangowm 0.16.0 -> 0.16.1 and quickshell-git c494063 -> 78672a8405f95adb7e4bf6ecacff97f1a535716e (gitdate 20260815205319). Both verified against upstream:
  - mangowm: gh api releases/latest tag = 0.16.1; tarball github.com/mangowm/mango/archive/refs/tags/0.16.1.tar.gz downloads OK (528K). Spec diff = clean version bump only.
  - quickshell-git: git ls-remote git.outfoxxed.me/quickshell/quickshell.git AND github mirror quickshell-mirror/quickshell HEAD = 78672a8405f95adb7e4bf6ecacff97f1a535716e == spec %global commit. Spec diff = commit + gitdate only.
- COPR builds verified succeeded on ALL 3 chroots: 10869359 (mangowm 0.16.1-1), 10869360 (quickshell-git 0.3.0^20260815205319git78672a8-1).

### 3. Version accuracy sweep - 65/65 CURRENT (zero spec diffs)
- ALL 33 update.sh in a fresh /tmp/opencode/sweep clone: 33/33 exit 0, ZERO spec diffs vs main.
- All 32 non-scripted GitHub specs re-scanned via gh api releases/latest (SANE_TAG logic): 32/32 MATCH (all hyprwm 0.56.2-family, aquamarine 0.14.0, glaze 8.0.0, hyprlang 0.6.8, hyprutils 0.14.1, hyprcursor 0.1.13, hyprgraphics 0.5.1, hypridle 0.1.8, hyprlock 0.9.6, hyprpaper 0.8.4, hyprpicker 0.4.7, hyprsysteminfo 0.2.0, hyprtoolkit 0.5.4, hyprwayland-scanner 0.4.6, hyprwire 0.3.1, hyprland-protocols 0.7.0, hyprland-guiutils 0.2.2, hyprland-qt-support 0.1.0, hyprqt6engine 0.1.0, hyprpolkitagent 0.1.3, hyprsunset 0.4.0, hyprlauncher 0.1.6, hyprshutdown 0.1.1, xdg-desktop-portal-hyprland 1.4.1, matugen 4.1.0, mangowm 0.16.1, mpvpaper 1.9, nwg-look 1.1.1, starship 1.26.0, stoat-desktop 1.4.2, vesktop 1.6.5, waypaper 2.8).
- Git pins vs live HEAD ALL MATCH: niri 6062844, xwayland-satellite 3bc915f, lazyvim 459a4c3, hyprland-contrib 3dcbce7, app2unit 47e23ec, caelestia-cli be54856, caelestia-shell cae55ed, material-symbols e083cc6, quickshell 78672a8. hyprland-plugins pin 00862ca = INTENTIONAL (hyprpm 0.56.2 compat; hyprland still v0.56.2; HEAD faf5ef1b chases future hyprland; update.sh guard correctly exits 0).
- Non-GitHub verified: materialyoucolor 3.0.4 (PyPI JSON), ly v1.4.1 (codeberg tags), awww v0.12.1 (codeberg releases/latest), wlroots 0.20.2 (gitlab tags 0.20.2 newest), gpu-screen-recorder 6.0.0 (snapshot tarball https://dec05eba.com/snapshot/gpu-screen-recorder.git.6.0.0.tar.gz HTTP 200), fluxer 2026.815.214426 (X-Fluxer-Version header on api.fluxer.app/latest/rpm), ghostty 1.3.1 (PPA latest tag 1.3.1-0-ppa2).
- README reconcile: 65 repo dirs == 65 README rows; COPR package/list = 63 (hyprlauncher + hyprshutdown pending owner registration per README caveat; materialyoucolor/ dir -> RPM name python3-materialyoucolor via spec Name:). Consistent.

### 4. Dependency audit (only changed packages this run)
| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| mangowm 0.16.1 | meson.build: libdrm, xcb (opt xwayland), xcb-icccm, wayland-server>=1.23.1, wlroots-0.20>=0.20.0, xkbcommon, libinput>=1.27.1, wayland-client, libpcre2-8, scenefx-0.5>=0.5.0, pixman-1, libcjson, pangocairo | pkgconfig(xcb), pkgconfig(xcb-icccm), pkgconfig(wayland-protocols)>=1.41, pkgconfig(wayland-server)>=1.23.1, pkgconfig(wlroots-0.20), pkgconfig(xkbcommon), pkgconfig(libinput)>=1.27.1, pkgconfig(wayland-client), pkgconfig(libpcre2-8), pkgconfig(libcjson), pkgconfig(pangocairo), pkgconfig(pixman-1), pkgconfig(libdrm), scenefx-devel | none | none | deps-verified (build 10869359 + install green all 3 chroots) |
| quickshell-git 78672a8 | identical to c494063 (audited run 31920615923): find_package Qt6/CLI11/WaylandScanner/XCB/PkgConfig, pkg_check_modules JEMALLOC; cpptrace only in src/crash (CRASH_HANDLER=OFF). Source diff vs c494063 is cosmetic only (colorquantizer.hpp adds qtclasshelpermacros.h include + Q_DISABLE_COPY_MOVE). | unchanged from prior audit (cmake, ninja-build, gcc-c++, cli11-devel, qt6-*-devel, wayland-devel, etc.) | none | none | deps-verified (build 10869360 + install green all 3 chroots) |
| ALL OTHER 63 packages | upstream unchanged since prior audits, versions re-verified current this run | carried | - | - | deps-verified (carried; prior install-test ledger green) |

### 5. Install-test sweep (FRESH containers, LIVE COPR repo) - changed packages on all 3 chroots
- mangowm + quickshell-git (the only NVR changes this run) installed clean on:
  - fedora:44 -> mangowm-0.16.1-1.fc44, quickshell-git-0.3.0^20260815205319git78672a8-1.fc44, INSTALL_EXIT=0, rpm -V CLEAN, ldd clean; smoke: `quickshell --version` = "Quickshell 0.3.0", `mango --help` exits 0 (binary is `mango`, not mangowm).
  - fedora:43 -> mangowm-0.16.1-1.fc43, quickshell-git-0.3.0^...-1.fc43, INSTALL_EXIT=0, rpm -V CLEAN, ldd clean, quickshell --version OK.
  - fedora:rawhide(fc46) -> mangowm-0.16.1-1.fc46, quickshell-git-0.3.0^...-1.fc46, INSTALL_EXIT=0, rpm -V CLEAN (--nogpgcheck for known fresh-rawhide missing-GPG-key), smoke `mango --help` + `quickshell --version` OK.
- Other 61 packages: versions re-verified current this run, builds all succeeded, install-test ledger carried from run 31920615923 (63/63 green fc44 + samples rawhide/fc43).

### 6. Issue #9 triage - CLOSED (gate-check CI quirk, not a repo defect)
- Issue: "autonomous run failed: 2026-08-16" (auto-created after run 31920615923 gate check failed 4x).
- Verdict: the run's work was COMPLETE. Relay on main at every relevant commit (7c3857a, 8e78568, 5d4854c, 0c82539) had `run_id: 31920615923` + `status: complete` (verified via git show + exact grep). All COPR builds succeeded, installs green.
- Root cause: gate check does `git fetch origin main >/dev/null 2>&1`; when that fetch silently fails the runner keeps a STALE `origin/main` pointing at the previous run's relay (run_id 31912760586), so both greps fail and the run is misreported as failed. Reproduced the exact gate commands in fresh shallow clones of current main -> fetch succeeds, greps PASS.
- Closed via gh issue comment + gh issue close 9 (evidence in comment 5306312235). No repo changes needed.

### 7. Final state
- No package commits needed this run (auto-updater's bump was correct and already built). Relay-only commit. Working tree clean.
- Actions health: auto-updater 31932425443 SUCCESS; this run in_progress. No open issues, no open PRs.
- Adversarial audit: (1) "auto-updater bump could be wrong" - mangowm tag + tarball + quickshell ls-remote (Forgejo AND GitHub mirror) all confirm the pinned versions; (2) "build could fail in COPR" - 10869359/10869360 both SUCCEEDED all 3 chroots, verified by live-repo installs; (3) "version sweep could be wrong" - 65/65 cross-checked via gh api / codeberg API / PyPI / gitlab tags / X-Fluxer-Version / snapshot URLs; (4) "issue #9 could be a real defect" - relay content verified correct at all 4 commits, gate reproduced passing, so it is a CI fetch quirk, documented + closed.
- Known gaps (unchanged): (1) hyprlauncher/hyprshutdown not in COPR until owner registers - README caveat; (2) GUI apps can't run headless - verified install+rpm -V+ldd+smoke instead; (3) COPR webhook/config API 403 (token scope) - verified empirically via push->build timing in prior runs; (4) rawhide install tests use --nogpgcheck (fresh image lacks base GPG keys); (5) hyprland-plugins intentionally pinned to 0.56.2 compat commit 00862ca; (6) gitlab.freedesktop.org behind Anubis - wlroots version cross-checked via gitlab tags API.

## Independent re-verification (fresh checker, invocation 2 of run 31920615923)
Fresh-checker pass for the re-run of this run ID: re-verified ALL claims independently (not trusting prior relay text) before finalizing. Every claim below is backed by a command I ran in THIS invocation:
- COPR chroots (API project endpoint): 3 only — fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64 (authoritative chroot_repos field; matches prior).
- Fedora lifecycle (websearch endoflife.date + Wikipedia + Fedora wiki): F44 stable (GA 2026-04-28, EOL 2027-06-02), F43 supported (EOL 2026-12-09), F45 Preview (GA 2026-10-20, branched 2026-08-11, beta 2026-08-25), Rawhide=46. Enabled chroots are exactly the supported ones - no README/chroot change needed.
- Failed builds: latest 100 builds scanned - the ONLY non-succeeded are the 3 old mass-rebuild failures (10868178 hyprtoolkit-0.5.4-2, 10868177 hyprsysteminfo-0.2.0-4, 10868172 hyprpaper-0.8.4-4) all superseded by succeeded builds of the same packages (10868285/10868289/10868291, current NVRs). 0 pending/starting/running. Nothing to fix.
- quickshell-git (only package touched this run): build 10868680 (0.3.0^20260815195751gitc494063-1) succeeded all 3 chroots; RPM published in live repo (verified in fc44/fc43/fc46 repodata primary.xml via pulp Packages/q/ paths). HEAD pin matches BOTH git.outfoxxed.me/quickshell/quickshell.git AND github quickshell-mirror/quickshell = c494063ac95568a9e6b59fcd53dee381e0b66c06 (== spec %global commit).
- Version sweep: all 33 update.sh in fresh /tmp/opencode/fresh-sweep clone -> 33/33 exit 0, ZERO spec diffs. Replicated the workflow's standard-package python scanner (SANE_TAG logic) over all 32 non-scripted GitHub specs -> 32/32 MATCH releases/latest (vesktop 1.6.5, starship 1.26.0, all hyprwm 0.56.2..., matugen 4.1.0, nwg-look 1.1.1, mpvpaper 1.9, scenefx 0.5, stoat-desktop 1.4.2, xdg-desktop-portal-hyprland 1.4.1, glaze 8.0.0, mangowm 0.16.0, waypaper 2.8, hyprlauncher 0.1.6, hyprshutdown 0.1.1). Git pins verified via git ls-remote: niri 6062844, xwayland-satellite 3bc915f, lazyvim 459a4c3, hyprland-contrib 3dcbce7, app2unit 47e23ec, caelestia-cli be54856, caelestia-shell cae55ed, material-symbols e083cc6 (master HEAD confirmed == pin; one transient ls-remote returned a 2021 commit 469cd83a = cache artifact, re-checked 3x consistent), quickshell c494063. hyprland-plugins pin 00862ca = INTENTIONAL (commit msg "hyprpm: add pin for 0.56.2", hyprland latest v0.56.2; HEAD faf5ef1b is README-only, guard correctly exits 0).
- Non-GitHub verified directly: materialyoucolor 3.0.4 (PyPI JSON), ly v1.4.1 (codeberg tags), awww v0.12.1 (codeberg releases/latest), gpu-screen-recorder 6.0.0 (cgit tag parse + snapshot tarball 6.0.0 returns 200), ghostty 1.3.1 (PPA latest tag 1.3.1-0-ppa2), wlroots 0.20.2 (gitlab refs/tags 0.20.2 newest), fluxer 2026.815.214426 (X-Fluxer-Version header on api.fluxer.app/latest/rpm).
- localsend hold at 1.18.0 CORRECT: gh api v1.18.1 assets = NO linux-x86-64/deb (empty), v1.18.0 has the .deb (URL 200). Source0 URLs all curl-verified 200 after %{url}/%{version}/%{name} substitution: obsidian, vesktop, zen-browser, heroic-games-launcher, localsend, logseq, rootapp.
- Fresh install test (CLEAN containers, LIVE COPR repo, --setopt=install_weak_deps=False), the c494063-1 RPM downloaded fresh from the pulp repo:
  - fedora:44 -> INSTALL_EXIT=0, rpm -q = quickshell-git-0.3.0^20260815195751gitc494063-1.fc44.x86_64, rpm -V CLEAN, ldd no missing libs, `quickshell --version` = "Quickshell 0.3.0".
  - fedora:43 -> INSTALL_EXIT=0, same NVR fc43, rpm -V CLEAN, ldd CLEAN.
  - fedora:rawhide(fc46) -> INSTALL_EXIT=0, NVR fc46, rpm -V CLEAN, ldd CLEAN.
- README reconcile: 65 repo dirs == 65 README rows (materialyoucolor/ dir -> RPM name python3-materialyoucolor); COPR package/list = 63 (hyprlauncher + hyprshutdown pending owner registration per README caveat). Consistent.
- Issues: 0 open (gh api). Pulls: 0 open (gh api). Actions: auto-updater 31920332365 SUCCESS, no failed logs, made NO commits since 2026-08-15T23:00Z; all recent runs SUCCESS.
- Git identity: opencode-agent[bot] / 41898282+opencode-agent[bot]@users.noreply.github.com (verified via git config + git log -1).
- Working tree clean; local HEAD == origin/main == 5d4854c; nothing left to fix, no churn (this is a fresh-checker re-run of an already-complete run).

## Run 31920615923 (2026-08-16 02:00Z) - COMPLETE: refreshed quickshell-git to upstream master c494063 (build 10868680 succeeded all 3 chroots), 65/65 versions current, 63/63 COPR packages install-tested green fc44 + samples rawhide/fc43.

### 0. Pickup
- Picked up main at `7bc9212` (owner-applied workflow update to opencode-schedule.yml, only .github/workflows file - expected, owner-managed). RELAY=false, RUN_ID=31920615923. Unshallowed the shallow checkout (1037 commits) to run NO-CHURN checks.
- COPR project has THREE chroots (API-verified via chroot_repos): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64 ONLY (owner's tumbleweed removal stands).
- Fedora lifecycle (web-verified): F44 stable (EOL 2027-06-02), F43 supported (EOL 2026-12-09), F45 branched (GA 2026-10-20, mass rebuild done 2026-07-15, branched 2026-08-11), Rawhide=46. No README/chroot change needed.
- Open issues/PRs: NONE (gh api verified). Actions health: auto-updater 835 SUCCESS 01:46Z (made no commits - specs already current); all recent opencode runs SUCCESS; old failures are from Aug 4-10 (historical).
- Git identity verified: opencode-agent[bot] / 41898282+opencode-agent[bot]@users.noreply.github.com. Commit e20e49b authored correctly (verified post-commit).

### 1. Failed COPR builds: NONE actionable
- Latest 100 builds scanned: all succeeded except the 3 OLD mass-rebuild failures (10868178 hyprtoolkit-0.5.4-2, 10868177 hyprsysteminfo-0.2.0-4, 10868172 hyprpaper-0.8.4-4) already superseded by successful fixes from run 31905208256 (10868285/-5, 10868289/-5, 10868291/-5). Zero pending/starting/running at start.

### 2. Version accuracy sweep - 65/65 CURRENT (2 refreshes pushed)
- Ran ALL 33 update.sh in a FRESH checkout: 32 exit 0 no-diff; 1 package detected GENUINE upstream moves (TWICE during this run - upstream force-pushed/rebased):
  - **quickshell-git 28771c7 -> f27fd7b -> c494063**: upstream quickshell master advanced (f27fd7b "x11/panelwindow: avoid using destroyed panel stack after free", dated 2026-08-15T18:52-07:00), then force-pushed a REBASED commit c494063 (same message, added `#include <utility>` to src/core/colorquantizer.cpp - verified by tree diff of the two tarballs) dated 19:57-07:00. Both moves verified via git ls-remote git.outfoxxed.me/quickshell/quickshell.git HEAD AND GitHub mirror quickshell-mirror/quickshell. update.sh produced correct spec edits (commit + gitdate + Release 1%{?dist} + %changelog). After each push update.sh re-run = "already at the latest commit", NO CHURN. NOTE: this was genuine upstream rebase churn, not a script flaw - the -git package correctly tracks HEAD.
- All 29 GitHub-release specs re-scanned via gh api releases/latest: ALL MATCH (hyprwm aquamarine 0.14.0 ... xdg-desktop-portal-hyprland 1.4.1, glaze 8.0.0, matugen 4.1.0, mangowm 0.16.0, scenefx 0.5, nwg-look 1.1.1, starship 1.26.0, waypaper 2.8, mpvpaper 1.9, vesktop 1.6.5, extension-manager 0.6.5, stoat-desktop 1.4.2, obsidian 1.13.7, logseq 2.0.1, heroic 2.22.1, opencode-desktop 1.18.18).
- Git pins vs live HEAD ALL MATCH: niri-git 6062844, xwayland-satellite 3bc915f, lazyvim 459a4c3, hyprland-contrib 3dcbce7, app2unit 47e23ec, caelestia-cli be54856, caelestia-shell cae55ed, material-symbols e083cc6 (master HEAD; main branch is stale 2022 - update.sh tracks master correctly), hyprland-plugins 00862ca = INTENTIONAL 0.56.2 pin.
- Non-GitHub verified: ly v1.4.1 (codeberg API), awww v0.12.1 (codeberg API), wlroots 0.20.2 (Arch extra + buildroot + debian cross-check; gitlab API behind Anubis), gpu-screen-recorder 6.0.0 (cgit tag parse), ghostty 1.3.1 (mkasberg/ghostty-ubuntu latest PPA tag; ghostty.org 1.3.1 stable), materialyoucolor 3.0.4 (PyPI JSON), fluxer 2026.815.214426 (update.sh X-Fluxer-Version check), freebuff 0.0.63, helium-browser 0.15.5.1, rootapp 0.9.126 (sha256 pin match).
- localsend HELD at 1.18.0 CORRECT: v1.18.1 assets = Android APKs ONLY (gh api verified), no linux-x86-64; update.sh's asset-existence guard correctly skips.
- Source URLs curl-verified 200 (with macro substitution): obsidian, zen-browser, logseq, fluxer, freebuff, helium-browser, gpu-screen-recorder, opencode-desktop, vesktop, localsend, ghostty (ghostty_1.3.1-0.ppa2_amd64_24.04.deb 200), protonplus (forgesource), rootapp (update.sh sha256 match).

### 3. quickshell-git fix (this run's only change) - pushed and verified
- Applied the update.sh-generated spec changes. Two commits, each pushed as it verified:
  - e20e49b: 28771c7 -> f27fd7b. Local docker rpmbuild on fedora:44 SUCCEEDED; local install clean. COPR build 10868653 SUCCEEDED all 3 chroots.
  - 49ab8c1: f27fd7b -> c494063 (upstream rebased immediately after; genuine tree change verified). Local docker rpmbuild on fedora:44 SUCCEEDED; local install clean. COPR build 10868680 SUCCEEDED all 3 chroots (fedora-43/fedora-44/rawhide), NVR 0.3.0^20260815195751gitc494063-1.
- Live-repo install tests (dnf copr enable + install, fresh containers): final c494063 state clean on fc44, fc43, rawhide (fc46). Fresh-checker pass: independent re-install from a new container after build 10868680 - clean (INSTALL_EXIT=0, rpm -V CLEAN, ldd no missing libs).

### 4. Webhook verification
- Webhook/config API 403 (token scope, known gap). Verified EMPIRICALLY: before each push latest quickshell-git build was 10868148 (28771c7); after e20e49b push a NEW build 10868653 appeared (f27fd7b); after 49ab8c1 push a NEW build 10868680 appeared (c494063). Both webhook-fired, both succeeded.

### 5. Dependency audit
| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| quickshell-git c494063 (RE-DERIVED from new artifact) | CMake find_package: Qt6 (Gui Qml Quick QuickControls2 Widgets ShaderTools DBus Network Test WaylandClient + QuickPrivate/WaylandClientPrivate), CLI11, VulkanHeaders, WaylandScanner, XCB; pkg_check_modules: jemalloc, libdrm gbm egl, glib-2.0>=2.36, gobject-2.0, libpipewire-0.3, polkit-gobject-1, polkit-agent-1, wayland-client, wayland-protocols>=1.41. cpptrace only in src/crash (CRASH_HANDLER=OFF in spec, not built). | cmake, ninja-build, gcc-c++, cli11-devel, pkgconf-pkg-config, qt6-qtbase-devel + -private-devel, qt6-qtdeclarative-devel, qt6-qtsvg-devel, qt6-qtwayland-devel, qt6-qtshadertools-devel, libglvnd-devel, wayland-devel, wayland-protocols-devel, libxcb-devel, mesa-libEGL-devel, mesa-libgbm-devel, libdrm-devel, pam-devel, polkit-devel, pipewire-devel, jemalloc-devel, hicolor-icon-theme, spirv-tools, vulkan-headers | none | none | deps-verified (local builddep+build OK, install green all 3 chroots) |
| ALL OTHER 64 packages | upstream unchanged since prior audits, versions re-verified current this run (section 2) | carried | - | - | deps-verified (carried; install green fc44 this run) |
- Confidence high for quickshell-git (re-derived from the NEW f27fd7b tarball, rebuilt + installed this run). Carried audits medium-high (upstream unchanged, versions re-verified, 63/63 install green fc44 this run).

### 6. Install-test sweep (FRESH containers, LIVE COPR repo)
- fc44 full sweep - ALL 63 COPR packages installed clean, rpm -V CLEAN: hyprland, hyprlock, hyprpaper, hyprsysteminfo, hyprtoolkit, hyprwayland-scanner-devel (base ships only -devel; binary runs --version = 0.4.6), hyprland-protocols-devel (header-only), niri-git, xwayland-satellite-git, starship, aquamarine, hyprutils, hyprlang, hyprcursor, hyprgraphics, hypridle, hyprpicker, hyprsunset, hyprpolkitagent, hyprland-guiutils, hyprqt6engine, hyprland-qt-support, xdg-desktop-portal-hyprland, hyprwire, fluxer, freebuff, helium-browser, obsidian, vesktop, zen-browser, heroic-games-launcher, localsend, logseq, rootapp, ghostty, protonplus, opencode-desktop, extension-manager, glaze-devel (library ships only -devel), mangowm, matugen, mpvpaper, nwg-look, waypaper, cliphist, bibata-cursor-theme, cascadia-code-nerd-fonts, material-symbols-fonts, python3-materialyoucolor, app2unit, caelestia-cli-mango, caelestia-shell-mango, gpu-screen-recorder, ly, lazyvim-git, hyprland-contrib, hyprland-plugins, scenefx, wlroots, libcava, awww, quickshell-git.
- rawhide (fc46) sample: quickshell-git, hyprland, fluxer, starship - all clean (--nogpgcheck needed, known fresh-rawhide missing-GPG-key issue).
- fc43 sample: quickshell-git, hyprland, fluxer, freebuff - all clean.
- Smoke tests: hyprwayland-scanner --version = 0.4.6, starship --version 1.26.0, ly --version 1.4.1 (prior runs), ghostty --version 1.3.1 (prior runs).
- hyprlauncher/hyprshutdown: NOT in COPR (pending owner registration, README caveat) - not install-testable from repo, versions verified 0.1.6/0.1.1.

### 7. Final state
- 2 package commits this run: e20e49b (f27fd7b) + 49ab8c1 (c494063) + this relay-only commit. Working tree clean.
- Adversarial audit: (1) "commits could be churn" - COPR build list showed quickshell-git last built at 28771c7 (10868148/10842778/10805819); new NVRs are genuinely f27fd7b then c494063 (upstream force-pushed mid-run, verified by tree diff: `#include <utility>` added). update.sh idempotent after each push; (2) "upstream could be a stale mirror" - verified via Forgejo ls-remote AND GitHub mirror both return the same SHA; (3) "build could fail in COPR" - builds 10868653 AND 10868680 both succeeded all 3 chroots, verified from live repo installs; (4) "version sweep could be wrong" - 65/65 cross-checked via gh api / codeberg API / PyPI / cgit / X-Fluxer-Version / PPA tags.
- Known gaps (unchanged): (1) hyprlauncher/hyprshutdown not in COPR until owner registers - README caveat; (2) GUI apps can't run headless - verified install+rpm -V+ldd instead of launch; (3) COPR webhook/config API 403 (token scope) - verified empirically via push->build timing; (4) rawhide install tests use --nogpgcheck (fresh image lacks base GPG keys); (5) hyprland-plugins intentionally pinned to 0.56.2 compat commit 00862ca; (6) gitlab.freedesktop.org behind Anubis anti-bot - wlroots version cross-checked via Arch/Debian/Buildroot instead.

## Run 31912760586 (2026-08-15 22:40Z) - COMPLETE: bumped fluxer/freebuff/helium-browser to latest upstream, all rebuilt + install-tested green, 65/65 versions current.

### 0. Pickup
- Picked up main at `26877a6` (prior relay record). RELAY=false, RUN_ID=31912760586.
- COPR project has THREE chroots (API-verified via chroot_repos): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64 ONLY. Owner's opensuse-tumbleweed removal stands - not re-added.
- Fedora lifecycle: F44 stable, F43 supported (EOL 2026-12-09), F45 branched (GA 2026-10-20), Rawhide=46. No README/chroot change needed (README table has no version column).
- Open issues/PRs: NONE (gh api verified). Actions health: all recent opencode + auto-updater runs SUCCESS (auto-updater 31901665013 SUCCESS at 18:38Z, schedule now every 6h).
- Git identity verified: opencode-agent[bot] / 41898282+opencode-agent[bot]@users.noreply.github.com.

### 1. Failed COPR builds: NONE actionable
- Latest 100 builds scanned: only 3 non-succeeded are the OLD mass-rebuild failures (10868178 hyprtoolkit-0.5.4-2, 10868177 hyprsysteminfo-0.2.0-4, 10868172 hyprpaper-0.8.4-4) already superseded by succeeded fixes (10868285/-5, 10868289/-5, 10868291/-5). Zero pending/starting/running at start.

### 2. Version accuracy sweep - 65/65 CURRENT after 3 bumps
- Ran ALL 33 update.sh in a FRESH checkout (/tmp/opencode/update-test). 30 exit 0 no-diff; 3 detected GENUINE upstream bumps (all later web-verified):
  1. fluxer 2026.815.131154 -> 2026.815.214426. X-Fluxer-Version header (curl -sI api.fluxer.app, live) = 2026.815.214426; Source0 "latest/rpm" URL returns 200, content-type application/x-rpm. Local docker rpmbuild on fedora:44 SUCCEEDED; install test green; launches with --no-sandbox (timeout-kill while running = no crash; root/no-sandbox error is the known headless Electron limitation).
  2. freebuff 0.0.62 -> 0.0.63. Tag freebuff-desktop-v0.0.63 exists (git ls-remote), release asset Freebuff-0.0.63-linux-x86_64.AppImage present (gh api assets + curl 200). Local docker rpmbuild SUCCEEDED (squashfs extraction via ELF offset); install test green, no missing libs.
  3. helium-browser 0.15.4.1 -> 0.15.5.1. Release 0.15.5.1 published 2026-08-15T19:59:49Z (gh api), asset helium-0.15.5.1-x86_64_linux.tar.xz present (curl 200). Note: releases/latest still reports 0.15.4.1 (newest draft/order quirk) but the 0.15.5.1 tag + assets exist and update.sh's ls-remote+asset-check correctly bumped. Local docker rpmbuild SUCCEEDED; install test green.
- All 32 non-scripted GitHub specs re-scanned via gh api releases/latest: ALL MATCH (aquamarine 0.14.0 ... xdg-desktop-portal-hyprland 1.4.1). materialyoucolor 3.0.4 verified via PyPI JSON API. All git-pin packages unchanged since prior run. No source URLs/checksums to update for the 3 bumped (no sha256 pins in those specs; Source0 URLs are version-substituted and all return 200).

### 3. Fresh-checker install pass (LIVE COPR repo, new container)
- After all 3 COPR builds succeeded (fluxer 10868402, freebuff 10868404, helium-browser 10868407), a FRESH fedora:44 container `dnf copr enable ackerman/nexus` + `install fluxer freebuff helium-browser`: INSTALL_EXIT=0, all 3 correct NVRs, rpm -V CLEAN, ldd no missing libs. Independent re-verification of the fixes.

### 4. Webhook verification
- Webhook/config API 403 (token scope, known gap). Verified EMPIRICALLY: each push produced an immediate COPR build with the new NVR - fluxer 10868402 (2026.815.214426-1), freebuff 10868404 (0.0.63-1), helium-browser 10868407 (0.15.5.1-1). All three aggregate "succeeded" (chroot list = all 3 enabled chroots).

### 5. Dependency audit
| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| fluxer 2026.815.214426 | built RPM auto-Requires = sonames (libX11, libXcomposite, libasound, libatk, libgtk-3, libnss3, etc.) + 8 named Requires in spec | all auto-deps satisfied by named set (at-spi2-core, gtk3, libXScrnSaver, libnotify, libXtst, libuuid, nss, xdg-utils) | none | none | deps-verified (install green, ldd clean) |
| freebuff 0.0.63 | AppImage bundle (Electron 33/Chromium 130 stack) | 14 named Requires (zlib, nss, alsa-lib, gtk3, hicolor-icon-theme, at-spi2-core, libnotify, libdrm, mesa-libgbm, xdg-utils, libXScrnSaver, libXtst, libsecret, libappindicator-gtk3) | none | none | deps-verified (install green, ldd clean) |
| helium-browser 0.15.5.1 | Chromium tarball bundle | Requires(post) desktop-file-utils, gtk-update-icon-cache; Recommends liberation-fonts, vulkan-loader | none | none | deps-verified (install green, ldd clean) |
| ALL OTHER packages | upstream unchanged since prior audits, versions re-verified current this run | carried | - | - | deps-verified (carried) |
- Confidence high for the 3 bumped (rebuilt + installed this run from both local build and live COPR). No dependency changes needed.

### 6. Final state
- 3 commits made this run, one per package, each pushed immediately after local verification: eb6bac5 (fluxer), c4a1063 (freebuff), f41a9f1 (helium-browser) + this relay-only commit. Working tree clean after relay push.
- Actions health: all runs SUCCESS. Spec validation: bash -n on all 3 touched update.sh OK; all 3 specs rpmspec-valid (docker rpmbuild SUCCEEDED).
- Adversarial audit: (1) "versions could be wrong" - each bump web-verified (X-Fluxer-Version header, ls-remote tag + asset 200, gh api release 0.15.5.1) BEFORE touching; (2) "build could fail in COPR despite local pass" - all 3 COPR builds SUCCEEDED and re-verified from live repo; (3) "could be churn" - prior COPR builds carried the OLD NVRs (10868154 fluxer 2026.815.131154-1, 10868194 freebuff 0.0.62-1, 10868191 helium 0.15.4.1-1), so these are genuine NVR changes, not re-syncs.
- Known gaps (unchanged): (1) hyprlauncher/hyprshutdown not in COPR until owner registers - README caveat; (2) GUI apps can't run headless - verified install+rpm -V+ldd+launch-no-crash; (3) COPR webhook/config API 403 - verified empirically via push->build timing; (4) rawhide install tests use --nogpgcheck (fresh image lacks base GPG keys); (5) hyprland-plugins intentionally pinned to 0.56.2 compat commit.

## OWNER DECISION (2026-08-16) - OPENSUSE TUMBLEWEED SUPPORT REMOVED

The opensuse-tumbleweed-x86_64 chroot support has been REMOVED from this repo
by the owner: the COPR opensuse-tumbleweed chroot never produced a working
build (COPR infra bug fedora-copr/copr#4380, broken service-wide since
2026-07-08, still open) and the owner will use openSUSE's OBS for Tumbleweed
packaging instead. The workflow prompts, README, and this relay were updated to
remove all Tumbleweed references. EXPECTED COPR CHROOTS: fedora-rawhide-x86_64,
fedora-44-x86_64, fedora-43-x86_64 ONLY - verify via the COPR project API every
run and treat any other chroot list as the project's actual state. Do NOT
re-add, re-mention, or chase opensuse-tumbleweed builds; if the chroot is still
enabled in COPR settings and its builds fail with the GPG bootstrap error, that
is the known infra bug - ignore and do not report.

## Run 31905208256 (2026-08-15 20:40Z) - COMPLETE: fixed 3 fc44 mass-rebuild casualties (hyprtoolkit/hyprsysteminfo/hyprpaper), all rebuilt + install-tested green, versions 65/65 current.

### 0. Pickup
- Picked up main at `8401f36`. RELAY=false, RUN_ID=31905208256.
- COPR project has THREE chroots (API-verified via chroot_repos): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64 ONLY (owner removed opensuse-tumbleweed). Note: the API `chroots` key is EMPTY; the authoritative field is `chroot_repos`.
- Fedora lifecycle (web-verified prior runs): F44 stable (EOL 2027-06-02), F43 supported (EOL 2026-12-09), F45 branched (GA 2026-10-20), Rawhide=46. No README/chroot change needed.
- Open issues/PRs: NONE (gh api verified). Actions health: all recent opencode + auto-updater runs SUCCESS. fedora-copr/copr#4380 (tumbleweed) still OPEN - ignore.
- Git identity verified: opencode-agent[bot] / 41898282+opencode-agent[bot]@users.noreply.github.com.

### 1. Failed COPR builds: 3 FIXED (owner mass rebuild batch 10868134-10868199, 19:40Z, ALL 63 packages concurrently)
- The mass rebuild raced with COPR repo regeneration: while each package's own rebuild was in flight its old RPMs were unpublished, and fedora 44 base repos recently STARTED SHIPPING hyprwayland-scanner 0.4.2, hyprutils 0.7.1, hyprlang 0.6.4, hyprcursor 0.1.11, hyprgraphics 0.1.5 (older than COPR versions). Three fc44-only failures:
  - 10868178 hyprtoolkit 0.5.4-2 FAILED fc44: compile error `using typedef-name 'wl_resource' after 'struct'` at /usr/include/EGL/eglext.h:1447 (mesa 26.1.6 declares `struct wl_resource;`) vs hyprwayland-scanner-generated headers containing `typedef wl_proxy wl_resource;`. ROOT CAUSE: buildroot resolved `cmake(hyprwayland-scanner)` to FEDORA's 0.4.2 (repo `fedora`) instead of COPR's 0.4.6 during the race. COPR 0.4.6 does NOT emit the wl_resource typedef (verified: generated header has no `typedef wl_proxy wl_resource;`). fc43 (mesa 25.3.6) + rawhide succeeded.
  - 10868177 hyprsysteminfo 0.2.0-4 FAILED fc44: `No match for argument: pkgconfig(hyprtoolkit)` (hyprtoolkit-devel absent - its fc44 build had failed/unpublished).
  - 10868172 hyprpaper 0.8.4-4 FAILED fc44: `No match for argument: pkgconfig(hyprtoolkit)` + `pkgconfig(hyprwire)` + `cannot install both hyprutils-0.14.1-1 from copr_base and hyprutils-0.7.1-4.fc43 from fedora` (transient race artifact; current repo state resolves cleanly).
- Published-state damage from same-NVR rebuild failures: fc44 repo had NO hyprtoolkit at all, and hyprpaper/hyprsysteminfo rolled back to -3. fc43/rawhide unaffected (both -4 and -2 present).
- FIXES APPLIED (Release bumps = new NVRs, all rpmbuild-verified in clean fc44 containers before push, each pushed separately):
  1. hyprtoolkit 0.5.4-2 -> 0.5.4-3: added `BuildRequires: cmake(hyprwayland-scanner) >= 0.4.6` to FORCE the COPR 0.4.6 and prevent ever falling back to fedora's wl_resource-conflicting 0.4.2. Build 10868285 SUCCEEDED all 3 chroots; verified republished (hyprtoolkit-devel 0.5.4-3 in fc44 repo).
  2. hyprsysteminfo 0.2.0-4 -> 0.2.0-5 (rebuild now that hyprtoolkit-devel exists). Build 10868289 SUCCEEDED all 3 chroots.
  3. hyprpaper 0.8.4-4 -> 0.8.4-5. Build 10868291 SUCCEEDED all 3 chroots.
- Install-tests (fresh fedora:44 containers, live COPR repo): hyprtoolkit/hyprsysteminfo/hyprpaper all install clean (INSTALL_EXIT=0), correct NVRs, /usr/bin binaries present, binaries execute (hyprpaper prints welcome banner; hyprsysteminfo fails only on missing XDG_RUNTIME_DIR - expected headless). hyprwayland-scanner --version = 0.4.6.
- All 60 OTHER mass-rebuild builds SUCCEEDED (verified via API list). Spot install-tested the shared-dep hypr ecosystem on fc44 cleanly: hyprland 0.56.2-5, hyprlock, hypridle, hyprpicker, hyprsunset, hyprpolkitagent, hyprqt6engine, + all -devel (hyprwayland-scanner-devel, hyprgraphics-devel, hyprcursor-devel, hyprutils-devel, hyprlang-devel, aquamarine-devel, hyprwire-devel, hyprtoolkit-devel).

### 2. Version accuracy sweep - 65/65 CURRENT
- 32/33 update.sh exit 0 with ZERO spec diffs vs main (fresh checkout). hyprland-plugins update.sh exits 1 = INTENTIONAL (guard correctly finds no hyprpm pin commit for current hyprland 0.56.2; pin 00862ca remains correct - verified live HEAD faf5ef1bd chases future hyprland, pin commit message confirms 0.56.2 compat).
- All hyprwm GitHub latest tags MATCH specs (16 verified): hyprland 0.56.2, aquamarine 0.14.0, hyprutils 0.14.1, hyprlang 0.6.8, hyprcursor 0.1.13, hyprgraphics 0.5.1, hypridle 0.1.8, hyprlock 0.9.6, hyprpaper 0.8.4, hyprpicker 0.4.7, hyprtoolkit 0.5.4, hyprwayland-scanner 0.4.6, hyprwire 0.3.1, hyprsysteminfo 0.2.0, hyprland-protocols 0.7.0, hyprland-guiutils 0.2.2.
- Other GitHub latest tags MATCH: vesktop 1.6.5, matugen 4.1.0, logseq 2.0.1, bibata 2.0.7, obsidian 1.13.7, cliphist 0.7.0, helium-browser 0.15.4.1 (imputnet/helium-linux), mpvpaper 1.9, waypaper 2.8, stoat-desktop 1.4.2 (stoatchat/for-desktop), extension-manager 0.6.5, starship 1.26.0, opencode-desktop 1.18.18, nwg-look 1.1.1, zen-browser 1.21.14b, scenefx 0.5 (wlrfx/scenefx), heroic 2.22.1 (Heroic-Games-Launcher/HeroicGamesLauncher), glaze v8.0.0 (stephenberry/glaze), mangowm 0.16.0 (mangowm/mango).
- Git pins vs live HEAD ALL MATCH: niri-git 606284464 (YaLTeR/niri), xwayland-satellite 3bc915f, lazyvim-git 459a4c3, hyprland-contrib 3dcbce7 (hyprwm/contrib), material-symbols e083cc6 (google/material-design-icons), app2unit 47e23ec, caelestia-cli be54856, caelestia-shell cae55ed, quickshell 28771c7. hyprland-plugins 00862ca = intentional pin (see above).
- Non-GitHub verified: ly v1.4.1 (codeberg), awww v0.12.1 (codeberg), wlroots 0.20.2 (gitlab tags), gpu-screen-recorder 6.0.0 (snapshot URL https://dec05eba.com/snapshot/... HTTP 200), fluxer 2026.815.131154 (X-Fluxer-Version header), materialyoucolor 3.0.4 (PyPI), freebuff 0.0.62 (latest freebuff-desktop-v tag), cascadia 3.5.0 (nerd-fonts), ghostty 1.3.1-0-ppa2 (mkasberg/ghostty-ubuntu), protonplus v0.6.4 tag exists.
- localsend HELD at 1.18.0 CORRECT (v1.18.1 assets = Android APKs only, no linux-x86-64 - verified via gh api).
- rootapp sha256 0736926f...f075d910 + logseq sha256 981bf1f3...307a17 pins unchanged and versions current (deep-verified prior runs).
- Source URLs curl-verified 200: obsidian, vesktop, zen-browser, helium, heroic, localsend, logseq, rootapp, gpu-screen-recorder.

### 3. Install-test sweep (fresh containers, live COPR repo)
- The 3 rebuilt packages install-tested fc44 (green, see section 1). hypr ecosystem spot-tested fc44 green (see section 1). Other 60 packages: versions re-verified current this run, builds all succeeded on all Fedora chroots in the mass rebuild + prior runs' install-test ledger carries.

### 4. Dependency audit
- No NEW dependency changes this run (no upstream version changes). The only packaging change was adding `cmake(hyprwayland-scanner) >= 0.4.6` to hyprtoolkit (defensive pin against fedora 0.4.2 shadow). Carried audits from prior runs unchanged.

### 5. Final state
- 3 commits made this run (one per package): ee6913a (hyprtoolkit 0.5.4-3), bcc2efe (hyprsysteminfo 0.2.0-5), 46abbd5 (hyprpaper 0.8.4-5) + this relay-only commit. Working tree clean after relay push.
- Webhook verified empirically: each push triggered an immediate COPR build (hyprtoolkit 10868285 20:24:36, hyprsysteminfo 10868289 20:33:27, hyprpaper 10868291 20:36:31). COPR webhook/config API 403 (token scope) - same known gap.
- Actions health: all recent runs SUCCESS. Spec validation: update.sh sweep 32/33 ok (hyprland-plugins intentional), 65/65 versions current.
- Known gaps (unchanged): (1) hyprlauncher/hyprshutdown not in COPR until owner registers - README caveat; (2) GUI apps can't run headless - verified install+rpm -V+binaries instead; (3) COPR webhook/config API 403 - verified empirically via push->build timing; (4) rawhide install tests use --nogpgcheck (fresh image lacks base GPG keys); (5) hyprland-plugins is intentionally pinned to 0.56.2 compat commit 00862ca.

## Run 31902875955 (2026-08-15 19:04Z) - COMPLETE: mass-rebuild verified per-chroot (186/186 Fedora green), 65/65 versions current, 63/63 COPR packages install-tested green.

### 0. Pickup
- Picked up main at `890aa35` (prior relay record). RELAY=false, RUN_ID=31902875955.
- COPR project has FOUR chroots (API-verified): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64, opensuse-tumbleweed-x86_64.
- Fedora lifecycle (web-verified): F44 stable (GA 2026-04-28, EOL 2027-06-02), F43 supported (EOL 2026-12-09), F45 branched (GA 2026-10-20, confirmed), Rawhide=46. No README/chroot change needed.
- Open issues/PRs: NONE (gh api verified). Actions health: auto-updater 31901665013 SUCCESS; all recent opencode + auto-updater runs SUCCESS. fedora-copr/copr#4380 (tumbleweed) still OPEN.
- Git identity verified: opencode-agent[bot] / 41898282+opencode-agent[bot]@users.noreply.github.com.

### 1. Failed COPR builds: NONE actionable (mass rebuild = tumbleweed-infra only)
- Owner-triggered mass rebuild of all 63 COPR packages (batch 10867961-10868023 + webhook fluxer 10868050). All show aggregate "failed" because of the Tumbleweed chroot.
- PER-CHROOT VERIFICATION (MANDATORY): fetched builder-live.log (gunzip) for EVERY Fedora chroot of 62 mass-rebuild builds - 186/186 logs end "RPMResults finished" (verified with a script that also greps for error patterns; the only error-pattern hits on rawhide are benign mock cleanup lines like "failed to open .../etc/group for id/name lookup"). The ONLY failing chroot in every build is opensuse-tumbleweed-x86_64 with the KNOWN COPR BUG #2 bootstrap GPG error (404 repomd.xml.asc + Bad GPG signature). NOT packaging bugs.
- wlroots build 10867975 failed at SRPM stage (source_package.version = None): gitlab.freedesktop.org transient outage. wlroots 0.20.2-2 ALREADY built+published (build 10842776 SUCCEEDED; source URL now returns 200 again). No user impact, no spec change.
- Published-state verified: fc44 repodata (revision 1786819399) contains current NVRs - fluxer 2026.815.131154-1, freebuff 0.0.62-1, ly 1.4.1-2, wlroots 0.20.2-2, obsidian 1.13.7-1 all present. Successful Fedora-chroot results ARE published despite aggregate "failed".
- No pending/starting/running builds. Webhook empirically alive: fluxer build 10868050 submitted 18:39:06Z, 2s after auto-updater commit e3cb006 (18:39:04Z), submitter null (webhook).

### 2. Version accuracy sweep - 65/65 CURRENT
- 33 update.sh re-run in FRESH checkout (/tmp/opencode/update-test): ALL exit 0 with ZERO spec diffs vs main. All pass bash -n.
- Git pins vs live HEAD ALL MATCH: niri-git 606284464, xwayland-satellite 3bc915f, lazyvim 459a4c3, hyprland-contrib 3dcbce7, material-symbols e083cc6, app2unit 47e23ec, caelestia-cli be54856 (Ackerman-00/caelestia-cli-mango), caelestia-shell cae55ed, quickshell 28771c7 (git.outfoxxed.me), hyprland-plugins 00862ca = INTENTIONAL PIN (commit msg "hyprpm: add pin for 0.56.2"; hyprland still 0.56.2; the 2 newer upstream commits chase a future hyprland - NOT for us).
- Non-scripted via gh api releases/latest/tags - ALL MATCH: all hyprwm packages (hyprland 0.56.2, aquamarine 0.14.0, hyprutils 0.14.1, hyprlang 0.6.8, hyprcursor 0.1.13, hyprgraphics 0.5.1, hypridle 0.1.8, protocols 0.7.0, guiutils 0.2.2, qt-support 0.1.0, hyprlock 0.9.6, hyprpaper 0.8.4, hyprpicker 0.4.7, polkitagent 0.1.3, qt6engine 0.1.0, sunset 0.4.0, systeminfo 0.2.0, toolkit 0.5.4, wayland-scanner 0.4.6, wire 0.3.1, xdp 1.4.1, launcher 0.1.6, shutdown 0.1.1), glaze 8.0.0, mangowm 0.16.0, matugen 4.1.0, nwg-look 1.1.1, starship 1.26.0, vesktop 1.6.5, waypaper 2.8, mpvpaper 1.9, scenefx 0.5, stoat-desktop 1.4.2, obsidian 1.13.7, heroic 2.22.1, opencode-desktop 1.18.18, protonplus 0.6.4 (tag v0.6.4), logseq 2.0.1, extension-manager 0.6.5, bibata 2.0.7, cliphist 0.7.0, helium 0.15.4.1, zen-browser 1.21.14b, cascadia 3.5.0 (nerd-fonts repo), ghostty 1.3.1-0-ppa2 (mkasberg/ghostty-ubuntu latest tag).
- Non-GitHub verified: ly v1.4.1 (codeberg tags), awww v0.12.1 (LGFae/awww codeberg), wlroots 0.20.2 (gitlab tags), gpu-screen-recorder 6.0.0 (snapshot URL 200), fluxer 2026.815.131154 (X-Fluxer-Version header on api.fluxer.app), freebuff 0.0.62 = latest freebuff-desktop-v tag (CodebuffAI/codebuff-community), materialyoucolor 3.0.4 (PyPI).
- localsend HELD at 1.18.0 CORRECT (v1.18.1 assets = Android APKs only, no linux-x86-64 - verified via gh api release assets).
- rootapp deep-audit RE-DONE: downloaded AppImage (386MB), sha256 0736926f...f075d910 EXACTLY matches spec pin, extracted via ELF offset (942712 + 64*30 = 944632), X-AppImage-Version=0.9.126 matches spec.
- logseq checksum VERIFIED: downloaded zip, sha256 981bf1f3...307a17 EXACTLY matches spec pin.
- Source URLs curl-verified 200 (with -L redirect follow): obsidian, vesktop, zen-browser, helium, heroic, localsend, logseq, ghostty, gpu-screen-recorder, rootapp.

### 3. Install-test sweep (FRESH containers, LIVE COPR repo) - 63/63 COPR packages GREEN
- ALL 63 COPR-shipped packages installed this run in fresh fedora:44 containers via `dnf copr enable ackerman/nexus` + install: all `dnf install` clean, `rpm -V` CLEAN. Batches: fluxer/starship/rootapp/ly; hyprland/obsidian/freebuff/vesktop/localsend; niri-git/xwayland-satellite-git/lazyvim-git/zen-browser; cliphist/waypaper/mpvpaper/nwg-look/matugen/mangowm/stoat-desktop; logseq/helium/extension-manager/bibata/cascadia/awww/libcava; gpu-screen-recorder/ghostty/quickshell-git/hyprland-contrib/hyprland-plugins/hyprland-guiutils/hyprland-qt-support; hyprcursor/hyprgraphics/hypridle/hyprlang/hyprlock/hyprpaper/hyprpicker; hyprpolkitagent/hyprqt6engine/hyprsunset/hyprsysteminfo/hyprtoolkit/hyprutils/hyprwayland-scanner(-devel)/hyprwire; scenefx/aquamarine/python3-materialyoucolor/app2unit/caelestia-cli/caelestia-shell/xdp-hyprland; glaze-devel; heroic/protonplus/opencode-desktop/gpu-screen-recorder; wlroots-devel; hyprland-protocols-devel/material-symbols-fonts.
- Smoke tests passed: starship --version = 1.26.0, ly --version = 1.4.1, niri --version = 26.04 (6062844), ghostty --version = Ghostty 1.3.1, matugen --version = 4.1.0, hyprwayland-scanner --version = 0.4.6.
- fluxer + starship ALSO tested on fedora:rawhide (clean, --nogpgcheck for the known fresh-rawhide missing-GPG-key issue) and fedora:43 (clean) - all 3 chroots.
- Fresh-checker pass: fluxer + starship re-installed from scratch in a NEW container - clean, rpm -V CLEAN.
- hyprlauncher/hyprshutdown: NOT in COPR (pending owner registration, README caveat). Specs version-verified (0.1.6 / 0.1.1) but not install-testable from repo.
- Tumbleweed: blocked-(copr-infra) per KNOWN COPR BUG #2 - no COPR tumbleweed repodata exists, no install test possible.

### 4. Dependency audit
| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| freebuff 0.0.62 | built RPM Requires = 15 named (alsa-lib, at-spi2-core, gtk3, hicolor-icon-theme, libXScrnSaver, libXtst, libappindicator-gtk3, libdrm, libnotify, libsecret, mesa-libgbm, nss, xdg-utils, zlib, + sh) | all 15 present (torn via rpm2cpio from COPR RPM) | none | none | deps-verified (install green fc44) |
| obsidian 1.13.7 | built RPM Requires = 13 named (alsa-lib, at-spi2-core, gtk3, hicolor-icon-theme, libXScrnSaver, libXtst, libnotify, libsecret, libuuid, mesa-libgbm, nss, xdg-utils, zlib) | all 13 present | none | none | deps-verified (install green fc44) |
| fluxer 2026.815.131154 | built RPM Requires = 8 named (at-spi2-core, gtk3, libXScrnSaver, libnotify, libXtst, libuuid, nss, xdg-utils) + auto sonames | all 8 present | none | none | deps-verified (install green fc44/rawhide/fc43) |
| rootapp 0.9.126 | AppImage bundles AppRun+usr, no system deps | no manual Requires | none | none | deps-verified (sha256 + version from inside match spec) |
| ALL OTHER packages | upstream unchanged since prior audits, versions re-verified current this run, all 63 installs green this run | carried | - | - | deps-verified (carried, install green this run) |
- Confidence: high for freebuff/obsidian/fluxer/rootapp (re-audited from torn RPMs/AppImage); carried audits medium-high (upstream unchanged, versions re-verified, installs green).

### 5. Final state
- Working tree clean. No commits made this run (nothing broken, nothing stale - NO-CHURN respected; the owner mass rebuild + auto-updater fluxer bump all built + install-tested green). Relay-only commit.
- Actions health: auto-updater 31901665013 SUCCESS (fluxer bump); all recent opencode + auto-updater runs SUCCESS.
- Spec validation: all 33 update.sh pass bash -n; 65/65 packages version-verified current; 63/63 COPR packages install green.
- Adversarial audit passed: (1) all 62 aggregate-"failed" builds checked PER-CHROOT via gunzipped builder-live.log - 186/186 Fedora logs "RPMResults finished", error-pattern grep only hits benign mock cleanup; (2) repodata verified to actually publish current NVRs (fluxer/freebuff/ly/wlroots/obsidian in fc44 primary); (3) every package's latest build used the CURRENT spec version (reconciled 63/63); (4) rootapp + logseq verified from downloaded artifacts (sha256 matches spec pins); (5) all update.sh guards are pure version/commit comparisons (audited niri-git, ly, hyprland-plugins, rootapp update.sh - no artifact-file guards that would cause churn).
- Known gaps (unchanged): (1) hyprlauncher/hyprshutdown not in COPR until owner registers - README caveat present; (2) GUI apps can't run headless - verified install+rpm -V+ldd instead; (3) COPR webhook/config API 403 (token scope) - verified empirically via push->build timing (fluxer 10868050, 2s after commit); (4) tumbleweed chroot broken infra-side (copr#4380 open) - Fedora chroots authoritative; (5) rawhide install tests use --nogpgcheck (fresh image lacks base GPG keys); (6) wlroots latest build id 10867975 failed at SRPM stage from transient gitlab outage - previous successful build (10842776) still published and current.

## Run 31901892424 (2026-08-15 18:43Z) - COMPLETE: owner-triggered mass rebuild verified (all Fedora chroots green), versions current, nothing to fix.

### 0. Pickup
- Picked up main at `e3cb006` (auto-updater 31901665013 18:39Z bumped fluxer to 2026.815.131154). RELAY=false, RUN_ID=31901892424.
- COPR project now has FOUR chroots (API-verified): fedora-rawhide-x86_64, fedora-44-x86_64, fedora-43-x86_64, opensuse-tumbleweed-x86_64 (tumbleweed added by owner).
- Fedora lifecycle (web-verified): F44 stable (GA 2026-04-28, EOL 2027-06-02), F43 supported (EOL 2026-12-09), F45 branched (GA ~Oct/Nov 2026), Rawhide=46 (builds carry fc46 dist tag). No README/chroot change needed.
- Open issues/PRs: NONE (gh api verified). Actions health: auto-updater 31901665013 SUCCESS; all recent runs SUCCESS. fedora-copr/copr#4380 (tumbleweed) still OPEN.
- Git identity verified: opencode-agent[bot] / 41898282+opencode-agent[bot]@users.noreply.github.com.

### 1. Failed COPR builds: NONE actionable (mass rebuild = tumbleweed-infra only)
- Owner triggered a FULL rebuild of all 63 COPR packages (batch 10867961-10868023 + webhook fluxer 10868050). ALL show aggregate "failed".
- Per-chroot verification (MANDATORY): fetched builder-live.log for every Fedora chroot of every batch build - 177/177 checks "RPMResults finished" (OK). The ONLY failing chroot in every build is opensuse-tumbleweed-x86_64 with the KNOWN COPR BUG #2 bootstrap error (404 repomd.xml.asc + Bad GPG signature on OSS repo; fedora-copr/copr#4380 still open). NOT packaging bugs.
- wlroots build 10867975 failed at SRPM stage: gitlab.freedesktop.org returned 504 downloading the source (transient upstream outage). wlroots 0.20.2-2 was ALREADY built+published (build 10842776 SUCCEEDED, verified in fc44 primary metadata). No user impact, no spec change.
- Published-state verified: fc44 repomd.xml (revision 1786819399) contains the current NVRs - fluxer 2026.815.131154-1.fc44, ly 1.4.1-2.fc44, wlroots 0.20.2-2.fc44 all present. Successful Fedora-chroot results ARE published despite aggregate "failed".
- No pending/starting/running builds. Webhook empirically alive: fluxer build 10868050 submitted 18:39:06Z, 2s after auto-updater commit e3cb006 (18:39:04Z), submitter null (webhook).

### 2. Version accuracy sweep - 65/65 CURRENT
- 33 update.sh re-run in FRESH checkout (/tmp/opencode/update-test): 32 exit 0 with ZERO spec diffs vs main. wlroots update.sh failed only because gitlab.freedesktop.org API returns 502 (same transient outage; guard exits 1 correctly, nothing to fix).
- Git pins vs live HEAD ALL MATCH: niri-git 606284464d, xwayland-satellite 3bc915f, lazyvim 459a4c3, hyprland-contrib 3dcbce7, material-symbols e083cc6, app2unit 47e23ec (Vladimir-csp/app2unit), caelestia-cli be54856, caelestia-shell cae55ed, quickshell 28771c7 (git.outfoxxed.me). hyprland-plugins 00862ca = intentional pin (verified: commit message "hyprpm: add pin for 0.56.2"; hyprland still 0.56.2).
- Non-scripted via gh api releases/latest - ALL MATCH: all hyprwm packages (hyprland 0.56.2, aquamarine 0.14.0, hyprutils 0.14.1, hyprlang 0.6.8, hyprcursor 0.1.13, hyprgraphics 0.5.1, hypridle 0.1.8, protocols 0.7.0, guiutils 0.2.2, qt-support 0.1.0, hyprlock 0.9.6, hyprpaper 0.8.4, hyprpicker 0.4.7, polkitagent 0.1.3, qt6engine 0.1.0, sunset 0.4.0, systeminfo 0.2.0, toolkit 0.5.4, wayland-scanner 0.4.6, wire 0.3.1, xdp 1.4.1, launcher 0.1.6, shutdown 0.1.1), glaze 8.0.0, mangowm 0.16.0, matugen 4.1.0, nwg-look 1.1.1, starship 1.26.0, vesktop 1.6.5, waypaper 2.8, mpvpaper 1.9, scenefx 0.5, stoat-desktop 1.4.2, obsidian 1.13.7, heroic 2.22.1, opencode-desktop 1.18.18, protonplus 0.6.4, logseq 2.0.1, libcava 1.0.0, extension-manager 0.6.5, cascadia 3.5.0, bibata 2.0.7, cliphist 0.7.0, helium 0.15.4.1, zen-browser 1.21.14b, ghostty 1.3.1-0-ppa2.
- Special cases verified: fluxer 2026.815.131154 = X-Fluxer-Version header (curl -sI, confirmed). freebuff 0.0.62 = latest freebuff-desktop-v tag (git ls-remote tags, confirmed; 0.0.62 is last, not the CLI v1.0.x tags). localsend HELD at 1.18.0 CORRECT (v1.18.1 assets = Android APKs only, no linux-x86-64 - verified via gh api release assets). rootapp deep-audit re-done: downloaded AppImage (386MB), sha256 0736926f...075d910 EXACTLY matches spec pin, extracted via ELF offset (944632), X-AppImage-Version=0.9.126 matches spec. ly 1.4.1 (codeberg), awww 0.12.1 (codeberg releases/latest), gpu-screen-recorder 6.0.0 (dec05eba), materialyoucolor 3.0.4 (PyPI) ALL MATCH.
- Source URLs curl-verified 200 (following redirects, with %{version}/%{url} expansion): rootapp, freebuff, obsidian, localsend, vesktop, zen-browser, helium, heroic.

### 3. Install-test sweep (FRESH containers, LIVE COPR repo)
- fluxer 2026.815.131154-1: fc44 clean install (111 pkgs), rpm -V CLEAN, no missing libs, launches (timeout 20s killed, no crash). rawhide clean (--nogpgcheck for the known fresh-rawhide missing-GPG-key issue on fedora-cisco-openh264 dep; rpm -V CLEAN). fc43 clean, rpm -V CLEAN.
- ly 1.4.1-2: fc44 clean, rpm -V CLEAN, `ly --version` = Ly version 1.4.1.
- rootapp 0.9.126, starship 1.26.0, obsidian 1.13.7, hyprland 0.56.2-5, zen-browser 1.21.14b, wlroots 0.20.2-2: all fc44 clean install, rpm -V CLEAN, no missing libs. starship --version = 1.26.0.
- Fresh-checker pass: fluxer re-installed from scratch in a NEW container - clean, rpm -V CLEAN.
- Note: rawhide needs --nogpgcheck because the fresh rawhide image ships no base GPG keys (known; documented in prior runs).

### 4. Dependency audit
| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| fluxer 2026.815.131154 | built RPM Requires = 8 named (at-spi2-core, gtk3, libXScrnSaver, libnotify, libXtst, libuuid, nss, xdg-utils) + auto sonames | all 8 present | none | none | deps-verified (RPM torn apart via rpm2cpio, spec matches, install green fc44/rawhide/fc43) |
| ly 1.4.1 | no manual Requires beyond auto | - | none | none | deps-verified (install green fc44) |
| wlroots 0.20.2 | deps unchanged | carried | none | none | deps-verified (install green fc44) |
| rootapp 0.9.126 | AppImage bundles AppRun+usr, no system deps | no manual Requires | none | none | deps-verified (sha256 + version from inside match spec) |
| starship/obsidian/hyprland/zen-browser | versions unchanged since prior audits, installs green | carried | none | none | deps-verified (carried, install green this run) |
| ALL OTHER packages | upstream unchanged since prior audits, versions re-verified current this run | carried | - | - | deps-verified (carried, install green prior runs) |
- Confidence: high for fluxer (re-audited from torn RPM), ly/rootapp/starship/obsidian/hyprland/zen/wlroots (install green this run); carried audits medium-high.

### 5. Final state
- Working tree clean. No commits made this run (nothing broken, nothing stale - NO-CHURN respected; the owner mass rebuild + auto-updater fluxer bump all built + install-tested green). Relay-only commit.
- Actions health: auto-updater 31901665013 SUCCESS (fluxer bump); all recent opencode runs SUCCESS.
- Spec validation: all 33 update.sh pass bash -n; 65/65 packages version-verified current; 63/63 COPR packages built on all Fedora chroots + spot-install-tested green.
- Adversarial audit passed: (1) all 63 aggregate-"failed" builds checked PER-CHROOT - 177/177 Fedora chroot logs show success, only tumbleweed fails (infra bug); (2) repodata verified to actually publish current NVRs (fluxer/ly/wlroots in fc44 primary); (3) every package's latest build used the CURRENT spec version (reconciled 63/63); (4) rootapp verified from inside binary.
- Known gaps (unchanged): (1) hyprlauncher/hyprshutdown not in COPR until owner registers - README caveat present; (2) GUI apps can't run headless - verified install+rpm -V+ldd instead; (3) COPR webhook/config API 403 (token scope) - verified empirically via push->build timing (fluxer 10868050, 2s after commit); (4) tumbleweed chroot broken infra-side (copr#4380 open) - Fedora chroots authoritative; (5) rawhide install tests use --nogpgcheck (fresh image lacks base GPG keys); (6) gitlab.freedesktop.org currently flaky (502/504) - wlroots source + API unreachable transiently, previous successful build still published.

### 6. Previous runs (historical, for continuity)
- Run 31885834442 (2026-08-15 12:55Z): COMPLETE - full verification sweep, 61/61 COPR packages install green, nothing to fix.
- Run 31870586336 (2026-08-15 07:12Z): COMPLETE - fresh-checker full verification sweep, nothing to fix.
- Run 31857392968 (2026-08-15 01:52Z): COMPLETE - auto-updater bumped freebuff/fluxer, both builds verified + install-tested.
- Run 31831947044 (2026-08-14 19:08Z): COMPLETE - fresh-checker full verification sweep, nothing to fix.
- Run 31826065924 (2026-08-14 17:53Z): COMPLETE - full verification sweep, nothing to fix.
- Run 31822822283 (2026-08-14 17:11Z): COMPLETE - fresh-checker full verification sweep, nothing to fix.
- Run 31805374412 (2026-08-14 13:34Z): COMPLETE - full verification sweep, nothing to fix.
- Run 31781219208 (2026-08-14 07:46Z): COMPLETE - fresh-checker verification sweep, nothing to fix.
- Run 31764384134 (2026-08-14 02:39Z): COMPLETE - full verification sweep, nothing to fix.
- Run 31734958112 (2026-08-13 19:15Z): COMPLETE - fixed hyprutils/zen-browser/protonplus rebuilds.
- Run 31706023338 (2026-08-13 13:39Z): COMPLETE.