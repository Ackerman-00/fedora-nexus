# fedora-nexus autonomous-run relay state

status: complete
run_id: 31249958731

## Run - RUN_ID 31249958731 (2026-08-08) - COMPLETED (fresh-checker pass, 0 defects found)

### 0. Pickup status
- Previous run (31241213248) declared `status: complete`. This run acted as an
  independent fresh checker with a clean slate: every claim re-derived from
  scratch, none trusted. `git log` HEAD (e87b433) matched the relay's last
  entry. RELAY=false, so no relay continuation work.
- The previous run's headline findings reproduced exactly: 56 package dirs,
  80 rpm names, 78 directly-installable (glaze + hyprland-protocols are
  source-only; hyprwayland-scanner installs by pulling its -devel).

### 1. COPR Build Status
- Chroots (COPR API `project?ownername=ackerman&projectname=nexus` is
  authoritative): **fedora-rawhide-x86_64, fedora-44-x86_64,
  fedora-43-x86_64**. Unchanged. **fedora-45-x86_64 does NOT exist yet** in
  api_3/mock-chroots/list (mass branching is 2026-08-11). Nothing to enable.
- All 56 packages: per-package `build/list?...&packagename=<pkg>&limit=1`
  shows latest build **succeeded** for every package (56/56).
- Historical failures (only 4 in last 300 builds) each superseded by a
  successful successor: hyprgraphics 0.5.1-1 -> 0.5.1-2 (10822291/2),
  aquamarine 0.14.0-1 -> 0.14.0-2 (10822291), gpu-screen-recorder 5.15.3-3 ->
  6.0.0-3 (10837627), cascadia-code-nerd-fonts (no NVR) -> 3.5.0-1.
- **0 builds in state skipped/starting/running/pending/canceled** across the
  last 300 -> no "Release-not-bumped => fix not shipped" smell.
- Per-chroot `success` marker files confirmed present (HTTP 200) for the most
  recent builds on ALL THREE chroots: extension-manager 10837791, logseq
  10837693, hyprland-contrib 10837596, zen-browser 10837121, niri-git
  10836874.
- Repodata reachable via dnf on 43, 44, rawhide (actual installs succeeded).

### 2. Bug found / fixed this run
- **None.** Every check that historically found bugs came back clean: changelog
  date validator (0 bogus dates), repo spec parse (`rpmspec -P` 0 failures in
  container), external-command scans, repoclosure, install tests, rpm -V, ldd,
  artifact tear-downs. No spec/update.sh/workflow/README edits were needed,
  and none were made. Working tree left clean (only the relay file is
  rewritten by this run).

### 3. Version Accuracy Sweep - 100% of 56 packages re-verified
- **8 git-pinned packages** pinned to exact upstream HEAD commits
  (`git ls-remote HEAD` == spec pin): app2unit, caelestia-shell-mango,
  hyprland-contrib, lazyvim-git, material-symbols-fonts, niri-git,
  quickshell-git (git.outfoxxed.me + github mirror both match), and
  xwayland-satellite-git. **hyprland-plugins is ALSO git-pinned but to
  00862ca = upstream `hyprpm: add pin for 0.56.2` on purpose** - it tracks the
  hyprland 0.56.2 stable release, so its pin is NOT upstream HEAD (a9eaa52
  "chase hyprland" follows hyprland-git). Verified: this is a legitimate
  compatibility pin, not a missed bump.
- **GitHub-release packages** (`ghapi releases/latest` / tag queries): all
  match the spec, incl. hyprland v0.56.2, zen-browser 1.21.12b, starship
  v1.26.0, vesktop v1.6.5, localsend v1.17.0, extension-manager v0.6.5,
  logseq (latest release), scenefx v0.5, mpvpaper v1.9, heroic 2.22.0, etc.
- Non-GitHub upstreams re-verified live: awww (codeberg) 0.12.1; wlroots
  (gitlab) 0.20.2; gpu-screen-recorder (repo.dec05eba.com) 6.0.0; matugen
  (crates.io max_stable) 4.1.0; fluxer (CDN `x-fluxer-version`) 2026.731.
- **Binary artifacts torn apart, version read from INSIDE** (10 packages):
  rootapp sha256 **0736926fde9238...** == spec's pinned `# sha256:` value
  AND `X-AppImage-Version=0.9.126`; obsidian deb control 1.13.4;
  opencode-desktop deb control 1.18.15; vesktop rpm 1.6.5; heroic rpm 2.22.0;
  localsend deb control 1.17.0+58 (release tag v1.17.0 == spec); stoat
  (app.asar) desktop 1.4.2; zen-browser tar.xz application.ini 1.21.12b;
  logseq zip sha256 == spec's pinned 981bf1f4...; fluxer rpm app.asar reports
  internal 0.0.8 (its build-time bom) but the CDN `x-fluxer-version:
  2026.731.153836` is authoritative and matches spec. All ==, no mismatch.
- Source-URL sweep (curl -6I within container over macro-expanded urls):
  all 200/203 or further verified by actual GET. Only benign outages: wlroots
  gitlab 403-on-HEAD (GET works, tarball 200), matugen crates.io 403-on-HEAD
  (static.crates.io GET works), bibata 404-on-HEAD (GET redirects 200). No
  real source-URL defects.

### 4. GitHub Actions Health
- Recent `Fedora Nexus Auto-Updater` runs all `success`. The latest one
  (31248640501, 2026-08-08): all steps incl. "Commit and Push"
  success/skipped because no changes (no-churn behavior = correct).
- update-engine.yml parses as clean YAML; no automation defects found.
- No opencode-schedule failures: all recent `opencode-schedule` runs success
  except this (currently in-progress, as is normal).
- update.sh audit in a throwaway clone: all ran with rc=0 and produced zero
  spec changes (no churn). Scanned all update.sh for the flawed "guard
  requires local artifact" pattern: only awww has `aws-*.tar.gz` but that line
  is a log print, its up-to-date guard is a pure version string comparison -
  no flaw.

### 5. Install Verification Sweep (fresh `dnf install` per name, live copr)
- Fresh `docker run --rm` containers, copr enabled, live repo. Name list built
  inside a container with `rpmspec -q --qf="%{NAME}\n"` over all 56
  specs = **80 distinct rpm names** (incl. -devel and contrib/plugin
  subpkgs).
  - **fedora:43 -> 78/80** installed
  - **fedora:44 -> 78/80** installed
  - **fedora:rawhide (F45) -> 78/80** installed
  The 2 non-installing names are **glaze** and **hyprland-protocols** - the
  source-only base packages whose real content ships under their **-devel
  subpackage** (glaze-devel, hyprland-protocols-devel both install). This is
  by design, reproducing the historical 78 baseline exactly.
- rpm -V over the installed set on fc44: 0 failures.
- Runtime smoke (fc44 container, true exit codes): hypridle 0, hyprlock 0,
  hyprpaper 0, starship 0, nwg-look `-v`, extension-manager 0. All OK.

### 6. Dependency Audit (repo-wide)
- `dnf repoclosure --newest --check=copr:...:ackerman:nexus`:
  - fc43 -> 0 unresolved
  - fc44 -> 0 unresolved
  - rawhide -> **0 nexus-maintained packages in failure**. The historical
    single hit (noctalia-shell) is gone: issue #6 was closed and the orphaned
    noctalia repodata was pruned from COPR this morning (verified via
    `pkg/list?query=noctalia` = 0 entries). Remaining rawhide repoclosure
    noise is entirely base-Fedora, not nexus. (The practical gate is §5: all
    78 installable nexus rpms install clean on all three chroots.)
- Script-level external-command scan: hyprland-contrib subpackages and
  gpu-screen-recorder scripts' tool references are all covered by the specs'
  Requires/Recommends (verified against each script).
- No concurrent package audits flagged by COPR: latest-build-success status +
  install success in clean chroot = the authoritative proxy already used and
  confirmed this pass.

### 6. Fedora Lifecycle
- Fedora 44 = current stable. Fedora 43 supported (no EOL until ~Dec 2026).
  Rawhide detects as "Fedora release 45 (Rawhide)". Fedora 45 mass-branching
  2026-08-11; fedora-45 x86_64 chroot does NOT exist yet -> nothing to enable
  or retire, README already accurate (no Fedora-version pinning).
- Next run after 2026-08-11 must re-check mock-chroots for fedora-45-x86_64
  and enable it if present.

### 7. Issues & PRs
- Open PRs: **none** (`gh pr list` -> empty).
- Open issues: **none.** Issue **#6** (orphaned noctalia-shell/noctalia-qs RPMs
  left in the COPR repo, uninstallable on rawhide) was **CLOSED** on
  2026-08-08T09:30:50Z. Re-verified owner-side prune: COPR `pkg/list?query=
  noctalia` now returns **0 entries**. Rawhide repoclosure: the FAILING
  packages are all base-Fedora noise (0 nexus-maintained names match), and
  every nexus rpm still installs clean on rawhide as verified in §5, so the
  repo is fully self-consistent now. No owner-credentialed cleanup needed
  anymore.

### 8. Webhook delivery
- `gh api repos/Ackerman-00/fedora-nexus/hooks` -> 403 (token without
  `admin:repo_hook`), so the delivery ids/statuses for last run's pushes are
  unreadable (same as previous runs). Empirical proof instead: the last push
  (588f707 for extension-manager, commit on 2026-08-06 mornings) produced COPR
  build 10837791 within ~3 min, fully succeeded on all chroots, and no
  "skipped" state appeared; the COPR repoclosure at that NVR confirms the
  fixed RPM is live. Webhook therefore alive and verified empirically.

### 9. Not-verified-by-docker apps (runtime smoke only)
- seed apps (obsidian, logseq, fluxer, heroic, localsend, stoat-desktop,
  opencode-desktop, vesktop, rootapp) were NOT launched (headless/runner,
  most need display/Wayland).
- Compositors not started (hyprland, niri, mangowm need real GPU/Wayland).
  They are verified via install + rpm -V + version-from-artifact reads.

### 10. End-notes for next run
1. **2026-08-11 = F45 mass branching**: enable fedora-45-x86_64 chroot if it
   exists, rebuild/verify all, update README, transition rawhide tracking to
   F46. If the chroot still isn't there, nothing to do.
2. Issue #6 is CLOSED and noctalia fully pruned (verified this run) - no
   further action unless a new unresolved dep appears.
3. Keep the checks that find bugs: changelog date/order validator,
   per-package repoclosure on the test chroots, the 78/80 fresh-install sweep,
   version-from-binary tear-downs. Plain `dnf install` alone does not find
   the historical defect classes.
4. Build the package-name list inside a Fedora container with rpmspec
   `--qf="%{NAME}"` (go-rpm-macros); pipelines with the runner's older rpm do
   not expand `%format` source URLs.

### Closing note
This run found **0 new defects**: all 56 packages build on all 3 chroots, no
build left in nonterminal/skipped state, 78/80 install clean on fc43/fc44/F45
(glaze + hyprland-protocols are legitimately source-only), every version
verified against live upstream, the one open issue (#6) got closed by its
owner this morning and the repo is now fully consistent. No spec or CI edits
were needed; nothing was pushed. Relay rewritten to record this fresh-checker
run; working tree otherwise clean.