# fedora-nexus autonomous-run relay state

status: complete
run_id: 31408592662

## Run 31408592662 (2026-08-10) - COMPLETE: obsidian 1.13.6 build fix + niri-git refresh both shipped, verified to build + install clean; 61-pkg sweep all current; issue #8 closed

### 0. Pickup
- Picked up main at `1b1fb4d` ("fix(ci): remove invalid workflows: write scope"). Previous
  relay (31393778033) was `status: complete`. Started as FRESH CHECKER.
- Note: commits 5c4de0a (SANE_TAG guard in update-engine scanner) and 1b1fb4d (removed the
  invalid `workflows: write` permission scope it added) landed on main between relays — so
  issue #8's requested guard is NOW actually applied, and the broken workflow runs (#78/#3
  which failed at 5c4de0a) are fixed.
- COPR chroots (APIfc): fedora-43-x86_64, fedora-44-x86_64, fedora-rawhide-x86_64.
- Fedora lifecycle (web+COPR): F44 stable, F43 supported, rawhide=F45. Chroots correct.

### 1. Actions health (priority 3)
- opencode-schedule #78 and opencode #3 failed at 5c4de0a because `workflows: write` is not a
  valid GITHUB_TOKEN permission scope -> validator rejected whole workflow. Fixed by 1b1fb4d.
- This run (opencode-schedule #79) ran from 1b1fb4d -> proves the fixed workflow runs.
- Triggered auto-updater health-check manually: run **31409642941** SUCCEEDED
  (`update-packages: success`), and it bumped obsidian 1.13.4 -> 1.13.6 (commit dd842c6),
  executing the SANE_TAG guard code path (logs show `if not tag_is_sane...`).

### 2. COPR build scan (priority 1) — obsidian failure FOUND + FIXED + VERIFIED
- **obsidian 10843364 (1.13.6-1) FAILED** on all 3 chroots:
  `error: File not found: .../BUILDROOT/usr/share/applications/obsidian.desktop`.
  Root cause: upstream 1.13.6 renames `obsidian.desktop` -> `md.obsidian.Obsidian.desktop`
  (verified by extracting the official obsidian_1.13.6_amd64.deb).
- FIX: obsidian.spec Version stays 1.13.6, Release bumped 1->2, %files glob changed to
  `%{_datadir}/applications/%{app_id}.desktop`, changelog entry added. Commit **2189c40**
  pushed 17:23 UTC. Webhook fired -> COPR build **10843472** (1.13.6-2).
- **COPR 10843472 SUCCEEDED** (all chroots). COPR-downloaded 1.13.6-2 RPM install-tested in
  fresh fedora:44: INSTALL_EXIT=0, rpm -V clean, desktop file present. Local docker rpmbuild
  also passed. FRESH-CHECKER re-run (container `obfresh`, copr-enabled, COPR artifact only):
  INSTALL_EXIT=0, md.obsidian.Obsidian.desktop present, rpm -q obsidian-1.13.6-2.fc44,
  asar present, rpm -V clean. Status: verified.
- Dep audit (obsidian): upstream deb Depends libgtk-3-0/libnotify4/libnss3/libxss1/libxtst6/
  xdg-utils/libatspi2.0-0/libuuid1/libsecret-1-0 all mapped in spec Requires (+ Electron
  extras zlib/alsa-lib/hicolor-icon-theme/mesa-libgbm). ldd of bundle: no missing libs.
  Status: deps-verified.

### 3. Issue #8 — CLOSED with evidence
- Guard live on main (lines 76-79 update-engine.yml), regex verified, yaml+python valid,
  live run 31409642941 executed the guard. Closed with comment.

### 4. Version sweep (priority 2) — ALL 61 packages checked, single update shipped
- re-ran every update.sh in a fresh copy (31/31 pass + report up-to-date) + gh api
  releases/tags + web/CDN for the other 30 (no update.sh). Verdict: 60 up-to-date, 1 updated:
  - obsidian 1.13.4 -> 1.13.6 (UPDATED via updater dd842c6; build fix 2189c40; shipped+verified)
  - niri-git refreshed 97c96a1 -> **464d45d** (see below).
- Latest versions verified by direct upstream query: vesktop v1.6.5, starship v1.26.0,
  nwg-look v1.1.1, waypaper 2.8, aquamarine v0.14.0, hyprland v0.56.2, hyprcursor/hyprgraphics/
  hypridle/hyprlang/hyprlock/hyprpaper/hyprpicker/hyprpolkitagent/hyprsunset/hyprsysteminfo/
  hyprtoolkit/hyprutils/hyprwire/hyprland-guiutils v<right>, hyprland-qt-support v0.1.0,
  hyprqt6engine v0.1.0, xdg-desktop-portal-hyprland v1.4.1, hyprland-protocols v0.7.0,
  hyprwayland-scanner v0.4.6, glaze v8.0.0, stoat-desktop v1.4.2, mangowm 0.15.6, gpu-screen-
  recorder 6.0.0 (git.dec05eba tags), libcava 1.0.0, scenefx 0.5, mpvpaper 1.9,
  matugen 4.1.0 (crates.io), materialyoucolor 3.0.4 (PyPI), bibata 2.0.7, cascadia 3.5.0,
  extension-manager 0.6.5, heroic 2.22.1, globs of binary URLs all HTTP 200 (incl. vesktop/
  localsend/heroic/helium/logseq/stoat), protonplus v0.6.2 (repo = vysp3r/ProtonPlus),
  rootapp teardown: X-AppImage-Version=0.9.126 + sha256 matches update.sh pin.

### 5. niri-git refresh — SHIPPED + VERIFIED
- HEAD advanced 97c96a1 -> 464d45daa229f16934399ceb16e94783ed2c241c (committer
  2026-08-10T15:20:26Z), confirmed via gh api + git ls-remote. The 16:35 auto-updater said
  "no update" (protocol cache lag).
- update.sh patched commit/gitdate/Version/changelog -> NVR 1:26.04^20260810152026git464d45d-1.
- Commit **84cd05e** pushed 17:47 UTC. Webhook fired -> COPR build **10843507**.
- **COPR 10843507 SUCCEEDED** (started 17:48, ended 18:05, all chroots). FRESH-CHECKER re-run
  (container `nirifresh`, copr-enabled, COPR artifact only): INSTALL_EXIT=0,
  `niri 26.04 (464d45d)` exit 0, rpm -V clean.
- Dep audit (niri-git): Cargo.toml deps unchanged vs prior audit; no new direct deps
  (vendored crates + pkgconfig system deps). Status: deps-verified.
- NOTE: local docker rpmbuild of this NVR ALSO passed (RPMBUILD_EXIT=0); local install passed
  only WITH copr enabled (Requires xwayland-satellite-git) — install via copr is the real path.

### 6. Final state
- Open PRs: none. Open issues: NONE. Working tree clean except this relay.
- COPR: latest builds for every package green; only historical failures remain
  (protonplus 10841179, ly 10840572, obsidian 10843364 - superseded.
- Adversarial re-checks done: niri HEAD == pinned 464d45d, obsidian latest v1.13.6,
  protonplus v0.6.2, xwayland-satellite-git & quickshell-git HEAD == spec pin.
- All commits pushed to Ackerman-00/fedora-nexus main.