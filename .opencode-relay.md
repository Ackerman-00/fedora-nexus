# fedora-nexus autonomous-run relay state

status: complete
run_id: 31497000426

## Run 31497000426 (2026-08-11) - COMPLETE: fluxer stale -> fixed + shipped (2026.810.212541, build 10846901 green all 3 chroots, install-tested fc44+rawhide); 61/61 latest COPR builds green; new hyprlauncher/hyprshutdown specs pushed but COPR records need owner creation

### 0. Pickup
- Picked up main at `7135e1d` (auto-update refreshing niri-git 9474d05->0add05d + caelestia-cli-mango 875edaa->be54856; owner manually bumped caelestia-shell-mango to 2.1.0 in df46fee). Relay of previous run (`status: complete`, run 31468747081). Fresh run (RELAY=false, RUN_ID=31497000426). History unshallowed for no-churn checks.
- COPR chroots (API-verified via `/api_3/project/list` + `/build-chroot/list`): fedora-44-x86_64, fedora-rawhide-x86_64, fedora-43-x86_64.
- Fedora lifecycle (web-verified 2026-08-11): F44 stable (EOL Jun 2027), F43 supported (EOL Dec 9 2026), F45 branched (GA target ~Oct 20 2026, NOT yet a release), rawhide = F46. COPR chroots match supported set; no chroot changes warranted.

### 1. COPR build scan (priority 1) — NO FAILED BUILDS
- All 61 packages' latest builds succeeded (verified via per-package API scan: `/api_3/build/list` + `/build/{id}`). Historical failures (obsidian 1.13.6-1, localsend 1.18.0-1) already fixed in prior runs.
- This run: fluxer was STALE (spec 2026.731.153836, upstream X-Fluxer-Version now 2026.810.212541). Fixed via update.sh, docker build+install verified on fc44, pushed 831bc2c -> COPR build 10846901 succeeded (all 3 chroots) -> install-tested from live COPR on fc44 AND rawhide (fluxer-2026.810.212541-1, rpm -V clean, `--no-sandbox --version` exit 0). Webhook confirmed empirically (push -> new build appeared within seconds).

### 2. Version sweep (priority 2) — ALL 61 CURRENT, no-churn
- 31 update.sh scripts run in clean sandbox copy: all exit 0, all report up-to-date (exact same pins as main). 0 files changed.
- GitHub releases API vs spec: aquamarine, glaze, hyprcursor, hyprgraphics, hypridle, hyprland (0.56.2), hyprland-guiutils/protocols/qt-support, hyprlang, hyprlock, hyprpaper, hyprpicker, hyprpolkitagent, hyprqt6engine, hyprsunset, hyprsysteminfo, hyprtoolkit, hyprutils, hyprwayland-scanner, hyprwire, matugen, mpvpaper, nwg-look, starship, stoat-desktop, vesktop, waypaper, xdg-desktop-portal-hyprland, bibata-cursor-theme, cascadia-code-nerd-fonts, extension-manager, heroic-games-launcher, localsend, logseq, mangowm, obsidian, scenefx, zen-browser, helium-browser - ALL match spec.
- Codeberg check: ly 1.4.1 (GitHub mirror stale at v1.0.3 — Codeberg authoritative). sentriz/cliphist 0.7.0, LukashonakV/cava 1.0.0, vysp3r/ProtonPlus v0.6.2 all match. PyPI materialyoucolor 3.0.4 match.
- Git-based: niri-git 0add05d, caelestia-cli-mango be54856, caelestia-shell-mango cae55ed — all match upstream HEAD. hyprland-plugins pinned 00862ca = hyprpm pin for hyprland 0.56.2 (verified via API search of hyprpm.toml commits) — correct by design.
- Nothing bumped beyond fluxer. No churn.

### 3. Actions health (priority 3) — HEALTHY
- Auto-updater runs all green. 2 failed runs (31407635573, 31407634776) are workflow-file runs (opencode-schedule.yml / opencode.yml) — known benign (token lacks workflows scope). NOT real failures.

### 4. Issues / PRs
- Open issues: NONE. Open PRs: NONE. Nothing to reply/merge/close.

### 5. Webhook health
- Direct `gh api hooks` = 403 (token lacks hooks scope). Empirical verification instead: fluxer commit 831bc2c pushed -> build 10846901 submitted seconds later. Webhook alive and rebuilding EXISTING packages on push.

### 6. NEW PACKAGE REGISTRATION LIMITATION (important for owner)
- Pushed new verified specs hyprlauncher 0.1.6 + hyprshutdown 0.1.1 (commit 30890c9). Both build+install PASS in docker fc44 (rpmbuild clean, rpm -V clean, no missing libs).
- COPR does NOT auto-create package records from pushed spec files: verified `/api_3/package/list?ownername=ackerman&projectname=nexus` still returns 61 packages, no hyprlauncher/hyprshutdown, and no builds were triggered for them.
- COPR docs confirm: webhooks only rebuild packages that already exist in the project. Creating a new package record requires owner-level COPR access (`copr-cli add-package-*` or web UI under the project Packages tab) — not available from this environment (read-only unauthenticated API only).
- ACTION NEEDED FROM OWNER: in the COPR project ackerman/nexus, add packages `hyprlauncher` and `hyprshutdown` (SCM source -> https://github.com/Ackerman-00/fedora-nexus, subdir `hyprlauncher`/`hyprshutdown`, spec `hyprlauncher.spec`/`hyprshutdown.spec`). Once the records exist, the webhook will build them automatically on future pushes. Until then they are NOT installable from the repo (README now carries an explicit caveat).

### 7. Install-test coverage ledger (this run)
- **fluxer 2026.810.212541-1** from live COPR: fc44 + rawhide fresh install, rpm -V clean, no missing libs, `--no-sandbox --version` exit 0.
- **niri-git 1:26.04^0add05d-1**: local rpmbuild + install fc44 PASS (this run). Earlier bulk sweep from prior runs already covered install on all 3 chroots.
- **caelestia-cli-mango 2.0.0^be54856-1** + **caelestia-shell-mango 2.1.0^cae55ed-1** + **mangowm 0.15.6-2**: local rpmbuild + install fc44 PASS (`mango` exit 0).
- **hyprlauncher 0.1.6-1** + **hyprshutdown 0.1.1-1**: local rpmbuild + install fc44 PASS (rpm -V clean, no missing libs; not in COPR — see section 6).
- Prior-run coverage still valid: bulk fc44 install sweep (34 packages), rawhide spot checks, fc43 niri-git.

### 8. Final state
- Working tree: has uncommitted README caveat edit (this run's final commit). Push will carry relay=complete + README caveat.
- 61/61 COPR packages green. fluxer current. All versions current. No open issues/PRs.
- Known gaps (documented): (1) hyprlauncher/hyprshutdown in repo but not in COPR until owner registers them (section 6); (2) full GUI smoke of Electron apps headless not possible; (3) COPR webhook direct API 403 (token scope) — verified empirically.
