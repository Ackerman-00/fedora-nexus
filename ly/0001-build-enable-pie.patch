--- a/build.zig
+++ b/build.zig
@@ -72,6 +72,8 @@
         .use_llvm = true,
     });
 
+    exe.pie = true;
+
     const ly_ui = b.dependency("ly_ui", .{
         .target = target,
         .optimize = optimize,
